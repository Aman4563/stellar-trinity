# UNITED STATES NON-PROVISIONAL UTILITY PATENT APPLICATION

**Filed under 35 U.S.C. § 111(a)**

**Title:** Cryptographically Controlled Certification of Machine-Learning Evaluation Tasks Using Isolated Generation, Audit, and Evidence Domains

**Inventor:** Sarvex Jatasra

**Applicant / Assignee:** Ethara.AI

**Docket Reference:** Trinity

---

## 1. TITLE OF THE INVENTION

Cryptographically Controlled Certification of Machine-Learning Evaluation Tasks Using Isolated Generation, Audit, and Evidence Domains

## 2. CROSS-REFERENCE TO RELATED APPLICATIONS

Not Applicable.

## 3. STATEMENT REGARDING FEDERALLY SPONSORED RESEARCH OR DEVELOPMENT

Not Applicable.

## 4. FIELD OF THE INVENTION

The present disclosure relates generally to machine-learning evaluation infrastructure, and more particularly to computing systems that control the certification, release, and continued use of executable tasks employed to evaluate or train machine-learning solvers. Aspects of the disclosure relate to cryptographic attestation of task execution, to information-flow control among cooperating but mutually distrusting execution domains in a multi-agent benchmark production pipeline, to append-only evidence ledgers secured by signed Merkle checkpoints, and to fail-closed state machines that gate the release of executable evaluation artifacts upon externally derived, cryptographically bound, and temporally bounded execution evidence.

## 5. BACKGROUND OF THE INVENTION

Machine-learning systems are increasingly assessed by executable benchmark tasks. Such a task typically comprises executable environment bytes that establish an initial machine state, task-instruction bytes that state an objective to a candidate solver, and verifier bytes that programmatically grade whatever artifact or state change the solver produces. The scientific and commercial value of such a task rests entirely on a claim about its difficulty: the assertion that a defined population of solvers, exercised under defined conditions, fails the task at or below some rate. Conventional approaches to producing, validating, and publishing such tasks exhibit several technical deficiencies that undermine that claim, and those deficiencies are properties of the computing pipelines that produce the tasks rather than properties of the tasks themselves.

A first deficiency concerns benchmark contamination. Where a candidate task is derived from, or is a near-duplicate of, material already present in the training corpora or public evaluation corpora available to the solver population, a measured failure rate reports memorization rather than capability. Contamination is frequently discovered after publication. In conventional pipelines, a post-publication contamination discovery voids the evidentiary value of every prior measurement retroactively, because those measurements were never bound to a machine-checkable record of what corpora, exclusion lists, freeze dates, or near-duplicate indices they were screened against, nor to the exact byte-level version of those screening resources. Absent such a binding, a downstream consumer cannot compute, from the delivered artifact alone, whether a given measurement remains applicable, and must instead contact the producer and accept the producer's assurance.

A second deficiency concerns shortcut exploitation. A solver may satisfy verifier bytes without performing the work described by the task-instruction bytes, for example by exploiting lexical overlap between the instruction and the expected answer, by exploiting subsequence leakage in a graded target list, by exploiting the polarity handling of a naive matcher, by exploiting positional artifacts in a fixed answer ordering, or by exploiting stylistic cues that correlate with correctness in the reference material. When such a shortcut exists, the measured construct silently changes: the pipeline reports a difficulty figure for a task the solver never attempted. Conventional pipelines typically address shortcuts by ad hoc human review, which is neither reproducible nor machine-verifiable, and which leaves no artifact against which a later party can confirm that any particular class of shortcut was probed at all.

A third deficiency concerns weak verifiers. A verifier that parses runner logs, applies patches, or matches a graded target list may be sensitive to line ordering, indentation, dialect prefixes, ordinal suffixes, timestamps, interleaved output, or the particular patch-application path taken. Such sensitivity corrupts the reward signal in both directions: a correct solution may be scored zero, and an incorrect solution may be scored full. Where the same verifier is subsequently used to generate reinforcement-learning reward, the corruption propagates into a trained model. Conventional pipelines commonly validate a verifier only against a reference solution, which cannot reveal a parser defect that manifests only on solver-generated output.

A fourth deficiency concerns reward hacking at the level of the production pipeline itself. Where a task author and a task auditor exchange information directly, the author obtains, over successive iterations, a model of the auditor's acceptance behavior. The author then optimizes candidate tasks toward that acceptance behavior rather than toward the intended measurement. The resulting artifact passes audit reliably and measures progressively less. This failure mode is structural: it arises from the topology of the production pipeline, is invisible in the produced artifact, and is not cured by increasing auditor rigor, because the author's optimization target tracks whatever rigor the auditor applies. Conventional approaches address the problem, if at all, by administrative separation-of-duties policy, which is advisory, is not enforced by the computing system, and leaves no machine-checkable record establishing which information crossed between the roles.

A fifth deficiency concerns unfalsifiable difficulty claims. In conventional pipelines the party that authors a task frequently also selects the solver population against which the task is measured, selects the number of attempts, observes intermediate outcomes, and decides when to stop measuring. Each of those degrees of freedom permits a difficulty figure to be manufactured: an easier solver population can be chosen, attempts can be repeated until an unfavorable sample appears, a stopping rule can be selected after outcomes are visible, and a favorable subset of runs can be reported. A difficulty figure produced under such conditions is not falsifiable by a third party, because the third party cannot reconstruct which runs occurred, which policy governed them, or which population was exercised.

A sixth deficiency concerns evidence staleness. Solver populations improve continuously. A measurement establishing that a task defeated a given population at a given instant does not establish that the task defeats the population that exists later. Conventional pipelines publish difficulty figures without machine-readable expiry, so a stale figure is indistinguishable from a current one at the point of consumption, and a system that consumes the figure has no mechanism by which reliance on it can be automatically withdrawn.

These deficiencies compound. A contaminated task with a weak verifier, tuned toward an auditor whose criteria the author has learned, measured by the author against a population the author selected, and published without expiry, produces a difficulty figure that is simultaneously wrong, unfalsifiable, and permanent. What is needed is a computing architecture in which the accessibility of information between production roles is enforced by the system rather than asserted by policy, in which difficulty evidence is produced by a party that neither production role controls, in which the evidence is cryptographically bound to the exact bytes measured and to the exact population exercised, in which independent verifiers can each confirm they are examining the same execution without either learning the other's private material, and in which certification is a machine-enforced state transition that fails closed and that automatically lapses as the underlying evidence ages.

## 6. SUMMARY OF THE INVENTION

The present disclosure provides systems, methods, and non-transitory computer-readable storage media for controlling certification of executable tasks used to evaluate or train machine-learning solvers. Four cooperating architectural pillars are described.

In some embodiments an externally controlled difficulty measurement is provided, in which a pilot controller freezes a representation of a candidate task under a task digest and freezes a versioned solver registry under a registry digest. Before a pilot is executed, the system verifies an operator-signed pre-execution commitment that identifies an execution-control policy for the pilot, the commitment being timestamped strictly earlier than the execution interval it purports to bind and committing to a hidden decoy seed together with a complete job schedule or an attempt budget and a stopping policy. The frozen task representation is executed against a solver population identified by the frozen solver registry under an externally rooted execution-operator credential that is not issuable or usable by security principals of the author or auditor execution domains. The execution operator then emits a signed pilot attestation binding at least the task digest, a trajectory commitment, a policy-manifest digest, a scorer-closure digest, a solver-population commitment, an execution interval, and a signer identity. In some embodiments the difficulty criterion applied to the resulting outcome is a one-sided Clopper-Pearson exact upper confidence bound on solver success probability required to sit strictly below a predeclared frontier floor by at least a predeclared margin, evaluated as a maximum over the solvers of the frozen registry under a multiple-comparison correction selected and committed before execution.

In some embodiments computed sole-channel deterministic projections are provided, in which a communication fabric prevents direct machine-readable communication between the author execution domain and the auditor execution domain, and permits information to cross between them only through the record-keeper execution domain. A projection engine computes an author projection and an auditor projection by respective deterministic transformations of ledger state under respective field policies. The author projection excludes auditor-private acceptance information and includes an auditor finding only after a many-to-one coarsening transformation has been applied, which is configured to prevent recovery of the auditor-private acceptance information from the coarsened finding. The auditor projection structurally excludes author-provided design-intent information, including a hardness contract carrying author-selected difficulty targets, intended solution strategies, and expected failure explanations. Each projection is, in some embodiments, a pure function over an enumerated closure comprising ledger bytes, supersession records, a hardness contract, canary standing, pinned trust roots, and one declared evaluation instant, with a recorded projector digest and schema version sufficient for a third party to reproduce the projection byte-for-byte.

In some embodiments dual-blind attestation routing is provided, in which an evidence router provides a complete representation of the signed pilot attestation exclusively to the auditor execution domain, and provides to the record-keeper execution domain a restricted representation containing binding fields of the attestation while withholding pilot telemetry designated as auditor-private, including runtime telemetry, decoy values, and recorded trajectories. Each of the auditor execution domain and the record-keeper execution domain then independently verifies alignment with a common pilot execution identity by comparing digest fields of its own representation against locally recomputed digest fields. Neither domain thereby learns the contents held by the other, yet each can establish that it is examining the same execution.

In some embodiments triadic access-controlled execution domains with a fail-closed controller are provided. The system arranges three domains with disjoint ownership: an author execution domain that invokes at least one machine-learning model to generate the candidate task; an auditor execution domain that evaluates committed bytes of the candidate task; and a record-keeper execution domain holding exclusive write authority over certification records in an append-only evidence ledger. The execution-operator credential is externally rooted and is not issuable or usable by security principals of the author execution domain or the auditor execution domain. A certification controller maintains a fail-closed certification state and permits an upward transition to a certified state, and corresponding insertion of a certification record into the append-only evidence ledger, only after the auditor domain verifies the attestation, the task digest, the execution-control policy, and satisfaction of a predeclared difficulty criterion; the record-keeper domain verifies the signer identity against a trust-policy store and verifies alignment with the common pilot execution identity; and the certification controller determines that the author domain neither supplied the execution-operator credential nor possessed write authority for the certified state.

Dependent aspects further provide, among other features: association of respective failure modes with respective validity intervals having valid-at and invalid-at times derived from failure-mode-specific evidentiary half-lives, with automatic prevention of reliance on lapsed evidence; a monotone lever state machine comprising ACTIVE, WATCH, EXPIRED, and RETIRED states in which upward transitions require new qualifying evidence and entry into RETIRED requires a human-signed retirement authorization; solver-registry versions linked by predecessor digests with monotone preservation of a required cohort and denial of author authority over registry membership; canary-normalized canonical hash domains in which token-slot values are replaced by fixed placeholders while slot counts and positions remain represented; opt-in per-project completion bounds declared in human-authored requirements, orthogonal to hardness tiers, and reconciled against budget-stamped signed duration evidence; both-halves liveness proofs requiring each deterministic grounding instrument to detect a planted-defect fixture and to refrain from detecting a corresponding clean fixture during a common run; eight named grounding-instrument families; Merkle trajectory commitments requiring exact equality between an independently recomputed root and an operator-signed root; DSSE-wrapped in-toto attestation envelopes verified against an external trust root; Merkle checkpoints, signed tree heads, and a recovery function that marks the ledger broken on any byte inequality; a closed coarsening vocabulary consisting of lever:saturated, checker:broken, and task:blocked; recorded projection closures with allow-lists and named forbidden-field lists; dual-host byte-identical regeneration of ground truth; structural exclusion of the hardness contract from the auditor projection; SHA-256 human approval gates recomputed before each approved operation; recording of human operational acceptance against a capped disposition without waiver of the execution-attestation requirement; write-once UUID version 5 task identity under a pinned namespace, derived from a canonical content hash computed under canary normalization from which the bundle's canary token set is itself purely derived; a single authoritative bundle home per task identity with a separately keyed rollout-evidence mirror, and a layering of an interoperable base delivery format beneath stricter private extension artifacts; mandatory batches of at least twenty isolated parallel task slots per invocation; solution-centric mutation with lineage acceptance criteria; adversarial hardening sequences of attacker, fixer, and solver processes; five predeclared shortcut classes from a versioned taxonomy; six contamination-screening atoms over an immutable named exclusion baseline and a calibrated near-duplicate floor; a hardness catalog owned solely by the record-keeper, regenerated byte-identically into a human-readable knob sheet, and bound by a fixed headroom multiple over anchored frontier evidence as a design obligation that never raises a disposition; conformal risk-controlled judge reliability with forced abstention; rubric compilation isolated from author-provided solution provenance with a rubric digest committed before judged trajectories are received; a governing-specification currency gate that hard blocks every domain whose vendored specification bytes fall behind the tracked upstream tip and that admits no waiver; unconditional capture of every human turn into an append-only feedback ledger attested by a hash chain whose failed end-to-end walk hard blocks the instrument; out-of-band discharge of digest-bound sign-off gates by a signed approval envelope requiring distinct producer and approver identities, a unanimous council of at least three seats of mutually distinct model lineages, and a bound failed-attack transcript, with an enumerated class of acts remaining permanently human; and a harvest lane that stages machine-proposed candidate calibration bundles under a propose-never-admit rule.

## 7. BRIEF DESCRIPTION OF THE DRAWINGS

FIG. 1 is a block diagram of a system architecture according to some embodiments, depicting three access-controlled execution domains, namely a record-keeper execution domain, an author execution domain, and an auditor execution domain, together with a communication fabric enforcing access-control boundaries among them, a pilot execution interface operating under an externally rooted execution-operator credential, a trust-policy store, an append-only evidence ledger, a projection engine, an evidence router, and a certification controller.

FIG. 2 is a dataflow diagram of projection computation according to some embodiments, depicting an enumerated input closure supplied to a projector, the application of respective allow-list and forbidden-field policies to produce an author projection and an auditor projection, the application of a many-to-one coarsening transformation to an auditor finding prior to inclusion in the author projection, and the recording of a projection closure comprising a projector digest, a projection schema version, a contract commit identifier, and a declared evaluation instant.

FIG. 3 is a sequence diagram of pre-run commitment and pilot execution according to some embodiments, depicting freezing of candidate-task bytes under a task digest, freezing of a versioned solver registry under a registry digest, emission and timestamping of an operator-signed pre-execution commitment to a hidden decoy seed and to a complete job schedule or an attempt budget and stopping policy, verification that the commitment timestamp precedes the execution interval, execution of the frozen task representation against the identified solver population, and emission of a signed pilot attestation.

FIG. 4 is a sequence diagram of dual-blind attestation routing according to some embodiments, depicting an evidence router providing a complete representation of a signed pilot attestation exclusively to an auditor execution domain and a restricted representation containing binding fields to a record-keeper execution domain, and depicting the withholding of pilot telemetry, decoy material, and trajectory bytes from the record-keeper execution domain.

FIG. 5 is a flow diagram of digest-recomputation verification according to some embodiments, depicting independent local recomputation of digest fields by each of the auditor execution domain and the record-keeper execution domain, comparison of the recomputed digest fields against received digest fields, and determination of alignment with a common pilot execution identity without disclosure of either domain's private contents to the other.

FIG. 6 is a state diagram of a certification state machine according to some embodiments, depicting a fail-closed initial state, conditioned upward transitions to a certified state, and monotone-downward transitions to held and blocked states, together with the conjunction of cryptographic, access-control, temporal, and execution-derived conditions required for the upward transition and the architectural ceiling imposed on locally verified candidates.

FIG. 7 is a state diagram of an evidence expiry and lever lifecycle according to some embodiments, depicting an ACTIVE state, a WATCH state, an EXPIRED state, and a RETIRED state, the derivation of a valid-at time and an invalid-at time from a failure-mode-specific evidentiary half-life, the prohibition of upward transitions absent new qualifying evidence, and the restriction of entry into the RETIRED state to a human-signed retirement authorization.

FIG. 8 is a flow diagram of Merkle checkpointing and byte-identical recovery according to some embodiments, depicting construction of a Merkle tree over canonical bytes of accepted attestation envelopes and derived supersession records in an authoritative append-only sequence, appending of a signed tree head carrying a prior root and a new root, reconstruction of ledger state from the transitive closure of verified envelopes, byte-for-byte comparison against committed ledger bytes, and transition of the ledger to a broken state upon any byte inequality.

FIG. 9 is a flow diagram of an invocation preflight according to some embodiments, depicting resolution of a vendored governing-specification checkout against a fetched upstream tip and a hard block on a checkout that is behind, diverged, locally modified, or unverifiable, followed by unconditional capture of a received human turn into an append-only feedback ledger, appending of a corresponding hash-chained attestation line linked to its predecessor, an end-to-end walk of the chain, publication of the chain head in a root report, and a hard block of the instrument upon a failed walk.

FIG. 10 is a sequence diagram of out-of-band discharge of a digest-bound sign-off gate according to some embodiments, depicting recomputation of a bound artifact digest, execution of recorded break-attempt lanes over frozen bytes, independent blind balloting by a council of at least three seats of mutually distinct model lineages each distinct from the producer lineage, a unanimity requirement for an approving verdict, emission of a signed approval envelope binding the bound digest, the producer and approver identities, the council roster, and a failed-attack transcript digest, writing of the legacy digest file as a compatibility interface, and fallback to human sign-off upon dissent, lineage collision, or an unseatable council.

## 8. DETAILED DESCRIPTION OF THE INVENTION

### 8.1 Overview

Reference is made to FIG. 1. In some embodiments, a system 100 for controlling certification of executable tasks used to evaluate or train machine-learning solvers comprises one or more processors and one or more non-transitory memories, and further comprises a record-keeper execution domain 110, an author execution domain 120, an auditor execution domain 130, and a pilot execution interface 140, the four being interconnected by a communication fabric 180 configured to enforce access-control boundaries among them. The record-keeper execution domain 110 holds a hardness-catalog store 111, an append-only evidence ledger 112, a supersession relation 113, a projection engine 114, a Merkle checkpoint log 115, a trust-policy store 116, a freshness controller 117, a cohort-registry store 118, and a harvest staging store 119. The author execution domain 120 produces a candidate task 121 comprising executable environment bytes 122, task-instruction bytes 123, and verifier bytes 124, and additionally maintains a hardness contract 125 recording author design intent, a feasibility bundle 126, an adversarial hardening subsystem 127, a shortcut-probe subsystem 128, and a mutation lineage store 129. The auditor execution domain 130 maintains a grounding-instrument registry 131, an audit evidence store 132, an attestation ingress 133, and a judge-reliability controller 134. The pilot execution interface 140 operates under an externally rooted execution-operator credential identified by the trust-policy store 116, and cooperates with a pilot controller 141, a frozen solver registry 142, a pre-execution commitment 143, and a statistical rule module 144 to produce a signed pilot attestation 150. An evidence router 153 derives from the signed pilot attestation 150 a complete representation 151 and a restricted representation 152, and routes them respectively to the auditor execution domain 130 and the record-keeper execution domain 110. A certification controller 160 maintains a fail-closed certification state and controls insertion of a certification record 162 into the append-only evidence ledger 112. The projection engine 114 emits an author projection 170 and an auditor projection 172, the author projection 170 receiving auditor-derived information only after passage through a many-to-one coarsening transformation 174. A human sign-off controller 190 enforces digest-bound approval gates, and a recovery function 195 reconstructs ledger state from signed evidence. Each of the three execution domains is further gated by a governing-specification currency controller 196 that resolves the vendored specification bytes the domain executes against the current upstream tip before any phase work, and each holds a feedback capture subsystem 197 writing an append-only feedback ledger together with a hash-chained feedback attestation log 198 whose end-to-end walk conditions every invocation. Where the trust-policy store 116 authorizes it, an out-of-band approval controller 199 may discharge a digest-bound sign-off gate in place of a human approver on the terms described in section 8.28.

In one implementation, the three execution domains correspond to three prompt-contract instruments operated on a coding-agent runtime, respectively designated a memory instrument owning a hidden directory `.memory/`, an author instrument owning a hidden directory `.seed/`, and an auditor instrument owning a hidden directory `.audit/`, each a sibling of the vendored governing-specification checkout at the parent project root. The three own disjoint filesystem subtrees, emit disjoint root reports, and are peers rather than a hierarchy. The description that follows uses the claim vocabulary throughout: the memory instrument and its owned subtree constitute the record-keeper execution domain 110, the author instrument and its owned subtree constitute the author execution domain 120, and the auditor instrument and its owned subtree constitute the auditor execution domain 130. Nothing in that particular realization is limiting; in other embodiments the domains are realized as ordinary services, and the described access-control boundaries are enforced by conventional operating-system, container, hypervisor, or capability mechanisms.

### 8.2 Definitions

The following definitions apply throughout this description and are intended to give objective, machine-checkable meaning to terms recited in the claims.

**Execution domain.** An "execution domain" is a logical access-control boundary within a computing system, characterized by a set of security principals, a set of resources those principals may read, and a set of resources those principals may write. An execution domain is not necessarily a separate physical machine. In various embodiments, execution domains are realized as separate operating-system processes with distinct user identities, as separate containers with distinct mount namespaces and network policies, as separate virtual machines, as separate physical machines, as distinct capability sets within a capability-based operating system, or as distinct labels within an information-flow-control system. Two execution domains may share hardware, share a kernel, and share a filesystem, and remain distinct execution domains provided that the access-control boundary between them is enforced by the computing system rather than by convention. What makes the boundary claim-relevant is the enforcement of read and write authority, not the physical topology.

**Computed projection.** A "computed projection" is an output of a deterministic transformation applied to a defined body of state, in which each field of the output is either an allow-listed field of the input, a deterministic function of allow-listed fields of the input, or a deterministically coarsened representation of an input field, and in which fields named on a forbidden-field list are structurally absent from the output. A computed projection is not a filtered view materialized on demand from a live query with variable results; it is a value produced by executing a named transformation over a named input closure at a named instant, such that any party holding the input closure and the transformation can recompute the identical value.

**Pure function over an enumerated closure.** A transformation is a "pure function over an enumerated closure" when the complete set of inputs on which its output depends is enumerated by name and bound by digest, when the transformation consults no input outside that enumeration, and when repeated evaluation over identical input bytes yields byte-identical output. In some embodiments the enumerated closure of a projection comprises: the canonical bytes of the append-only evidence ledger 112; the canonical bytes of the supersession relation 113; the canonical bytes of a hardness contract catalog; recorded contamination-screening standing; the pinned identities and immutable digests of trust roots; and exactly one declared evaluation instant. Because the transformation itself may change, the recorded closure additionally carries a projector digest identifying the transformation bytes, a projection schema version, and a contract commit identifier, so that recomputability survives a change in the transformation. In some embodiments a recorded closure is never rewritten: it remains valid under the projector version that produced it, is marked superseded for current use, and the next projection is generated from the whole enumerated closure at a new evaluation instant.

**Non-invertible coarsening.** A "many-to-one coarsening transformation," also referred to as a non-invertible coarsening, is a total function from a set of source findings onto a strictly smaller closed target vocabulary, such that at least two distinct source findings map to a common target value and no inverse function recovering the source finding from the target value exists. Non-invertibility is a structural property of the mapping and does not depend on the computational difficulty of any inversion attempt. In some embodiments the target vocabulary consists of exactly three values, and the source domain comprises auditor findings of arbitrary textual content, defect fixtures, acceptance thresholds, severity rationales, and pilot telemetry, none of which is transmitted.

**Externally rooted credential; controlled by neither.** A credential is "externally rooted" and "controlled by neither" the author execution domain 120 nor the auditor execution domain 130 when the credential's issuance authority and authorization policy are recorded in a trust-policy store 116 that neither of those domains may write, when no security principal of either of those domains is able to cause issuance of that credential, and when no security principal of either of those domains is able to present that credential to obtain a signature. In some embodiments the credential is a signing key identity or a keyless OIDC subject, the authorization is expressed as a role binding in the trust-policy store 116 associating a signer identity with an authorized role such as an execution-operator role, and a role that a signer asserts about itself is treated as a claim rather than as an authorization.

**Frozen.** An artifact is "frozen" when its bytes have been reduced to a cryptographic digest under a declared canonicalization, and when every subsequent operation that purports to concern that artifact recomputes the digest from current bytes and refuses to proceed on inequality. Freezing is therefore a binding rather than a storage mode: a frozen artifact may be copied, transported, and re-materialized, and remains frozen so long as the digest binding is checked. In some embodiments any change to bytes bound by a release record voids the release record absolutely and requires a new pilot.

**Fail-closed.** A control is "fail-closed" when the absence, unavailability, or unresolvability of a required input causes the control to emit a non-passing disposition rather than a passing one, and when the control's emitted disposition is capped at or below a non-passing value while the required input remains unresolved. Fail-closed is distinguished from inert: a fail-closed control rejects bad evidence while remaining able to accept good evidence, whereas an inert control rejects all evidence including good evidence. In some embodiments an inert required instrument is treated as a broken instrument and forces a blocking disposition, because an instrument that cannot fire certifies a clean result it never tested.

**Difficulty.** "Difficulty" is used herein as a three-place relation over a task, a solver population, and a time, rather than as a scalar property of a task. A statement that a task is difficult is, in this disclosure, an abbreviation for a statement that a named task, measured against a named and frozen solver population, at a named instant, produced an outcome whose one-sided upper confidence bound on solver success probability sat strictly below a predeclared floor by at least a predeclared margin. Because the second and third places of the relation move, the relation is perishable, and the disclosure treats a difficulty record as evidence with a validity interval rather than as a permanent label. In some embodiments latent-ability and latent-difficulty estimates derived from item-response-theory models are computed from signed pilot outcomes as derived state that may describe and bound measured evidence but may not itself create a certification record, promote a lever, or raise a disposition.

**Attestation envelope.** An "attestation envelope" is a signed data structure comprising a payload of typed, closed-schema predicate fields and one or more detached signatures over the canonical bytes of that payload, the signatures being verifiable against an external trust root. In some embodiments the envelope is a Dead Simple Signing Envelope encapsulating an in-toto Statement carrying a declared predicate type, and an envelope that does not parse to the schema named by its declared predicate type is rejected without creating any record. Signing is detached and external, and the record-keeper execution domain 110 does not self-produce a trusted signature.

**Validity interval; evidentiary half-life.** A "validity interval" is a pair comprising a valid-at time and an invalid-at time carried by a certification record and associated with a specific failure mode. An "evidentiary half-life" is a failure-mode-specific duration parameter from which the invalid-at time is derived, expressing the empirical rate at which evidence about that failure mode ceases to describe the current solver population. Different failure modes carry different half-lives, so a single certification record may carry several validity intervals, one per failure mode it evidences.

**Digest and hash function.** Where this description recites a digest, a hash, a content hash, or a Merkle root, any collision-resistant hash function may be used. SHA-256 is described throughout as a concrete example and is used in one implementation, including in human approval files; the description is not limited to SHA-256, and other functions of comparable or greater collision resistance may be substituted without departing from the disclosure. Similarly, where a signature scheme is recited, any digital signature scheme with detached signatures and an external trust root may be used.

### 8.3 The three execution domains and disjoint ownership

In some embodiments, the record-keeper execution domain 110, the author execution domain 120, and the auditor execution domain 130 are peers with strictly disjoint write authority. In one implementation the record-keeper execution domain 110 owns a subtree `.memory/`, the author execution domain 120 owns a subtree `.seed/` together with the per-task bundle directories under the task lane roots `samples/<uuid>/` and `delivery/<uuid>/` and their bundle-local rollout-evidence subtrees `samples/<uuid>/trajectories/` and `delivery/<uuid>/trajectories/`, and the auditor execution domain 130 owns a subtree `.audit/`. Two further hidden sibling subtrees are owned by work orders outside the three domains, namely `.trial/` for the out-of-band approval controller 199 of section 8.28 and `.podium/` for the conductor of section 8.35, and no domain writes into any of them. Each domain writes only within its own subtree and a small fixed set of root-level reports, and no domain writes into another domain's subtree. The communication fabric 180 is configured to enforce these boundaries; in various embodiments the enforcement is by filesystem permissions, by container mount policy, by capability restriction, or by an information-flow-control label lattice.

The communication fabric 180 is further configured to prevent direct machine-readable communication between the author execution domain 120 and the auditor execution domain 130, and to permit information to cross between them only through the record-keeper execution domain 110. The prohibition is not limited to an explicit message channel. In some embodiments the prohibition extends to any machine-readable surface by which one of those two domains could observe outputs of the other, including report files, evidence files, finding records, private research-staging areas, and gate-internal fixtures. In one implementation the author execution domain 120 is denied read access to auditor findings, auditor reports, auditor gate internals, and auditor pass criteria, and the auditor execution domain 130 is denied read access to author-authored difficulty claims, intended solution strategies, golden trajectories, generated ground-truth bytes, executable oracle bytes, rubric criterion prose, rubric judgment text, per-item rubric weights, drift schedules, checker fixtures, pilot keys, and issued canary bytes.

In some embodiments the prohibition is additionally applied to indirect channels that would otherwise arise from shared inputs. A shared corpus of external research papers is written by exactly one domain, namely the record-keeper execution domain 110, and is read-only to the other two. Where the author execution domain 120 or the auditor execution domain 130 requires a document absent from the shared corpus, it stages the document in a domain-private staging area, and that staging area is named as a forbidden field in both projections, so that neither the stems, the citations, nor the staging timestamps of the private staging area reach the other domain. Admission of a document into the shared corpus occurs only upon independent rediscovery by the record-keeper execution domain 110, and an admitted copy carries no origin attribution, staging timestamp, or originating-domain annotation, because unfiltered document selection would otherwise constitute a sender-controlled channel from author to auditor.

The author execution domain 120 is denied write authority over certification records in the append-only evidence ledger 112. In some embodiments the author execution domain 120 is additionally denied authority to resolve or modify membership of the solver registry 142, to author, select, version, amend, or reorder any contamination-screening root, to resolve a solver-cohort registry or a provider release feed, or to author its own hardness catalog. The rationale for each denial is the same: an author able to name the population it is measured against, or the corpus it is screened against, or the target it is designed toward, can name an easier one.

In some embodiments the sole-writer corpus boundary also binds content identity, as recorded 2026-09-20. The record-keeper execution domain 110 retains source bytes and a rendering derived by a named tool at a pinned version. A digest of retained bytes, not a path or sender, identifies the artifact. Section 8.48 describes the identity sidecar and its checks. CONTRACT-ONLY (ENGRAM.md:24, 238, 486) governs sender-neutral admission; ENFORCED (tools/corpus_identity.py, first landed 2026-09-20; tests/test_corpus_identity.py) covers pair and identity checks, not independent rediscovery or reproduction of conversion.

Prior art positioning: see section 8.58, information barrier, and in particular Fides arXiv 2505.23643; the novelty residue here is sender-neutral corpus admission through independent rediscovery, whereas information-flow control alone does not establish that a shared document selection carries no originating-domain signal.

### 8.4 The append-only evidence ledger and its certification records

Referring to FIG. 1 and FIG. 8, the append-only evidence ledger 112 holds certification records 162, each of which is referred to in one implementation as a CFER, standing for a certified failure-evidence record. In some embodiments a certification record 162 is born only from a signed attestation envelope 150 that has been wrapped in a Dead Simple Signing Envelope encapsulating an in-toto Statement, that declares a recognized predicate type, that parses to the closed schema named by that predicate type, and whose detached signature verifies against an external trust root designated by the trust-policy store 116. An envelope failing any of those conditions creates no certification record and causes the ledger to be marked broken, because an ingestor that cannot name what it is reading cannot fail closed on it.

In some embodiments the closed predicate schema of a certification-bearing envelope comprises the following fields. A `cfer_id` field carries a stable identifier, one per isolated lever per proof. A `signer_identity` field carries the external signer by key identity or keyless OIDC subject, matched against the trust-policy store 116. A `signer_role` field carries the role asserted for the pilot, which is required to match the role that the external trust-root policy authorizes for that signer identity; a mismatch caps the ledger at a broken state. A `solver_registry_digest` field carries the frozen solver-registry version the pilot ran against. A `task_hash` field carries the canonical content hash of the frozen task bytes, computed under a bound canary-normalization domain and carrying that domain version. A `checker_hash` field carries the hash of the checker set that graded the rollouts. A `transcript_hash` field carries the hash of the complete signed rollout job. An `environment_hash` field carries the harness image and environment digest. A `pilot_outcome` field carries rollout count, pass count, and per-checker vectors. A `statistical_rule` field carries the predeclared rule identity and its parameters. A `binomial_upper_bound` field carries the computed upper confidence bound on pass rate. A `model_cohort` field carries the named solver cohort against which the bound is measured. Measurement, signature, and ingest timestamps are carried separately. A `validity_interval` field carries the window from a valid-at time to an invalid-at time derived from the evidentiary half-life for the relevant failure mode. A `supersession` field carries a named supersession pointer, null for an original envelope, and for a superseding envelope carries the prior identifier together with the instant from which the prior record becomes invalid. A `disposition` field carries the lever state the proof supports, never above what the computed bound permits.

In some embodiments the envelope carries optional signed outcome groups, each honored only when the external signer signed over the frozen bytes carrying them: a reference-fidelity group carrying an upstream identity, a pinned upstream version digest, a real-upstream score obtained by running the real upstream artifact through the identical submission invocation contract, and a per-class divergence summary over a closed divergence taxonomy; a calibration group carrying a declared tier, per-cohort pass-rate aggregates under a predeclared pass-at-k rule, and a clean-host reproducibility outcome; a verifier-robustness group carrying signed outcomes for score stability, format invariance, baseline cleanliness, target flakiness, apply-path parity, and zero-score attribution; and a rubric-compilation group carrying signed outcomes for schema conformance, identifier reconciliation, compiled weight share, regeneration determinism, and compiled-test liveness. In some embodiments a contamination-screening group is carried on stricter terms than the other groups, being schema-optional so that a historical envelope still parses but required for every newly certified task, such that a new task whose envelope omits it is treated as unmeasured rather than exempt.

Certification records are never deleted and never overwritten. A superseding measurement is expressed as a new envelope carrying its own identifier and a signed supersession pointer, and the record-keeper execution domain 110 writes exactly one row into the append-only supersession relation 113 in the same pass that admits the superseding envelope. In some embodiments the supersession relation carries the closed schema comprising the superseded identifier, the superseding identifier, an invalid-at instant, and the digest of the signed envelope that authorized the supersession, so that each relation row is recoverable from the signed envelopes alone. A relation that cannot be rebuilt from the signed envelopes would constitute a second source of truth, and a second source of truth is a split brain.

In some embodiments the research-catalog supersession relation is distinct from the signed certification-envelope relation above. In one implementation it uses `engram.supersession/v1`, retaining a first-class edge and a `superseded_kind` of `revoked` or `deprecated` without deleting the superseded row. A `row_version` moves with each content change under `engram.hardness/v2`, while a four-level GRADE confidence records its invoked domains. These annotations neither create a certification record nor replace its external signature. Sections 8.47 and 8.49 define the tier-gated supersession authority and confidence calculation. CONTRACT-ONLY (ENGRAM.md:212, 243, 253-254, 408), as recorded 2026-09-20.

Prior art positioning: see section 8.58, pilot and expiry, and in particular in-toto; the novelty residue here is a certification record born only from an authorized closed-schema execution envelope with recoverable supersession, whereas a signed supply-chain statement alone does not establish that difficulty evidence authorizes the record.

### 8.5 Merkle checkpoints, signed tree heads, and byte-identical recovery

Referring to FIG. 8, in some embodiments the record-keeper execution domain 110 maintains an authoritative append-only accepted-envelope sequence with a closed schema comprising a monotonically increasing position beginning at one with no gaps and no reuse, the digest of the accepted envelope's canonical bytes, and the declared predicate type. One row is appended per accepted envelope at admission and is never rewritten. A gap, a duplicate position, a digest matching no accepted envelope, or an unrecognized predicate type causes the ledger to be marked broken, because an order that two readers can derive differently is not an order.

After every ingest, the record-keeper execution domain 110 computes a Merkle tree over the canonical bytes of every accepted envelope and each derived supersession record in the accepted-envelope sequence. In some embodiments envelope leaves are ordered by ascending position and each derived supersession record is placed immediately after the leaf for the envelope that authorized it, so that leaf ordering is a deterministic function of the signed material. A signed tree head is appended to the Merkle checkpoint log 115 carrying the prior root, the new root, the leaf count, an ingest timestamp, and a detached signature from an external trust root. A tree head whose signature does not verify, whose prior root does not chain to the last accepted head, or whose recomputed root does not match the recorded root causes the ledger to be marked broken.

The recovery function 195 is a pure function. In some embodiments it reconstructs the evidence ledger 112, the supersession relation 113, and recorded contamination-screening standing from the transitive closure of verified attestation envelopes, compares reconstructed ledger bytes with committed ledger bytes, and marks the evidence ledger 112 as broken in response to any byte inequality. In some embodiments the recovery function is exercised on every invocation as part of a liveness proof, being required to round-trip a non-empty ledger byte-for-byte, to accept a signed re-audit envelope that changes screening standing, to accept a signed clean re-audit envelope that preserves clean standing, and to recover the resulting screening standing byte-for-byte.

In some embodiments a fixed recovery rank governs reconstruction: signed envelopes of every accepted predicate type together with the signed checkpoints outrank approved scope and current artifact bytes, which in turn outrank any derived progress cache. The derived cache never outranks either higher tier. The cache is kept outside sign-off-bound bytes so that a phase transition never invalidates a human signature, and the cache is domain-private so that it never enters a peer domain's inputs and never appears in either projection. A cache disagreeing with disk invalidates its phase and every dependent phase; it escalates to a broken ledger state only when truth cannot be recomputed, when authoritative signed state conflicts, or when a passing disposition had rested on the mismatch.

Prior art positioning: see section 8.58, pilot and expiry, and in particular Sigstore Rekor v2; the novelty residue here is byte-identical reconstruction of certification and supersession state from verified envelopes as a condition of ledger standing, whereas log inclusion alone does not prove equality with the derived ledger bytes.

### 8.6 Evidentiary half-life, validity intervals, and the lever state automaton

Referring to FIG. 7, in some embodiments each certification record 162 associates respective failure modes with respective validity intervals having a valid-at time and an invalid-at time derived from a failure-mode-specific evidentiary half-life. The freshness controller 117 is a pure function whose closure comprises certification-record bytes, resolved solver-registry bytes, verified cohort-registry bytes, pinned configuration, and one declared evaluation instant. It never queries the live world, so currency remains a comparison over bytes rather than a lookup, and repeated evaluation over frozen bytes is required to produce bit-identical state vectors.

In some embodiments the certification controller 160 maintains, for each certified task or task lever, a monotone state machine comprising four states. In one implementation the illustrative state tokens are ACTIVE, WATCH, EXPIRED, and RETIRED. A lever is ACTIVE when it holds fresh verified signed evidence within the freshness horizon, a binomial upper bound below the frontier floor, no current frontier-clearing proof, and cohort currency, the last requiring that an accepted cohort-registry snapshot stands valid at the evaluation instant, that its refresh deadline has not passed, and that the lever's evidence covers the required cohort that snapshot names. A lever is WATCH when it is measured and verified but flagged, for example when approaching the horizon, when inclusion has narrowed, or when a frontier signal exists that does not yet clear it; a missing, expired, or overdue cohort registry narrows every affected lever to WATCH, because unavailable currency proves uncertainty rather than defeat, and WATCH never counts as ACTIVE for disposition purposes. A lever is EXPIRED when it is over-horizon or frontier-caught; the expiry condition is satisfied and independently computable when a frontier-clearing proof lands or the horizon passes, while the recorded state changes on the next invocation that admits the proof or evaluates the horizon. A lever is RETIRED only terminally and only by human action; no automated transition reaches RETIRED and no transition leaves it.

In some embodiments the certification controller 160 prevents an upward state transition without new qualifying evidence, so that the automaton is monotone downward under all machine-initiated transitions. Entry into RETIRED is permitted only in response to a human-signed retirement authorization, and in some embodiments an append-only retirement record carries a closed schema comprising the lever identifier, the task hash whose withdrawal motivated the retirement, an RFC 3339 UTC retirement instant, an approver identity matched against the same external trust-root policy that authorizes signers, and the digest of the signed human approval.

Separately from difficulty freshness, in some embodiments a contamination-screening standing is derived from signed time bounds. The age origin is the signed screening measurement instant recorded as an RFC 3339 UTC timestamp, the maximum re-audit interval is a whole number of days, and expiry is the origin instant plus the interval. A unit is overdue when the comparison instant is greater than or equal to expiry. The comparison uses exact UTC instants rather than date truncation, because truncation would recognize an expiry up to a day late and a control that is late by construction is not a control. Because the record publishes an absolute expiry, a downstream consumer holding only the delivered unit can evaluate standing without contacting the producer, and every instrument that runs refuses to pass an overdue or unretracted unit.

Solver-registry currency is handled on parallel terms. In some embodiments successive versions of the solver registry are linked by predecessor digests, and an update is accepted only when it monotonically preserves a required cohort from a preceding version. A cohort-registry snapshot is accepted only as a detached signed statement of a declared predicate type, signed by an identity that the external trust policy authorizes for a cohort-registry-issuer role, and stored as immutable bytes. Its closed schema carries a registry identity, a schema version, a monotonically increasing sequence beginning at one with no gaps and no reuse, a predecessor digest chaining to the last accepted snapshot, issued-at and effective-at instants, a validity interval, an inclusion-policy digest, the required cohort naming exact provider, model, and version identifiers, and release-evidence digests. A mutable alias, a moving pointer, a fork, a repeated or lowered sequence, a predecessor that does not chain, an unrecognized or absent field, or the removal of a previously required model absent a separately signed policy amendment is rejected, and each rejection caps the ledger at broken. The freshness controller 117 then joins each certification record's cohort and registry digest against the required cohort at the declared evaluation instant. Because a signature authorizes rather than completes, an issuer omitting a shipped model would be invisible to signature verification alone; accordingly a refresh deadline is computed as the earlier of the accepted snapshot's validity end and the last verified refresh instant plus a bound maximum refresh interval, compared as exact UTC instants, and at or after that deadline a named coverage gap is opened, a family flag is raised, every affected lever narrows to WATCH, the ledger is held below current, and the next author invocation is capped at a hold disposition.

Prior art positioning: see section 8.58, pilot and expiry, and in particular TUF; the novelty residue here is failure-mode-specific withdrawal of difficulty reliance with human-only retirement and a cohort refresh deadline, whereas metadata expiry and rollback protection alone do not define that lever lifecycle.

### 8.7 The projection engine and the two computed projections

Referring to FIG. 2, in some embodiments the projection engine 114 is the sole information bridge between the author execution domain 120 and the auditor execution domain 130. Neither of those domains reads the append-only evidence ledger 112 directly, and neither reads the proof store, the checkpoint log 115, or the other domain's surfaces. Instead, the projection engine 114 emits exactly two computed projections: an author projection 170, referred to in one implementation as FORGE_VIEW and materialized as a file the author execution domain 120 may read, and an auditor projection 172, referred to in one implementation as CRUCIBLE_VIEW and materialized as a file the auditor execution domain 130 may read. A direct read of raw ledger bytes by either domain is a violation that the communication fabric 180 is configured to prevent.

Each projection is a pure function over an enumerated closure as defined above. In some embodiments the recorded closure comprises the canonical ledger bytes, the supersession relation bytes, the hardness-contract catalog bytes, recorded contamination-screening standing, the pinned identities and immutable digests of trust roots, and exactly one declared evaluation instant, and is recorded together with a projection schema version, a projector digest, a contract commit identifier, and the declared evaluation instant. The projector digest is recorded because identical inputs at an identical instant do not reproduce a projection once the transformation itself has changed; a closure naming its inputs but not the function over them stops being recomputable the moment an allow-list moves. In some embodiments the record-keeper execution domain 110 stores a projection closure sufficient for a third party to reproduce the respective projection byte-for-byte, the stored closure including an allow-list of permitted fields and a named forbidden-field list for that projection. A recorded closure is never rewritten; it remains valid under the projector version that produced it, is marked superseded for current use, and the next projection is generated from the whole enumerated closure at a new evaluation instant.

In some embodiments each projection is a read and never a command channel, and local work in a consuming domain may proceed only where that work's declared inputs do not include a projected value. An operation that declares a projected input refuses to run while that input is absent and never substitutes a locally authored value. A missing, unreadable, over-horizon, or unresolvable projection is recorded as a named coverage gap in the consuming domain's evidence file and in its root report, and caps the consuming domain's disposition at a non-passing value; absence never lowers a projected floor and never permits a consuming domain to author its own catalog, floor, threshold, or escalation flag.

In some embodiments the author projection 170 carries an enumerated allow-list and nothing else, comprising: lever identity and category together with a screening-neutral lever state drawn from ACTIVE, WATCH, EXPIRED, and RETIRED computed from non-screening inputs alone; allowed archetype pressure and the current frontier floor expressed as a number; signed pilot failure aggregates and budget-stamped signed pilot duration aggregates, each expressed as counts and bounds rather than per-rollout traces; the difficulty freshness horizon and difficulty measurement dates; public lineage identifiers for superseded levers; coarsened escalation family flags, including a budget-required escalation flag; the contamination-screening roots expressed as identity and immutable digest alone with schema or algorithm version, supplier identity, effective date, and supersession pointer; the calibrated near-duplicate parameters; the cohort-registry public facts comprising registry identity, immutable snapshot digest, schema version, sequence, effective date, validity end, absolute refresh deadline, and required-cohort identity; and a projected hardness contract carrying, per row, a mechanism sentence, category, axis, grading mode, orthogonality group, anchor confidence, already-public anchor identity, already-public anchor cohort, already-public anchor reported score and report date, per-category frontier targets, the archetype eligibility map, the intended composition, and bound tier thresholds. In some embodiments the projected thresholds may only raise the difficulty floors named in the author domain's own contract and never lower them. No budget value is projected through the author projection 170; a declared budget binds directly from the human requirements corpus, and the budget-required escalation flag creates an obligation to obtain that human-authored declaration rather than supplying a value.

In one embodiment, the time-budget mechanism is opt-in per project and applies only when a human-authored requirements corpus declares a budget or a standing `budget:required` escalation holds. An unbudgeted project carries no budget obligation, and the system invents no default. When the mechanism applies, each task manifest `task.toml` binds three values sourced directly from the human requirements corpus: `budget_hours`, specifying a per-attempt completion bound; `budget_envelope`, specifying the hardware and execution class against which those hours are meaningful; and `budget_margin`, specifying a reconciliation tolerance that the author instrument carries by name but that only the auditor instrument resolves as a bare value. Budget and hardness are orthogonal: the hardness catalog carries no budget value, no tier derives a budget, no budget derives a tier, and a tier claim binds to the frontier-defeat target alone. No budget value is projected through the author projection 170, and the declared budget binds directly from the human requirements corpus. The reference solution is required to complete within `budget_hours` on the bound `budget_envelope`, with its measured `reference_hours` recorded, and a reference exceeding that bound renders the task infeasible rather than hard, producing `BLOCK:INFEASIBLE_OR_UNVERIFIED` with reason `budget-unverified`. Each external signed pilot records the budget under which it ran, including the applicable `budget_hours` and `budget_envelope`, and duration evidence measured under one budget never supports a claim at another. Per-rollout durations are recorded only inside an execution attestation retained by the auditor execution domain 130, and only budget-stamped signed duration aggregates comprising counts and bounds may cross either computed projection; per-rollout durations and wall-clock instants are forbidden in both projections. A pilot conducted under a configured budget smaller than the declared `budget_hours` produces `HOLD:PILOT_REQUIRED` with reason `budget-uncovered` and cannot anchor a difficulty tier; absent signed duration evidence produces `HOLD:PILOT_REQUIRED` with reason `budget-unverified`; and measured duration exceeding the declared budget beyond the auditor-resolved bare margin produces `HOLD:MISCALIBRATED` with reason `budget-inverted`. An eighth deterministic grounding-instrument family, the budget-conformance family, comprises three instruments that re-derive the budget binding from committed bundle bytes, match the attested execution envelope to the bound `budget_envelope`, and reconcile attested duration aggregates against the bound budget and resolved margin. Each such instrument may only lower or cap a disposition, and a clean result never raises one. The current project maintains three budget ledger views: append-only `.memory/budget.yaml` keyed by task hash with bound budgets and signed duration evidence, dated author-side `.seed/budget.yaml` with direct human-requirements bindings and supersession history, and grounded auditor-side `.audit/budget.yaml` with re-derived bindings, attested duration aggregates, and reconciliation outcomes.

In some embodiments the auditor projection 172 carries a different enumerated allow-list, comprising: task artifact hashes and claimed scope, including a per-task upstream-provenance declaration of origin, upstream identity, license, modifications, and per-instance train-cutoff date together with a source sub-record carrying a source kind, a canonical repository identity, a fork-ancestry snapshot digest, a base commit SHA, and a task-generating-event timestamp, all treated as declarations to be reconciled rather than results to be trusted; provenance manifests and proof digests; the same contamination-screening roots and calibrated near-duplicate parameters on identical terms, so that both domains receive the same roots and each computes its own result; the execution-operator trust policy expressed as identity, immutable digest, authorized role, and validity window, together with an expected execution-attestation receipt expressed as predicate version, attestation digest, signer identity, bound task hash, bound policy-manifest digest, and bound trajectory root; a remediation receipt for a cleared coarse invalidation carrying an invalidation identifier, the prior coarse class, a repair-proof digest, a signer, a timestamp, and a supersession link; checker metadata limited to lane, carrier class, and binding identity; and integrity-priority escalation flags under a closed mapping.

In some embodiments the forbidden-field lists are named, greppable, and absolute, and the projection engine 114 strips the named fields by name rather than by heuristic, with conformance fixtures planting every forbidden class and failing closed if any survives into the wrong projection. Named forbidden fields of the author projection 170 include, in some embodiments: auditor pass criteria; gate-internal thresholds; finding text; exploit reasoning; severity rationales; scanner soundness notes; issued canary bytes; per-cohort pass-rate scores measured by a signed pilot in the project, which are distinct from and never conflated with an already-public anchor score cited from a published paper; tier-inversion detail; reproducibility detail; parser identity; perturbation-outcome detail; replay reconciliation detail; rubric residue reasoning; measured compiled weight share; the entire content, digest, presence signal, and generation outcome of any optional rubric display artifact; any invalidation reason originating in the auditor execution domain 130 other than in coarsened form; the superseding anchor behind a superseded hardness row; any fitted orthogonality-group estimate not bound to a signed pilot outcome; the auditor-private research staging area in full; and the recorded rollouts of every task bundle in full, so that no rollout byte crosses into the author projection. In some embodiments the execution attestation 150 is forbidden in the author projection 170 in full beyond a receipt digest, signer identity, and predicate version that a release proof already publishes, and the operator's decoy commitment, decoy values, access telemetry, and blinding results cross under no entry, because an author who learns what the runtime watched can author against it.

Named forbidden fields of the auditor projection 172 include, in some embodiments: author-authored difficulty claims; intended solution strategy; golden trajectory; generated ground-truth bytes; executable oracle-solution bytes; rubric criterion prose; rubric judgment text; per-item rubric weights; the entire content, digest, presence signal, and generation outcome of any optional rubric display artifact; drift schedules; checker fixtures; pilot keys; issued canary bytes; any self-attested probability that is not a signed pilot outcome; the entire projected hardness contract, such that no hardness row, category, axis, orthogonality group, frontier target, archetype eligibility entry, intended composition, or tier threshold crosses into the auditor projection 172; the entire cohort registry, such that no registry identity, snapshot digest, sequence, effective date, validity end, refresh deadline, required-cohort entry, inclusion-policy digest, release-evidence digest, candidate-release record, or cohort escalation flag crosses; and the author-private research staging area in full. The rationale for excluding the hardness contract 125 and the cohort registry is stated in the disclosure directly: the hardness contract is the standing design intent the author builds toward, and an auditor holding the author's playbook grades the intent instead of the bytes.

In some embodiments certain classes are forbidden in both projections, including sender-produced semantic adjudication text, attestation-resolution results, declared-versus-attested mismatches, and attestation-unavailable reasons. Only roots, calibrated parameters, attestation digests, and signer identities cross, and each recipient computes its own result. In some embodiments the author projection 170 additionally forbids per-task review-band neighbours, screening measurement instants, screening expiry instants, and screening interval values, on the express ground that a timestamp marking when an audit ran is an outcome signal wearing a schedule. Consistently with that treatment, metadata channels, field-omission patterns, field ordering, and timing are themselves treated as projection outputs subject to the same field policy, rather than as incidental transport properties outside the policy.

Prior art positioning: see section 8.58, information barrier, and in particular Trillian Verifiable Log-Derived Map; the novelty residue here is a pair of role-specific deterministic projections with forbidden fields and a reproducible input closure, whereas a verifiable derived map alone does not establish the author-auditor disclosure partition.

### 8.8 Many-to-one coarsening of auditor findings

Referring to FIG. 2, in some embodiments the many-to-one coarsening transformation 174 is applied to an auditor finding to produce a coarsened finding, and the coarsened finding is included in the author projection 170 in place of the auditor finding. In one implementation the transformation is a fixed total mapping onto a closed vocabulary consisting of exactly three illustrative values: `lever:saturated`, `checker:broken`, and `task:blocked`. No underlying defect fixture, acceptance threshold, pilot telemetry, finding text, or auditor rationale accompanies the coarsened finding, and the transformation is configured to prevent recovery of the auditor finding, and of the auditor-private acceptance information the auditor finding reflects, from the coarsened finding.

In some embodiments the mapping is applied under a fixed precedence so that two readers derive the same coarse class from the same underlying result. In one implementation the precedence is: a confirmed exclusion match, a freeze violation, or a near-duplicate neighbour at or above the calibrated floor maps to `task:blocked`; a surviving sanitization finding, a positive empty-submission result, a provenance carrier that fails verification or reconciliation, an unavailable or unresolvable attestation, an unresolvable root, an unadjudicated review-band neighbour, or a malformed or unparseable atom result maps to `checker:broken`; and a result that fails only because a root was superseded or an interval lapsed maps to `lever:saturated`. Because the mapping is total and strictly many-to-one, the author execution domain 120 receives at most a small, bounded number of bits per coarsened finding and cannot invert the coarsened finding to recover either the auditor finding or the auditor's acceptance criteria.

In some embodiments the coarsening is supplemented by a remediation receipt visible only in the auditor projection 172. Because coarsening runs one way, a coarse bit that merely stops appearing cannot otherwise be distinguished from a coarse bit that was repaired, and laundering would read exactly like remediation. The receipt records the invalidation identifier, the prior coarse class, a repair-proof digest, a signer, a timestamp, and a supersession link, and carries no finding text, no gate internals, and no pass criteria; it is named forbidden in the author projection 170 in full, so that neither the prior coarse class, nor the repair-proof digest, nor the supersession link, nor the bare fact that a clearing was recorded crosses to the author execution domain 120.

In some embodiments the coarsening discipline is also applied to family-level escalation flags. A family flag such as a screening-required or cohort-required flag is raised on an outcome-independent trigger, namely a root supersession event or an interval lapse rather than a failure event, and is cleared on an outcome-independent condition, namely that every remaining unit in the family carries evidence measured against currently effective roots with an expiry strictly after the declared evaluation instant. Because raising and clearing are outcome-independent, the flag conveys scheduling state rather than auditor findings.

Prior art positioning: see section 8.58, information barrier, and in particular US20200159946A1; the novelty residue here is a total many-to-one finding map with fixed precedence and no recoverable private rationale, whereas an access-category redacted ledger alone does not specify that closed coarsening transformation.

### 8.9 The externally controlled difficulty pilot

Referring to FIG. 3, in some embodiments the pilot controller 141 freezes a representation of the candidate task 121 under a task digest and freezes the versioned solver registry 142 under a registry digest before any pilot is contemplated.

The task digest is computed over a canonical hash domain under a versioned canary-normalization rule. In some embodiments the normalization replaces the value occupying each designated canary-token slot with a fixed public placeholder and changes nothing else, so that slot count, slot ordering, slot position, surrounding bytes, and every byte outside a designated slot remain represented inside the hash domain. The normalization domain version is recorded and bound at a human sign-off gate, so that a hash computed under one domain version is never compared against a hash computed under another. This construction makes the binding acyclic: the canonical content hash is computed over the placeholder-bearing provisional bundle, the canary token set is then derived from that hash by a pure key-derivation function, the tokens are then planted into the designated slots, and a binding block is then applied and signed. Planted token bytes therefore sit outside the hash preimage yet are each fixed exactly by the derivation relation, so no slot is a free region and no region of the identity domain is both mutable and valid.

The solver registry 142 is external to the author execution domain 120 and fixes adapters, a model list, budgets, temperatures, a retry policy, a rollout count, and health checks. In some embodiments the model list is required to cover every entry of a required cohort named by an accepted, signed cohort-registry snapshot, and a registry short of the required cohort is treated as measuring an easier frontier than the one standing, rendering its result void for release purposes and capping the disposition. The author execution domain 120 is denied authority to resolve or modify registry membership, and is denied the ability to resolve a registry, a release feed, a provider announcement, or a model list locally. Because the registry is frozen and its entry order is declared rather than derived from execution, the solver-population commitment defined in section 8.10 is computable before any rollout begins, and in some embodiments the pre-execution commitment 143 binds it alongside the execution-control policy digest, so that the population to be exercised is fixed at the same instant as the policy governing the exercise. Likewise, because the scorer closure defined in section 8.10 is assembled from committed bundle bytes and pinned environment references rather than from run-time observations, the scorer-closure digest is computable before any rollout begins and may be bound at the same instant.

Before any rollout, the execution operator emits a pre-execution commitment 143 under the externally rooted execution-operator credential. In some embodiments the commitment binds a hidden decoy seed together with either a complete job schedule or an attempt budget and a stopping policy, and the commitment timestamp is required to precede the execution interval it purports to cover. A pilot whose operator is unauthorized for the execution-operator role under the trust-policy store 116, whose commitment is absent, or whose commitment postdates the execution it claims to bind is void for release purposes and caps the disposition. The reason is expressly recorded in the disclosure: a commitment made only after results are known constrains nothing, because an operator free to choose what to commit once outcomes are visible can still commit to a flattering subset.

In some embodiments the pre-execution commitment 143 identifies an execution-control policy for the pilot, and does so by binding the identity or the digest of the complete execution-control policy rather than by reciting selected parameters. As used herein, the "execution-control policy" is the complete enumerated set of parameters that govern how a pilot is conducted and how its outcome is reduced to a difficulty determination, and comprises at least: the predeclared statistical rule identity together with its parameters, including the rollout count n, the one-sided confidence level, the estimator identity, the frontier floor, and the predeclared margin; the multiple-comparison correction identity and its parameters; the per-task attempt budget; the stopping rule; the solver-registry freeze window; the registry digest that the freeze window pins; the hidden decoy seed commitment; and either the complete job schedule or the attempt budget together with the stopping policy. In some embodiments the execution-control policy is serialized under the same canonicalization described in section 8.10, a schema version is bound into the serialized form, and the digest of that canonical serialization is the value the commitment 143 carries, so that a reader holding a candidate policy document can determine from the commitment alone whether that document is the one that was committed. Because the predeclared statistical rule is a constituent of the execution-control policy rather than a separate later declaration, any change to the rollout count, the confidence level, the estimator, the floor, the margin, the correction, the attempt budget, or the stopping rule after the commitment timestamp moves the execution-control policy digest and therefore fails verification. That structural coupling is the mechanism by which post hoc tuning of the difficulty criterion is precluded, rather than a policy undertaking not to tune it.

In some embodiments the statistical rule module 144 fixes, before any rollout runs and frozen for the pilot window, a rollout count n, a one-sided confidence level, and the estimator, the estimator being a Clopper-Pearson exact one-sided upper confidence bound on solver success probability. A solver is treated as clearing the frontier floor when its upper bound sits at or above the floor, and a task is treated as defeating the frontier only when the upper bound stays strictly below the floor by a predeclared margin. In some embodiments the difficulty criterion is evaluated according to a maximum, across solvers in the frozen solver registry 142, of respective solver success measures, so that a single solver clearing the floor is sufficient to withhold certification. In some embodiments sequential-testing and multiplicity controls are predeclared in the same frozen policy: a per-task attempt budget caps how many pilots may be submitted, a stopping rule is fixed in advance, a registry freeze window bounds the interval over which the registry digest may not change, and a multiple-comparison correction is applied across every attempt and every primary solver, so that re-running until one pilot happens to fail the frontier cannot manufacture difficulty by chance. The corrected bound, rather than any single raw attempt, is the difficulty evidence. A pilot that exceeds the attempt budget, that runs outside the freeze window, or that reports an uncorrected bound is void and caps the disposition.

In some embodiments, where the realized rollout count is below the predeclared minimum needed to separate the floor at the declared confidence, the pilot resolves to a distinct named hold disposition, recorded in one implementation as `HOLD:UNDERPOWERED_PILOT`. A pilot that runs a single cohort, a single attempt where k exceeds one, or fewer cohorts than a predeclared minimum resolves to the same underpowered hold. A pilot in which a higher declared tier realizes an easier pass rate than a lower tier by more than a predeclared calibration margin resolves to a distinct named hold, recorded in one implementation as `HOLD:MISCALIBRATED`, and the affected task family is returned for re-tiering rather than released with a mislabelled tier. A pilot in which any current primary solver clears the frontier floor resolves to a distinct named hold, recorded in one implementation as `HOLD:FRONTIER_NOT_DEFEATED`, and the task is escalated rather than released.

In some embodiments the execution operator is required to emit every rollout as one complete signed job, so that cherry-picking of a favorable subset is precluded by the structure of the evidence rather than by policy. The author execution domain 120 may store a proof artifact only after the external operator emits it, and the author execution domain 120 is architecturally unable to self-produce a trusted difficulty proof.

Prior art positioning: see section 8.58, pilot and expiry, and in particular US12192372 / EP4280543A1; the novelty residue here is pre-execution binding of population, hidden decoy commitment, stopping policy, and familywise difficulty criterion under excluded author credentials, whereas assessment provenance alone does not prevent post-outcome selection of those inputs.

### 8.10 The execution attestation and its binding set

In some embodiments the execution operator emits a signed pilot attestation 150 binding at least: the frozen task digest; a trajectory commitment, realized in one implementation as a Merkle root computed over canonical rollout entries each carrying at least a sequence index, a rollout identity, a relative path, and a trajectory byte hash; a policy-manifest digest; a scorer-closure digest; a solver-population commitment, realized in one implementation as a population sequence commitment; an execution interval; and the operator's own signer identity. In some embodiments the attestation is a Dead Simple Signing Envelope encapsulating an in-toto Statement, and the record-keeper execution domain 110 is prevented from creating a certification record 162 unless the envelope signature verifies against an external trust root designated by the trust-policy store 116.

Each of the bound fields is a digest or commitment over a canonically serialized closure rather than over an ad hoc concatenation, and each is defined below in terms of its enumerated inputs, its excluded inputs, its canonical ordering and serialization, its version binding, the digest or commitment operation applied, and the local recomputation-and-comparison procedure by which a verifying domain establishes alignment with the common pilot execution identity. Throughout, a "canonical serialization" means a serialization in which object member names are emitted in lexicographic order, array order is the order specified for the closure, numbers and timestamps are emitted in a single declared normal form, no insignificant whitespace is emitted, and a declared character encoding is used, such that two independent implementations serializing the same logical closure produce identical byte strings. Throughout, "collision-resistant hash function" carries the meaning given in section 8.2, with SHA-256 used as a concrete example.

**Scorer-closure digest.** In some embodiments the "scorer-closure digest" is a digest over a canonical serialization of a scorer closure, the scorer closure being the enumerated set of every artifact whose bytes can influence the score assigned to a rollout. In one implementation the enumerated inputs of the scorer closure comprise: the bytes of the scoring program, including the verifier entry point and every module it imports; the bytes or content digests of every checker binary, checker script, and checker fixture reachable from that entry point; the bytes or content digest of the parser that converts runner output, test output, or patch-application output into checker outcomes; the reward-mapping configuration, comprising every reward metric identifier, its declared range, the exact value designated as full reward, and any multi-turn or multi-step aggregation rule; the container image digest of the verifier environment in which scoring executes, expressed as an immutable digest reference rather than a mutable tag; the declared network policy of that verifier environment; and the versioned invocation contract, comprising the argument vector, the environment-variable set, the working directory convention, and the reward-contract output path at which the verifier is required to write its result. In some embodiments the scorer closure is required to be closed under reachability, so that an artifact that the scoring program can load at run time but that is absent from the enumeration causes the closure to be rejected as incomplete rather than silently scored.

In some embodiments the scorer closure expressly excludes inputs that are not properties of the scoring computation, including wall-clock time, host identity, hostname, user identity, working-directory path outside the declared convention, locale, random seeds not themselves enumerated in the closure, runtime telemetry, decoy material, per-rollout content, and any value observed during execution. The exclusion is functional as well as declarative: in some embodiments the scoring program is parsed under an abstract-syntax-tree allow-list and a scoring program that reads a clock, a random source, an environment secret, or a file outside the enumerated closure is blocked, so that the closure enumeration and the actual dependency set are held to agree. Because the excluded inputs are precisely the inputs that would otherwise vary between the operator's execution and a verifier's later recomputation, their exclusion is what makes the digest reproducible off the operator's host.

In some embodiments the canonical ordering of the scorer closure is by a declared field order at the top level and by lexicographic order of artifact identity within each artifact set, so that set members are serialized in an order that does not depend on filesystem enumeration order. A schema version identifying the closure layout is bound into the serialized form, so that a digest computed under one closure schema is never comparable against a digest computed under another. The scorer-closure digest is then the output of a collision-resistant hash function applied to the resulting canonical byte string. In other embodiments the scorer-closure digest is a Merkle root over per-artifact leaf hashes ordered by artifact identity, which permits selective disclosure of individual closure members without disclosure of the whole; the recomputation-and-comparison procedure is unchanged.

In some embodiments the local recomputation-and-comparison procedure for the scorer-closure digest is as follows. The auditor execution domain 130 independently assembles the scorer closure from committed bundle bytes it holds and from the pinned image digest and invocation contract it holds, serializes that closure under the declared canonicalization and schema version, applies the collision-resistant hash function, and compares the recomputed digest for exact equality against the scorer-closure digest field carried in the complete representation 151. Inequality is treated as establishing that the scored artifact is not the committed artifact, and resolves every channel depending on it as unresolved rather than as passing. The record-keeper execution domain 110 performs the corresponding comparison against the scorer-closure digest field carried in the restricted representation 152 and against the value bound into a release record, without holding the closure contents themselves; the record-keeper thereby confirms field-level identity without acquiring checker fixtures, parser bytes, or reward-mapping configuration.

**Solver-population commitment.** In some embodiments the "solver-population commitment" is a commitment over a canonical serialization of the ordered solver population that the frozen solver registry 142 identifies. In one implementation the population is an ordered sequence of entries, each entry comprising: a solver identity, expressed as a stable provider identifier together with a model identifier; a version identifier for that solver; a weights-or-endpoint digest, being a content digest of the model weights where the solver is executed from local weights, or a digest over the canonically serialized endpoint descriptor, comprising the endpoint identity, the served model identifier, and the served model version, where the solver is executed through a served interface; and a decoding configuration, comprising at least the sampling temperature, the nucleus or top-k parameters, the maximum token budget, the stop-sequence set, the retry policy, and the number of samples drawn per rollout under the declared pass-at-k rule. In some embodiments each entry additionally carries an adapter identity and adapter version, being the identity of the scaffold through which the solver is exercised, because two populations differing only in scaffold are different populations for difficulty purposes.

In some embodiments the solver-population commitment expressly excludes wall-clock time, host identity, execution telemetry, per-rollout content, observed outcomes, latency, and cost, so that the commitment characterizes which solvers were exercised and how they were configured, and characterizes nothing about what they produced. The population sequence order is the order declared in the frozen registry rather than an order derived from execution, so that the commitment is computable before any rollout begins and is therefore capable of being bound by the pre-execution commitment 143.

In some embodiments a schema version identifying the entry layout is bound into the serialized form. The solver-population commitment is then computed either as the output of a collision-resistant hash function applied to the canonical serialization of the entire ordered registry sequence, or as a Merkle root over per-solver leaf hashes, each leaf being the hash of the canonical serialization of one entry and the leaves being ordered by their position in the declared sequence. The Merkle form is used in some embodiments because it permits an operator to disclose that a particular solver was a member of the committed population, by supplying that solver's leaf and an inclusion path, without disclosing the remaining membership of the population.

In some embodiments the local recomputation-and-comparison procedure for the solver-population commitment is as follows. A verifying domain independently obtains the frozen solver registry 142 bytes, or the per-solver entries it is entitled to hold, serializes them under the declared canonicalization and schema version, computes the hash or the Merkle root by the same declared construction, and compares the result for exact equality against the solver-population commitment field carried in its own representation of the attestation. The auditor execution domain 130 additionally reconciles the recomputed commitment against the registry digest bound in the release record and against the required cohort projected through the auditor projection 172, and treats a population that does not cover the required cohort as measuring a different frontier than the one standing. The record-keeper execution domain 110 compares the commitment field of the restricted representation 152 against the registry digest it holds, without holding decoding configurations or endpoint descriptors. Exact equality of the independently recomputed commitment establishes that the two domains are examining the same population without either domain disclosing to the other what it observed of that population's behavior.

**Policy-manifest digest and the policy closure.** In some embodiments the "policy-manifest digest" bound by the signed pilot attestation 150 is a digest over a canonical serialization of a policy closure, the policy closure comprising two constituents: the execution-control policy defined in section 8.9, and the capability policy manifest described below. The two constituents are distinct in origin, in ownership, and in verification path, and are bound together in one digest because a difficulty determination is only meaningful when both the conduct of the measurement and the capability envelope under which it ran are fixed. In one implementation the policy closure is serialized as an ordered pair of constituent digests together with a schema version, so that the policy-manifest digest is a digest over the two constituent digests rather than over their concatenated contents, which permits each constituent to be verified independently by whichever domain holds it.

The execution-control policy constituent originates with the execution operator, is committed before execution by the pre-execution commitment 143, and is verified by recomputing its digest from the policy bytes and comparing that digest against both the value bound in the pre-execution commitment 143 and the corresponding constituent of the policy-manifest digest carried in the attestation 150. Because the same execution-control policy digest appears in the pre-execution commitment 143 and in the post-execution attestation 150, a verifier can establish that the policy in force during execution is the policy that was committed before execution, which is the temporal property the commitment exists to create.

The capability policy manifest constituent originates with the author execution domain 120, is emitted into the task bundle, and is included in the canonical content hash of that bundle, so that the declared capability policy cannot drift from the bytes it governs. It enumerates every capability channel exactly once, each channel carrying a value drawn from a closed vocabulary of denied, restricted, and allowed, and it carries no detector rule, threshold, fixture, canary material, watched location, or audit outcome. It is verified by the auditor execution domain 130 by recomputing its digest from committed bundle bytes, comparing that digest against the corresponding constituent of the policy-manifest digest, and separately reconciling the emitted manifest against the design contract the human sign-off gate bound, an exact-agreement requirement on every channel. The disclosure states the division of authority expressly: the capability policy manifest states intent and settles nothing, because the claim that a capability control was enforced belongs to the operator attestation 150 alone. In some embodiments the record-keeper execution domain 110 holds only the policy-manifest digest and neither constituent's contents, and therefore verifies the field for identity without learning the execution-control policy parameters or the declared capability envelope.

**Trajectory commitment and task digest.** For completeness, the remaining bound fields follow the same pattern. The trajectory commitment is a Merkle root over canonical rollout entries as described above, recomputable by the auditor execution domain 130 from committed trajectory bytes and compared for exact equality against the operator-signed root, while the record-keeper execution domain 110 holds the root value alone and never the trajectory bytes. The task digest is the canonical content hash computed under the versioned canary-normalization domain described in section 8.9, recomputable independently by either domain from committed bundle bytes. The execution interval is a pair of RFC 3339 UTC instants compared against the pre-execution commitment timestamp and against validity windows using exact-instant comparison rather than date truncation. The signer identity is matched against the trust-policy store 116 and against the role that store authorizes for the execution-operator role.

In some embodiments the signed attestation envelope is the only authoritative home for the trajectory commitment. Any copy of a trajectory root that the author execution domain 120 stores beside its own rollouts is an index for convenience and is never evidence, because a root that the author can recompute and rewrite proves nothing about the author. In some embodiments the auditor execution domain 130 recomputes the trajectory Merkle root from committed trajectory bytes and the certification controller 160 requires exact equality between the recomputed root and the operator-signed root, together with exact agreement on rollout cardinality, canonical-byte deduplication, completeness, schema, binding, and the population sequence.

In some embodiments a capability policy manifest is emitted by the author execution domain 120 into the task bundle and included in the canonical content hash, so that the declared policy cannot drift from the bytes it governs. The manifest enumerates every capability channel exactly once, each carrying a value drawn from a closed vocabulary of denied, restricted, and allowed; a channel that is absent, duplicated, or holding any other value forces a blocking disposition rather than a permissive default. The manifest states intent and nothing more: it never establishes that a control was enforced, since that claim belongs to the operator attestation 150 alone, and it carries no detector rule, threshold, fixture, canary material, watched location, or audit outcome.

Prior art positioning: see section 8.58, pilot and expiry, and in particular in-toto; the novelty residue here is one execution identity binding scorer closure, population, policy, task, and trajectories with independently recomputable fields, whereas a signed step record alone does not define those evaluation-specific closures and exclusions.

### 8.11 Dual-blind attestation routing and mutual verification without disclosure

Referring to FIG. 4 and FIG. 5, in some embodiments the evidence router 153 provides the complete representation 151 of the signed pilot attestation 150 exclusively to the auditor execution domain 130, and provides to the record-keeper execution domain 110 the restricted representation 152 containing binding fields of the attestation while withholding pilot telemetry designated as auditor-private.

In one implementation the complete envelope is deposited at an auditor-owned ingress path such as `.audit/attestations/execution/<attestation-digest>.dsse` and is treated as immutable external evidence that the auditor execution domain 130 verifies and retains but never manufactures or modifies. The envelope does not travel inside the task bundle, which the author execution domain 120 owns and reads, and it does not travel through either computed projection, which would cause the record-keeper execution domain 110 to hold backend topology, effective mounts, runtime telemetry, and decoy material it has no reason to keep. The restricted representation 152 retained by the record-keeper execution domain 110 comprises binding facts only: digests, signer identities, predicate versions, bound task hashes, bound policy-manifest digests, bound trajectory roots, and validity windows. It never comprises runtime telemetry, decoy commitments, decoy values, access telemetry, blinding results, per-rollout traces, or trajectory bytes.

Each domain then independently verifies alignment with a common pilot execution identity by comparing digest fields of its own representation against locally recomputed digest fields. In some embodiments the auditor execution domain 130 verifies the envelope signature, verifies the signer's authorization for the execution-operator role against the trust policy resolved through the auditor projection 172, verifies the validity window, and requires exact agreement on task hash, policy-manifest digest, scorer-closure digest, and trajectory root between the envelope it verified at its own ingress and the receipt fields the projection carries. In some embodiments the record-keeper execution domain 110 verifies the signer identity against the trust-policy store 116 and verifies that the restricted representation's digest fields equal the digest fields it recomputes from ledger-resident material. Neither domain receives the other's private contents, and no attestation-verification result crosses either projection in either direction. The property established is therefore alignment on execution identity rather than agreement on findings: each side proves it is looking at the same execution, and neither learns what the other found.

In some embodiments the disclosure treats fragmentation, redaction, selective disclosure, and multi-message encoding of the attestation as producing different representations of a single execution identity rather than as producing different execution identities. Where an implementation separately signs a complete representation and a restricted representation, the two representations share a common execution-identity commitment, and the routing, withholding, and independent digest-comparison operations described above apply unchanged.

Prior art positioning: see section 8.58, information barrier, and in particular US11849023B2; the novelty residue here is asymmetric routing of one pilot identity followed by independent digest alignment without exchange of private findings, whereas verifiable audit-log redaction alone does not specify that two-domain verification protocol.

### 8.12 The fail-closed certification controller and disposition vocabularies

Referring to FIG. 6, in some embodiments the certification controller 160 maintains a fail-closed certification state and permits an upward transition to a certified state, and corresponding insertion of a certification record 162 into the append-only evidence ledger 112, only after a conjunction of conditions is satisfied: the auditor execution domain 130 verifies the signed pilot attestation 150, the task digest, the execution-control policy, and satisfaction of the predeclared difficulty criterion; the record-keeper execution domain 110 verifies the signer identity against the trust-policy store 116 and verifies alignment with the common pilot execution identity; and the certification controller 160 determines that the author execution domain 120 neither supplied the execution-operator credential nor possessed write authority for the certified state. Absent any of those conditions, the state does not advance, and no certification record is created.

In some embodiments the auditor execution domain's verification of the execution-control policy is performed constituent by constituent against the policy closure described in section 8.10. The auditor execution domain 130 recomputes the execution-control policy digest from the policy bytes and requires it to equal both the value bound in the pre-execution commitment 143 and the corresponding constituent of the attested policy-manifest digest, thereby establishing that the policy in force during execution was fixed before execution; it recomputes the capability policy manifest digest from committed bundle bytes and requires it to equal the other constituent, thereby establishing that the capability envelope attested by the operator is the envelope the signed-off design declared; and it recomputes the policy-manifest digest itself from the ordered pair of constituent digests and the bound schema version and requires it to equal the attested field. The record-keeper execution domain 110 verifies only the policy-manifest digest field for identity against the value carried in the restricted representation 152 and bound into the release record, and does not receive either constituent's contents. Failure of any of these equalities prevents the upward transition rather than lowering a score.

In some embodiments each domain resolves to exactly one value from its own closed disposition vocabulary, and a value outside the set is a contract violation rather than a novel state. In one implementation the auditor execution domain 130 resolves to exactly `SHIP`, `HOLD`, or `BLOCK`, bare and without a substate. In one implementation the author execution domain 120 resolves to exactly `SHIP`, `HOLD:PILOT_REQUIRED`, `HOLD:UNDERPOWERED_PILOT`, `HOLD:FRONTIER_NOT_DEFEATED`, `HOLD:MISCALIBRATED`, `BLOCK:INFEASIBLE_OR_UNVERIFIED`, or `BLOCK:INVALID_TASK`, and a bare hold or block is not a valid author disposition. In one implementation the record-keeper execution domain 110 resolves the ledger to exactly `CURRENT`, `STALE`, or `BROKEN`.

In some embodiments dispositions are monotone downward under machine action: a subordinate, non-deterministic validation lane may lower a disposition, narrow a lever to WATCH, surface a coverage gap, or enrich a report, but may never create a certification record, promote a lever, raise a disposition, extend a horizon, or pass when unavailable. The disclosure states the asymmetry directly: such a lane may find doubt and lower a disposition, and may never certify and raise one. Correspondingly, the unavailability of a subordinate lane caps rather than passes.

In some embodiments local verification within the author execution domain 120 is architecturally capped. However complete the local deterministic evidence, the highest disposition reachable without an external signed pilot over frozen bytes is a named hold, recorded in one implementation as `HOLD:PILOT_REQUIRED`. No authored probability, no self-solve, no local pass, and no derived estimate may raise the disposition above that ceiling. The certified disposition is reachable only at a release stage, only with external signed evidence over frozen task bytes, only with deterministic gates satisfied, and only with a pilot that defeats the frontier under the predeclared rule.

In some embodiments a coverage gap is a first-class recorded object rather than a silence. A missing instrument, a missing source, an unrun control, an unverified root, or an uncomputable check is recorded by name with a reason and a disposition cap, and the enumerated set of open coverage gaps is itself bound into the release record, such that a coverage gap absent from the release record voids the release.

Prior art positioning: see section 8.58, pilot and expiry, and in particular SLSA; the novelty residue here is a fail-closed upward certification transition conditioned on external measurement and independent domain checks, whereas provenance assurance levels alone do not establish the measured difficulty criterion or the author's inability to mint certification.

### 8.13 Anti-waiver-laundering and byte-bound release

In some embodiments the execution-attestation requirement is expressly non-waivable. The release record binds the execution-attestation digest, its predicate version, and its signer identity, and the certified disposition is refused for any trajectory-bearing task whose attestation is absent, expired, or bound to a different task hash, policy-manifest digest, scorer closure, or image digest. A human operator who must proceed regardless records an operational acceptance against the existing capped disposition and leaves the release record, the coverage ledger, and the disposition vocabulary unedited. The disclosure states the reason for this asymmetry: a waiver that converts missing execution evidence into a release makes repeated acceptance the cheapest path to release.

In some embodiments any byte change after sign-off voids the release absolutely and forces a fresh pilot together with a fresh human approval. Signed pilot evidence does not carry across changed bytes; where a bundle's hash-bound layout is rewritten, a new identity is derived, a new bundle is written, the legacy predecessor is recorded in a lineage store 129 without being overwritten or deleted, and the migrated bundle re-enters at the local verification ceiling and must earn certification through its own pilot.

In some embodiments a post-delivery re-screen never edits the original release measurement. A fresh result is appended to the existing release evidence, the prior release record is marked superseded when withdrawal occurs, and the reason is named from a closed set such as a confirmed-violation reason or an unresolvable-evidence reason. The retraction remains resolvable by task hash through the record-keeper execution domain 110 and through the shipped provenance carrier. Silent removal is forbidden, because a retraction that a downstream consumer cannot resolve is indistinguishable from a cover-up.

Prior art positioning: see section 8.58, pilot and expiry, and in particular TUF; the novelty residue here is refusal to turn operational acceptance into difficulty certification and invalidation on bound-byte change, whereas signed metadata authority alone does not distinguish a capped human acceptance from fresh pilot evidence.

### 8.14 Human sign-off gates bound by digest, and the halt protocol

In some embodiments the human sign-off controller 190 implements digest-bound approval gates. A human approval file receives a SHA-256 digest of a candidate artifact. Before performing an approved operation, the controller recomputes a SHA-256 digest from the current bytes of the candidate artifact and refuses the approved operation when the received and recomputed digests differ. The digest is the only version of the approval: a semantic change that does not move the digest fails closed, and any bound change invalidates the approval and re-triggers sign-off.

In some embodiments the bytes a gate binds are an enumerated closed set. In one implementation a scope approval binds requirements digests, calibration-library digests, dataset digests, hardness-catalog digests, cohorts, trust roots, the cohort-registry root with its inclusion policy, required cohort, and maximum refresh interval, the required-instrument set, harness bytes, difficulty freshness, screening, maximum re-audit and citation horizons, difficulty bands, the frontier floor, projected tier thresholds, the task namespace, and a capability opt-in block including a generated instrument-liveness status block. In another implementation an author-side design approval binds the entire task required set, the entire harness block, and the capability opt-in block, so that any re-scope, verifier change, normalizer change, argument-vector change, threshold change, image-policy change, allow-list change, conformance-obligation change, or capability-state flip invalidates the approval.

In some embodiments the halt protocol at a gate is prescribed. The run writes the artifact awaiting approval, prints the exact approval instruction naming the file to inspect, the digest command to run, and the target path to write, records the waiting state in a derived progress cache, and stops. The run never self-approves and never fabricates an approval digest or a signature. Re-invocation of the same prompt resumes from the recorded state, never redoes finished work, and never re-opens a satisfied gate unless the bytes that gate binds have changed.

Prior art positioning: see section 8.58, information barrier, and in particular US12671588; the novelty residue here is recomputation of the approved artifact digest before the operation and a mandatory halt on mismatch, whereas identity-bound delegated authorization alone does not establish continuing equality with the approved task bytes.

### 8.15 Write-once task identity, the single bundle home, and delivery-format layering

In some embodiments a task is assigned a UUID version 5 derived from a fixed namespace and a canonical task-identity input, the canonical task-identity input being the canonical content hash computed under the bound canary-normalization domain described in section 8.9. In one implementation the namespace is pinned once at project genesis, recorded in the approved scope, and never rotated. The identifier is therefore a deterministic function of the frozen bytes it names, and the two properties compose: the identity of a bundle is computable by any party holding its bytes, and no party can hold two different byte sequences under one identity. The resulting identifier is recorded as a write-once identifier: the directory bearing it is never overwritten, and reassignment of the identifier to different task bytes is rejected. Changed bytes derive a new identifier and produce a new bundle rather than mutating an existing one, and the legacy predecessor is recorded in the lineage store 129 without being overwritten or deleted.

In some embodiments the identity construction and the canary construction are mutually acyclic by ordering rather than by convention. The canonical content hash is computed first over the placeholder-normalized bundle, the per-bundle canary token set is then derived from that hash alone by a pure key-derivation function so that the token set is a pure function of the task hash, the tokens are then planted into the designated slots, and the identifier is then derived from the same hash. Because the tokens depend on the hash and the hash is computed under a normalization that erases the token values, neither construction consumes the other's output, and a slot whose planted value is not the exact derived token is detectable from frozen bytes without reference to any authoring record.

In some embodiments each task has exactly one bundle home, and the disclosure does not maintain a second authoritative copy of bundle bytes at any other path. In one implementation the single bundle home is a directory named by the identifier beneath exactly one task lane root, the lane roots being `samples/` and `delivery/` under the routing described in section 8.32. A state transition such as promotion of a bundle into a curated public set is recorded as standing in an index file that names the identifier and its state, rather than by copying bytes to a second location. The disclosure states the reason expressly: two authoritative copies of the same bundle constitute two sources of truth that can diverge, and a reconciliation check between them can only detect a divergence that the single-home construction makes unrepresentable. In one implementation the bundle-local rollout-evidence subtrees `samples/<uuid>/trajectories/` and `delivery/<uuid>/trajectories/` hold recorded trajectories, a generated inspection page, and generated reward plots rather than a second copy of task bytes. The earlier separate mirror root is superseded, as recorded 2026-09-18. Each evidence subtree is keyed by the same write-once identifier as its containing bundle, so evidence remains joinable without creating a second authoritative task-byte home.

In some embodiments the bundle layout is layered into a base delivery format and a set of private extensions, and the two layers carry different obligations. The base delivery format is the interoperable layout that an external delivery framework requires, comprising in one implementation a task manifest `task.toml`, a task-instruction file `instruction.md`, and a test tree `tests/`, and nothing further; the manifest key naming the format revision is a schema-version key, and the reward-contract output path at which the verifier writes its result is fixed by that format. The private extensions are stricter obligations of the present disclosure that the base format neither requires nor knows about, comprising in one implementation an oracle-solution tree `solution/` carrying an executable oracle entry point `solve.sh`, a private derivation source `grounding.yaml`, a deterministic recomputation script `recompute.py`, generated private ground truth `TRUTH.md`, and a generated reference-based rubric verifier `rubrics.json`, together with a shipped provenance carrier `provenance.yaml` and a capability policy manifest `policy.yaml`. In some embodiments the extension artifacts sit inside trees the base format already defines, so that a consumer implementing only the base format reads a conforming bundle while a consumer implementing the extensions additionally obtains the private evidence. The private extension artifacts are precisely the artifacts into which canary tokens are planted under section 8.22, and an extension artifact reaching the agent-visible delivery surface forces a blocking disposition. In some embodiments a numeric reward range, a bundle-level index skeleton, and the file names of generated plots are treated as local policy of the present disclosure rather than as obligations of the base delivery format, and the disclosure does not represent local policy as format conformance, because a conformance instrument that tests local policy under the name of the format reports agreement with a specification it never consulted.

Prior art positioning: see section 8.58, identity and lanes and release authority, and in particular RFC 9562 UUIDv5; the novelty residue here is a write-once task identity with one authoritative bundle home and separately keyed bundle-local evidence, whereas deterministic identifier derivation alone does not prevent two authoritative copies from diverging.

### 8.16 Batch protocol with isolated parallel slots

In some embodiments the batch cadence is the mandatory unit of authoring work rather than an optional aggregation of separately authored tasks. The author execution domain 120 generates, per invocation, a batch of at least twenty parallel task slots, each slot having an isolated task manifest, instruction representation, execution environment, test representation, and oracle solution, and each slot designed by default against the frontier-defeat tier within its bound completion budget. A batch manifest records the batch identity and the per-slot roster, per-slot instruments are maintained under a per-slot instrument directory, and the author root report carries a per-slot disposition table. Each slot owns its own bundle directory and its own per-slot instrument directory, and no slot reads, writes, or depends on another slot's bundle bytes, checkers, or evidence. In some embodiments the system is configured to prevent an artifact produced for one task slot from satisfying a verifier of another task slot.

In some embodiments slot dispositions are reported individually and are never averaged. A per-slot disposition table lists every slot's identifier, primary archetype, target tier, any granted tier exemption, and disposition, followed by a batch roll-up; one slot's blocking disposition never caps its siblings, and each slot earns its own pilot proof and release record. A batch that closes with fewer than the required number of scoped slots is recorded as a named coverage gap with a named reason and caps the batch at a hold disposition, rather than silently authoring fewer slots. In some embodiments independent slots are worked as parallel subagent lanes that share no mutable state beyond their own outputs, the orchestrating run alone merges lane results into batch-level singletons, and a failed lane surfaces as a named gap rather than being silently dropped.

In some embodiments batch composition is constrained for coverage: every eligible archetype from an enumerated archetype set is covered at least once, no single archetype claims more than a fixed fraction of the slots, and where an eligibility map blocks an archetype the omission is recorded as a named coverage gap.

Prior art positioning: see section 8.58, author hardening, and in particular BenchEvolver arXiv 2606.01286; the novelty residue here is isolated per-slot authoring with independent dispositions and prohibition on cross-slot verifier satisfaction, whereas benchmark evolution alone does not establish those batch execution boundaries.

### 8.17 Both-halves instrument liveness

In some embodiments each deterministic grounding instrument executed by the auditor execution domain 130 is required, during a common evaluation run, both to detect a planted-defect fixture and to refrain from detecting a corresponding clean fixture, and an instrument that fails either requirement forces a blocking disposition. The same discipline is applied in the author execution domain 120 to the feasibility bundle 126, to every pure checker, to every control, and to the recomputation script: the feasibility bundle 126 must accept the reference solution and reject every known-wrong negative control, each checker must accept a known-good deliverable and reject a known-wrong one, and each control must fail below the threshold on its targeted shortcut while a legitimate solution stays above it.

In some embodiments the disclosure distinguishes three conditions that conventional practice conflates. A missing instrument is a coverage gap. A fail-closed instrument is closed on bad evidence and open on good evidence. An inert instrument is present but effectively hardcoded to a single answer, so it is closed on all evidence or open on all evidence and can never discriminate. An inert required instrument is neither a coverage gap nor fail-closed; it forces a blocking disposition, recorded in one implementation with the reason `deterministic_lane_inert`. In some embodiments the record-keeper execution domain 110 maintains a generated per-instrument status block with three fields, namely whether the defining bytes are present, whether implementation is claimed rather than merely declared, and whether liveness was proven by this run's positive control, the block being generated from filesystem and conformance results rather than hand-maintained, bound at the human sign-off gate, and failing closed on drift. A row claiming implementation while lacking a proven positive control is treated as theater and caps at broken; a scaffold in which every instrument is honestly recorded as not yet implemented is a benign stale state and is never treated as broken for that reason alone. Correspondingly, a conformance suite carrying only negative controls is itself a coverage gap, because a one-sided proof cannot distinguish a working instrument from an inert one.

Prior art positioning: see section 8.58, auditor instruments, and in particular OWASP Benchmark; the novelty residue here is both-halves fixture liveness in the same evaluation run as a blocking precondition, whereas a true-positive and false-positive corpus alone does not require a particular instrument to demonstrate both halves before it may pass.

### 8.18 The eight grounding-instrument families

In some embodiments the deterministic grounding instruments executed by the auditor execution domain 130 comprise each of eight named families: a delivery-conformance family, a reference-fidelity family, a calibration family, a verifier-robustness family, a rubric-compilation family, a contamination-screening family, a reward-integrity family, and a budget-conformance family. Each instrument declares a name, a category, a binary, an argument-vector builder, applicable ecosystems, a timeout, required conditions, a critical-capability flag, an evidence class, a parser requirement, a raw-artifact requirement, and a disposition cap on absence. Each is deterministic and is permitted to lower or cap a disposition; a clean result never raises a disposition and is never itself the reason an atom passes. Producer-bound inputs come only from the auditor projection 172 and from committed bundle bytes, never from author self-attestation, and an absent, unprojected, or unresolvable required input is a recorded coverage gap capping at a hold disposition rather than a silent pass. Instruments run inside a hermetic sandbox whose image and network policy are pinned, and an unavailable or unpinned sandbox caps affected results at a hold disposition.

The delivery-conformance family, in some embodiments, resolves every required delivery-format path, confirms that no committed artifact sits at a path the format does not define, reconciles declared steps with on-disk step trees in both directions, validates the task manifest against a schema version projected through the auditor projection 172 as bound at sign-off rather than against whatever version happens to be installed, confirms that every container image reference carries an immutable digest reference rather than a floating tag, confirms that the declared network policy matches the environment producing the committed evidence, and confirms that the verifier terminates by writing the bound reward-contract path with every committed zero score carrying a machine-readable reason. This family further includes a ground-truth regeneration instrument that re-runs the committed generator and requires the regenerated ground truth, oracle, and checker fixtures to be byte-identical to the committed bytes, and then reconciles ground-truth steps against the committed checker set by identifier set equality in both directions.

In some embodiments the ground-truth regeneration instrument is run under dual-host discipline. The committed generator is executed twice under sandbox identities differing in hostname, working directory, user identity, and environment, and the regenerated bytes are required to be byte-identical across both executions. Host-dependent output is treated as fingerprinting the auditor and is a retained finding on the same footing as regeneration drift, an unreconciled ground-truth step set, or hand-edited ground truth. The same dual-host discipline is applied to rubric regeneration. In some embodiments the author execution domain 120 performs a corresponding dual-host regeneration of its own, requiring the final planted bytes rather than the normalized bytes alone to agree across two host identities.

The reference-fidelity family, in some embodiments, obtains the real upstream artifact at a pinned version, replays the committed live checkers against that real artifact through the exact invocation contract used for model submissions, records the resulting real-upstream score, compares that score against the candidate score under a discrimination margin, and classifies every real-artifact check failure under a closed divergence taxonomy comprising an exit-code-contract class, an output-literal class, an invented-or-assumed-interface class, and a validation-locus class, recording any unclassifiable failure verbatim and capping fail-closed. An upstream that cannot be obtained at the pinned version is a recorded coverage gap with a named reason rather than a silent skip.

The calibration family, in some embodiments, recomputes cross-task ordering from each declared hardness tier and signed pilot pass-rate aggregate and retains a finding where a higher-tier task is easier than a lower-tier task by more than a bound calibration margin, and separately rebuilds and grades each frozen bundle from only its pinned image digest and recorded invocation contract inside the sandbox, with no private registry, no authoring-host state, and no hidden dependency.

The verifier-robustness family, in some embodiments, comprises seven instruments: a stability instrument that re-grades the committed reference solution a bound number of times and retains score variance; an invariance instrument that applies a bound perturbation set to recorded runner logs and replays every variant through the committed parser, retaining score changes and retaining target matching that is positional rather than set membership over normalized test names; a replay instrument that replays every committed run log and reconciles recomputed against reported score, which is mandatory because a deterministic parser defect is invisible to a reference-solution re-run; a baseline instrument that grades the unmodified base state and requires every preserve and non-target test to pass; a flakiness instrument that repeats every graded target a bound number of times and retains a flapping target; an attribution instrument that requires every zero-score result to carry a machine-readable reason and reconciles reported score with recorded target counts; and an apply-parity instrument that scores one submission through every enumerated patch-application path and retains divergence between exact and fallback paths.

The rubric-compilation family, in some embodiments, comprises five instruments: a schema instrument validating every committed rubric item against a declared closed item schema and evaluation-target vocabulary; a completeness instrument requiring set equality in both directions between compiled item identifiers and committed compiled tests and recomputing compiled weight share against a declared floor; a residue instrument grounding each judged item's stated judgment against its named evidence and retaining a judgment reducible to a deterministic relation over committed artifacts or lacking a named reason for remaining judged; a regeneration instrument running the committed generator twice under deliberately different sandbox identities and requiring byte-identical rubrics and compiled tests; and a replay instrument running compiled tests against the committed oracle and every committed known-wrong control, treating a test that accepts both as inert.

The budget-conformance family, in some embodiments, comprises three instruments and runs only where the opt-in completion-bound mechanism of section 8.7 applies. A binding instrument confirms that each task manifest carries a completion bound, an execution envelope, and a reconciliation tolerance, that each value traces to the human-authored requirements corpus that the sign-off gate bound rather than to an author-supplied default, and that no value was derived from a declared difficulty tier. An envelope instrument confirms that every duration figure relied upon was measured under the declared envelope and stamped with the bound under which it ran, and refuses to compare durations measured under different bounds or different envelopes. A reconciliation instrument compares the recorded reference-solution duration and the budget-stamped signed pilot duration aggregates against the bound, resolving a reference that exceeds the bound as rendering the task infeasible rather than hard, and resolving a pilot conducted under a smaller configured bound than the declared bound as unable to support a conformance claim. Consistently with the general rule, a locally measured duration may only lower a disposition, and only budget-stamped signed pilot duration evidence supports an affirmative conformance claim.

The contamination-screening family and the reward-integrity family are described in the following subsections.

Prior art positioning: see section 8.58, auditor instruments, and in particular OWASP Benchmark and muSE TOSEM 2021; the novelty residue here is a named roster binding delivery, fidelity, calibration, robustness, rubric, screening, reward, and budget obligations, whereas labelled fixtures and mutation-based soundness analysis alone do not define that complete evaluation-artifact coverage contract.

### 8.19 Contamination-screening roots and the six screening atoms

In some embodiments the record-keeper execution domain 110 alone owns, names, versions, and pins by immutable digest exactly four screening roots, and neither the author execution domain 120 nor the auditor execution domain 130 authors, selects, versions, amends, or reorders a root. Each root row carries a canonical identity, an immutable content digest, a schema or algorithm version, a supplier identity, issued and effective dates, and supersession lineage. A mutable pointer, a moving branch, or a floating alias caps the ledger at broken, because mutable bytes are not a measurement.

The first root is an exclusion list of public evaluation sets against which a candidate repository, fork ancestor, or commit must not match. In some embodiments the list is required to include an enumerated immutable baseline, and a missing baseline row is a coverage gap capping the disposition rather than passing. In one implementation the immutable baseline names rows for SWE-bench, SWE-bench Verified, Terminal-Bench, LiveCodeBench, and the Stack lineage. A client-supplied list is canonicalized and pinned without editing as a named source with supplier identity and effective date, and an authority mode drawn from a closed set of union and replace governs its combination with the baseline, with union as the default and with replacement applying only to the client-supplied extension, so that a narrower supplied list cannot erase a named baseline row and no supplied list, authority mode, or requirements grant can remove a baseline row from the screen. The second root is a freeze-date table carrying one versioned cutoff per named set and per release rather than a universal date. The third root is a near-duplicate index with a bound normalization algorithm, bound indexed representations, and a bound operating threshold, together with a review threshold, an enumerated normalization set, a measured detection floor, a measured floor recall, and a calibration digest identifying a pinned labelled set of known-equivalent problem pairs transformed by identifier renaming, structural reordering, interface rewriting, statement paraphrase, and test regeneration. A root with no measured floor, or a floor measured against an absent or unpinned set, is a coverage gap capping at broken, because an uncalibrated threshold reports a number nobody has shown detects anything. The fourth root is a source-attestation trust root naming the trust roots accepted for signed platform attestations of repository ancestry, source event timestamps, and generator origin, with a closed generator-origin predicate asserting generator source identity and version digest, the complete set of source corpora and artifacts by identity and digest, and the absence of every named public evaluation set among those sources.

In some embodiments the screening class carries exactly six atoms and no class-level exempt path: exclusion matching, freeze holdout, near-duplicate screening, sanitization, empty-submission validation, and a per-unit provenance record. The exclusion atom carries exactly three relations, namely canonical repository identity resolved through renames, transfers, and mirrors to a stable host-assigned identifier, membership of any ancestor in the fork-ancestry closure rather than the direct parent alone, and base commit SHA. Only those three relations and the freeze-holdout atom may resolve as not applicable, each not-applicable determination carrying a deterministically derived reason; the other four atoms are always applicable. Freeze holdout requires the task-generating-event timestamp to fall strictly later than every applicable versioned cutoff, and a rewritten or back-dated commit timestamp does not satisfy it alone. Near-duplicate screening compares every indexed representation both raw and under every bound semantic-preserving normalization, so that laundering must defeat every bound view rather than one representation, with a neighbour at or above the operating threshold blocking and a neighbour in the review band opening a mandatory recorded subordinate adjudication that may only lower a disposition and may never certify absence. Sanitization runs deterministic absence checks across the agent-facing delivery closure, the closure comprising every agent-visible bundle byte, the image manifest, image configuration, build history, and every unpacked layer including whiteouts, every runtime mount, build context, environment variable, and credential, and the network policy, bound to the exact delivery image digest so that scanned and executed images cannot differ; a merged-filesystem-only scan is expressly insufficient because lower layers retain deleted files. Empty-submission validation proves that a no-change submission leaves every intended fail-to-pass target unresolved and scores exactly zero under the bound reward schema. The per-unit provenance atom emits a generated, never hand-authored, carrier inside the delivered unit under an exhaustive closed schema that accepts no free-form prose, any unknown or extra key forcing a blocking disposition, with a canonical payload comprising its own bytes excluding every signature and excluding a later binding block, the binding block comprising the canonical bundle hash, the pinned image digest, and a signed envelope authenticating the canonical payload hash, the canonical bundle hash, and the image digest. In some embodiments the detached signature covers the signed screening measurement instant, the screening interval in days, and the absolute screening expiry, so that the delivered unit alone suffices for a downstream consumer to compute expiry without contacting the producer.

In some embodiments the atoms are executed independently in each of the author execution domain 120 and the auditor execution domain 130 against the identical roots, and neither domain's per-atom outcome crosses either projection. Only the roots, the calibrated parameters, the attestation digests, the signer identities, and the coarsened family flag cross; each recipient computes its own result. In some embodiments the auditor execution domain 130 treats the shipped provenance carrier as a permitted one-way producer-claim channel present in committed bytes rather than in the projection, because the carrier can only lower a disposition; carrier agreement with independently recomputed results never raises a disposition and never causes an atom to pass. In some embodiments the auditor execution domain 130 independently recomputes the binding block from committed bytes using the same canary-normalized canonical bundle hash used by the leak gate, so that the carrier and the leak gate never bind different domains, and divergence between the recomputed and committed blocks is a hash-bound critical floor breach.

In some embodiments a completed post-delivery re-audit is wrapped in a signed envelope of a distinct declared predicate type and delivered to the record-keeper execution domain 110, which verifies the signature against an external trust root, requires the signed role to be an auditing role rather than a pilot role, and folds the result into screening standing only. Such an envelope updates screening standing and never creates a certification record, because a re-audit measures contamination rather than difficulty.

Prior art positioning: see section 8.58, auditor instruments, and in particular OWASP Benchmark; the novelty residue here is digest-pinned screening roots and six independently reconciled atoms with absolute expiry, whereas a labelled test corpus alone does not bind the screened evaluation artifact to immutable exclusions and time-bounded standing.

### 8.20 The reward-integrity family and the reward coverage ledger

In some embodiments the reward-integrity family comprises four instruments operating inside the hermetic pinned sandbox whenever a trajectory surface is present. A reward-definition instrument composes the delivery, verifier-robustness, and rubric families with feasibility, no-op, known-wrong, and oracle-full-reward re-runs to test whether the committed checker encodes the stated objective, duplicating no check those families already own. A reward-provenance instrument re-derives every reward figure componentwise from raw per-rollout bytes before scalarization, then independently reassembles the scorer closure from committed bundle bytes, recomputes the scorer-closure digest under the canonicalization and schema version declared in section 8.10, requires exact equality against the scorer-closure digest bound by the signed pilot attestation 150, and tests the declared capability channels and the private-boundary carriers against the capability policy manifest constituent of the policy closure. A rollout-integrity instrument recomputes rollout cardinality, canonical-byte deduplication, completeness, schema, and binding, recomputes the trajectory Merkle root from committed bytes, independently recomputes the solver-population commitment from the frozen solver registry 142 entries under the same declared construction, and requires exact agreement with both the operator-signed trajectory root and the attested solver-population commitment. Because each of these three recomputations is performed from bytes the auditor execution domain 130 holds independently of the operator, agreement establishes that the audited artifacts are the measured artifacts, and disagreement resolves the dependent channels as unresolved rather than as passing. A reward-distribution instrument computes preregistered exact statistics over the reward population, opening investigation and capping where warranted, on the express understanding that a distributional anomaly alone never confirms exploitation.

In some embodiments the auditor execution domain 130 emits a reward coverage ledger with one row for every applicable pair of threat and capability channel drawn from closed vocabularies. In one implementation a reward claim is organized as a set of seven causal boundaries at which it may first become false, namely definition, input, computation, attribution, population, reduction, and evidence; a closed threat vocabulary enumerates the ways a claim becomes false at each boundary; and a closed capability vocabulary enumerates eight scorer-influence channels, namely read, write, execute, network, environment, timing, inter-process communication, and resource. A capability is treated as a means of reaching a boundary rather than as a threat in itself, and an observed influence matching no enumerated channel is recorded as a coverage gap rather than as a pass. Each row resolves to exactly one value from a closed set, and in some embodiments a certified disposition for a trajectory-bearing reward claim additionally requires every applicable row to resolve as detected, externally enforced, or deterministically derived not applicable, with a missing execution evidence, an unauthorized operator, and an unresolved capability channel each being non-waiver-eligible.

Prior art positioning: see section 8.58, auditor instruments, and in particular OWASP Benchmark and muSE TOSEM 2021; the novelty residue here is a closed threat-by-capability coverage ledger with independently recomputed reward and execution bindings, whereas labelled fixtures and mutation-based testing alone do not require every applicable reward-influence channel to resolve before certification.

### 8.21 Adversarial hardening, shortcut probes, and solution-centric mutation

In some embodiments the author execution domain 120 conducts, for the candidate task 121, an adversarial hardening sequence performed by the adversarial hardening subsystem 127 and comprising three roles. An attacker process, designated Hacker in one implementation, attempts to satisfy the verifier bytes 124 without satisfying the task-instruction bytes 123. A fixer process, designated Fixer, modifies the verifier bytes 124 in response to the attempt. A solver process, designated Solver, executes a legitimate solution against the modified verifier bytes and confirms that the legitimate solution remains accepted. In some embodiments every attack, patch, and solver confirmation is appended to an adversarial log bound to the canonical bundle hash. The system is configured to prevent advancement of the candidate task 121 unless the attempt is rejected and the legitimate solution remains accepted; a successful patch cannot itself raise the disposition above the local verification ceiling, and an undefeated verifier hack forces the blocking disposition recorded in one implementation as `BLOCK:INVALID_TASK`. In some embodiments the strongest available frontier models are additionally probed as author-side adversaries, and a clean frontier solve is treated as a defect signal that returns the task to construction rather than as pilot evidence.

In some embodiments the shortcut-probe subsystem 128 executes probes corresponding to a bound plurality of predeclared shortcut classes stored in a versioned shortcut taxonomy. In one implementation five classes comprise lexical overlap, subsequence leakage, negation, positional artifact, and style cue. Every graded outcome is scored against the taxonomy and adversarial probes attempt to satisfy each checker by exploiting each class without solving the task. A graded outcome that a taxonomy probe can satisfy is treated as a broken lever, and the system is configured to prevent submission of the candidate task 121 to the auditor execution domain 130 while such a probe succeeds. Every defeated probe and its fix accumulate into an author-internal defense pool bound to the canonical bundle hash. In some embodiments the defense pool is derived only from the author execution domain's own adversarial passes and from signed pilot failure aggregates read through the author projection 170, and never from auditor findings, trajectory detail, or auditor heuristics, because a defense pool fed from the auditor would constitute exactly the covert author-to-auditor channel the communication fabric 180 is configured to prevent.

In some embodiments the author execution domain 120 generates a child task by mutating a known-executable solution rather than by generating a new task from scratch, re-derives the task instructions and verifier bytes from the mutated solution, and accepts the child task into a mutation lineage recorded in the lineage store 129 only after determining that the child task is executable end to end through the feasibility bundle 126, that the child task is empirically harder than its parent task against a common solver population under the author domain's own adversarial passes, and that the child task is diverse from previously accepted tasks according to a predeclared diversity measure rather than being a restatement. Each lineage node is tagged with exactly one of three lineage tags, namely seed, evolved, and saturated, so that a saturated branch is retired rather than re-mutated and so that the record-keeper execution domain 110 can reason about task families.

In some embodiments the mutation step is subject to a runtime input-provenance refusal. The mutation step may consume only the author projection 170 and the bundle's own private feasibility artifacts. Where the author projection 170 is absent, or where any non-projected auditor-derived input is detected among the mutation inputs, the mutation is refused and the bundle holds. The disclosure records the reason for placing this control last and gating it most strictly: solution-centric mutation is the authoring operation most capable of laundering auditor information into task design, because mutation tuned toward auditor behavior is precisely the covert channel the architecture forbids. A mutated child carries the same disposition ceiling as any other locally produced output and is never difficulty evidence on its own; only a signed external pilot over the frozen child bytes establishes difficulty.

Prior art positioning: see section 8.58, author hardening, and in particular BenchEvolver arXiv 2606.01286; the novelty residue here is solution-centric mutation admitted only after executability, common-population hardness, and diversity checks with an author-private defense pool, whereas benchmark evolution alone does not establish that isolated acceptance conjunction.

### 8.22 Private-boundary enforcement, canary tokens, and paraphrase containment

In some embodiments privacy of answers, derivations, drift schedules, checker secrets, and pilot keys is enforced first by layout and then by a two-control leak gate operating over the assembled agent-visible bytes. Mounting behavior of a delivery framework is expressly not accepted as proof that a boundary held; the leak gate is run over the assembled agent-visible package and fails closed.

The first control is a canary tripwire that is deterministic and belongs to the deterministic lane. A fixed set of per-bundle canary tokens is derived from the canonical bundle hash through a pure key-derivation function, so that the token set is reproducible under frozen bytes and unique to the bundle. Tokens are planted inside every private artifact, including the reference solution, the derivations, the drift schedule, the checker secrets, the pilot keys, the generated ground truth, the generated rubric, and the recorded rollouts. Each private artifact carries exactly one format-valid canary block holding a contract-fixed count of ordered token slots. A missing, duplicated, malformed, or misplaced block, or a slot whose value is not the exact derived token, forces a blocking disposition. A token-shaped string outside a designated slot is ordinary content, remains inside the hash domain, and is never normalized away. The canary catches verbatim byte leakage: a planted token surfacing in the agent-visible package establishes that a private artifact crossed the boundary intact.

The second control is a paraphrase-overlap containment control, also deterministic, because a canary never fires on a reworded answer and reworded leakage is the live threat in an authoring loop. A normalized token n-gram containment of each private answer and derivation against every agent-visible carrier is computed, containment being the share of private n-grams reappearing in the agent-visible bytes. The n-gram width, the normalization, and the containment threshold are fixed in the design contract and are therefore bytes bound at the human sign-off gate. The control fires when containment meets or exceeds the threshold. A model judge is admissible only as an optional subordinate overlay on the containment control, never as a substitute for it: the judge may lower a disposition or block on semantic leakage the deterministic controls miss, may never raise a disposition, and its unavailability caps rather than passes.

In some embodiments the auditor execution domain 130 independently re-runs the entire leak gate rather than trusting the author self-scan, normalizing every designated canary slot with its own normalizer rather than the authoring one, recomputing the canonical content hash under the bound normalization domain, requiring that hash to equal the task artifact hash carried in the auditor projection 172 and to derive the bundle identifier, re-deriving the token set, and scanning the assembled agent-visible surface. In some embodiments the record-keeper execution domain 110 owns canary derivation and cross-task contamination memory, binding the derivation root to canonical task bytes under the bound canary-normalization domain, keeping issued tokens in an append-only registry keyed by task hash so that reuse across bundles is detected, and treating a leak or a reuse as a measured integrity failure that moves the affected lever to EXPIRED and that never raises a disposition. The registry is private: canary bytes cross neither projection, and only the derivation-root binding crosses.

Prior art positioning: see section 8.58, author hardening, and in particular BIG-bench canary GUID; the novelty residue here is a canary-normalized identity followed by derived-token planting and independent private-boundary rescanning, whereas a corpus canary identifier alone does not establish the acyclic identity construction or paraphrase containment.

### 8.23 Rubric drafting-provenance isolation

In some embodiments a rubric compiler operates in an execution domain isolated from author-provided solution provenance. The compiler receives committed task bytes and formal scoring criteria, commits a rubric digest before receiving judged solver trajectories, and is prevented from using a rubric whose drafting provenance includes an oracle solution or an author explanation excluded by a field policy governing the auditor projection 172.

In some embodiments each rubric item is one atomic, independently gradable obligation under a closed item schema comprising an identifier, a dimension, a weight, an evaluation target drawn from a closed vocabulary of readable grading artifacts, a criterion, a judgment, an evidence list each entry of which names an evaluation target, and a mode drawn from a closed set of compiled and judged. A judgment that is deterministically reducible over its named evidence must carry the compiled mode and must be emitted as a compiled deterministic test; the judged mode is never an authoring choice for such an item. Every judged item names the semantic residue that blocks compilation, or opens a coverage gap. Compiled tests belong to the deterministic lane; judged residue belongs to the subordinate lane, may lower a disposition, and may never raise one above the compiled tests.

In some embodiments a machine-learning model may draft criterion prose once at authoring time. The accepted string is then frozen as a literal in a private derivation source, and the deterministic regeneration script reads only that frozen literal and never invokes a model, a network, a clock, a locale, or a random source. Drafting is performed against the structured obligation rather than against raw ground-truth bytes, and only a candidate clearing a drafting acceptance predicate fixed in the design contract and bound at the human sign-off gate over style, reference literals, and consistency with the item it describes is frozen. The drafting model identity and the prompt and output digests are recorded in the private derivation source as non-evidentiary metadata that no closed schema receives and no gate reads. Drafting never changes an item's mode, so a deterministically reducible judgment remains compiled whoever wrote its criterion. A model invoked inside the regeneration script, or criterion prose that is not a frozen literal of the derivation source, forces a blocking disposition. The disclosure states the limit of the guarantee expressly: reroll history is unobservable from frozen bytes, so no clause claims a draft was the first, and the bound acceptance predicate limits what any accepted string may say, which is the whole of the guarantee.

In some embodiments an optional natural-language display of the frozen rubric may be materialized on human request. Such a display is admitted at the point of use rather than by regeneration equality: before it is opened, shown, or exported, the canonical content hash and the frozen rubric byte digest are recomputed and the display preamble is required to match both, rubric item identifier equality is required in both directions, exactly one section per item is required in canonical order, every section digest is required to match its canonical item, the planted canary block is validated, and the containment comparison is run against the assembled agent-visible closure. A candidate failing any check is deleted and treated as absent rather than retained. The display validates nothing, produces no evidence, is excluded from the canonical content hash and from every delivered surface, is named forbidden in both projections, and its presence, absence, wording, generation failure, or rejection never raises, lowers, or caps a disposition for task standing, while its bytes appearing on the agent-visible surface force a blocking disposition under the private-boundary controls.

Prior art positioning: see section 8.58, auditor instruments, and in particular Reliability without Validity arXiv 2606.19544; the novelty residue here is rubric compilation isolated from oracle provenance and committed before solver trajectories arrive, whereas judge-reliability analysis alone does not enforce that provenance and temporal boundary.

### 8.24 Conformal judge-reliability discipline

In some embodiments the auditor execution domain 130 evaluates the reliability of a machine-learning judge against blinded reference labels according to a predeclared conformal risk-control procedure, and forces an abstention or blocking disposition when the empirical coverage required by that procedure is not established. In one implementation the discipline requires at least eleven trials, position randomization across presented alternatives to control ordering bias, construction of conformal prediction sets rather than point judgments, and a perturbation suite exercising semantically neutral rewrites. A judge that fails perturbation testing is not deployed. An absent discipline caps the gate at a hold disposition. A low-confidence conformal call narrows a disposition and never raises one, consistently with the general rule that the subordinate lane may lower but never raise.

Prior art positioning: see section 8.58, auditor instruments, and in particular conformal risk control arXiv 2208.02814; the novelty residue here is a predeclared blinded-label admission gate that abstains or blocks an unreliable machine judge, whereas a risk-control method alone does not establish its mandatory use before evaluation certification.

### 8.25 Determinism, canonicalization, and reproducibility disciplines

In some embodiments every deterministic instrument is required to be pure and replayable under frozen bytes. The freshness controller 117 is run twice over frozen certification-record bytes and is required to produce byte-identical state vectors. Checkers are parsed under an abstract-syntax-tree allow-list and traced to live-state reads, and a checker that reads a planted file, an environment secret, a random source, a clock, or another forbidden source is blocked. Each graded outcome resolves to exactly one authoritative deterministic carrier and one checker path. Every reward figure is re-derived componentwise from raw per-rollout bytes before scalarization rather than accepted as a reported scalar.

In some embodiments work is differential across invocations. Every tool re-samples its inputs on each run, diffs the digests against the prior run, and reconciles only what changed, and a changed binding input re-triggers the corresponding sign-off gate. A resume preflight rebuilds phase state from disk before any phase runs, resumes at the earliest phase whose binding inputs moved rather than at the highest-numbered phase a record calls complete, invalidates that phase together with every dependent phase, and treats a phase recorded complete whose defining artifacts are absent or whose recorded digest does not match current bytes as not complete. Expensive external signed work is reused only while its complete attestation closure holds and never on artifact hash alone, the closure comprising every pinned envelope field together with the validity interval, with every outcome-moving toolchain component sitting inside the environment digest or the signed manifest, and with reuse lapsing after interval lapse, a stale cohort or registry digest, any bound-value move, or a governing-policy demand.

In some embodiments emitted artifacts obey canonicalization rules that keep digests stable and comparisons meaningful, including a rule that every paragraph of every emitted document is one continuous line that the renderer soft-wraps, so that a newline inside a paragraph is a detectable violation rather than an invisible formatting variation.

Prior art positioning: see section 8.58, identity and lanes and release authority (canonicalization), and in particular RFC 8785 JCS; the novelty residue here is replayable evaluation over an enumerated input closure with dependent-phase invalidation, whereas canonical serialization alone does not establish which changed inputs invalidate an approved execution state.

### 8.26 Governing-specification currency and the contract-currency gate

In some embodiments each of the three execution domains executes against a body of governing specification bytes that is vendored into the project as a tracked dependency rather than pinned at a frozen revision, and the governing-specification currency controller 196 conditions every invocation of every domain on the currency of those bytes. Before any phase work, the controller resolves the vendored specification checkout against the current tip of the tracked upstream branch and admits only a checkout that is clean and whose resolved revision equals the fetched tip. A checkout that is behind the tip, that has diverged from it, that carries local modifications, or that is unverifiable because the dependency, its remote, or the network cannot be reached is treated as stale.

In some embodiments a stale checkout is a hard block rather than a lowered score. The record-keeper execution domain 110 emits its root report in a broken state, the author execution domain 120 emits its root report at a blocking disposition for the entire batch and authors nothing, and the auditor execution domain 130 emits its root report at a blocking disposition and audits nothing, each recording the resolved revision, the fetched tip, and a dated staleness entry as a named coverage gap. In some embodiments the gate carries no blast-radius tier and admits no waiver: it is not a human sign-off gate, no cached currency record satisfies it, no prior approval discharges it, and no out-of-band approval envelope of the kind described in section 8.28 may stand in for it, because an instrument reasoning from superseded specification bytes certifies against obligations that no consumer of its output is held to. In some embodiments the same gate binds the out-of-band approval controller 199 on identical terms, so that an approver cannot discharge a gate under a rulebook the producing domain has already left behind.

Prior art positioning: see section 8.58, tamper-evident governance, and in particular OpenSSF Scorecard; the novelty residue here is an unwaivable equality check against the current upstream tip that treats unresolvability as stale, whereas pinned-dependency assurance alone does not require that moving-reference currency before every invocation.

### 8.27 Total prompt capture and tamper-evident feedback chains

In some embodiments each execution domain captures, without exception and without filter, every human-originated turn that a run receives, and the feedback capture subsystem 197 writes one append-only ledger record per turn. Capture is unconditional rather than conditioned on a judgment that the turn carries a correction: a first invocation, a bare re-invocation of the same specification text, a correction, a course change, a complaint, a question, a bare acknowledgement, and an argument record delivered by an out-of-band approver are all captured alike. The disclosure states the reason directly: a capture rule that fires only on a turn the run classifies as corrective lets a project run for an extended period and hold an empty ledger that is indistinguishable, at the point of consumption, from a project whose operators never intervened.

In some embodiments classification never gates capture. Each ledger record carries a verbatim copy of the turn, a kind drawn from a closed vocabulary, and a chain-sequence field naming the chain line that attests it, and a turn that resists classification is recorded under an other value rather than dropped. In one implementation the closed kind vocabulary consists of a specification-paste value, a correction value, an instruction value, a question value, an approval-record value, and an other value. A turn that produced no ledger record is recorded as a named coverage gap, so that an uncaptured human is a finding rather than a silence. In some embodiments each record additionally carries ranked machine-generated hypotheses about what the human most likely observed and which clause or artifact the turn most plausibly targets, each with a bounded confidence value, and a hypothesis is revised only by appending a further dated block, never by editing the verbatim copy or a prior block.

In some embodiments the ledger is attested by a hash chain that is machine-written and read-only to the human. Beside each domain's ledger the feedback capture subsystem 197 maintains the feedback attestation log 198 as one canonical serialized object per line under a closed field set. In one implementation that field set comprises a monotonically increasing sequence number, a capture instant, the record identifier, the record kind, a digest of the verbatim turn bytes, a digest of the ledger record bytes, a writer identity, a hosting-platform login identifying the human whose turn the line attests, the digest of the preceding line, and a digest sealing the line's own fields. Each line is therefore sealed over its own content and linked to its predecessor, so that an edit anywhere in the ledger either fails to reproduce a line digest or fails to chain. Signed checkpoint logs anchor the chain head outside the file that holds it, and every invocation publishes the current chain head in that domain's root report, so that the head is attested in a surface an editor of the chain does not control.

In some embodiments each domain's resume preflight walks its chain end to end before any phase work, recomputing each line digest and verifying each link. A chain that fails its walk is treated as tampering and hard blocks the instrument: the record-keeper execution domain 110 emits its root report in a broken state, and the author execution domain 120 and the auditor execution domain 130 each emit at a blocking disposition, in each case before any authoring, auditing, or certification work is attempted. The block is not a lowered score and is not waivable by an approval, because a ledger whose integrity is in question cannot establish which corrections a run was given, and a run that cannot establish that cannot establish that it applied them.

In some embodiments each captured turn is attributed to a named human by a fixed resolution ladder that the feedback capture subsystem 197 runs once per invocation, before the first record of that invocation is appended, as recorded 2026-09-14. The ladder consults the login of an authenticated hosting-platform command-line session first and the actor variable that a continuous-integration runtime supplies second, and the record names which rung answered. An identity that neither rung resolves is recorded under a reserved unattributed value together with a named coverage gap, and the system never infers the identity from a version-control configuration name, an electronic-mail address, or a filesystem owner, because each of those is writable by the party whose turn is being attributed. An unresolved identity never suppresses capture, so an unattributable turn is a recorded gap rather than a missing line. The resolved login is carried into the closed chain field set as well as the ledger record, so a line missing the field or carrying an empty value fails the closed-set walk and blocks phase work under the preceding paragraph.

The attribution mechanism is positioned here against named prior art. Certificate transparency binds a log entry to an issuer and proves append-only structure without asserting which operator submitted the entry, and code-signing schemes such as Sigstore bind an artifact to a workload or account identity rather than to the human turn that directed the workload. The novelty residue is that the attributed subject is an instruction rather than an artifact: the system binds each human-originated turn, captured unconditionally, to a resolved platform login inside the same tamper-evident chain that seals the turn bytes, and treats an unresolvable attribution as a blocking coverage gap rather than as an anonymous but valid entry. In some embodiments no check verifies the resolved login against an authenticated session or a signature, so the disclosure records the attribution as a recorded provenance field rather than as an authenticated one, and a stronger binding by signature over the login is prospective.

### 8.28 Out-of-band discharge of sign-off gates by a unanimous council

In some embodiments a digest-bound sign-off gate of the kind described in section 8.14 may be discharged, in place of a human approver, by the out-of-band approval controller 199, and only where an opt-in capability declaration in the gated domain's capability bytes has been flipped to an implemented state by a human-signed authorization recorded in the trust-policy store 116. Absent that authorization the gate remains human, and the authorization is itself irreducibly human because an automated grant of automated approval authority is self-attesting.

In some embodiments an out-of-band approval is expressed as a signed envelope of a distinct declared predicate type, carried in one implementation under a predicate type string `trinity.approval/v1`, and the bare digest file that the legacy gate reads is a compatibility interface rather than the authority. The envelope's closed predicate field set carries, in some embodiments, a monotonic approval identifier, the gate identity naming the governing specification, phase, and bound artifact path, the exact canonical digest the gate binds, the approver identity together with an approver role that must resolve against the project trust root as an approver role rather than being self-asserted, the producer identity naming the run that produced the bound bytes, the producer's model lineage, a council roster, the digest of a recorded failed break-attempt transcript, the verdict, a signing instant, and a validity bound after which the gate re-fires. On an approving verdict the controller writes the bound digest into the legacy digest file byte-identically to what a human approver would have written, so that the consuming domain's recompute-first check of section 8.14 passes unchanged; on a rejecting verdict no digest file is written and the gate stays shut.

In some embodiments the controller is subject to four structural constraints, each enforced by the computing system rather than asserted by policy. First, producer and approver identities must differ, and a self-signed approval fails closed exactly as if no approval existed. Second, the verdict is reached by a council of at least three seats whose model lineages are mutually distinct and each distinct from the producer's model lineage, each seat weighing the same frozen record independently and blind to the other seats' ballots, and the verdict may be approving only where every seat casts an approving ballot, so that any dissent resolves to rejection and a runtime that cannot seat the required number of mutually distinct lineages fails closed to human sign-off. The disclosure states the rationale: a blind spot that every seated lineage shares is the only blind spot a unanimity rule cannot catch, and lineage diversity is what makes that residue small. Third, approval is void without an attached transcript of a recorded break attempt that failed to break the bound bytes, the transcript digest being bound into the envelope, so that approval evidences a survived attack rather than an absence of inspection. Fourth, the controller never edits the artifact it gates and never reads a surface the gated domain may not read, so the information barrier of section 8.3 binds it exactly as it binds the domains.

In some embodiments the discharge authority is bounded by blast radius and by class of act. Lower-tier gates binding scope and design digests are dischargeable from first authorization; higher-tier release gates are dischargeable only after a bound count of consecutive externally piloted, audit-clean cycles has been met, and a controller-signed release before that count is met caps at the local verification ceiling and leaves the gate human. A further class of acts is permanently human and is never dischargeable by the controller under any authorization, comprising in one implementation publication of task bundles or of a research manuscript beyond the repository, patent filing, recording of an operational acceptance against a capped disposition, and admission of a staged candidate into the human-graded calibration library. In some embodiments every approval the controller signs is treated as a prediction that the approved artifact survives downstream evidence, a later contradicting signed pilot result, auditor finding, or human reversal is recorded as a dated false-approval record charging every seat whose approving ballot joined it, the record-keeper execution domain 110 ingests those records as fidelity input, and demotion of the controller's authority returns every affected gate to human sign-off without any edit to the governing specification bytes.

Prior art positioning: see section 8.58, council and judge reliability, and in particular PoLL arXiv 2404.18796; the novelty residue here is blind unanimity among producer-distinct lineages with a digest-bound failed-attack transcript and permanently human exclusions, whereas a panel of model judges alone does not establish that gate-discharge conjunction.

### 8.29 Candidate-calibration harvest staging under a propose-never-admit rule

In some embodiments the record-keeper execution domain 110 operates a harvest lane that extracts dataset references from admitted external research documents, pins each reference at an immutable revision digest, screens each candidate through the six screening atoms of section 8.19 against the effective screening roots, and stages surviving candidates as candidate calibration bundles in the harvest staging store 119 under a distinct declared closed schema, carried in one implementation under a schema identifier `engram.harvest/v1`.

In some embodiments the lane proposes and never admits. A staged candidate is advisory only, is never calibration input, and never influences a projection, a disposition, a lever state, or a certification record while it remains staged. Admission of a staged candidate into the human-graded calibration library is one of the permanently human acts of section 8.28: only the human copies the bundle into the calibration library, assigns its verdict, updates the verdict table, and re-signs the approved scope. The disclosure states the reason: the calibration library is the standard against which the instruments are graded, and an instrument that can enlarge its own standard grades itself.

In some embodiments harvest binds the retained artifact's content identity before following its task-corpus and harness references. A dated or versioned slice may anchor the catalog, but a source still appending to itself is only a lead. Relocation under its source class changes no content identity. In one implementation a pair carries an `engram.identity/v1` sidecar, as section 8.48 describes, and a harness whose data loader uses a mutable alias receives the advisory finding of section 8.52 rather than an inferred immutable dataset identity. CONTRACT-ONLY (ENGRAM.md:237-238, 486); ENFORCED (tools/corpus_identity.py, first landed 2026-09-20; tests/test_corpus_identity.py) covers stored identity, not the harvest admission decision.

Prior art positioning: see section 8.58, evidence admission, and in particular Scale AI US12189719 / US11308364; the novelty residue here is a staged candidate that cannot affect calibration or certification before human admission, whereas automatic benchmark admission does not preserve that propose-never-admit partition.

### 8.30 Hardness-catalog generation, projection, and the headroom floor

In some embodiments the hardness-catalog store 111 is owned exclusively by the record-keeper execution domain 110, holds one machine-readable row per configured difficulty mechanism, and is the single source of truth from which a human-readable knob sheet is regenerated byte-identically. In one implementation the machine-readable source is a file `.memory/hardness.yaml`, the generated knob sheet is a document `HARDNESS.md` carrying a do-not-hand-edit banner and naming its source, and drift between the two fails closed. Each row records the mechanism, its category, its axis, its grading mode, its orthogonality group, an anchor confidence, an already-public anchor identity together with the anchor's cohort, reported score, and report date, and the frontier-defeat target derived for that category. The author execution domain 120 never reads the machine-readable source or the generated sheet directly and receives the catalog only through the author projection 170, and the projected thresholds may only raise the difficulty floors carried in the author domain's own bytes and never lower them.

In some embodiments a headroom floor binds both sides of the catalog, expressed as a fixed multiple over anchored evidence rather than as an aspiration, and the two sides carry different multiples because they constrain different quantities. On the record-keeper side, each category's frontier-defeat target is derived from published evidence and the bound floor and is configured at or above one-and-six-tenths times the strongest anchored frontier evidence the row cites, a target headroom floor of sixty percent recorded 2026-09-14. A target below that multiple is non-conformant and remains a named gap until re-derived, and each generated row renders its computed headroom over its anchor so that a rendered headroom below the floor is itself a named gap. On the author side, the realized composition of a task over-provisions every governing tier floor by one-and-one-half times, rounding up, a composition headroom of fifty percent, so that a frontier-defeat floor of twelve instrument-bound levers drawn from nine categories composes in practice to at least eighteen instrument-bound levers drawn from at least fourteen categories. The same composition multiple applies to whatever projected threshold governs a given slot. The disclosure separates the two multiples deliberately: the target multiple governs how far a configured knob sits above the published result it cites, while the composition multiple governs how many levers a slot carries above the floor its tier names, and a single multiple over both quantities would let a generous composition substitute for an under-derived target.

In some embodiments the headroom floor is expressly a design obligation and never an evidentiary one. It never raises a disposition, never claims measured difficulty, and never substitutes for the externally controlled pilot; a composition below the floor is a named coverage gap capping at the local verification ceiling. The catalog carries no completion-bound value, because bound and difficulty are orthogonal under section 8.7: no tier derives a bound and no bound derives a tier. The disclosure states the limit directly: meeting every configured target without a signed pilot establishes that the task was built as intended, not that it is hard.

In some embodiments each catalog row carries a schema version and a row version that changes whenever its content changes, as recorded 2026-09-20. In one implementation these are `schema_version` equal to `engram.hardness/v2` and `row_version`. Confidence uses the GRADE levels and domains of section 8.49 rather than an invented certainty score. Superseded rows remain queryable through the edges of section 8.47, but the author projection excludes the superseding anchor, the edge, and the supersession kind. Neither confidence nor supersession substitutes for the external pilot or relaxes either headroom multiple. CONTRACT-ONLY (ENGRAM.md:243, 253-254, 408).

Prior art positioning: see section 8.58, evidence admission, and in particular GRADErater; the novelty residue here is a row-versioned source-grounded catalog with distinct target and composition headroom floors that never certify difficulty, whereas evidence-confidence appraisal alone does not establish those design-only constraints and their projection boundary.

### 8.31 Single-actor operation and scope of the disclosure

In some embodiments a single certification-platform operator performs or causes the performance of every step described herein, including establishing the access-controlled execution domains, configuring the communication fabric 180, operating the projection engine 114, freezing the candidate task 121 and the solver registry 142, causing the pilot to be conducted, routing the representations 151 and 152, and operating the certification controller 160, notwithstanding that a separate execution operator holding an externally rooted credential signs the pre-execution commitment 143 and the pilot attestation 150. The externality of the credential is a property of the issuance and authorization rules recorded in the trust-policy store 116 and of the exclusion of author and auditor principals from obtaining or presenting the credential; it is not a requirement that the credential be held by a distinct legal entity, and in some embodiments the credential is held by a distinct organizational unit, a distinct automation identity, or a distinct software key custodian of the same operator. Section 8.39 describes the software-only signing policy that governs every signing principal of the described implementation, so the disclosure does not rely on a hardware-backed custodian to establish externality.

The particular file names, directory names, predicate type strings, disposition strings, state names, role names, vocabulary sizes, thresholds, and counts recited in this description are illustrative of specific implementations and are not limiting. Where a numeric parameter is recited, such as a minimum of twenty parallel task slots, a minimum of eleven judge trials, a minimum of three council seats, a one-and-one-half headroom multiple, or a lever and category count for a tier floor, other values may be used, and in some embodiments the value is itself a bound parameter carried in an approved scope and re-triggering sign-off when changed. Where a specific hash function, signature scheme, envelope format, statement format, or confidence-bound estimator is recited, any functionally comparable substitute may be used. Where a domain is described as a directory-owning prompt-contract instrument executed on a coding-agent runtime, the same domain may in other embodiments be a conventional service, and the described access-control boundaries may be enforced by any mechanism the computing system provides. Features described in connection with one embodiment may be combined with features described in connection with another embodiment except where such combination is technically inconsistent, and the scope of the invention is defined by the claims that follow rather than by the illustrative embodiments. The subsections numbered 8.32 through 8.44 describe further mechanisms of the same system, and the scope statements of this subsection govern those subsections on identical terms.

In some embodiments the acts by which this disclosure is prepared are themselves bounded by the permanently human class of section 8.28. The drafting party reconciles this description against the current governing specification bytes and never files it. Filing remains a human act that no execution domain, no out-of-band approval controller, and no conductor may perform or discharge. The same rule holds for publication of a task bundle or a manuscript beyond the repository, for recording an operational acceptance against a capped disposition, and for admission of a staged candidate into the human-graded calibration library. The drafting party is the Trinity project of Ethara AI, whose governing specification bytes are published at <https://github.com/Ethara-Ai/trinity>, and a scribe pass over this description updates the description alone. No statement in this disclosure asserts that any filing has been made, that any described service has been deployed, or that any pass rate has been measured; every quantity recited is a configured parameter, a bound, or a schema field.

The scope statements also govern sections 8.45 onward. Planned divisional or continuation subject matter includes `tamper-evident-governance`, "Tamper-Evident Capture, Attribution, and Anti-Sabotage Control of Agent-Operated Repositories", and `release-authority`, "Externally Governed Release Authority with Threshold Dispositions, Control Receipts, and Software-Only Signing for Evaluation Artifacts". It also includes `council-gate`, "Out-of-Band Discharge of Digest-Bound Sign-Off Gates by a Unanimous Multi-Lineage Model Council, with Hazard-Scoped Conduct of Isolated Instruments", and `identity-lanes-epochs`, "Canary-Normalized Write-Once Task Identity, Structural Lane Routing, Sealed Queues, and Epoch-Published Multi-Runner Memory". Further subjects are `evidence-admission`, "Tiered Evidence Admission with Exhaustion Receipts, Supersession Edges, and Blinded Hardness Memory for Benchmark Authoring", and `auditor-instruments`, "Both-Halves Liveness of Deterministic Grounding Instruments, Three-Valued Execution-Fidelity Forensics, and Conformal Admission of Machine Judges". The seventh is `author-hardening`, "Isolated Batch Authoring of Evaluation Tasks with Adversarial Hardening, Shortcut Probes, Solution-Centric Mutation, and Discovery Asymmetry". These are working titles, and `patent/portfolio.md` carries the family map. Filing decisions, including any choice of divisional or continuation procedure, remain human and PROSPECTIVE. This reconciliation is draft-only because this contract repository holds no memory ledger; the dated CHANGELOG is its research log, and no ledger digest or measured outcome is invented.

### 8.32 Task lanes, the structural sample ceiling, and anchor starters

In some embodiments every task slot of a batch binds a task lane at the digest-bound human sign-off gate that binds the batch, and the lane value is drawn from a closed set of exactly three values, designated `starter`, `sample`, and `delivery` in one implementation. The lane is routing rather than classification. It names the task lane root beneath which that slot's single bundle home of section 8.15 is written, with starter and sample slots writing under a curated sample root and delivery slots writing under a separate delivery root. The author contract states the routing at its invariant 35 and the record-keeper contract states the partition at its invariant E37.

In some embodiments the registered lane binds once and remains immutable for the life of an identifier, while physical residency follows deterministic merge-time reconciliation. The earlier no-move residency arrangement is superseded by anchor graduation and overflow routing, as recorded 2026-09-18; neither changes the registered lane or creates a second bundle home. No identifier may reside beneath two lane roots. Residency under two roots is refused under a named overlap reason that forces a blocking disposition, and an assignment beyond a structural ceiling is refused under a named quota reason that caps at a hold disposition. A deterministic grounding instrument re-derives residency from committed bytes across both roots and compares it with the registered lane and its lawful reconciliation standing, not a declaration alone. ENFORCED (tools/lanes.py, first landed 2026-09-17; tests/test_lanes.py) covers write-once registration; ENFORCED (tools/pipeline.py, first landed 2026-09-16; tests/test_pipeline.py) covers residency and reconciliation.

In some embodiments the sample root carries a structural ceiling that no human grant relaxes, fixed at thirty resident bundles in one implementation, with starter bundles counted inside that same constant. The ceiling sits in the same class as the twenty-slot batch floor of section 8.16 and the bound single-accelerator execution envelope, meaning that it is a constant of the architecture rather than a policy value a requirements grant may raise. Because the lane is per-slot, the batch floor and the ceiling never collide. Assignment walks the batch's stable scoped slot order, designated starters consume capacity first, eligible ordinary slots consume the remaining sample capacity, and every surplus slot routes to the delivery lane instead of shrinking the batch below its floor.

In some embodiments a starter is the designated anchor task, so the anchor designation and the starter lane name one object rather than two registries that can disagree. The designation is a grant carried in the human-authored requirements corpus, comprises at least three and at most five tasks spanning at least two distinct axes, and a sixth designated anchor is reported as a defect of the requirements corpus rather than honoured. Anchors are piloted first, so the project learns what its own levers cost in elapsed time before it commits a batch to a composition. That learning shapes design and never becomes difficulty evidence, consistently with the orthogonality rule of section 8.7. As recorded 2026-09-18, an anchor holds a sample seat only until its signed duration outcome folds. In one implementation ENGRAM publishes `.memory/anchor_standing.yaml` with every epoch, carrying only identifier and `CANDIDATE`, `ANCHORED`, or `SUPERSEDED` standing. Reconciliation reads that file rather than the private fitted mapping, moves an anchored or superseded anchor into delivery, and seats the next ordinary bundle by seal order. An unreadable standing file graduates nothing and records a gap. ENFORCED (tools/pipeline.py, first landed 2026-09-16; tests/test_pipeline.py) covers that movement using tools/lanes.py; CONTRACT-ONLY (ENGRAM.md:247) covers the evidentiary fold. DEFERRED (DEFERRED.md: "Starter anchor cardinality and identity"; "Fitted time-anchor mapping") preserves the distinction between routing code and unimplemented evidence checks.

The disclosure records an earlier arrangement as superseded. That arrangement kept a working copy and a delivered copy of one bundle's bytes under two separate roots and reconciled them by a twin check, which can only detect a divergence after it exists. The partition described here keeps every identifier under exactly one root, so there is no second copy from which to drift and the reconciliation check has no subject. Prior art positioning: the Chinese Wall policy of Brewer and Nash partitions subjects into conflict-of-interest classes computed from a subject's accumulated access history, and conventional storage quotas cap capacity by configuration. The novelty residue here is a write-once routing field bound at a digest-bound human gate, fixing a single authoritative bundle home whose residency is enforced by re-derivation from committed bytes rather than by the declaration, together with a capacity ceiling declared structural so that no grant, projection, or operator argument may raise it.

### 8.33 The measured pass ceiling and the familywise exact bound

In some embodiments a measured pass ceiling caps the pass rate at which a task may be released, and the ceiling is distinct from the composition headroom of section 8.30. Section 8.30 over-provisions design-time lever counts, while the ceiling constrains the measured quantity itself. In one implementation the ceiling is bound in the frozen probe parameter block under the name `measured_pass_ceiling` on a pass-at-eight rate scale with a conservative default of 0.40, as recorded 2026-09-14. The ceiling is carried by name. A requirements grant, a recorded calibration disposition, or a stricter projected threshold may lower it, and no source may raise it above that default.

In some embodiments the effective frontier-defeat floor applied at the pilot sits at or below the ceiling, so a slot earns a certified disposition only where the corrected exact upper confidence bound on every primary solver's pass-at-eight rate stays strictly below that floor. The correction is familywise across every declared attempt and every primary solver, and the corrected Clopper-Pearson upper bound rather than any single raw attempt is the release condition. Each pass-at-eight rate is estimated from at least eight independent pass-at-eight groups per solver under an unbiased combinatorial estimator over the recorded rollouts, with the sampling temperature pinned in the frozen probe configuration and matched between probe and pilot. A pilot too small to place the upper bound below the ceiling at the predeclared confidence resolves to the underpowered hold disposition of section 8.9, and a pilot evaluated against a floor above the ceiling is void for release purposes.

In some embodiments the ceiling caps and never certifies. A bound below the ceiling is a necessary release condition and is never difficulty evidence on its own, and the externally controlled pilot of section 8.9 remains the sole source of difficulty evidence. In one implementation the controller evaluating this predicate validates a declared ceiling as a value inside the half-open interval from zero to the default and refuses a declared ceiling outside that interval under a named refusal. The same controller refuses a complete attempt registry whose recomputed familywise Clopper-Pearson bound sits at or above the declared ceiling under a second named refusal, while a provisional registry below its attempt budget stays provisional and a policy declaring no ceiling is unaffected, as recorded 2026-09-14.

Prior art positioning: mutation adequacy fixes a threshold on a measured mutation score and admits a test suite only above it, and the supply-chain assurance levels of SLSA grade the strength of a production process rather than the outcome of a measurement. The novelty residue here is that the acceptance threshold is a constituent of the same execution-control policy digest that the pre-execution commitment of section 8.9 binds, so the threshold cannot be tuned after outcomes are visible, and that the ceiling is monotone downward across every source permitted to set it, so the architecture admits tightening and structurally refuses loosening.

### 8.34 Task economics computed from granted rates

In some embodiments the system computes what a task cost the project to build and what that task is priced at, and it computes both only from monetary rates a human granted. The mechanism is opt-in per project. Where the human-authored requirements corpus declares neither a rate card nor a pricing policy, the project binds neither and carries no economics obligation, and the system invents no default. Where the corpus declares them, the author execution domain 120 binds the rate card and the pricing policy at the same gate that binds the batch, as the author contract states at its Phase 1 item 2e.

In some embodiments the rate card declares monetary rate classes for tokens, instance-hour compute, accelerator-hour compute, and a human day, each carried by name alone and never authored by an execution domain. It also declares the structural workday length that normalizes an authoring-effort count, which is eight hours unless the requirements corpus names another. The pricing policy declares the margin over cost by name alone and never by default. The author execution domain 120 meters its authoring lanes, and a distinct signed consumption receipt resolves the authoring and complete stages of a cost outcome, so a cost figure names the stage at which it resolved rather than mixing an open estimate with a settled one. An enabled economics binding that cannot resolve fails the affected work closed.

In some embodiments cost and price are structurally excluded from the difficulty plane. The hardness catalog carries no cost value, no tier derives a cost, no cost derives a tier, and a cost figure is never evidence of hardness. In one implementation the record-keeper execution domain 110 folds every stage-complete signed cost outcome admitted in one reconciliation pass into a running aggregate and appends exactly one revision for that pass, the revision carrying a running average cost, a running average price, a fold count, a dispersion bound, the rate-card and pricing-policy stamp, and every consumed envelope digest. The aggregate stores no per-task row, a fold count below a bound minimum projects no pair, and an average over a single task is refused as an average. Per-task cost and price figures, per-lane and per-rollout token totals, compute figures, authoring-effort counts, resolved rate values, and the envelope digest behind a revision are named forbidden fields of the author projection 170, and the auditor projection 172 carries no economics field at all.

Prior art positioning: the Sarbanes-Oxley auditor-independence rules separate fee and audit authority, and activity-based costing attributes cost to a produced unit. The statutory reference names the existing auditor-independence comparison rather than asserting a new retrieved art item. Trillian Verifiable Log-Derived Map supplies the named register comparison for verifiable derived state. The novelty residue here is that the pricing inputs are confined to a human-write-only corpus bound by a digest-bound approval, that the resulting figures are named forbidden fields of both computed projections so that neither production role can read them, and that the architecture therefore makes the statement that an expensive task is a hard task unrepresentable rather than merely discouraged.

### 8.35 Grant proposal surfaces and the human-write-only input roots

In some embodiments two input roots are human-write-only: a requirements corpus holding human-authored grants and a calibration library holding human-graded calibration bundles. Both are read-only to every execution domain. In one implementation the parent gate reads version-control history and refuses a commit touching either root under an agent-attributed or bot-attributed identity, and refuses a shallow or unattributable history outright rather than passing it.

In some embodiments a value an agent receives during an invocation never reaches those roots by transcription, because a transcription is an agent write however human its source. In one implementation a budget, a saturation floor, a rate, a margin, or an authoring-effort count supplied in conversation is captured verbatim in the receiving domain's feedback ledger and is then routed to a proposal surface rather than to the requirements corpus, as recorded 2026-09-14. The record-keeper execution domain 110 stages such a value as a dated grant-proposal record under a brief surface whose sole writer it is, and the author execution domain 120 instead keeps the value on its own harness surfaces and raises a to-do row naming the exact requirements row a human must author. Each path then halts under the halt protocol of section 8.14 and binds the value only after the human-authored row lands and the sign-off gate re-approves the bytes.

In some embodiments a staged proposal is never a grant. It binds nothing, authorizes nothing, and is recorded superseded once the human authors or declines the matching row. The disclosure states the reason for the asymmetric routing directly: the brief surface has the record-keeper execution domain 110 as its sole writer, so a proposal written there by the author execution domain 120 would open exactly the author-to-auditor channel the communication fabric 180 is configured to prevent.

Prior art positioning: the Biba integrity model forbids a low-integrity subject from writing to a high-integrity object, and the Bell-LaPadula lattice governs read-down and write-up by label. The novelty residue here is that the integrity boundary is evaluated on the provenance of the writing act rather than on the content of the value, so an agent may not write a value a human dictated, while a bounded proposal surface supplies a lawful channel that carries no authority and whose records expire by supersession once the human acts.

### 8.36 The conductor work order, fixed movement order, and hazard-scoped gate holds

In some embodiments a conductor stands outside the three execution domains and performs none of their work. The conductor authors no task, audits no surface, records no lever, and writes no byte into a harness the three domains own. It owns exactly one harness, disjoint from the three domain harnesses and from the out-of-band approval controller's harness, and writes nothing outside that harness beyond its own root report and its own thin front door. In one implementation the conductor harness holds an append-only cycle ledger carrying one dated record per movement and a derived non-authoritative progress cache. The conductor stands as a standalone work order, as recorded 2026-09-07.

In some embodiments the conductor's read set is restricted so that sequencing cannot become a covert channel. It reads only the three root reports, its own harness, and the count of resident bundle directories under the sample lane root. It never reads a computed projection, a raw ledger, a proof store, or any harness internal, and it never relays a finding, a pass criterion, a hardness target, or a difficulty claim between lanes. What it carries across a cycle is dispositions, gate names, stop reasons, digests, and the residency count, and never the bytes behind them. Each movement launches one lane whose prompt names the governing specification file to execute and the verified project root, and nothing more.

In some embodiments a cycle is three movements in fixed order: the record-keeper execution domain 110, then the author execution domain 120, then the auditor execution domain 130. The record keeper runs first and never beside its peers, because its projections are inputs to both and a peer reading a projection mid-write reads a view that no party can recompute. The next cycle's record-keeper movement is also the reconciliation of the last, so the conductor carries no reconciliation state of its own.

In some embodiments an undischarged human gate holds exactly the movements it hazards, under a hazard relation the specifications fix rather than one the conductor computes per run, as recorded 2026-09-14. The record keeper's projections feed both peers, so an open record-keeper gate holds the author and the auditor alike. The author and the auditor share no channel, so an open author gate never holds the auditor's launch, and a movement outside the hazard runs to its own stop with the gate still open on the consolidated sheet. The conductor writes the gate sheet at the moment a lane halts, presents the verbatim approval instruction, and waits, and it never writes an approval digest, never fabricates a signature or a pilot result, and never discharges a gate. The cycle boundary is a synchronization barrier: a new cycle opens only when every gate of the closing cycle is discharged and every lane has finished, and a gate still open when the loop would otherwise continue stops the invocation under a named pending-gates reason. Two consecutive cycles with identical report fingerprints, identical residency counts, and an identical pending-gate set stop the loop under a named steady-state reason.

Prior art positioning: auditor-independence statute separates engagement management from the audit opinion, the Chinese Wall policy denies a subject access across conflicting classes, and conventional workflow orchestrators sequence tasks while reading and forwarding their outputs. The novelty residue here is an orchestrator whose read set is confined to derived report surfaces and a residency count, so that sequencing carries no substantive content between the roles it sequences, combined with a hold relation fixed in the specification text so that the blast radius of a pending human gate is a property of the architecture rather than a decision the orchestrator makes.

### 8.37 Signed machine-readable disposition records and the separate governance publisher

In some embodiments a disposition is a machine record rather than report prose, and the local gate that a candidate workspace runs is structurally incapable of minting the terminal release value. In one implementation the local gate mints only a blocking value, a hold value, and a substantive release-eligible qualification, and never a final release value, as recorded 2026-09-16. The disposition line of each root report is copied verbatim from the record the gate minted for that domain, and a report stating a token above its record is refused under a named unbound-disposition classification of section 8.38.

In some embodiments final release authority lives in a separate governance publisher repository that runs an independently pinned verifier over immutable candidate and export snapshots. The publisher's expected repository identities, verifier and trust commits, trust roots, signer policy, trusted-root version, and closed release policy are fixed outside the candidate tree and are not operator inputs. The release record binds the complete export snapshot digest, the canonical bundle-set identity, the immutable candidate commit, the repository and project identity, and the trusted verifier revision. Missing external trust, missing acceptance evidence, or a missing final disposition from either instrument fails closed, and no waiver promotes an operational acceptance to a release.

In some embodiments new final author-side and auditor-side disposition records default to a closed group predicate type, carried in one implementation under the type string `trinity.release-disposition/v3`, evaluated under externally enrolled producer-role and approver-role thresholds rather than under a single named approver, as recorded 2026-09-17. Existing valid signed records of the preceding version remain accepted and are never reserialized, and an explicit named-approver invocation selects that compatibility path. Individual principals sign partial envelopes with their own software private keys, inspect authenticated role progress through a status command, and combine only exact matching partial envelopes through a repeatable envelope argument, so no principal assembles a threshold from envelopes that do not agree byte for byte.

In some embodiments the publisher additionally requires exactly one authorized execution receipt per bundle, carried in one implementation under the predicate type string `trinity.oracle-run/v4` and evaluated under closed `trinity.release-policy/v3`, as recorded 2026-09-18. Each receipt carries exactly one outcome for every required control under one manifest closure digest, and the controls manifest is required to include six core controls: build assets and harness; oracle together with no-op, known-wrong, and adversary integrity; alternate correct solution together with mutant ladder, grading, and instruction scope; judge completeness together with repeatability; trial together with reward accounting; and execution fidelity, carried as `execution_fidelity`. A missing, failed, skipped, deferred, waived, duplicate, or extra control refuses release, and only trusted policy may mark the judge control non-applicable; execution fidelity is never non-applicable. Configuration is pinned under `trinity.harness-config/v1`, group policy and per-rollout conformance are carried under `trinity.pilot-policy/v2` and `trinity.pilot-attempt/v2`, and externally signed `trinity.execution/v2` binds the effective configuration, roster, observation capture, and telemetry. The authorized external execution signer is trusted to have observed the outcomes, and the verifier never runs the benchmark. The disclosure records that the external quality-controls runner remains absent in the described implementation, so the cheap-adversary matrix, the alternate correct solutions, the mutant ladder, the graded-literal reconciliation, and the independent judge replay remain registered blockers rather than satisfied controls.

Prior art positioning: in-toto binds a supply-chain step to a signed link and a layout, TUF assigns role thresholds over repository metadata and protects against rollback, Sigstore binds a signature to a workload identity, and SLSA grades build provenance. The novelty residue here has three parts. Threshold roles govern a disposition over evaluation evidence rather than over a build artifact. The producing workspace is architecturally unable to mint the terminal value, however complete its local evidence. The verifier is forbidden from executing the artifact it authorizes, so an authorized receipt of an externally observed control outcome rather than a verifier re-run is the evidence the release rests on.

### 8.38 Sabotage classification and the historical sabotage doctrine

In some embodiments a classifier runs in the project root and registers a closed set of refusals over commit shape and workspace state, as recorded 2026-09-16. In one implementation the registered refusals comprise nine conditions. Four of them concern evidence integrity: a disposition that no signed record binds, a disposition whose signature does not verify, an inert required instrument detected by abstract-syntax-tree scan of the auditor and author harnesses, and an uncommitted auditor evidence surface. Five concern authority and workspace state: a governing-specification remote other than the canonical remote, a governing-specification checkout off the tracked branch, a typed sign-off standing in place of a recomputed digest, a governance edit committed beside its subject, and a dirty working tree. Each refusal ships with fixtures proving both halves of its decision.

In some embodiments the governance paths for the same-commit rule are the trust roots file, the signer policy, the trusted-root version, and the root-rotation directory, so a commit combining a governance edit with a root report, a disposition, or a specification subject produces the workflow-tamper refusal while a governance-only rotation commit remains clean, as recorded 2026-09-16. The disclosure states the limit of this control directly: it is a narrow commit-shape detector, it is not external trust, and it does not prevent an administrator from changing governance.

In some embodiments every refusal is appended to a hash-chained ledger at the project root, a repeated attempt inside a rolling window raises a repeat classification and caps the disposition at blocking until a principal distinct from the actor records a clearance, and continuous integration re-derives both the ledger and the refusal from the pushed commit on runners the operator does not administer. A finding indicates suspicion that the gate records and never proof of intent.

In some embodiments the disclosure states a historical sabotage doctrine, as recorded 2026-09-17. Rewriting, force-pushing, replacing, or padding published history so that a finding leaves the scan window is itself a sabotage attempt and is never a recovery, and no specification, instrument, root report, or delegated lane may recommend it. The only remedy is an append-only signed remediation record authorized by a distinct externally enrolled principal group, never by a key the flagged commit introduced, binding the flagged commit identities, the corrective commits, the re-verified trust snapshot, and the rerun evidence, and preserving the flagged commits and their finding as evidence. The prescribed recovery is forward-only and each run names it beside the finding: review the flagged diff, re-establish authority through the externally governed rotation ceremony so no key the flagged commit introduced remains trusted, land governance and subject corrections as separate commits, rerun the gate on the corrected snapshot, and submit the remediation record for the distinct principal group to sign. In one implementation the refusal message itself carries that recovery and a registered test refuses a message recommending a history rewrite. The disclosure records that no check yet reads such a remediation record, so a historical finding truthfully holds at blocking until one lands, and the recorded standing is the honest state rather than a defect to erase.

Prior art positioning: conventional history-rewriting controls deny force-pushes on protected branches, require signed tags, and preserve reflogs, and certificate transparency detects an equivocating log by gossip between monitors over an append-only structure. The novelty residue here has two parts. The act of erasing the evidence is classified as an instance of the same class the detector reports, so an operator cannot reach a clean scan by rewriting. The remedy is defined as an append-only record signed by a principal group disjoint from the flagged authority rather than as a return to a clean state, which makes preservation of the finding a precondition of clearing it.

### 8.39 Software-only signing and the canonical byte profile

In some embodiments every signing principal of the system uses a software private key, and the policy is a refusal rather than a preference, as recorded 2026-09-16. Hardware security keys, physical tokens, discovery protocols for such tokens, and hardware security modules are forbidden, together with the two security-key signature algorithm names and both of their certificate forms. In one implementation the signing backend parses bounded private-key public headers before signing, parses public-key and signature fields together with the public blobs of the allowed-signers file before verification, and refuses a forbidden algorithm under a named refusal before any subprocess runs and before any physical device is discovered. The backend retains software Ed25519, RSA, and the three standard elliptic-curve variants. The documented key-generation command uses a prompted passphrase and a project-relative path. The disclosure states the reason for the refusal posture: a policy that calls a hardware custodian optional leaves a path an operator may take, and a verifier that has begun discovering a device has already left the byte domain in which every other check of this disclosure operates.

In some embodiments the signing suite is a dependency-free implementation of a signing envelope and an attestation statement, and it fixes one named canonical byte profile that every digest and signature input uses. In one implementation the profile is identified by the string `trinity.jcs-lf.v1` and is defined as RFC 8785 canonical JSON followed by exactly one terminating line feed byte, that byte being part of every digest and signature input under the profile. The profile refuses lone surrogates, non-finite numbers, duplicate object keys, cyclic containers, integers outside the exactly representable binary64 range, and nesting past sixty-four levels. The envelope implementation builds its pre-authentication encoding and verifies against the exact transmitted payload bytes and never against a reserialization, because reserializing reintroduces the parser differentials the envelope construction exists to close. A domain-specific pre-transform such as the canary normalization of section 8.9 runs before the profile and carries its own separate domain identifier and version.

Prior art positioning: Sigstore, TUF, and in-toto specify trust roles, metadata, and link formats while leaving key custody largely to deployment, and JSON canonicalization schemes and signing envelopes are each established independently. The novelty residue here is a custody policy the verifier enforces by parsing carrier bytes before any device discovery occurs, combined with a single named byte profile whose terminating byte is inside the signature input, so that a disagreement between two canonicalizer implementations surfaces as a signature failure rather than as a silent divergence between two parties that each believe they signed the same value.

### 8.40 Versioned project-structure migration

In some embodiments the project structure itself is migrated by a versioned migrator rather than by a prose instruction to rename directories, as recorded 2026-09-16. In one implementation the migrator owns a closed journal schema, takes an exclusive lock, performs atomic writes, records immutable content-addressed backups, supports rollback and retry, accepts only canonical project-root paths, and emits an actionable hold report naming the exact record and action when it declines to proceed.

In some embodiments the supported matrix is closed and separately published rather than open-ended. In one implementation it comprises bootstrapping a missing project manifest, upgrading bare standard paths to the canonical hidden sibling layout, upgrading recognized and retired delivery-format schema fields through the existing format migration recipe, and accepting a historical queue digest only where that digest independently seals the current bytes. Task identifiers and pending queue progress survive the migration. Existing verdicts and existing signed disposition bytes are left untouched, while affected receipts, audits, and signatures require current revalidation rather than grandfathering.

In some embodiments the migrator is confined to authoring preflight. An authoring preflight applies the supported safe plan before phase work, while the check, report, signing, promotion, and release paths are read-only with respect to migration, so a migration is never usable as release verification. The migrator holds on an unknown structure version, an unknown disposition format, an unsafe or symbolic-link path, changed evidence, an unproved legacy seal, or a required revalidation. It never moves an arbitrary directory, never reads or creates a key, never edits an approval or a trust root, never changes role membership or a threshold, and never rewrites a signed envelope. The disclosure states the governing principle directly: the migrator may carry bytes forward and may never carry authority forward, so a signature over pre-migration bytes never approves post-migration bytes and changed bytes require ordinary validation, audit, and signature again.

Prior art positioning: TUF protects against metadata rollback by requiring monotone version numbers and a signed snapshot, schema-migration frameworks apply ordered transformations under a recorded version, and SLSA records how an artifact was produced. The novelty residue here is that the migration is defined as compatibility rather than grandfathering and is bounded by an explicitly closed matrix, that unknown metadata is a hold rather than a pass-through, and that the migrator is structurally excluded from every path that grants, evaluates, or records authority, so a structural upgrade can never become an implicit re-approval.

### 8.41 The sealed bundle queue and the delivery-format currency pin

In some embodiments the author execution domain 120 and the auditor execution domain 130 are permitted to run as one pipeline movement without acquiring a channel between them, by way of an append-only hash-chained queue that the conductor of section 8.36 owns, as recorded 2026-09-16. In one implementation the author seals each slot into the queue the moment that slot's deterministic bucket passes, and the auditor claims, audits, and releases sealed bundles as they appear. Each queue record carries only an identifier, a bundle digest, a seal instant, and a producer run identity, and a record carrying any other field stops the movement under a named chain-broken refusal. Neither lane reads the other's harness. A parent check additionally refuses a mutated sealed bundle, an unsealed audit, queue backpressure past a bound unaudited depth, a claim older than its bound lifetime, and an invalid queue configuration. Because the queue carries digests and identities alone, the concurrency it enables is throughput rather than communication.

In some embodiments the delivery format the bundles conform to is pinned by currency rather than by freezing, as recorded 2026-09-16. In one implementation a resolver writes a lock file from the format's package index recording the current stable release with its release date, resolution instant, and source; a validator runs the format's own manifest validator under that pinned release in an isolated interpreter and writes digest-bound receipts; and a migration path upgrades a manifest authored before a named release boundary. Two parent checks refuse a missing, malformed, stale, or untested pin, a missing, malformed, or stale lock, a drifted or invalid manifest schema, and a tampered or missing receipt. The record-keeper execution domain 110 tracks format releases on the same footing as it tracks external research documents, a changed schema digest is recorded as a format-drift row that raises a delivery integrity priority so the author re-pins at the next batch open, and a lock older than a bound interval is a named coverage gap.

Prior art positioning: in-toto layouts order supply-chain steps and bind each to a signed link, SLSA grades the provenance of a produced artifact, and conventional dependency lock files freeze a resolved revision. The novelty residue here is a producer-consumer channel deliberately reduced to digests and identities so that pipelining two mutually isolated roles cannot become the covert channel the architecture forbids, together with a dependency pin inverted from the conventional one: the pin is required to equal the current upstream release, an exact but never stale value, and an aged lock is treated as a coverage gap rather than as stability.

### 8.42 The secretless candidate sentinel and the separately governed observer

In some embodiments the workspace under observation runs a diagnostic that holds no authority. In one implementation the candidate workflow is a static diagnostic with no secrets, no dispatch capability, no delivery capability, and no release authority, it runs only an immutable reviewed probe over candidate bytes, and it never executes candidate scripts, as recorded 2026-09-16. Authority instead lives in a separately governed observer installed only in its own repository. The observer accepts dispatch only from a deployed allowlisted application bridge, authenticates the sender identity and its own source identity from governance configuration, resolves the target repository, run, actor, ref, and commit through the hosting platform interface, checks that commit out as data, and reruns the same probe read-only. Candidate installation of the observer workflow is forbidden, and the local installed-gate check compares only the local scaffold against candidate templates and cannot prove that any external installation exists.

In some embodiments the observer's state is transport-free, as recorded 2026-09-17. The incident store retains the authenticated source and target identity, a closed bounded incident schema, equivocation evidence, threshold logic, and durable repeat-alert records in external relational state, and it exposes bounded status and check commands that return a nonzero result only for a meaningful repeat alert. There is no reset path, no recipient, no sender configuration, no credential lookup, no delivery scheduling, and no messaging substitute. A versioned migration preserves existing incident records, renames the complete historical delivery-queue table to an inert legacy name, seeds repeat-alert history, and preserves the exact prior configuration bytes together with unknown fields in a private backup before activating the closed successor configuration schema. The disclosure records that a finding indicates suspected tampering and does not prove intent, and that nothing here asserts that the bridge, the application credentials, the allowlists, the observer runner, the durable volume, the repository rulesets, or any operational monitoring has been deployed.

Prior art positioning: certificate transparency separates logs from monitors and auditors and detects an equivocating log by independent observation, and conventional continuous-integration alerting grants the observed pipeline the credentials with which it reports on itself. The novelty residue here is that the observed party's own automation is deliberately stripped of every capability by which it could suppress, forge, or address an observation, and that the observer establishes an incident by re-executing the identical immutable probe over the same commit treated as data, so an admitted incident is a reproduction rather than a report the observed party authored.

### 8.43 Absolute path containment and canonical project-root paths

In some embodiments the project root is defined by a structural relation rather than by configuration. The parent project root is exactly the directory whose direct child is the vendored governing-specification checkout, never that directory's own parent and never any higher ancestor. Every path an execution domain resolves, reads, writes, or scaffolds stays inside that root, and a run that would touch any path above or outside it stops that work and records a named blocking coverage gap in its root report. The rule is stated identically in all three specifications and is pinned as a shared block, so a specification that stops stating it is detected as drift, as recorded 2026-09-14. The conductor of section 8.36 confirms the direct-child relation before its first lane launches and names that verified root in every lane prompt, and the out-of-band approval controller of section 8.28 binds every path it touches, including its own harness, inside the same root.

In some embodiments the invocation root is the process working directory that a trusted orchestrator fixes at startup, and it is never selected by candidate bytes or by configuration file contents. Every path value an execution domain authors, persists, prints, or passes to a command-line interface is a canonical project-root path beginning with a leading dot and separator, while absolute resolution is internal only and exists for containment and security checks. External trust material, software private keys, and service state remain outside the resolved candidate tree while staying inside the governed project root, so the candidate can neither read nor rewrite the material that governs it.

Prior art positioning: filesystem confinement primitives such as chroot and jail bound a process to a subtree chosen by configuration, and the Bell-LaPadula lattice bounds access by label rather than by path. The novelty residue here is that the boundary is derived from the presence of a specific vendored artifact as a direct child, so no candidate-supplied value can move the root, combined with a discipline in which the canonical relative form is the authored, persisted, and printed form while the absolute form exists only inside the containment check. ENFORCED (tools/project_paths.py, first landed 2026-09-16; tests/test_project_paths.py; tests/test_cli_path_scope.py) evaluates canonical CLI paths and containment within the invoking tree. ENFORCED (tools/layout.py, first landed 2026-09-18; tests/test_layout.py) evaluates the repository-internal roster and symlink boundary. The runtime guard for an agent scaffolding above the true root remains PROSPECTIVE and DEFERRED (DEFERRED.md section 8: "Every path an instrument resolves stays inside the parent project root whose direct child is `trinity/`"); an audit of the directory it is handed cannot observe the agent's choice of a different root.

### 8.44 The multi-work publication registry, the grounded case surface, and the generated tracker

In some embodiments the record-keeper execution domain 110 maintains a publication work registry as the single source of the work list, naming each registered work with its slug, its title, its kind, the surfaces it carries, its anchor evidence scope, and its status. A work is registered, renamed, or retired only on explicit human instruction, and a surface subdirectory carrying no registry row is a defect recorded as a named coverage gap. Every publication path resolves inside the current work's subdirectory, one work's claims never cite another work's data artifact, and a cross-work restatement either re-derives from the citing work's own evidence chain or names the sibling work as related work. A project holding no registry carries a single implicit work on the legacy flat layout, and the run that registers a second work first migrates the flat surfaces into that implicit work's subdirectories by reconciliation rather than by overwriting. The manuscript surface is the anchor, and the deck, media, patent, and case surfaces derive from it and introduce no evidence of their own, so a derived surface refuses to build from a manuscript whose gates fail.

In some embodiments the case surface holds a decision-forcing narrative, a paired teaching note, and exhibits regenerated from the manuscript's figures or derived from its data artifacts by regenerable code. Every named fact, number, quotation, and exhibit traces to a data artifact, to dated ledger evidence, or to a dated answer in a closed-schema append-only interview record whose fields are the question identifier, the coverage item, the question, the offered candidate answers, the answer, and the asked and answered instants. Elicitation is directed and closed-ended: each question names the single fact it seeks and offers the candidates the existing evidence already suggests. An interview answer is classed as attested rather than measured, a declined or unanswered item becomes a named limitation of the teaching note rather than filler prose, and a case fact grounded in neither measured evidence nor a dated answer is a defect.

In some embodiments a generated executive tracker sits at the project root, as recorded 2026-09-20, superseding the three-section self-rewrite arrangement. In one implementation each run emits a write-once `trinity.tracker-card/v1` card through tools/tracker_card.py and tools/tracker.py, derived solely from its own run marker, report, and progress bytes and binding their digests. Equal re-emission preserves the original close instant; changed input, missing input, mismatched instrument identity, and unreadable shape produce `TRACKER_CARD_CONFLICT`, `TRACKER_CARD_INPUT_MISSING`, `TRACKER_CARD_IDENTITY_MISMATCH`, and `TRACKER_CARD_MALFORMED`, respectively. No run writes a tracker byte. The tools/dashboard.py renderer and tools/pipeline.py `render-reports` entry point render `TRACKING.md` whole from cards and run reports without moving bundles. Sixteen ordered sections cover Headline, Organizational lanes, Disposition board, Decisions needed, Changed since boundary, Cycle timeline, Bundle flow, Samples occupancy, Aging work, Blockers and escalations, Sign-off log, ENGRAM, FORGE, CRUCIBLE, History, and Flag legend. Each instrument retains the four labelled digest lines for status, phase, progress, and pending; the as-of instant comes from recorded activity rather than a wall clock, and late history inserts chronologically. Hooks under `.githooks` check staged generated bytes or re-render working-tree bytes without staging, committing, or pushing. The `trinity-generated` merge driver keeps local generated bytes pending regeneration rather than requiring hand resolution. The tracker remains derived and non-authoritative, carries no difficulty, hardness, or quality claim, and root reports win on conflict. ENFORCED (tools/tracker_card.py, first landed 2026-09-20; tests/test_tracker.py) covers card shape alongside the writer; ENFORCED (tools/dashboard.py, first landed 2026-09-20; tests/test_dashboard.py; tests/test_hooks.py) covers rendering and hook expectations. CONTRACT-ONLY (ENGRAM.md:20, 110) forbids peer-card reads; DEFERRED (DEFERRED.md: "the projection-leak check") records that no checker proves runtime abstention. Prior art positioning: Trillian Verifiable Log-Derived Map separates source history from derived state. The novelty residue here is a write-once per-run input plus whole-file deterministic regeneration that removes shared tracker writes while preserving a contract-bound prohibition on using peer cards as direction.

### 8.45 Ordered trust-ladder discovery and separate admission

In some embodiments the record-keeper execution domain 110 searches a closed source-class ladder, as recorded 2026-09-20. In one implementation `T0` excludes agent recollection and model self-reported difficulty entirely. `T1` covers independently adjudicated evidence, including refereed papers, preprints, and dated independent evaluations, without a correctness premium for venue acceptance. `T2` covers maintainer-adjudicated boards, release records, proceedings, issued patents, official reviews and decisions, and resolved forecast markets. `T3` covers first-party self-reports, `T4` third-party measured evaluations, `T5` engineering records, `T6` hosted submissions or secondary reporting, and `T7` rumour. Tier is a default prior, not a verdict. CONTRACT-ONLY (ENGRAM.md:234).

In one implementation every admitted item records provenance, an adjudication selected from `adjudicated-by-issuer`, `merged-fix`, `admission-against-interest`, `reproduced-by-third-party`, `open-with-reproduction`, `open-unreproduced`, `disputed`, and `withdrawn`, and liveness selected from `active`, `maintenance`, `frozen`, `retired`, and `archived`. Corroboration counts independent provenances rather than hostnames. A retired board's dated slice remains evidence but is not a current frontier claim. A lower tier opens only after every substrate of the tier above records an exhausted receipt. A failed lane remains an explicit gap; incidental lower-tier leads cannot count as lawful exhaustion or discharge that gate. Descent is for discovery never admission: a low-tier lead is sought at higher tiers and never acquires anchor authority by the act of being found. CONTRACT-ONLY (ENGRAM.md:234, 240). Prior art positioning: PRISMA-S and US9087048B2 address search reporting and source-rating thresholds. The novelty residue here is exhaustion-gated discovery coupled to a separate admission boundary that cannot be relaxed by descent.

### 8.46 Closed discovery receipts and search-reporting correspondence

In some embodiments each substrate search produces an append-only discovery receipt per invocation, as recorded 2026-09-20 and extended as recorded 2026-09-21. In one implementation `engram.receipt/v1` has exactly the twenty-one fields below, replacing the earlier fifteen-field form while retaining the predicate name. Missing or unknown fields fail closed. A host-resolved identity precedes querying, the actual issued query is retained verbatim, and a result-set digest binds the returned population. A challenge, rate limit, failure status, unfollowed redirect, or unresolved rename leaves the lane unexhausted, not zero rows clean. An unexhausted high-tier lane is a named coverage gap and cannot be reported drained. The receipt promotes no row, creates no certification record, evidences no difficulty tier, and raises no disposition. CONTRACT-ONLY (ENGRAM.md:240).

The following correspondence maps each closed field onto the PRISMA-S reporting concern it supports, including protocol extensions where PRISMA-S has no identical field. It is not a claim that twenty-one fields alone satisfy every checklist item. Multi-database searches must identify each database; any missing checklist detail remains a named gap. CONTRACT-ONLY (ENGRAM.md:240; PRISMA-S, Rethlefsen et al., DOI 10.1186/s13643-020-01542-z).

| Field in one implementation | PRISMA-S correspondence or protocol extension |
| --- | --- |
| `substrate` | Items 1-7: name the database, registry, resource, citation index, contact, or other method searched |
| `substrate_kind` | Items 1-7: classify that information source; item 2 additionally requires constituent databases for a multi-database search |
| `substrate_tier` | Protocol extension: trust prior beside the items 1-7 source description |
| `canonical_identity_resolved` | Items 1-7 source identification, extended with host-returned canonical identity rather than an unresolved alias |
| `transport` | Item 1 platform and items 3-7 access method, extended with the actual transport |
| `endpoint` | Items 1-7 platform or resource location used |
| `query_verbatim` | Item 8: full search strategy exactly as issued, not a topic list |
| `filters` | Item 9 limits and restrictions and item 10 search filters |
| `filter_provenance` | Item 10: identify any published filter reused |
| `strategy_provenance` | Item 11: identify prior work from which the strategy is adapted |
| `date_bounds` | Item 9: date restrictions applied to the search, distinct from search execution date |
| `http_status` | Protocol extension: transport outcome making a failed search visible rather than zero-result evidence |
| `records_returned` | Item 15: total records obtained from that source |
| `records_admitted` | Item 15 accounting extension: admitted count distinguished from returned count |
| `deduplication` | Item 16: duplicate-removal process and software |
| `rung_answered` | Items 1-7 method description, extended with the answering resolution rung |
| `result_set_digest` | Item 15 accounting extension: cryptographic identity of the returned set |
| `instant_utc` | Item 13: date of the search, refined to an exact instant |
| `update_method` | Item 12: how an earlier search is updated |
| `peer_review` | Item 14: who reviewed the search strategy |
| `exhaustion` | Protocol extension: exhausted or unexhausted with its reason, never inferred from an empty result alone |

Prior art positioning: PRISMA-S and the search-history JSON structure PMC9682961 make searches reportable; Cormack and Grossman US10229117 and US10671675 concern stopping. The novelty residue here is the closed receipt's use as a machine-readable prerequisite to ordered tier descent, with unresolved transport or identity preserved as a gap and discovery receipt authority expressly denied for evidence admission. CONTRACT-ONLY (ENGRAM.md:234, 240).

### 8.47 First-class supersession edges and author blinding

In some embodiments catalog supersession retains a row and a permanent queryable edge, as recorded 2026-09-20. In one implementation `engram.supersession/v1` contains exactly `source_ref`, `target_ref`, `relationship` equal to `revoked-by`, `evidence_digest`, and `instant_utc`. Edges chain and are never deleted. Every superseded row resolves its edge; a missing edge fails closed. The row's `superseded_kind` is `revoked` where a successor stands or `deprecated` where the mechanism retires without one. This is an axis, not a fourth row state; the states remain candidate, anchored, and superseded. A retraction targets its retraction identity and deprecates the row. CONTRACT-ONLY (ENGRAM.md:212, 253, 255).

In some embodiments authority to supersede is tier-gated. Independently or maintainer-adjudicated evidence may supersede; first-party evidence may do so only as an admission against interest. A resolved forecast market on a named board and date may establish published saturation for this purpose, never signed pilot measurement. Hosted or rumour evidence alone supersedes nothing. The author projection excludes the superseding anchor, the edge, and the supersession kind, so the author execution domain 120 cannot learn the evidence behind a narrowed catalog row. CONTRACT-ONLY (ENGRAM.md:253, 408); the complete edge walk and runtime projection protection are PROSPECTIVE rather than established by the existence of a forbidden-field list. Prior art positioning: W3C PROV `wasInvalidatedBy` and Wikidata deprecated rank with P2241 preserve invalidation semantics. The novelty residue here is tier-gated authority combined with exclusion of both the invalidating edge and its anchor from the author projection.

### 8.48 Content-addressed research corpus identity

In some embodiments research admission retains source bytes and a derived rendering side by side, as recorded 2026-09-20. The record-keeper execution domain 110 computes a digest over the retained bytes; a source-class path and stem label those bytes but do not define identity. Relocation under the source class changes no content identity. A paper retains its source PDF and derives its text from those stored bytes using a named tool at a pinned version. A source still appending to itself is a lead never an anchor; a dated or versioned slice is required. CONTRACT-ONLY (ENGRAM.md:24, 238, 486) governs actual derivation and stable-source admission.

In one implementation each pair has an `identity.json` sidecar, concretely `<stem>.identity.json`, under `engram.identity/v1`. Its closed fields name schema, source class, stem, retained filename, rendering filename, retained content digest, rendering digest, deriving tool, and deriving tool version. The checker refuses loose artifacts, unknown source classes, missing renderings or retained bytes, missing or malformed manifests, stem disagreement, either digest mismatch, and empty tool or version. Corresponding refusals include `CORPUS_LOOSE_ARTIFACT`, `CORPUS_UNKNOWN_CLASS`, `CORPUS_RENDERING_MISSING`, `CORPUS_RETAINED_MISSING`, `CORPUS_MANIFEST_MISSING`, `CORPUS_MANIFEST_MALFORMED`, `CORPUS_STEM_MISMATCH`, `CORPUS_DIGEST_MISMATCH`, `CORPUS_RENDERING_DIGEST_MISMATCH`, and `CORPUS_TOOL_UNRECORDED`. ENFORCED (tools/corpus_identity.py, first landed 2026-09-20; tests/test_corpus_identity.py), with tools/corpus_identity_manifest.py defining the sidecar, validates recorded bytes and declarations. DEFERRED (DEFERRED.md: "Rendering derivation is declared, never reproduced") means a recorded tool is not proof that conversion occurred; no converter is invoked by this check.

Prior art positioning: SWHID ISO/IEC 18670:2025, IPFS CID, and Nix RFC 0062 already identify content independently of location. The novelty residue here is the paired retention and rendering binding at the sender-neutral admission boundary, together with a lead-never-anchor rule for unpinned mutable sources, not a claim to content addressing itself. CONTRACT-ONLY (ENGRAM.md:234, 486) covers that combined admission policy.

### 8.49 Row-versioned confidence with GRADE domains

In some embodiments the catalog uses a closed four-level confidence profile seeded by source tier, as recorded 2026-09-20. In one implementation `engram.hardness/v2` rows carry `row_version`, which changes whenever row content changes. GRADE levels are `high`, `moderate`, `low`, and `very_low`: further research is respectively unlikely to change confidence, likely to have an important impact, highly likely to change the estimate, or faced with deep uncertainty. Tier one starts high, tier two moderate, tiers three through five low, and tier six at the lowest level with a ceiling of low. Excluded and rumour material cannot supply an anchor. CONTRACT-ONLY (ENGRAM.md:242-243, 254).

In some embodiments five downgrade domains are risk of bias, inconsistency, indirectness, imprecision, and publication bias. Three upgrade domains are large effect, dose-response, and plausible confounding. Each invoked domain is recorded, lowers or raises one level, or lowers two levels when doubly serious; confidence cannot exceed the highest-tier anchor's support. The profile restates the GRADE vocabulary of Cochrane Handbook chapter 14, and the source ladder is shaped by the Oxford Centre for Evidence-Based Medicine 2011 levels without equating a source tier to an OCEBM grid level. These are the contract's named methodological sources, not an assertion of a newly retrieved prior-art reference. Confidence never promotes a row, creates a certification record, evidences a difficulty tier, or raises a disposition. Prior art positioning: the register's GRADErater and EvidenceGRADEr / URSE already structure evidence appraisal. The novelty residue here is a row-versioned catalog whose explicit domains and tier ceiling are subordinate to external measurement, with supersession evidence withheld from the author projection. CONTRACT-ONLY (ENGRAM.md:243, 254, 408).

### 8.50 Source-classed briefs and successor proposals

In some embodiments an evidence brief and a successor proposal bind their source identity rather than importing human grants, as recorded 2026-09-20. In one implementation `engram.brief/v2` carries artifact stem, source class, source identity, content digest, tier, adjudication, liveness, deriving tool and version, location-grounded candidate levers, pinned task-corpus revisions, pinned harness repository and commit, reported baselines with cohort and date, candidate failure modes, named gaps, and proposed, rejected, or expired standing. Reported baselines remain source claims, not local measurement. CONTRACT-ONLY (ENGRAM.md:238).

In one implementation `engram.next/v2` carries parent stem, source class, source identity and content digest; effective saturation floor and its requirements-or-default source; saturation rows with cohort, score, date, reporting source identity and tier; residual axes; unmeasured failure modes; location-grounded successor levers; cross-axis composition; targets with the stated headroom; pinned parent corpora and harness; gaps; and proposed, rejected, or expired standing. Unknown or absent fields fail closed by contract. Budget, compute envelope, deliverable asks, tier overrides, and exclusion authority stay named gaps until the human authors grants, and changed brief bytes reopen digest approval. CONTRACT-ONLY (ENGRAM.md:267-270); DEFERRED (DEFERRED.md: "Successor bundle schema and saturation floor") remains applicable despite that row's older schema spelling. Prior art positioning: BenchEvolver arXiv 2606.01286 and PROPEL arXiv 2606.18284 address benchmark evolution. The novelty residue here is a source-classed, digest-bound proposal that carries no grant or admission authority and cannot bypass approval by supplying its own missing requirements.

### 8.51 Maintainer-review resolution through a clearance window

In some embodiments official review evidence is pinned to the source's edit history rather than a secondary dump, as recorded 2026-09-20. In one implementation OpenReview decisions and official reviews occupy tier two. An authorized real browser session completes the managed challenge to obtain a short-lived address-bound clearance token, then ordinary requests retrieve forum notes and note edits within that window. The anchor binds forum identity, note identity, `tmdate`, and the SHA-256 of note text; a later edit time requires re-verification against the edit log. A third-party dump remains adjacent at tier six and cannot substitute as the anchor. CONTRACT-ONLY (ENGRAM.md:241), not a claim that a clearance session or retrieval occurred here. Prior art positioning: PRISMA-S identifies searched resources and SWHID identifies retained content. The novelty residue here is the conjunction of authorized transient access, edit-history re-verification, and refusal to promote a secondary dump to the maintainer's evidence tier.

### 8.52 Mutable-data-alias harvest findings

In some embodiments a harvest lane separates a pinned harness revision from the dataset that harness requests, as recorded 2026-09-20. In one implementation the lane first resolves the harness through the host's paper index, links in the versioned paper and one project-page hop, then repositories named by linked model or dataset cards. It pins a commit and records the answering rung. A name-similarity match is adjacent, not resolved. If the harness loads benchmark data through a mutable alias rather than an immutable revision, the lane records `mutable-data-alias` as a dated advisory finding. It stages the concern for human adjudication and neither admits a touchstone nor pronounces a verdict. CONTRACT-ONLY (ENGRAM.md:237). Prior art positioning: SLSA provenance and Bazel Remote Execution API action-cache content addressing bind dependency bytes. The novelty residue here is the explicit separation of a pinned program from its mutable data selector within a propose-never-admit harvest lane, so code identity alone cannot be mistaken for reproducible evidence.

### 8.53 Principal-bound signed run-operation chains

In some embodiments authority attaches to each authoritative write rather than to an editable namespace marker. In one implementation, as recorded 2026-09-20 by the first addition of tools/operations.py, `trinity.run-operation/v1` names the project, run, verb, record digest, chain position and predecessor, and principal. A DSSE envelope is verified against `allowed_signers` and the versioned trust root under `run_operator`. A valid signature naming another principal receives `OPERATION_PRINCIPAL_MISMATCH`; a removed, reordered, inserted, or foreign-run operation that breaks the verified sequence receives `OPERATION_CHAIN_BROKEN`; absent or rolled-back trust and unauthorized role membership receive `OPERATION_UNAUTHORIZED`; invalid carriers receive `OPERATION_ENVELOPE_INVALID`. The CHANGELOG narrative dates this work 2026-09-18; its file-addition history dates the implementation 2026-09-20. ENFORCED (tools/operations.py, first landed 2026-09-20; tests/test_operations.py) covers signature attribution and chain validation, not proof that every runtime write is mediated.

In some embodiments transition verification matches an authoritative record's content digest to a verified operation rather than trusting a filename. In one implementation tools/transition_signing.py applies this check to claims and verdicts when a signing policy is supplied; without that policy the transition is judged on structure alone. The broader every-authoritative-write coverage is PROSPECTIVE. DEFERRED (DEFERRED.md: "Run ownership is authenticated only by `run.json` bytes") retains external enrolment and key-to-human binding: a trust-root principal is not proof of a distinct person. Prior art positioning: in-toto with DSSE, Sigstore Rekor v2, and RSL already bind actions and repository history. The novelty residue here is a record-digest-bound per-run chain that rejects cross-principal attribution even under a valid signature and feeds the trusted-context successor decision of section 8.54.

### 8.54 Lawful succession from a caller-supplied trusted context

In some embodiments a validator decides whether a candidate commit is a lawful successor to an accepted base, as recorded 2026-09-18. Baseline, pinned trust-policy digest, enrolled principal set, and authenticated operator arrive from a caller-supplied trusted context, never the candidate's own baseline or policy. The validator compares protected accepted records to candidate records and treats accepted sequence, not a claimant-authored timestamp, as ownership authority. ENFORCED (tools/transition.py, first landed 2026-09-18; tests/test_transition.py), with tools/transition_records.py, tools/transition_signing.py, and tools/accepted_state.py supplying the record and signing readers; tools/accepted_state.py has no dedicated test file and is exercised through the transition tests.

In one implementation the closed refusal vocabulary is `TRANSITION_UNENROLLED_OPERATOR`, `TRANSITION_BASE_MISMATCH`, `TRANSITION_STREAM_REWRITTEN`, `TRANSITION_RECORD_MUTATED`, `TRANSITION_RECORD_DELETED`, `TRANSITION_FOREIGN_RUN`, `TRANSITION_CLAIM_CONTESTED`, `TRANSITION_VERDICT_UNOWNED`, `TRANSITION_EPOCH_FORK`, `TRANSITION_EPOCH_ROLLBACK`, `TRANSITION_BACKDATED`, `TRANSITION_TRUST_DRIFT`, and `TRANSITION_UNSIGNED_OPERATION`. These refuse an operator outside enrolment; an unresolvable or wrong sole parent, including a merge burying the base as second parent; rewritten or truncated streams; mutated or deleted accepted claims, verdicts, sentinel entries, or epochs; and foreign-run writes. They also refuse claims on already-owned bundles or two simultaneous claims; verdicts without an accepted claim, without a consumer, or by another consumer; a new epoch other than accepted current plus one; a backward current pointer; seals before the accepted watermark; trust bytes unequal to the external pin; and, with signing policy, records without digest-matching verified operations. ENFORCED (tools/transition.py, first landed 2026-09-18; tests/test_transition.py) is a decision procedure, not an admission service.

No process here holds exclusive write on an accepted ref. DEFERRED (DEFERRED.md: "Push admission is the local hook, and the rerun on `main` is detection"; "Verdict arbitration by claim instant"; "Epoch publication races between two clones") therefore preserves the serialization boundary, while enrolment remains external. Prior art positioning: RSL, gittuf, git-ratchet, and TUF already resist unauthorized history changes and rollback. The novelty residue here is deciding the coupled run, claim, verdict, and epoch transition against caller-supplied authority rather than any candidate-selected baseline, without conflating that decision with exclusive admission.

### 8.55 Proposal folding and manifest-bound memory epochs

In some embodiments concurrent record-keeper runs propose changes and one serialized publication produces a new immutable memory epoch, as recorded 2026-09-18. In one implementation proposals live under `.memory/proposals/<run_id>/` and bind the epoch read. Publication folds disjoint proposals into `.memory/epochs/<n>/` with a manifest, binds the two projections and anchor-standing file under `.memory/views/<n>/` to the manifest digest, advances `.memory/current.json`, and refreshes compatibility copies. Conflicting proposals for one file stop at `proposal-conflict`; stale bases stop at `proposal-stale`. A changed epoch or projection binding produces `EPOCH_TAMPERED`, and a compatibility copy that drifts produces `EPOCH_DRIFT`. ENFORCED (tools/epochs.py, first landed 2026-09-18; tests/test_epochs.py) covers local folding, existing-directory refusal, and manifest comparison. CONTRACT-ONLY (ENGRAM.md:14, 43, 177) requires peers to read only published, pinned views.

The serialized publication is the architecture's required boundary, not a claim of a cross-clone lock. DEFERRED (DEFERRED.md: "Epoch publication races between two clones") records that two clones can each publish the next number and that the manifest binds its parent by number rather than parent manifest digest. Section 8.54 decides a fork but cannot serialize remote admission. Prior art positioning: TUF consistent snapshots and Merkle-CRDTs already bind distributed snapshots and merged state. The novelty residue here is per-run proposals folded into one manifest-bound memory epoch whose role projections and anchor-standing view share the binding, with conflict stopping publication instead of silently selecting a writer.

### 8.56 Run namespaces and mergeable streams

In some embodiments every invocation owns a run namespace for mutable state, as recorded 2026-09-18 in the implementation history. In one implementation tools/runs.py assigns an instrument-prefixed `run_id`, writes a marker once under `<harness>/runs/<run_id>/`, and refuses a request to reopen that marker under a different principal or instrument. Per-run queue and sentinel streams and per-identifier lane and claim records allow independent preparations to merge as a union rather than fork one shared log. ENFORCED (tools/runs.py, first landed 2026-09-18; tests/test_runs.py) covers namespace grammar and marker ownership, not authentication or confidentiality. DEFERRED (DEFERRED.md: "Run ownership is authenticated only by `run.json` bytes"; "A full clone does not enforce the information barrier between humans") retains those limits. Prior art positioning: Merkle-CRDTs and Dolt/Noms prolly trees supply mergeable state. The novelty residue here is namespace-isolated preparation combined with separate scarce-decision reconciliation and signed operation attribution, not a claim that namespace isolation alone authenticates a writer.

### 8.57 Subject-closure binding instead of incidental head binding

In some embodiments qualification binds the inputs of one run rather than the repository's incidental tip, as recorded 2026-09-18 in the implementation history. In one implementation tools/subject.py computes a canonical manifest over the run namespace's inputs, requirements, touchstones, its consumed projection, its sealed or judged bundle digests, and the governing specification gitlink. Derived outputs, including the frozen tracker card, are excluded to avoid self-reference. A changed member changes the subject digest; another run's commit outside the closure does not. Unsafe symbolic links and unknown runs are refused. ENFORCED (tools/subject.py, first landed 2026-09-18; tests/test_subject.py). Prior art positioning: Bazel Remote Execution API action-cache keys and Nix RFC 0062 bind dependency closures. The novelty residue here is a qualification-specific closure spanning isolated run inputs, governed evidence, and the consumed role projection without coupling validity to unrelated concurrent commits.

### 8.58 Named prior art and novelty residue by family (Trillian, TUF, and PRISMA-S)

Information barrier. Prior art positioning: US12671713 (OpenAI Opco, granted 2026-06-30, agent-verified 2026-09-21 against a USPTO-derived mirror; human sign-off pending), US12671588 (The Crown and the Cross LLC, granted 2026-06-30, agent-verified 2026-09-21; the context-firewall and receipt-lineage teaching sits in this patent and not in its unrelated same-day neighbour US12671589), Trillian Verifiable Log-Derived Map (Eijdenberg, Laurie, Cutter 2015), Fides arXiv 2505.23643, GRACE-DS arXiv 2606.16000, US20200159946A1, US11849023B2, US12526244B2 (Citibank, granted 2026-01-13, pre-grant publication US20250358240A1; agent-verified 2026-09-21, the earlier cited publication number US20260180920A1 does not resolve and is withdrawn), Adversarial Reward Auditing arXiv 2602.01750, EvalStop arXiv 2606.04145, Scale Labs verifier design blog dated 2026-09-04, SWE-bench Verified, DeepSWE, and HLE arXiv 2501.14249 form the register's comparison set. The novelty residue here is the conjunction of isolated roles, dual deterministic projections, per-role forbidden fields, closed non-invertible coarsening, and outbound auditor citation checks, not secrecy alone.

Pilot and expiry. Prior art positioning: US12192372 / EP4280543A1, WO2025166192A1, US11843522, SLSA provenance with in-toto, TUF, MLPerf rules, Sigstore Rekor v2 with RFC 3161, Mazor et al. DOI 10.1101/213439, Miller arXiv 2411.00640, Sympson-Hetter with US5841655, Bjarnason et al. arXiv 2602.07150, Item Response Scaling Laws arXiv 2606.07616, US12587543 (KPMG, granted 2026-03-24, agent-verified 2026-09-21), US12694113B1 (Drata, third-party real-time security and compliance monitoring) and US12725169 (Referentia Systems, continuous attestation of security requirements), both agent-verified 2026-09-21 as compliance-renewal comparisons and neither a Vanta patent, with the earlier cited US12717930 not found and withdrawn, Drata renewal, and Machine Psychometrics arXiv 2605.23952 address provenance, exposure, uncertainty, or renewal. The novelty residue here is pre-execution population and decoy commitment under separated credentials, common attestation identity, familywise exact bounds, and failure-mode expiry with human-only retirement and deadline-bound cohorts, while latent psychometrics cannot certify.

Tamper-evident governance. Prior art positioning: Schneier and Kelsey secure audit logs, IBM US7305564B1, Snodgrass et al. VLDB 2004, draft-sharif-agent-audit-trail (revision 04 current as of 2026-09-15, revision 03 as retrieved; agent-verified 2026-09-21), draft-sahu-agent-action-receipts-00 (agent-verified 2026-09-21), draft-maintainer-1f916-agent-record-01 (agent-verified 2026-09-21), Sigstore Rekor v2 and gitsign, RSL, gittuf, git-ratchet, SLSA Source v1.2, in-toto with DSSE, RFC 9943 SCITT, OpenSSF Scorecard, US12613956B2, US12555008, US12651078, and US12602481 create a substantial combination challenge. The novelty residue here is unconditional instruction capture before work, an unwaivable currency gate, reflexive evidence-erasure classification with disjoint clearance, a powerless candidate probe with a separate no-reset observer, and trusted-context succession over operation-bound writes.

Council and judge reliability. Prior art positioning: draft-schrock-ep-authorization-receipts (revision 13 current as of 2026-09-12, revision 01 as retrieved carrying the SelfApprovalImpossible and m-of-n text; agent-verified 2026-09-21), US12001822B2, Collina et al. arXiv 2609.15803, US11328214B2, PoLL arXiv 2404.18796, US11392940B2, Avizienis N-version programming and DO-178C, informal quorum-style review tools (the reported Qorum Method repository describes single-reviewer risk-tiered delegation with a human approver rather than a multi-model panel, and the reported quorum-review skill was not located; both withdrawn as teachings on 2026-09-21), conformal risk control arXiv 2208.02814, Gupta and Kumar arXiv 2604.15302, OWASP Benchmark twins, StaAgent arXiv 2507.15892, Reliability without Validity arXiv 2606.19544, Coin Flip Judge arXiv 2606.13685, harness disclosure arXiv 2605.23950, and muSE TOSEM 2021 supply the comparisons. The novelty residue here is the conjunctive digest-bound, producer-distinct, lineage-distinct, blind unanimous failed-attack approval with human fallback, and the distinct judge gate coupling conformal admission to both-halves fixture liveness and coverage-blocking forensic readers.

Evidence admission. Prior art positioning: PRISMA-S DOI 10.1186/s13643-020-01542-z, PMC9682961, GRADErater, EvidenceGRADEr / URSE, W3C PROV `wasInvalidatedBy`, Wikidata deprecated rank with P2241, CrossRef Retraction Watch and the register's 2026 patents-citing-retractions study, US12639507, US9087048B2, WO2019043379A1 / US11526675, and SWHID ISO/IEC 18670:2025 supply reporting, appraisal, and identity comparisons. Further comparisons are IPFS CID with US10615979 / US11570001 / US12294654, Cormack and Grossman US10229117 / US10671675 / US10445374 / US10242001, US5841655 / US8348674, Duolingo S2A3 arXiv 2606.07364, Epoch AI Rosetta Stone dated 2025-12-02, BenchEvolver arXiv 2606.01286, PROPEL arXiv 2606.18284, Scale AI US12189719 / US11308364, Snorkel US2023/0419121, goldset, and golden-set-builder. The novelty residue here is ordered exhaustion receipts coupled to separate admission, paired content identity, tier-gated blinded supersession, row-versioned confidence, strict staging, and demotion-only reconnaissance; the reconnaissance coverage is the register's thinnest and is not treated as a novelty conclusion.

Identity and lanes and release authority. Prior art positioning: BIG-bench canary GUID, Thinkst Canarytokens, RFC 9562 UUIDv5, US6367012B1, Authenticode PE hashing, RFC 8785 JCS, Nix RFC 0062, Guix arXiv 1305.4584, US8560503B1 / US8335889, WO2015178944A1, and US7487178 / US9659029 / US8200721B2 / EP1604260A4 cover identity and storage building blocks. Further comparisons are TUF consistent snapshots and its offline/online role split with the PSF runbook, Gerrit and pre-receive ancestry checks, Bazel Remote Execution API, Dolt/Noms prolly trees, Merkle-CRDTs, IPLD, US9201693B2, US12602259, US12309078 / US12068973, and US11979336 (the earlier cited US12719813 was not found on 2026-09-21 and is withdrawn). The novelty residue here is the acyclic hash-derive-plant-bind construction, write-once lane registration with re-derived residency and a structural ceiling, proposal-to-epoch memory with subject-closure qualification, and a local gate incapable of terminal release paired with a verifier forbidden to execute. The register's truncated further release-authority hits are not silently treated as reviewed.

Auditor instruments. Prior art positioning: OWASP Benchmark supplies labelled positive and negative fixtures, muSE TOSEM 2021 examines mutation-based soundness, Reliability without Validity arXiv 2606.19544 examines judge reliability, and conformal risk control arXiv 2208.02814 supplies risk-control methods. The novelty residue here is their evaluation-specific use in named, live instrument families with coverage-blocking outcomes and rubric-provenance isolation, rather than reliance on fixtures, reliability estimates, or risk bounds alone.

Author hardening. Prior art positioning: BenchEvolver arXiv 2606.01286 addresses benchmark evolution, while BIG-bench canary GUID supplies a corpus-identification canary. The novelty residue here is isolated per-slot hardening, solution-centric mutation admitted through explicit executable and comparative checks, and canary-normalized identity with private-boundary containment, which neither evolving tasks nor planting an identifier alone establishes.

These family comparisons are drafting positions, not findings of patentability. Reported items require human verification before reliance. Earlier illustrative prior-art passages remain part of this disclosure, and the named register narrows the position rather than replacing an examination. The implementation-versus-prospective distinctions in sections 8.45-8.57 and the claims skeleton govern each combined embodiment.

## 9. CLAIMS

What is claimed is:

**1. A system for controlling certification of executable tasks used to evaluate or train machine-learning solvers, the system comprising:**

one or more processors;

one or more non-transitory memories;

a trust-policy store identifying an externally rooted execution-operator credential that is not issuable or usable by security principals of an author execution domain or an auditor execution domain;

a communication fabric configured to enforce access-control boundaries among: the author execution domain configured to invoke at least one machine-learning model to generate a candidate task comprising executable environment bytes, task-instruction bytes, and verifier bytes; the auditor execution domain configured to evaluate committed bytes of the candidate task; a record-keeper execution domain having exclusive write authority over certification records in an append-only evidence ledger; and a pilot execution interface operating under the execution-operator credential;

wherein the communication fabric prevents direct machine-readable communication between the author execution domain and the auditor execution domain and permits information to cross between the author execution domain and the auditor execution domain only through the record-keeper execution domain;

a projection engine configured to: compute an author projection and an auditor projection by respective deterministic transformations of ledger state under respective field policies; exclude auditor-private acceptance information from the author projection; apply a many-to-one coarsening transformation to an auditor finding to produce a coarsened finding, the many-to-one coarsening transformation preventing recovery of the auditor finding and of the auditor-private acceptance information from the coarsened finding; include the coarsened finding in the author projection in place of the auditor finding; and exclude author-provided design-intent information from the auditor projection;

a pilot controller configured to: freeze a representation of the candidate task under a task digest and freeze a versioned solver registry under a registry digest; before execution of a pilot, verify an operator-signed pre-execution commitment identifying an execution-control policy for the pilot; cause the frozen representation of the candidate task to be executed against a solver population identified by the frozen solver registry; and receive a signed pilot attestation binding at least the task digest, a trajectory commitment, a policy-manifest digest, a scorer-closure digest, a solver-population commitment, an execution interval, and a signer identity;

an evidence router configured to: provide a complete representation of the signed pilot attestation exclusively to the auditor execution domain; provide to the record-keeper execution domain a restricted representation containing binding fields of the signed pilot attestation while withholding pilot telemetry designated as auditor-private; and cause the auditor execution domain and the record-keeper execution domain each independently to verify alignment with a common pilot execution identity by comparing digest fields of its respective representation with locally recomputed digest fields; and

a certification controller configured to maintain a fail-closed certification state and to permit an upward transition to a certified state, and corresponding insertion of a certification record into the append-only evidence ledger, only after: the auditor execution domain verifies the signed pilot attestation, the task digest, the execution-control policy, and satisfaction of a predeclared difficulty criterion; the record-keeper execution domain verifies the signer identity against the trust-policy store and verifies the alignment with the common pilot execution identity; and the certification controller determines that the author execution domain neither supplied the execution-operator credential nor possessed write authority for the certified state.

**2.** The system of claim 1, wherein each certification record associates respective failure modes with respective validity intervals having a valid-at time and an invalid-at time derived from a failure-mode-specific evidentiary half-life, and wherein the certification controller automatically prevents reliance on evidence for a failure mode after the corresponding invalid-at time.

**3.** The system of claim 2, wherein the certification controller maintains, for each certified task or task lever, a monotone state machine comprising an active state, a watch state, an expired state, and a retired state, prevents an upward state transition without new qualifying evidence, and permits entry into the retired state only in response to a human-signed retirement authorization.

**4.** The system of claim 2, wherein successive versions of the solver registry are linked by predecessor digests, an update is accepted only if it monotonically preserves a required cohort from a preceding version, and the author execution domain is denied authority to resolve or modify membership of the solver registry.

**5.** The system of claim 1, wherein the operator-signed pre-execution commitment is timestamped earlier than initiation of the pilot and commits to a hidden decoy seed, a complete job schedule or attempt budget, and a stopping policy, and wherein a commitment timestamped after initiation of the pilot is rejected.

**6.** The system of claim 1, wherein the task digest is computed over a canonical hash domain in which values occupying designated canary-token slots are replaced with fixed placeholders while quantities and positions of the designated canary-token slots remain represented in the canonical hash domain.

**7.** The system of claim 1, wherein the predeclared difficulty criterion requires a one-sided Clopper-Pearson exact upper confidence bound for a solver success probability to be strictly below a predeclared frontier floor by at least a predeclared margin.

**8.** The system of claim 7, wherein the difficulty criterion is evaluated according to a maximum, across solvers in the frozen solver registry, of respective solver success measures, with a multiple-comparison correction selected and committed before execution of the pilot.

**9.** The system of claim 1, wherein each deterministic grounding instrument executed by the auditor execution domain is required, during a common evaluation run, both to detect a planted-defect fixture and to refrain from detecting a corresponding clean fixture, and an instrument that fails either requirement forces a blocking disposition.

**10.** The system of claim 9, wherein the deterministic grounding instruments comprise each of a delivery-conformance instrument, a reference-fidelity instrument, a calibration instrument, a verifier-robustness instrument, a rubric-compilation instrument, a contamination-screening instrument, a reward-integrity instrument, and a budget-conformance instrument.

**11.** The system of claim 1, wherein the trajectory commitment comprises a Merkle root recomputed by the auditor execution domain from committed trajectory bytes, and the certification controller requires exact equality between the recomputed Merkle root and a Merkle root signed under the execution-operator credential.

**12.** The system of claim 1, wherein the signed pilot attestation comprises a Dead Simple Signing Envelope encapsulating an in-toto Statement, and the record-keeper execution domain is prevented from creating the certification record unless a signature of the envelope verifies against an external trust root designated by the trust-policy store.

**13.** The system of claim 12, wherein the record-keeper execution domain creates Merkle-tree checkpoints and signed tree heads for the append-only evidence ledger and implements a recovery function that reconstructs the evidence ledger from a transitive closure of verified attestation envelopes, compares reconstructed ledger bytes with committed ledger bytes, and marks the evidence ledger as broken in response to any byte inequality.

**14.** The system of claim 1, wherein the many-to-one coarsening transformation maps auditor findings into a closed vocabulary consisting of exactly three values respectively denoting a saturated lever, a broken checker, and a blocked task, without transmitting an underlying defect fixture, acceptance threshold, pilot telemetry, or auditor rationale to the author execution domain.

**15.** The system of claim 1, wherein each of the author projection and the auditor projection is a pure function of ledger bytes, supersession records, pinned trust roots, and one declared evaluation instant, and the record-keeper execution domain stores a projection closure sufficient for a third party to reproduce the respective projection byte-for-byte, including an allow-list and a named forbidden-field list for the respective projection.

**16. A computer-implemented method for controlling certification of executable tasks used to evaluate or train machine-learning solvers, the method comprising:**

establishing, by one or more processors, access-controlled author, auditor, and record-keeper execution domains, the record-keeper execution domain having exclusive write authority over certification records in an append-only evidence ledger; generating, in the author execution domain using at least one machine-learning model, a candidate task comprising executable environment bytes, task-instruction bytes, and verifier bytes; preventing direct machine-readable communication between the author execution domain and the auditor execution domain; communicating information between the author execution domain and the auditor execution domain only by causing the record-keeper execution domain to compute respective author and auditor projections as deterministic transformations of ledger state under respective field policies, including: excluding auditor-private acceptance information from the author projection; applying a many-to-one coarsening transformation to an auditor finding to produce a coarsened finding, the many-to-one coarsening transformation preventing recovery of the auditor finding from the coarsened finding, and including the coarsened finding in the author projection in place of the auditor finding; and excluding author-provided design-intent information from the auditor projection; freezing a representation of the candidate task under a task digest and freezing a versioned solver registry under a registry digest; before execution of a pilot, verifying a pre-execution commitment identifying an execution-control policy for the pilot and signed under an externally rooted execution-operator credential that is not issuable or usable by security principals of the author execution domain or the auditor execution domain; causing execution of the frozen representation of the candidate task against a solver population identified by the frozen solver registry; receiving a signed pilot attestation binding at least the task digest, a trajectory commitment, a policy-manifest digest, a scorer-closure digest, a solver-population commitment, an execution interval, and a signer identity; routing a complete representation of the signed pilot attestation exclusively to the auditor execution domain and routing to the record-keeper execution domain a restricted representation containing binding fields while withholding auditor-private pilot telemetry; independently verifying, by each of the auditor execution domain and the record-keeper execution domain, alignment with a common pilot execution identity by comparing received digest fields with locally recomputed digest fields; and maintaining a fail-closed certification state that prevents creation of a certification record unless: the auditor execution domain verifies the signed pilot attestation, the task digest, the pre-execution commitment, and satisfaction of a predeclared difficulty criterion; the record-keeper execution domain verifies the signer identity against an external trust root and verifies alignment with the common pilot execution identity; and the author execution domain lacks both the execution-operator credential and write authority for the certification record.

**17.** The method of claim 16, further comprising regenerating ground-truth bytes independently on first and second execution hosts, comparing the independently regenerated ground-truth bytes, and forcing a blocking disposition unless the independently regenerated ground-truth bytes are byte-identical.

**18.** The method of claim 16, wherein computing the auditor projection comprises structurally excluding a hardness contract containing author-selected difficulty targets, intended solution strategies, or expected failure explanations, such that the auditor execution domain evaluates committed candidate-task bytes without access to the hardness contract.

**19.** The method of claim 16, further comprising receiving a human approval file containing a SHA-256 digest of a candidate artifact, recomputing a SHA-256 digest from current bytes of the candidate artifact before performing an approved operation, and refusing the approved operation when the received and recomputed digests differ.

**20.** The method of claim 19, further comprising recording a human operational acceptance against an existing capped disposition without modifying a release record or waiving an execution-attestation requirement, and, upon detecting any change to bytes bound by the release record, voiding the release record and requiring a new pilot and a new human approval.

**21.** The method of claim 16, further comprising assigning a task a UUID version 5 derived from a namespace pinned in an approved scope and from a canonical task-identity input, the canonical task-identity input comprising a content hash computed over a canonical hash domain in which values occupying designated canary-token slots are replaced with fixed placeholders, recording the UUID as a write-once identifier, maintaining bytes of the task at a single authoritative bundle location keyed by the UUID together with a separately keyed rollout-evidence location that holds no task bytes, and rejecting reassignment of the UUID to different task bytes.

**22.** The method of claim 16, wherein generating the candidate task comprises generating, per invocation of the author execution domain, a batch of at least twenty parallel task slots, each task slot having an isolated task manifest, instruction representation, execution environment, test representation, and oracle solution, resolving a disposition for each task slot independently such that a blocking disposition of one task slot does not cap another task slot, and preventing an artifact of one task slot from satisfying a verifier of another task slot.

**23.** The method of claim 16, further comprising generating a child task by mutating a known-executable solution, re-deriving task instructions and verifier bytes from the mutated solution, and accepting the child task into a mutation lineage only after determining that the child task is executable, empirically harder than a parent task against a common solver population, and diverse from previously accepted tasks according to a predeclared diversity measure.

**24. A non-transitory computer-readable storage medium storing instructions that, when executed by one or more processors, cause the one or more processors to control certification of executable tasks used to evaluate or train machine-learning solvers by:**

establishing access-controlled author, auditor, and record-keeper execution domains, the record-keeper execution domain having exclusive write authority over certification records in an append-only evidence ledger; preventing direct machine-readable communication between the author execution domain and the auditor execution domain; causing the record-keeper execution domain to operate as a sole information bridge by computing author and auditor projections as deterministic transformations of ledger state under respective field policies, the author projection excluding auditor-private acceptance information and containing, in place of an auditor finding, only a coarsened finding produced by applying a many-to-one coarsening transformation to the auditor finding, the many-to-one coarsening transformation preventing recovery of the auditor finding from the coarsened finding, and the auditor projection excluding author-provided design-intent information; causing the author execution domain to invoke at least one machine-learning model to produce a candidate task comprising executable environment bytes, task-instruction bytes, and verifier bytes; freezing a representation of the candidate task under a task digest and freezing a versioned solver registry under a registry digest; before execution of a pilot, verifying a pre-execution commitment identifying an execution-control policy for the pilot and signed under an externally rooted execution-operator credential that is not issuable or usable by security principals of the author execution domain or the auditor execution domain; causing the frozen representation of the candidate task to be executed against a solver population identified by the frozen solver registry; receiving a signed pilot attestation binding at least the task digest, a trajectory commitment, a policy-manifest digest, a scorer-closure digest, a solver-population commitment, an execution interval, and a signer identity; routing a complete representation of the signed pilot attestation exclusively to the auditor execution domain while routing to the record-keeper execution domain a restricted representation containing binding fields and withholding auditor-private pilot telemetry; causing the auditor execution domain and the record-keeper execution domain each independently to verify alignment with a common pilot execution identity through comparison of received and locally recomputed digest fields; and preventing an upward transition of a fail-closed certification state unless the auditor execution domain verifies the signed pilot attestation, the task digest, the execution-control policy, and satisfaction of a predeclared difficulty criterion, the record-keeper execution domain verifies the signer identity against an external trust root, both execution domains verify said alignment, and the author execution domain lacks write authority for the certification record.

**25.** The non-transitory computer-readable storage medium of claim 24, wherein the instructions cause the author execution domain to conduct, for the candidate task, an adversarial hardening sequence in which an attacker process attempts to satisfy the verifier bytes without satisfying the task-instruction bytes, a fixer process modifies the verifier bytes in response to the attempt, and a solver process executes a legitimate solution against the modified verifier bytes, and to prevent advancement of the candidate task unless the attempt is rejected and the legitimate solution remains accepted.

**26.** The non-transitory computer-readable storage medium of claim 24, wherein the instructions cause the author execution domain to execute probes corresponding to a bound plurality of predeclared shortcut classes stored in a versioned shortcut taxonomy and to prevent submission of the candidate task to the auditor execution domain when a probe demonstrates satisfaction of the verifier bytes through a shortcut represented by one of the predeclared shortcut classes.

**27.** The non-transitory computer-readable storage medium of claim 24, wherein the instructions cause the auditor execution domain to evaluate reliability of a machine-learning judge against blinded reference labels according to a predeclared conformal risk-control procedure and to force an abstention or blocking disposition when empirical coverage required by the conformal risk-control procedure is not established.

**28.** The non-transitory computer-readable storage medium of claim 24, wherein the instructions cause a rubric compiler operating in an execution domain isolated from author-provided solution provenance to receive committed task bytes and formal scoring criteria, commit a rubric digest before receiving judged solver trajectories, and prevent use of a rubric whose drafting provenance includes an oracle solution or an author explanation excluded by a field policy governing the auditor projection.

**29.** The system of claim 1, further comprising a feedback capture subsystem configured, in each of the author execution domain, the auditor execution domain, and the record-keeper execution domain, to write one record of an append-only feedback ledger for each human-originated turn received by that execution domain without conditioning the writing on a classification of the turn, and to append to an attestation log a corresponding line under a closed field set comprising at least a monotonically increasing sequence number, a digest of verbatim bytes of the turn, a digest of bytes of the record, a digest of a preceding line of the attestation log, and a digest sealing fields of the line, wherein the system is configured to walk the attestation log end to end before performing phase work in that execution domain and, upon a failure of the walk, to prevent the phase work and to force a blocking or broken disposition for that execution domain.

**30.** The system of claim 1, further comprising a governing-specification currency controller configured, before phase work is performed in any of the author execution domain, the auditor execution domain, and the record-keeper execution domain, to resolve a vendored governing-specification checkout against a fetched tip of a tracked upstream branch and to admit the checkout only when the checkout is unmodified and a resolved revision of the checkout equals the fetched tip, and configured, when the checkout is behind the fetched tip, diverged from the fetched tip, locally modified, or unresolvable, to prevent the phase work irrespective of any stored approval, cached currency record, or signed approval envelope.

**31.** The system of claim 3, further comprising a cohort-registry store holding successive externally signed cohort-registry snapshots, each snapshot carrying a monotonically increasing sequence number, a predecessor digest chaining to a preceding accepted snapshot, a validity interval, and a required cohort identifying solver entries by provider, model, and version, wherein the certification controller joins a certification record against the required cohort of an accepted snapshot valid at a declared evaluation instant, computes a refresh deadline as an earlier of an end of the validity interval and a last verified refresh instant advanced by a bound maximum refresh interval, and, at or after the refresh deadline, narrows an affected certified task or task lever to the watch state and caps a disposition of the author execution domain.

**32.** The system of claim 6, wherein an identifier of the candidate task is a UUID version 5 derived from a namespace pinned in an approved scope and from the task digest computed over the canonical hash domain, a set of canary tokens planted in the designated canary-token slots is derived from the task digest by a pure key-derivation function, and the certification controller prevents assignment of the identifier to a second representation of a candidate task whose task digest differs.

**33.** The method of claim 16, further comprising binding, in a task manifest of the candidate task, a completion bound, an execution envelope, and a reconciliation tolerance each sourced from a human-authored requirements representation bound by a digest-bound approval and none of which is derived from a declared difficulty tier, excluding the completion bound from the author projection and the auditor projection, permitting only duration evidence stamped with the completion bound and the execution envelope under which the duration evidence was measured to support a conformance determination, and forcing a blocking disposition when a measured reference-solution duration exceeds the completion bound on the execution envelope.

**34.** The non-transitory computer-readable storage medium of claim 24, wherein the instructions cause a digest-bound approval gate to be discharged in place of a human approval only upon receipt of a signed approval envelope of a declared predicate type that binds a canonical digest of a bound artifact, an approver identity resolving to an approver role authorized by the external trust root, a producer identity that differs from the approver identity, a producer model lineage, a roster of at least three council seats whose model lineages are mutually distinct and each distinct from the producer model lineage, a cast verdict for each council seat, and a digest of a recorded break-attempt transcript that failed to break the bound artifact, and only where every cast verdict is an approving verdict, and wherein the instructions prevent discharge of the digest-bound approval gate by the signed approval envelope for an enumerated class of acts comprising publication of a task bundle beyond a repository, a patent filing, recording of an operational acceptance against a capped disposition, and admission of a staged candidate into a human-graded calibration library.

**35. (Proposed.)** The system of claim 1, further comprising a disposition controller configured to mint, for each of the author execution domain and the auditor execution domain, a machine-readable disposition record whose value is restricted to a blocking value, a hold value, and a release-eligible qualification value, the disposition controller being prevented from minting a terminal release value; to copy a disposition line of a root report verbatim from the minted record and to refuse a root report stating a token above the minted record; and wherein the terminal release value is created only by a governance publisher separate from the workspace that produced the candidate task, the governance publisher being configured to evaluate a signed disposition record of a declared group predicate type against externally enrolled producer-role and approver-role thresholds, to accept an existing signed disposition record of a preceding predicate version without reserializing it, to require at least one authorized execution receipt per bundle under a closed release policy carrying exactly one outcome for every control of a closed core control set, and to refuse the terminal release value for a missing, failed, skipped, deferred, waived, duplicate, or extra control, and wherein a verifier of the governance publisher is prevented from executing the candidate task.


**36. (Proposed.)** The system of claim 1, wherein each task slot of a batch binds, at a digest-bound approval and before any bundle bytes are written, a task-lane value drawn from a closed set of a bound number of task-lane values, the task-lane value selecting exactly one task-lane root beneath which a single authoritative bundle location for that task slot is written, wherein the task-lane value is write-once for a life of an identifier of the task slot, wherein the system prevents residency of one identifier beneath two task-lane roots and prevents an assignment that would carry residency beneath a first task-lane root past a structural residency ceiling that no grant of a human-authored requirements representation relaxes, and wherein a deterministic grounding instrument re-derives task-lane residency from committed bytes across the task-lane roots and forces a retained finding upon disagreement with the bound task-lane value.


**37. (Proposed.)** The system of claim 1, further comprising a sabotage classifier configured to refuse each of a disposition unbound to a signed record, a disposition whose signature does not verify, a required instrument each branch of which returns a coverage gap, a governing-specification remote other than a canonical remote, a governing-specification checkout off a tracked branch, a typed sign-off standing in place of a recomputed digest, and a governance edit committed together with a subject the governance edit governs, and configured to treat a rewriting, force-pushing, replacement, or padding of published history that removes a prior refusal from a scan window as a further instance of the refused class rather than as a remedy, and to clear such a refusal only upon an append-only signed remediation record authorized by a principal group disjoint from an authority introduced by the flagged commit, the remediation record binding flagged commit identities, corrective commits, a re-verified trust snapshot, and rerun evidence, and preserving the flagged commits and the refusal as evidence.


**38. (Proposed.)** The system of claim 1, further comprising an operation verifier configured to require, for every authoritative write, a signed statement naming a project, a run, an operation verb, a digest of the record authorized by the write, a position and predecessor binding in a per-run hash chain, and a principal, to refuse a valid signature attributed to a principal other than its verified signer, and to refuse an operation sequence containing a removed, reordered, inserted, or foreign-namespace operation that fails the chain or namespace binding.

**39. (Proposed.)** The system of claim 1, further comprising a successor validator configured to decide a candidate transition against a baseline, a trust-policy digest, an enrolment set, and an authenticated operator supplied by a trusted caller independently of the candidate, never against a baseline supplied by the candidate, and to refuse an unenrolled operator, an unresolvable baseline or candidate lacking the baseline as its sole parent, a rewritten or truncated accepted stream, a mutated or deleted accepted record, a foreign-run write, a claim on an already assigned bundle or competing claims in one transition, a verdict without an accepted claim or an accepted consumer of that claim, a publication other than a next epoch succeeding an accepted current epoch, a backward epoch pointer, a seal earlier than an accepted watermark, changed trust-policy bytes, or, under a supplied signing policy, a claim or verdict without a verified operation binding its content digest.

**40. (Proposed.)** The system of claim 1, further comprising a memory publisher configured to receive per-run proposals bound to a previously read memory epoch, to stop publication upon conflicting proposals or a stale proposal base, and through one serialized publication to fold accepted proposals into an immutable successor epoch carrying a manifest, publish the author projection and the auditor projection bound to a digest of the manifest, and advance a current-epoch pointer without rewriting the preceding epoch.

**41. (Proposed.)** The system of claim 1, further comprising an evidence-admission controller configured to assign source classes to a closed tier vocabulary, open a lower discovery tier only after every substrate of a preceding tier of the closed tier vocabulary records an exhausted receipt, deny evidence admission by descent alone, require each receipt to bind a verbatim issued query and a result-set digest, and record an unexhausted lane as a named coverage gap rather than as a clean zero-result search.

**42. (Proposed.)** The system of claim 1, wherein the record-keeper execution domain represents supersession of an evidence row by a first-class never-deleted edge binding the superseded row, superseding evidence, an evidence digest, and an effective instant, permits creation of the edge only under tier-gated supersession authority, retains the superseded row, and excludes both the edge and its superseding anchor from the author projection.

## 10. ABSTRACT OF THE DISCLOSURE

Systems and methods control certification of executable tasks for evaluating or training machine-learning solvers. Access-controlled author, auditor, and record-keeper execution domains are arranged such that the author and auditor cannot communicate directly. The record-keeper supplies respective deterministic projections that disclose permitted ledger fields while excluding author design intent and auditor-private acceptance information. Candidate-task bytes and a solver registry are frozen under cryptographic digests. An externally authorized execution operator commits to an execution policy before conducting a pilot and signs an attestation binding the frozen task, solver population, trajectory, scorer closure, policy, and execution interval. A complete attestation representation is routed to the auditor while a restricted binding representation is routed to the record-keeper. Each domain independently verifies a common execution identity. A fail-closed controller creates an append-only certification record only after cryptographic alignment and a predeclared difficulty criterion are verified. Certification evidence may expire according to failure-mode-specific validity intervals.
