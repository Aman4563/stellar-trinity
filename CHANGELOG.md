# Changelog

- 2026-09-18: Close robustness finding F1: pilot qualification requires the auditor's fidelity-ledger digest and derived rollout statuses before any bound is computed, and the parent gate reads the private ledger with bounded, symlink-refusing access. Receipt verification with supplied ledger bytes binds the execution-fidelity control to the canonical digest and refuses HOLD or BLOCK evidence. Rollout disagreements disclose only the rollout id; execution-attestation and roster cross-binding remains deferred.

All notable changes to this project are recorded here. The format follows [Keep a Changelog 1.1.0](https://keepachangelog.com/en/1.1.0/) and this project adheres to [Semantic Versioning 2.0.0](https://semver.org/spec/v2.0.0.html).

Parents vendor this repository as a submodule wired to track `main`, never frozen at a raw commit SHA, and the Trinity freshness gate hard-blocks every instrument while that checkout sits behind the upstream tip, so that what a downstream project is held to is the current contract with a written diff behind it. Versioning here tracks obligations rather than code volume. Any change to `ENGRAM.md`, `FORGE.md`, `CRUCIBLE.md`, or `README.md` is a minor bump at minimum, because a contract edit changes what every downstream project owes. A change that removes an obligation, renames a disposition, or narrows a projection is a major bump. Harness fixes that change no obligation are a patch.

## [Unreleased]

### Fixed

- 2026-09-21: A parent that captured feedback before `github_id` joined the closed chain schema halted every instrument before Phase R, and nothing on disk could lift it. Every such chain lives at the harness root, its lines carry no login and a `writer` that is a bare string or a `{git_sha, model_lineage, runtime}` object, and `chain_line_shape` refuses the first line, so `check_feedback_chain` reported `chain line is missing the field github_id` in every legacy parent on disk. Migration was the stated remedy and the 2026-09-21 entry explicitly left it out. New `tools/migrate_feedback.py` decides over bytes alone whether a chain is that legacy shape and what its closed-shape replacement is, and `migrate.py` gains the `--feedback-github-id LOGIN` flag and a plan step that verifies the legacy links, recomputes `prev_hash` and `entry_hash` from the first line onward, backs the original up byte-exact, and records a revalidation row naming the signing step. It holds, naming the flag, when no usable login is supplied; holds rather than launders a chain whose own legacy links do not verify; holds on a writer shape outside the two recognized ones; and holds when a checkpoint log already publishes any head of the legacy chain, because rewriting beneath a signed head is the rollback the checkpoint exists to expose. The hand-written YAML ledger beside the chain is never touched. Signing the recovered head needed a door that did not exist, because `feedback.py checkpoint` addressed only `<harness>/runs/<run_id>/`: `checkpoint` and `walk` now accept `--legacy-root` in place of `--run` to address the frozen harness-root chain, `append` never does, and that walk holds the chain to the gate's own rule, which is the chain walk plus the checkpoint and never a re-derivation of legacy record digests the one-record-per-line ledger grammar postdates. Driven on a byte copy of a real legacy parent: `migrate.py --apply --feedback-github-id` rewrote three chains, `enrol.py` wrote the root, three `checkpoint --legacy-root` calls signed the heads, `check_feedback_chain` returned no finding, and one flipped byte in a signed chain was refused. Eleven tests in `tests/test_migrate_feedback.py` and three in `tests/test_feedback.py` carry both halves. No contract sentence changed: patch.
- 2026-09-21: A `harness/` root that runs its own CRUCIBLE blocked every push of its parent. `check_parent_layout` scanned each roster checkout with `scan_forbidden`, a flat walk that names any `.audit` path segment inside `harness/` a `PARENT_LAYOUT_BOUNDARY_LEAK`, which `BLOCKING_CODES` reads as `BLOCK` and `sentinel.py` maps onto `SAB_SUBMODULE_TAMPERED`, so a parent whose `harness/` is itself a trinity project, with its own `trinity/` submodule, its own root reports, and its own `.audit/TODO.md` and `capabilities.yaml` written by its own audit runs, had its pre-push hook refuse every push on `remove leaked paths from harness: .audit, .audit/TODO.md, .audit/capabilities.yaml` and every colleague running FORGE against the same parent stopped at preflight. Nothing about that harness was a leak: the bytes belonged to the nested project's own auditor, and the parent's scan was reading paths inside another project's root, which the README says no instrument may do. New `layout.is_trinity_project_root` recognizes the shape by the rule the README already uses to define a project root, a checkout whose own `.gitmodules` registers a submodule at exactly `trinity`, checked out with its `.git` metadata present and `tools/gate.py` a regular file inside it, refusing a symlink anywhere on that path and ignoring root reports and `AGENTS.md` entirely, so dropping markdown beside a leak exempts nothing. `check_parent_layout` skips the forbidden scan for a `RootKind.RUNNER` checkout that satisfies it and nothing else changes: the harness root's registration, remote, branch, gitlink, and symlink checks still run, and a `samples/`, `delivery/`, `.memory/`, or `staging/` root vendoring `trinity/` is still the leak the roster names. Four fixtures in `tests/test_layout.py` beside bite row A82's registered test carry the halves: a nested-project harness with `.audit/` holds at `SHIP_ELIGIBLE`, root reports alone still `BLOCK`, a `trinity` registration with no checkout still `BLOCK`, and a `samples/` root vendoring `trinity/` still `BLOCK`. ADR 0004 records the exemption beside the roster it narrows. No contract sentence changed: patch.
- 2026-09-21: A lawful resume permanently refused every push through the tracker card. The contracts direct a re-paste to resume under the same `run_id`, and a resume that reaches the report moment rewrites that run's `report.md` and `progress.yaml`, while the run's frozen `tracker.json` binds the SHA-256 of exactly those bytes. `tracker_card.read_card` re-hashed the frozen `sources` against the current bytes and answered any mismatch, and any absent source, with one undifferentiated string, and `check_tracker_cards` wrapped every string as `TRACKER_CARD_MALFORMED` at error severity, so an unparseable card and a run that lawfully continued after closure were the same refusal. `reconcile --check` and `render-reports --check` then exited nonzero on any finding whatever its severity, `.githooks/pre-push` refused the push, and because the scan covers every run directory and a frozen card is never repaired, no later run and no in-tree operation short of deleting a correct attestation or restoring stale bytes could clear it. The contracts already drew the line the scan path ignored: a changed source digest is `TRACKER_CARD_CONFLICT` at emission and `TRACKER_CARD_MALFORMED` is reserved for a card the renderer cannot parse. `read_card` now returns a typed `Refusal` under one closed code rather than a string, hashes each bound source separately so an absent or unreadable source is `TRACKER_CARD_INPUT_MISSING` rather than a phantom digest mismatch, compares the `run.json` digest apart from the derived ones so a card that no longer binds its identity stays `TRACKER_CARD_MALFORMED` at error, and names a card whose identity still binds `run.json` while only `report.md` or `progress.yaml` moved as the new advisory `TRACKER_CARD_SUPERSEDED`. `write_card` keeps every emission refusal the contracts name: a superseded or malformed frozen card is still `TRACKER_CARD_CONFLICT` and is still never repaired, so the fix rewrites no attestation and reopens no closure. `pipeline.py reconcile` and `render-reports` now refuse only on an error-severity finding and print an advisory without failing, which is the rule `gate.ceiling` already applied, and `reconcile` no longer re-wraps every root-report finding at error on its way through. The dashboard still projects a superseded card as closure unknown, so nothing derived becomes trustworthy through rendering. All three Card close sentences in `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md` now name the three scan-time findings and their severities. Contract edit: minor bump at minimum.
- 2026-09-21: `check_audit_cites_committed_files` refused every citation into `.memory/`, so the roster the contracts require and the checker that guards it could not both hold. `git_tracked_paths` listed the parent with `git ls-tree -r HEAD --name-only`, and `-r` recurses through trees and stops at a gitlink, which is a commit object, so a parent whose `.memory/` is a submodule tracked the bare name `.memory` and not one byte beneath it. Every backticked `.memory/` path a root report cited then missed the tracked set and raised `SAB_AUDIT_UNCOMMITTED`, which `gate.ceiling` reads as `BLOCK` because no `SAB_` code outside `HOLD_EXCEPTIONS` caps lower, so a conforming parent halted at preflight and each gate run minted a fresh attempt toward the threshold-5 repeat alert. Nothing about the parent was wrong: the layout the refusal fired on is the layout `check_parent_layout` demands, and stripping the backticks out of a generated report is hiding the citation rather than committing the file. New `git_gitlinks` reads the mode-160000 entries of the same tree and `owning_gitlink` resolves each cited path to the innermost submodule holding it, whose own HEAD answers the membership question through `tracked_contains`, cached per submodule so a report citing thirty memory paths still costs one listing. A cited path that is the gitlink itself is satisfied by the gitlink. `git_submodule_tracked` guards the crossing: git ascends out of an uninitialized submodule directory and answers from the parent repository, so an unguarded listing would hand back the parent tree and refuse every citation inside the submodule for exactly the original reason, and the working-tree root is now checked against the gitlink before the listing is trusted. A submodule with no listable HEAD in this clone is unverifiable rather than uncommitted and lands as an advisory carrying `SAB_SUBMODULE_UNVERIFIED`, which the ceiling does not read, so a parent-only clone holds at `SHIP_ELIGIBLE` instead of trading one unconditional block for another. Three fixtures carry the halves under bite row A7: a citation absent from the submodule HEAD still refuses, committed file, directory, and bare-root citations pass, and a deinitialized `.memory/` yields the advisory alone with `ceiling` proven clean. Behaviour for `.audit/` and `.seed/`, which are plain parent directories, is unchanged. No contract sentence changed: patch.
- 2026-09-20: The per-file bite-count check had never once read the table it guards. `FILE_ROW` matched a bare `shared.md` cell while `tests/bite/README.md` writes every filename backticked, so the pattern found nothing and `reconcile_readme` compared no per-file row against the corpus for as long as both have existed. Its fixture hid the gap rather than exposing it: `build_restatements` wrote the bare form, so the only test touching that table exercised a shape the real file never uses. The pattern now accepts the backticked form, and the fixture writes the table the way the real README writes it. Making the check live immediately caught what it had been missing: `forge.md` was restated at 75 against a corpus of 77 and `shared.md` at 161 against 169, both stale in committed bytes with nothing to notice. Two registered tests carry the halves, one drifting a backticked row and one leaving it matching, and the drift half fails if the pattern is reverted, so the check cannot go inert again without a red test. This is the same defect class as an inert instrument under verifier principle 7, which is that a check unable to fire is not a check, and it changes no contract obligation: patch.
- 2026-09-20: CRUCIBLE step 8o contradicted step 7g, and the contradiction emitted an unconditional refusal. Step 7g fixes that an instrument whose obligation `DEFERRED.md` defers caps exactly as the matching coverage gap would and never raises a disposition, and step 8o's matching gap is `D-COVERAGE-GAP` at `HOLD`; step 8o then read flatly that an absent preregistration is `BLOCK`. Both readings were textually supported, so the contract answered one question twice, and because `DEF-REWARD-STATISTICS` named no statistic set, no operator could ever bind one and the `BLOCK` fired on every corpus carrying rollouts. A finding that fires identically everywhere discriminates between nothing, and it crowded out the findings that do discriminate. Worse, step 8o resolved a present trajectories surface with no live instrument as `HOLD` while a live instrument reporting the same unavoidable absence reached `BLOCK`, so building the required instrument was strictly worse than omitting it. Step 8o now names the closed statistic set at six exact figures, `reward_cardinality`, `reward_support`, `reward_floor_mass`, `reward_ceiling_mass`, `reward_median`, and `reward_range`, each recomputed as an exact rational and never as a floating-point summary, with no seventh figure admitted because an open set lets a chooser add the one statistic that clears. The preregistration is committed at `.audit/preregistration/<uuid>.yaml` and its SHA-256 rides as a bound resource descriptor inside the `trinity.execution/v2` pre-run commitment, whose existing duty to predate the execution interval is the ordering proof, so no second ordering scheme and no carrier field is added. The disposition split is the fix: a moved preregistration stays `BLOCK` whether or not the register stands, because that is misconduct an operator chose, while an absent one resolves under step 7g at `HOLD` for as long as `DEF-REWARD-STATISTICS` stands and returns to `BLOCK` when that row closes. Step 8o also now states the rule the split protects, that building a required instrument never worsens a disposition against not building it. `tools/reward_distribution.py` computes the set and decides every branch, `tests/test_reward_distribution.py` proves both halves of each under verifier principle 7, and bite rows P15 through P21a replace three scenarios that had no threshold with twelve registered refusals. `rewarddistribution` therefore leaves `DEFERRED.md` section 5 alongside the code that evaluates its predicate; its section 4 row stays open and is narrowed to the external execution authority, because no operator has ever produced the pre-run commitment the ordering proof rests on. Separately, the reward-integrity bite rows were anchored to `CRUCIBLE.md:158`, which is step 8n rather than step 8o, so `just citations` passed against the wrong rule; all of them are re-anchored to step 8o. Contract edit: minor bump at minimum.
- 2026-09-20: The pre-push hook refused on a tree it never bound. It read `_local_sha` out of each ref line and then ignored it, so `git push origin HEAD~1:main` passed a reconciliation of HEAD while landing an older commit. The hook now refuses inside the ref loop when `_local_sha` is neither the zero sha nor `git rev-parse HEAD`, and names the reconciled HEAD as the commit to push. `reports._stable_tracker_lines` dropped the whole `Updated` line before comparison, and with it the samples ratio that line carries, so a stale tracker compared equal; the comparison is now exact over every byte and that exemption is gone. Every existing parent therefore reports tracker drift once on its first check after this change, and the installed hooks render the corrected bytes into the working tree for the human to stage. `tools/lanes.resident_uuids` now answers an absent lane root with an empty set rather than raising, so a parent that has not created its `delivery/` checkout still renders, and `tools/integrity.py` gained `_bootstrap_direct_execution` so the module resolves its imports the same way whether it runs as a package member or as a script. Contract edit: minor bump at minimum.
- 2026-09-19: A standing `SAB_REPEATED_ATTEMPT` refusal now carries its own arithmetic and its own way out, and the sentinel's commands agree with the gate about what stands. Every reader classifies an alert through one `alert_standings` pass as standing, superseded, or cleared: the ERROR names the distinct runs the repeat rule counted at their commits, so a reader can check the count against the ledger, and appends `clearer_remedy`, which asks `.memory/roots.yaml` the question a clearance verification would ask with every principal bound to `sentinel_clearer` offered as if verified and the alerted actor excluded, then names the eligible clearers, or says nobody can clear it and why: the root or `allowed_signers` or trusted version is absent, the role is undefined, its only binding is the alerted actor, or every binding is revoked or expired, in each case naming the trust-root rotation ceremony as the only remedy. This refuses nothing new and lowers no threshold, because a root that defines the role but binds nobody eligible was already a policy nothing could meet; it stops that state from presenting as a loop the operator could fix locally. `sentinel.py verify` and `status` refused a superseded alert the gate reads as an ADVISORY that caps nothing, so their exit codes disagreed with the disposition ceiling; both now refuse only a broken chain or a standing alert, `status` counts standing, superseded, and cleared alerts separately, and `record` prints only standing alerts. The pre-push hook's header claimed check mode mints no record while `gate.py` appends sabotage-class refusals to the sentinel ledger in every mode; the header and the module docstring now say so and name the per-commit attempt identity the hook writes under. `uncleared_alerts` is replaced by `standing_alerts`. No contract sentence changed and no new refusal exists, so no bite row is added; eight tests in `tests/test_sentinel.py` cover the remedy sentences and the command exit codes.
- 2026-09-18: The migration matrix now names re-issuance paths for the five refused measurement-fidelity schemas. `tools/migrate.py` detects legacy pilot records, execution envelopes, oracle receipts, and the externally configured release policy without rewriting them, records per-instance revalidation actions, and holds the authoring preflight. It never invents governance values or signs evidence. Patent implementation identifiers, the two execution-predicate descriptions in `DEFERRED.md`, the dated structural-gap status, and the bite-suite count now match the landed surfaces. Feedback-chain `github_id` compatibility is outside this fix.

### Added

- 2026-09-21: `tools/enrol.py` writes the first trust root for a parent that has none. Every feedback checkpoint must be signed by a principal the root binds to `feedback_checkpointer`, with no genesis exemption, and no door produced the root: `docs/trust-root.md` described a rotation ceremony that presumes a root already exists, `ENGRAM.md` listed `.memory/roots.yaml` among genesis outputs, and no tool emitted it, so every parent on disk halted at `FEEDBACK_CHECKPOINT_SIGNATURE_UNAVAILABLE` with no lawful signer and no lawful way to create one. `python3 ./trinity/tools/enrol.py ./ --principal LOGIN --public-key PATH` writes `roots.yaml` at version 1 binding one human to `feedback_checkpointer` and `run_operator` at threshold 1, `allowed_signers` carrying their software SSH public key under `namespaces="trinity.attestation.v1,git"`, and `trusted-root-version` at 1, then re-reads the root through `trustroot.authorize` for each role before it reports success. It refuses a parent already carrying any of the three files as `ENROL_ROOT_EXISTS`, because a change to an existing root is a rotation and never a second enrolment; refuses a private key, a security key, or a certificate offered as the public key under the software-only doctrine; and mints no key and holds no secret. The release and clearance roles stay unbound, since each needs a second distinct principal by construction. Seven tests in `tests/test_enrol.py` prove the written root lets the real `feedback.py append`, `checkpoint`, and `walk` sign and verify `seq` 1 with a real `ssh-keygen` key, and prove each refusal leaves no surface behind. `docs/trust-root.md` gains a first-enrolment section. No contract sentence changed: patch.
- 2026-09-21: A re-paste of a contract now resumes through code rather than prose. `tools/_phases.py` transcribes every phase table into data with an anti-fabrication test that holds each path to its own contract section, `tools/progress.py` reconstructs phase standing from authoritative bytes under a four-component `trinity.phase-fingerprint/v1`, writes `trinity.phase-receipt/v1` receipts that are never required and never sufficient for completion, and renders the run-scoped `trinity.progress/v1` projection in the flat grammar every existing consumer reads, `tools/feedback.py` writes the feedback record, the canonical chain line through `integrity.chain_digest`, and the signed checkpoint through the enrolled `feedback_checkpointer` root with no genesis exemption, `tools/render.py` regenerates `TODO.md` from `capabilities.yaml` and the run report's coverage gaps, `tools/preflight.py` wraps freshness, migration, the feedback walk, the frozen gate, the progress rebuild, and a precondition scan into one `trinity.preflight/v1` receipt that carries no disposition, and `tools/meter.py` derives per-lane tokens and wall clock from an `opencode export` session tree while refusing an output path inside any harness. ADR 0005 decides that a human gate pauses a run and only terminal closure freezes its tracker card, and the three Card close rules now say so. `docs/migrations.md` gains the legacy harness progress cache row, which `migrate.py` backs up without deleting. The three contracts share one sentence naming `trinity/tools/` as the sole implementation of the preflight, held byte-identical by `SHARED_BLOCKS`, and every `python3 ./trinity/tools/` invocation the contracts state is proven to parse by `tests/test_contract_commands.py`. The A/B acceptance run against a copy of a real parent stopped at the fixture: the preflight decided in 3379 ms and refused with a BLOCK ceiling because the legacy tree carries feedback chain lines with no `github_id`, harness ledgers that record no work digest for `requirements/`, `brief/`, `samples/`, `delivery/`, or `touchstones/`, a non-canonical `trinity` remote, and harness-root progress caches, so `resume_at` was withheld and no warm arm was run; the fixture and probe are recorded under `.omo/ulw-research/20260920-163526/assets/ab/` and the token and wall-clock reduction stays unmeasured until a parent passes its own never-skip checks. Contract edit: minor bump at minimum.
- 2026-09-21: ENGRAM Phase S scribe block, second pass of the day for the work `main`, closing the default-branch movement over `pitch/`, `media/`, and `study/` under steps 17, 18, and 20 and recording the prior-art verification pass over `patent/`. Sources: `paper/data/corpus.json` 1.2.0 with its self-check, `FORGE.md` line 16, `MAESTRO.md` line 13, `GAUNTLET.md` lines 10 and 14, `README.md`, `POSTMORTEM.md`, the 2026-09-17 interview record, and two read-only research lanes over Google Patents, the IETF datatracker, arxiv.org, GitHub, and DOI records. Ledger digest: none, so scribe stays capped at draft. Prior headline numbers on the deck: 20 anchors, 17 of 20 from 2026, 6 of 20 in the largest theme, a 1.5 times target floor, at least twenty slots. Current: 21 anchors, 18 of 21, 6 of 21, a 1.6 times target headroom floor beside the 1.5 times author-side composition headroom, thirty ordinary slots plus at most one anchor; the superseded round is recorded on a new erratum slide exactly as the paper's Erratum records it, `deck.pptx` is regenerated through `just pitch-deck` to twenty-four slides, and `pitch/figures/` is recopied from the regenerated paper figures. `media/` loses the twenty-slot cadence, the pilot-to-SHIP shortcut, and the nothing-to-install claim in favour of the thirty-slot batch, staged authoring with audited placement, SHIP_ELIGIBLE against a separate publisher, the five work orders, the five roster roots, the generated doors, and the tracker rendered from cards, with no number absent from the data artifact or a named clause. `study/` drops every fact that rested on the absent `Ethara-Orion-Quality-Assessment.md` into the teaching-note limitations, corrects the delivery-dispositions exhibit to state that its forty tasks are the initial thirty plus ten later argos tasks and that it is a last-recorded rather than pre-delivery snapshot, reads the exhibit caption from the data artifact instead of the generator, cites corpus 1.2.0, and leaves `interview.yaml` untouched. Prior-art verification: twenty-four backlog rows checked against primary records and recorded in `patent/portfolio.md` as agent-verified, mismatched, or not found with the source per row; five citations corrected across the applications on the same day, namely US12671589 replaced by US12671588 as the context-firewall reference, US20260180920A1 replaced by US20250358240A1, the compliance-expiry trio re-attributed to Drata and Referentia with US12717930 withdrawn, US12719813 withdrawn as not found, and the informal Qorum and quorum-review items withdrawn as panel teachings, while the two IETF drafts cited at stale revisions now name the current revision beside the one retrieved. Not scribed: no filing, deployment, or measured pass rate is asserted; a WO and EP family search for three fact-checking and credibility patents remains open; filing and every citation decision stay human. No contract obligation changed.
- 2026-09-21: ENGRAM Phase S scribe block for the work `main` on the legacy flat layout, scoped to the `patent/` surface under step 19, recorded here because the Trinity repository carries no `.memory/` research log. Sources: the five contracts, `DEFERRED.md`, `docs/`, `tools/` and `tests/` on disk with first-commit dates from git history, `CHANGELOG.md` entries dated 2026-09-16 through 2026-09-21, the twenty-six admitted `research/` pairs, and ten parallel research lanes whose prior-art register and filing-strategy notes are held in the orchestrator's working digest outside the tree. Ledger digest: none, this repository holds no measurement ledger, so scribe stays capped at draft, every technical assertion in the surface is labelled ENFORCED with a named `tools/` module and `tests/` file, CONTRACT-ONLY with a contract line, DEFERRED with a register row, or PROSPECTIVE, and no measured-difficulty claim appears on any surface. The step 9 paper gates are re-verified before deriving through `just --justfile tools/justfile paper-data`, `paper-latex`, and `paper-check`: data self-check, zero-warning LaTeX, eleven headlines mirrored, prose clean. Prior surface inventory: two files, `patent/application.md` with specification 8.1 through 8.44 and claims 1 through 37, and `patent/claims-skeleton.md`. Current inventory: seventeen files. The parent application gains subsections 8.45 through 8.58 for the mechanisms the 2026-09-18 through 2026-09-21 contract changes introduced and the patent never described, namely the eight-tier trust ladder with ordered descent, the `engram.receipt/v1` discovery receipt mapped onto PRISMA-S, the `engram.supersession/v1` edge with `superseded_kind` and its forbidden-field blinding, content-addressed corpus identity under `engram.identity/v1`, `engram.hardness/v2` with `row_version` and the GRADE-shaped confidence profile, the `engram.brief/v2` and `engram.next/v2` schemas, the OpenReview clearance-token rung, the mutable-data-alias finding, the `trinity.run-operation/v1` chain, the lawful-successor transition decided from a caller-supplied trusted context, memory epochs with proposals and views, run namespaces, and subject-closure binding, plus a named prior-art and novelty-residue section by family under step 12; subsections 8.44, 8.32, and 8.43 are reconciled in place to the write-once tracker card and rendered dashboard, anchor graduation, and the enforced half of path containment; proposed claims 38 through 42 join the ladder with no existing claim deleted, and the claims skeleton gains their drafting notes, Alice anchors, EPO and India s.3(k) technical-effect sections, an ENFORCED and PROSPECTIVE map over claims 1 through 42, and a portfolio map. Seven sibling applications are drafted as planned divisional or continuation subject matter, each self-contained with its own specification, named prior art, claims, and skeleton, at `patent/tamper-evident-governance/` (twenty claims), `patent/release-authority/` (twenty-eight), `patent/council-gate/` (twenty), `patent/identity-lanes-epochs/` (twenty), `patent/evidence-admission/` (twenty-seven), `patent/auditor-instruments/` (twenty-eight), and `patent/author-hardening/` (twenty), two hundred five claims and twenty-four independent claims across the family. `patent/portfolio.md` holds the family map, the filing chain relative to a first-filing date, the US, EP, and IN claim-form notes, the evidence classes with the named limitations the drafting lanes recorded (canary normalization and mutation seed-block tracking are contract-supported rather than implemented, anchor-duration fitting is arithmetic only, the council and conductor family is contract-only, admission, enrolment, publisher, observer, execution authority, quality-controls runner, and judge replay stay deferred), and the prior-art verification backlog every human must clear before filing. The `patent-lint` recipe now walks every Markdown file under `patent/` and stays advisory. Not scribed: no filing is made or asserted, no service is deployed or asserted, the subdirectories add no row to any works registry because this repository carries none, and the layout stays the flat implicit work `main` with the family members documented in the portfolio map. No contract obligation changed.
- 2026-09-20: Four follow-ups the Phase H evidence-surface rewrite deferred are discharged. Citations bound bytes without ever proving the bytes carried the rule: `citations.py update` relocates a reference by the SHA-256 of the line its anchor was recorded against, so a hand-corrected `Source` number is discarded whenever the stale anchor still resolves, and three references had been pointing at the wrong rule for as long as they have existed. Bite row E33 cited invariant E30 while naming the rule that lives at E35, and the Notes paragraph cited the `## Ground rules` heading rather than the bullet that states it and invariant E6 rather than E9. A new `citations.py rekey` subcommand releases the anchors of one named bite source line, which is the lawful replacement for hand-editing a tool-written lockfile, and a full audit of all three hundred twelve recorded citations printed each cited line beside the row that cites it so a human decided every verdict. `tests/bite/README.md` had also restated three hundred seven bound citations against a lockfile holding three hundred twelve, a count nothing checked, and a registered test now binds that restatement to the lockfile it claims to describe. Invariant E10 was prose nothing executed: `tools/corpus_identity.py` now holds every `research/` artifact to its source class, its stem, the SHA-256 over its retained bytes, the SHA-256 over its rendering, and a recorded deriving tool at a pinned version, refusing a loose artifact, an unknown class, a missing rendering, missing retained bytes, a missing or malformed manifest, either digest mismatch, a stem that disagrees with its path, and an empty tool or version, and a `corpus` recipe joins `just ci` so each refusal is a build failure. Identity is recorded beside each pair as `research/<source-class>/<stem>.identity.json` under the closed `engram.identity/v1` shape rather than in one shared manifest, because admission then rewrites no existing byte and two clones merge as a union. Five of the twenty-six renderings record `pdftotext` at version 26.09.0, reproduced from the retained bytes at bootstrap; the other twenty-one record `unestablished`, because no repository byte names the tool that produced them and at least three carry LaTeXML alt-text artifacts a PDF text extractor never emits, and that residual obligation is registered in `DEFERRED.md` rather than hidden behind a passing check. Bite rows E3 and E5 now claim CI coverage against registered tests, while E4 stays unverified because no byte distinguishes a hand-authored rendering from a derived one. The paper's anchor corpus is source-classed and content-addressed without moving its selection: every one of the twenty-one anchors carries `source_class` and the `content_digest` its `research/paper/` manifest records, `paper/data/corpus.py` asserts the two agree and exits non-zero on any disagreement, both manuscript mirrors gain a class column rendered by code, and Appendix A states that a row is bound to the exact file behind it rather than to an identifier a host may later reassign. Finally, the discovery receipt names the standard it follows: step 2f maps the receipt item by item onto the PRISMA-S literature-search reporting extension at doi 10.1186/s13643-020-01542-z, widens `engram.receipt/v1` from fifteen fields to twenty-one with `substrate_kind`, `filter_provenance`, `strategy_provenance`, `update_method`, `peer_review`, and `deduplication`, and fixes that a checklist item the receipt does not yet carry stands as a named `DIRECTIVE.md` coverage gap, while step 4 names the Cochrane Handbook chapter 14 GRADE vocabulary and the Oxford Centre for Evidence-Based Medicine 2011 levels as the sources of a scale it had been using without attribution. Deliberately unchanged: the paper's twenty-one-anchor selection, its seven themes, every aggregate, and `paper/latex/references.bib`, whose every entry already carries a stable key; the twenty-one renderings whose deriving tool no byte records, which are registered rather than re-derived; `ROW_STATES`, the two forbidden-path regexes, and `check_audit_forbidden_paths`. Not done and named as such: the OCEBM grading footnote, recovered but forty-nine words long and therefore quoted nowhere, and the reproduction of a declared derivation, which needs a converter the stdlib-only harness may not call. Contract edit: minor bump at minimum.
- 2026-09-20: Phase H discovery devours the whole digital record under a trust ladder, and the corpus that holds the evidence is content-addressed. Step 2 named exactly two surfaces, arXiv and GitHub, and no decision record, register row, changelog entry, or prior research pass ever chose that restriction or considered widening it, so the most consequential benchmark retractions and grader defects of the year, which live in blog posts and issue threads, could not supersede a row under step 3's rule that an artifact absent from `research/` satisfies no anchor. Step 2 now fixes eight tiers, `T1` independently adjudicated evidence through `T7` rumour with `T0` excluded outright, gives every admitted item an `adjudication`, a recorded provenance, and a `liveness`, and opens a tier only after every substrate of the tier above records an `exhausted` receipt; descent is for discovery and never for admission, a `T7` item opens a lead sought at `T1` or `T2` and never anchors a row, and a lane that answered with a challenge, a rate limit, an unfollowed redirect, or an unresolved rename stands `unexhausted` as a named coverage gap rather than as zero rows reported clean. New step 2f closes `engram.receipt/v1` over fifteen fields including `canonical_identity_resolved`, `query_verbatim`, `result_set_digest`, and `exhaustion`, because a search nobody recorded is a claim about recall rather than a measurement of it; new step 2g fixes one resolution ladder per source class, places OpenReview decisions and official reviews at `T2` behind a browser-minted clearance token pinning forum identity, note identity, `tmdate`, and the note digest, and records an archive digest as corroboration and never as identity. Step 2c pins the target harness shape at the OpenHands benchmarks commit `405bae7140d7e961a75f4910a0b2e7069731db96` and names `mutable-data-alias` as a harvest-lane finding class, because a benchmark whose data moves under its own harness reports a number nobody reproduces. Identity is now the SHA-256 ENGRAM computes over retained bytes: E10 generalises from a PDF pair to retained bytes, a derived rendering, a named deriving tool at a pinned version, and that digest, the stem becomes the label `<source-class>/<canonical-id>-<slug>`, a still-appending source is a lead and never an anchor, and the twenty-six existing pairs relocate under `research/paper/` with their identities unchanged because identity is the digest rather than the path. Step 4 retires `replicated`, `single-source`, and `adjacent-anchored` for `high`, `moderate`, `low`, and `very_low` with the downgrade and upgrade domains recorded beside the level, starting at the anchor's tier default and never exceeding what the highest-tier anchor supports. Step 10 gains `superseded_kind` of exactly `revoked` or `deprecated` as an axis on the existing `SUPERSEDED` state rather than a fourth state, plus the first-class `engram.supersession/v1` edge carrying `source_ref`, `target_ref`, a `relationship` of `revoked-by`, `evidence_digest`, and `instant_utc`, chainable and never deleted; a `T1` or `T2` item supersedes a row, a `T3` item supersedes one only as an admission against interest, a resolved forecast market is saturation evidence, and step 12 records a retraction check that re-tags a hit `deprecated` in place. Step 11 stamps every row with `engram.hardness/v2` and a `row_version`, Phase 2 item 7d asserts the digest, the tier floor, and the edge, and the edge and its `superseded_kind` join the named forbidden fields of `FORGE_VIEW` beside the superseding anchor, so the author still never learns what superseded its row. `engram.brief/v1` and `engram.next/v1` named an arXiv identifier as a typed field and are replaced by `engram.brief/v2` and `engram.next/v2` carrying `source_class`, `source_identity`, `content_digest`, `tier`, `adjudication`, and `liveness`, while `engram.harvest/v1` widens its paper stem to an artifact stem in place; the ingest door accepts a source-class-qualified identifier, and the FORGE and CRUCIBLE Phase R restatements widen to the same stem and stay short. `tools/integrity.py` closes the eight-member `SOURCE_TIERS` vocabulary and extends the ENGRAM vocabulary list, `tests/test_hardness_contract_vocab.py` holds the new tokens present and the three retired confidence tokens absent, `tests/test_research_fidelity_citations.py` holds the corpus to its source-class layout and refuses a digest mismatch, `tests/test_paper_corpus_backcompat.py` locks the paper artifact to its arXiv-keyed schema, and bite rows E3 through E5 widen while E43 through E47 refuse ordered-descent violation, a silently failed lane, a `T7` anchor, a missing receipt, and a `SUPERSEDED` row with no edge. Deliberately unchanged: `ROW_STATES` stays `CANDIDATE`, `ANCHORED`, and `SUPERSEDED`; the CRUCIBLE firewall path check, step 6a budget and hardness orthogonality, step 9's grading lock, E9, E9a, E12, E15, E23, and E30, the `ENGRAM.md` reconnaissance swarm's Bucket-N demotion-only role, which gains the tier vocabulary and nothing else, and the eleven-versus-ten doc-spine drift the parent-submodule decision record already carries. Not done and named as such: the paper's anchor tables and bibliography, the patent prior-art pass, the search-reporting checklist's full item set, the evidence-level table, and the relevance-adjudicated recall census, which is first-run work for the new Phase H. Contract edit: minor bump at minimum.
- 2026-09-20: `TRACKING.md` is a rendered founder dashboard and no run writes a byte of it. `tools/tracker.py card` freezes one `trinity.tracker-card/v1` close card per run at `<harness>/runs/<run_id>/tracker.json`, deriving every field from that run's own `run.json`, `report.md`, and `progress.yaml` and binding their SHA-256 digests, with `closed_at` write-once so a second emission is an idempotent no-op. A changed source digest after closure is `TRACKER_CARD_CONFLICT`, an absent input is `TRACKER_CARD_INPUT_MISSING`, and an instrument that disagrees with its harness is `TRACKER_CARD_IDENTITY_MISMATCH`, while `check_tracker_cards` names an unreadable card `TRACKER_CARD_MALFORMED`. `subject.OUTPUT_NAMES` and `release_evidence.RUN_SEALING_PATH` both carry the card, so closure changes no subject digest and breaks no run seal. `tools/tracker_snapshot.py` and `tools/tracker_cache.py` load one deterministic snapshot whose as-of instant is the newest instant recorded on disk rather than a wall clock, keyed by path, content digest, and parser version under the git admin directory at `trinity/tracker-cache/v1`, disposable, with an uncached fallback that renders identical bytes. `tools/dashboard.py`, with `dashboard_sections.py` and `dashboard_charts.py`, renders sixteen sections, from the headline, the Research, Engineering, and Operations organizational lanes, the disposition board, decisions needed, the changed-since view, the cycle timeline, bundle flow, samples occupancy, aging work, blockers, and the sign-off log through one section per instrument to a `## History` ledger regenerated chronologically, so a late-arriving run inserts in order rather than appending at the end. It embeds Mermaid `flowchart`, `pie`, `xychart-beta`, and `gantt`, and nothing else beyond plain Markdown. `tools/reports.py` delegates `TRACKING.md` to that renderer, renders a zero-run parent, compares the whole file exactly, and folds `check_tracker_cards` into `check_root_reports`. `pipeline.py render-reports ./ [--check] [--staged] [--json]` renders the same bytes without moving a bundle and exits 3 on `REPORT_NOT_RENDERED`, `REPORT_NOT_STAGED`, `REPORT_INPUTS_UNSTAGED`, or `REPORT_STAGED_UNAVAILABLE`, reading the index through `tools/report_staging.py`. Six hook templates, `pre-commit`, `pre-merge-commit`, `post-checkout`, `post-merge`, `post-rewrite`, and `pre-push`, call that report-only entry point and issue no git write. `templates/gitattributes` marks `TRACKING.md`, `DIRECTIVE.md`, `EDICT.md`, and `VERDICT.md` as `merge=trinity-generated`, and `gate.py install` writes each `.githooks/` hook executable, merges those four attribute lines the way it merges CODEOWNERS, and sets `merge.trinity-generated.name` beside `merge.trinity-generated.driver` at `true`. `check_gates_installed` refuses a missing or drifted hook as `GATE_HOOK_MISSING`, a missing attribute line as `GATE_ATTRIBUTES_MISSING`, and an absent or drifted driver as `GATE_MERGE_DRIVER_MISSING`. Contract edit: minor bump at minimum.
- 2026-09-18: `tools/operations.py` and `tools/transition_signing.py` attach authority to the operation rather than to the namespace. A run marker binds a namespace to a principal, but the marker is JSON the operator can edit and the principal inside it is whatever `GITHUB_ACTOR` or the git identity said, and signing that one file would still leave every later write unattributed. Each authoritative write is now a `trinity.run-operation/v1` statement naming the project, the run, the verb, the SHA-256 of the record it authorizes, its position in the run's hash chain, and the principal making it, carried in a DSSE envelope over the existing `tools/attest` suite and decided by `allowed_signers` plus the versioned trust root under the new `run_operator` role. Two properties follow: the signer and the claimed principal must be the same identity, so `OPERATION_PRINCIPAL_MISMATCH` refuses a valid signature attributed to someone else, and a run's operations chain from sequence one, so `OPERATION_CHAIN_BROKEN` refuses an operation removed, reordered, inserted, planted in another run's namespace, or carrying a second run id. An absent trust root and a rolled-back trusted version both refuse as `OPERATION_UNAUTHORIZED` rather than pass. `validate_transition` consumes the result: given a signing policy it refuses a claim or verdict whose bytes no verified operation names as `TRANSITION_UNSIGNED_OPERATION`, matching by content digest so the record on disk and the statement about it cannot drift apart, while a transition offered without a policy is judged on structure alone and every existing parent stays in that state. Eighteen tests cover the envelopes and six more cover the attribution rule, with bite rows A100 through A102 registering the refusals. Enrolment itself remains external: the trust root names principals and nothing here proves a principal is a distinct person. No contract sentence changed.
- 2026-09-18: Freeze sealed bundles until CRUCIBLE records a verdict and forbid resealing after either outcome. FORGE opens new batches of new uuids, leaves another run's unsealed staging work untouched with an EDICT coverage gap, places clean bundles without rework, and handles retained predecessors only through recorded lineage, preserved trajectories, and a successor under a new uuid. Pipeline verification names pre-verdict rework as PIPELINE_SEALED_MUTATED and status keeps frozen work in its existing pending and claimed buckets without auditor finding text. Contract edit: minor bump at minimum.
- 2026-09-18: `tools/transition.py`, `tools/transition_records.py`, and `tools/accepted_state.py` decide whether one candidate commit is a lawful successor to a pinned accepted base, which is the rule an admission boundary runs before it advances an accepted ref. The baseline, the trust-policy digest, the enrolment set, and the authenticated operator arrive in a caller-supplied `TrustedContext` and are never read out of the candidate, because a candidate naming its own baseline or its own trust policy has authorized itself. The closed `TRANSITION_` vocabulary refuses a candidate whose sole parent is not the base or that buries it as a merge's second parent, a rewritten or truncated queue stream, a mutated or deleted accepted claim, verdict, sentinel entry, or published epoch, a write into a run namespace the accepted marker binds to another principal, an operator outside the enrolment set, a claim on a bundle the accepted state already assigns, two claims offered at once, a verdict with no accepted claim or a consumer other than the accepted owner, an epoch that is not one past the accepted current, `.memory/current.json` moving backwards, a seal instant earlier than the accepted watermark, and trust-policy bytes other than the pinned ones. Ownership is therefore the accepted sequence rather than a self-reported `claimed_at` or `sealed_at`, which also closes the scarce-slot ordering `Resident.order` took on faith. Thirty tests drive it through real git repositories and bite rows A95 through A99 register the refusals. This decides transitions and serializes none of them: no process here holds exclusive write on an accepted ref, so the four register rows on push admission, run principal authentication, claim arbitration, and epoch races stay open and now name what the validator decides and what enforcement still needs. No contract sentence changed.
- 2026-09-18: A sentinel clearance is signed, and an attempt is attributed to the principal its own commit signature yields. Both halves of the adversarial-operator control were typed strings: `write_clearance` accepted an `--approver` name and `clearance_status` cleared the alert after a lowercase comparison against the actor's names, so an operator cleared their own `SAB_REPEATED_ATTEMPT` under a colleague's name, and `resolve_actor` built its actor from `git log -1 --format=%an <%ae>` and `GITHUB_ACTOR`, so `git -c user.name=Colleague commit` landed the alert on a colleague. A clearance is now a DSSE envelope at `.sentinel/clearances/<alert entry hash>.json.dsse` under payload type `application/vnd.trinity.sentinel-clearance+json`, signed by a software SSH key, verified against `.memory/allowed_signers`, and authorized by the new `sentinel_clearer` role in `.memory/roots.yaml` at threshold 1 with the alerted actor's verified principals supplied as `excluded_principals`; the alert and clearance now bind repo and attempt hashes so a clearance cannot be replayed across parents; `write_clearance` verifies the envelope it just signed through the same helper the gate uses and leaves no byte on disk when it does not clear. Attribution rests on a commit signature this repository verifies itself: the sentinel reads `git rev-parse HEAD` and `git cat-file commit HEAD`, recomputes the object id over those bytes before trusting them, strips the `gpgsig` header and its continuation lines to reconstruct exactly what git signed, and verifies the armored signature through `SshKeygenBackend` under the `git` namespace, which `tools/attest/backend_ssh.py` now accepts beside `trinity.attestation.v1` as a closed two-member set. A HEAD carrying no signature this repository can verify is attributed to the claimed identity `unverified:<author>`, which folds repeated unsigned attempts into one alert and never counts toward the impersonated principal. Records carry their schema: `trinity.sentinel-attempt/v2` and `trinity.sentinel-alert/v2` are written, v1 records stay readable and their bytes round-trip through `_record_body` unchanged, so every ledger written before this change still verifies. Existing typed clearances stop counting immediately: a `<alert entry hash>.json` file is read as `SAB_SENTINEL_CLEARANCE_UNSIGNED`, a finding-code string mapped onto `SabotageCode.SAB_SELF_APPROVAL` so the closed `SAB_` vocabulary and its weights are unchanged, the alert stands, and the typed clearance re-enters the ledger as a self-approval attempt. The `clear` command takes `--key`, the software SSH private key of a `sentinel_clearer` principal, beside `--reason`, and `--approver` is removed. One residual is registered instead of closed: an alert raised against a claimed actor names no verified principal, so `excluded_principals` is empty and a `sentinel_clearer` who is that operator can still clear it, which closes only when every principal that runs a gate enrols a commit-signing key, and `DEFERRED.md` section 10 carries the row. Bite rows A91 through A94 register the typed clearance, the self-signed clearance, the forged author, and the absent `allowed_signers`, and A24 and A39 are rewritten to the signed rule. Contract edit: minor bump at minimum.
- 2026-09-18: The machinery behind the measurement-fidelity prose is now code, recorded here because the per-wave entries covered the contracts and left the harness unrecorded. `tools/stats.py` carries the unbiased combinatorial `pass_at_k` over exact rationals, `group_indicator` and `worst_case_group_successes` for the worst-case rule, `bonferroni_confidence` and `group_upper_bound` for the corrected bound, and `minimum_groups` for the computed floor that replaced the hardcoded eight. `tools/pilot.py` parses closed `trinity.pilot-policy/v2` and `trinity.pilot-attempt/v2` with disjoint eight-member groups fixed before any outcome is observed, a pre-execution feasibility precheck, a bound harness-configuration digest, and the three-token trial vocabulary, and refuses a reason code at every schema depth so detector vocabulary never reaches `.seed/`. `tools/harness_config.py` parses closed `trinity.harness-config/v1` and separates approval from adequacy, and `check_harness_config` joins `QUALIFICATION_CHECKS` behind the pilot registry with eleven planted fixtures, a reachability enumeration, and an exemption for a parent carrying neither half. `tools/forensics/` lands the three-valued fidelity core over eight signal classes, five per-family native trace adapters, the per-family evidence contracts, the `G-FID` registration, and the private `.audit/fidelity.yaml` ledger whose projection carries three tokens and nothing else. `tools/attest/` gains the `trinity.execution/v2` predicate and refuses `trinity.execution/v1` as release evidence while keeping it parseable in a diagnosis mode that can never emit acceptance, and `tools/backfill.py` diagnoses historical rollouts behind forbidden-prefix, module-introspection, and diagnosis-only guards so it can never become an attestation path. `POSTMORTEM.md` carries the resubmission addendum. `tests/bite/` gains fourteen rows for these refusals, thirteen of them registered against executable tests and the fourteenth truthfully unverified because nothing discharges the four authority duties, and `tests/bite/citations.lock` is regenerated so every cited contract line resolves to the rule its row names. No contract obligation changed: patch.
- 2026-09-18: FORGE Phase 4 binds the independent execution authority's four duties by name and by refusal. Item 2a already required an `execution_operator` authorized by an external trust policy and a pre-run commitment predating the interval it covers, and stopped there, so a pilot could satisfy every word of it while one party declared its own population, ran its own image, recorded its own log, and retained its own bytes, which is a self-report with extra steps. New item 2b requires that operator to hold admission, execution, observation, and custody in effective control as `docs/execution-authority.md` fixes them, and states each duty by what it refuses: an unrostered rollout and an amendment to a window whose first rollout has begun for admission; a divergent runner digest, a configuration digest that is not the admitted one, and inherited state for execution; a capture taken after operator-side editing, or one resting on the agent's own account of itself, for observation; and a retrieval no roster row answers or a deletion inside the declared window for custody. Routing is exact and follows rule 2g: an executed population that cannot be proven against that roster is `BLOCK:INVALID_PILOT`, because an unprovable population is no measurement rather than a weak one; an unproven observation or custody duty caps at `HOLD:SUPPRESSED_MEASUREMENT` under the private audit-side classification `fidelity-unverified`; and a runner or effective configuration digest diverging from the admitted one caps there under `config-drift`, which rule 2g's fixed order selects ahead of the remainder. Those classifications stay on the audit side and the authoring lane reads the capped disposition alone, so the clause adds no reason vocabulary to `FORGE_VIEW`. Where no authority holds all four duties, the only admissible replacement measurement is a fresh independent re-execution under a pinned configuration by a party controlled by neither this agent nor the auditor; it never erases the original groups, which stay in the fixed N beside the replacement row, and no re-execution turns an unprovable population into a proven one. Provider-internal routing stays a declared trust assumption no duty here closes, so the item narrows the assumption set rather than emptying it, and every `DEFERRED.md` row this clause is the stated obligation for stays open, because no such authority has been deployed. Contract edit: minor bump at minimum.
- 2026-09-18: Measurement fidelity is now a refusal rather than an assumption. Every existing gate was one-sided: a task that measured easier than it declared already capped at `HOLD:MISCALIBRATED`, and a task whose measurement was itself violated in the suppressing direction shipped, because suppression lowers a measured pass rate and a lower pass rate is exactly what earns release. New `docs/measurement-fidelity.md` fixes the closed slippage taxonomy, holding mechanism, statistical consequence, and missing control on three separate axes, and names the eight signal classes every adapter reads. `FORGE.md` rule 2f now estimates each pass-at-8 rate from predeclared disjoint groups of exactly eight rollouts under the unbiased combinatorial estimator, fixes the group count, the permitted looks, and the comparison family before any outcome is observed, and scores a group by a worst-case indicator, so a suppressed trial drives the corrected bound up instead of down and an invalid group is never removed from the fixed count. The same rule binds `measured_group_floor` as the computed `minimum_groups(measured_pass_ceiling, familywise_confidence, comparisons)` in place of the hardcoded eight, which was infeasible arithmetic wearing a floor's clothes: eight groups cannot place the corrected bound below a ceiling of `0.40` once the comparison family reaches four, so the contract's own release rule could never be met. New rule 2g refuses suppressed measurement symmetrically, capping a violated or undemonstrable pilot at `HOLD:SUPPRESSED_MEASUREMENT` under six ordered closed reasons and refusing an unprovable executed population at `BLOCK:INVALID_PILOT`, because an unprovable population is no measurement rather than a weak one. Phase 4 item 3a pins the effective execution configuration as closed `trinity.harness-config/v1`, reconciles it against the attested configuration, and separates adequacy from approval; item 4i records trial conformance for every rollout under `conforming`, `nonconforming`, and `indeterminate`, derived by the auditor under `trinity.trial-conformance/v1` and carried beside the outcome without a reason code; item 8a names the principal and the surface its withholding binds, so CRUCIBLE's own retention and evidence ledger read as the different principal they always were. `CRUCIBLE.md` step 8q raises the reward-claim model to version 2 with execution as an eighth causal boundary and twenty-nine threats, and the capability vocabulary to version 2 with `context` as a ninth channel; step 8u builds the `G-FID` family, `G-FID-CONFIG`, `G-FID-TRACE`, `G-FID-POPULATION`, and `G-FID-HANDOFF`, into the private `.audit/fidelity.yaml` ledger no projection carries; step 8p ingests `trinity.execution/v2` and refuses a v1 attestation as release evidence; the new first-class finding class 17 retains every fidelity result; and verifier principle 8 makes detector standing three-valued, so a missing marker, an unsupported harness version, a conversion-only trace, and a self-reported zero never resolve as clean. `DEFERRED.md` gains six rows for the obligations this prose states and nothing evaluates: the independent execution authority over admission, execution, observation, and custody; observation capture preceding operator editing; governance-judged configuration adequacy; the precommitted population roster reconciled against realized groups; effective request and response capture; and declared, verified provider-internal routing. Contract edit: minor bump at minimum.
- 2026-09-18: The parent submodule roster is restored, enforced, and closed on a need-to-know basis. Commit `b49f381` demoted `samples/` and `delivery/` from submodules to plain directories in the parent, which folded the public marketing root and the private corpus root into one history and left `.memory/` where CRUCIBLE's mandated full-history walk reaches `HARDNESS.md`, the breach ADR 0002 adopted option 1 for and never implemented. Five roster roots are now required submodules, `trinity/`, `.memory/`, `samples/`, `delivery/`, and `harness/`, each against its own distinct remote with `branch = main` and `update = merge`, with `staging/` a directory or a submodule at the operator's choice; the parent records each root by gitlink alone and tracks no bundle byte of its own. New `tools/layout.py` holds the closed roster with a forbidden-path set per root, the one `.gitmodules` parser every caller now shares (`gate.gated_submodule_repos`, `sabotage.gitmodules_section`, `integrity.submodule_section` and `submodule_paths` were four independent scanners), and a bounded symlink-refusing leak scan; `SafeGitRepository.ls_files_stage` exposes the index read behind gitlinks. `check_parent_layout`, registered in `QUALIFICATION_CHECKS` after `check_parent_submodule`, emits twelve `PARENT_LAYOUT_*` codes: seven unfinished-scaffold codes hold (`GITMODULES_INVALID`, `SUBMODULE_MISSING`, `PLAIN_DIRECTORY`, `BRANCH_INVALID`, `RETIRED_ROOT`, `RUNTIME_UNIGNORED`, `SCAN_TRUNCATED`) and are deliberately absent from the sentinel map, and five crossed-boundary codes block through the new closed `gate.BLOCK_CODES` and map to `SAB_SUBMODULE_TAMPERED` (`UNKNOWN_SUBMODULE`, `SHARED_REMOTE`, `PARENT_TRACKS_BUNDLE`, `BOUNDARY_LEAK`, `SYMLINK`). `.gitmodules` is a governance path in `sabotage.py`, so a co-edit beside a governed subject is `SAB_WORKFLOW_TAMPER` and bite row A70 splits into A70 and A71; `CODEOWNED_PATHS` gains `/.gitmodules`; `GATED_SUBMODULES` derives from the roster. `deliverables/` is retired: every bundle is complete where it lives, with rollout evidence at `<lane>/<uuid>/trajectories/`, `check_bundle_layout` requires the directory on every placed lane bundle, `bundle_identity.BUNDLE_EVIDENCE_DIRS` excludes only that top-level subtree so every existing digest is unchanged and `IDENTITY_VERSION` does not bump, `release._trajectory_roots` keeps the legacy mirror as a third candidate for already-signed history, and a surviving `deliverables/` root holds at `PARENT_LAYOUT_RETIRED_ROOT`. `docs/adr/0004-parent-submodule-layout.md` records the decision, the severity line, and the three-axis privacy table; `docs/migrations.md` names the human steps for a plain-directory lane root and a surviving `deliverables/` root, with no matrix row; three `DEFERRED.md` rows have their `Needs` cells narrowed and none leave. Not closed and named as such: the broader CODEOWNERS doctrine beyond `/.github/`, `/trinity`, and `/.gitmodules`, and the eleven-versus-ten doc-spine drift ADR 0002 predicted. Bite rows A71 through A83, E38, F32, and R14 carry registered real-git tests. Contract edit: minor bump at minimum.
- 2026-09-18: Anchors graduate. A designated anchor was a `starter` that `reconcile` kept inside the thirty under `samples/` ahead of every ordinary bundle, so three to five of the public samples were calibration tasks and displaced ordinary ones into `delivery/`. `samples/` stays strictly thirty: an anchor holds a place inside the ceiling ahead of every ordinary bundle only while its standing is `CANDIDATE`, and once its signed `duration_outcome` has folded into `.memory/anchors.yaml` the anchor moves into `delivery/` because its purpose is served and the next ordinary bundle by seal order takes the freed place. The mover never reads the mapping: ENGRAM Phase H step 6b publishes `.memory/anchor_standing.yaml`, one `<uuid>: <state>` line per designated anchor with `CANDIDATE`, `ANCHORED`, or `SUPERSEDED` and no duration or hardness value, as a third file of every epoch beside the two projections, and `pipeline.py reconcile` graduates an `ANCHORED` or `SUPERSEDED` anchor from that file alone, naming an unreadable one as `PIPELINE_RECONCILE_DRIFT` and graduating nothing. `tools/lanes.py` carries the parser, `epochs.py` publishes the file with each epoch, FORGE Phase 0 item 7, Phase 4.5 item 5, invariant 35, and the tracker close, MAESTRO's quota rule, and the README say the same, and bite rows F80 and F81 carry registered tests. Contract edit: minor bump at minimum.
- 2026-09-18: No instrument commits. The shared gate binding paragraph told every run to commit its phase work before the report moment, ENGRAM Phase G step 1 and FORGE invariant 35 had runs committing inside submodule checkouts and recording gitlinks, FORGE Phase 2 committed the Harbor receipt, and `MAESTRO.md` had each lane committing and the conductor making boundary commits and pushes, so a Trinity run wrote history on its own. Every `git add`, `git commit`, submodule commit, gitlink update, and push is now a human act, and a run that issues one is the same class of sabotage attempt as bypassing a hook. The commit gate is a named human gate in `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md`: when the report moment needs a clean tree, the run prints the paths it wrote grouped by repository, submodule checkout first and parent gitlink second, a suggested message naming instrument, `run_id`, and phase, and the command to run next, records the waiting state in its progress cache, and stops; the human commits and re-pastes, and the run resumes at the report moment, halting once more for the post-gate bytes. MAESTRO presents a lane's commit gate under its rule 4 exactly as it presents a sign-off and owns no commit and no push. The README ground rules and bite row A90 carry the same rule. Contract edit: minor bump at minimum.
- 2026-09-18: One FORGE invocation fills `staging/`. The contract asked for a batch of no fewer than twenty slots and `pipeline.py` bounded the unaudited queue at eight, so a run authored twenty and sealed eight, and reaching thirty resident bundles took four cycles. A batch is now exactly `batch_size` ordinary slots, fixed at thirty and equal to `sample_ceiling`, plus at most one anchor slot, the earliest `requirements/` designated anchor without a resident or staged bundle, so an anchor never displaces an ordinary slot and a run never seeds the budget mapping twice; a batch under thirty is `batch-underfilled` and one with two anchors is `batch-overanchored`. `DEFAULT_MAX_UNAUDITED` is thirty-one, one batch, so a run seals every slot before the auditor claims a byte, and FORGE resume preflight item 13 places every uuid the previous CRUCIBLE invocation passed and reconciles before Phase R opens the next batch, so the cycle reads fill, audit, place. Every sealed slot also emits a design return, `.seed/returns/<uuid>.yaml`, a closed record of the targets, projection epoch, budget, predicted ceiling, and composition headroom it composed against, and new ENGRAM Phase H step 14 folds every return each invocation against `.memory/hardness.yaml`, `.memory/anchors.yaml`, and the headroom floors, marking `return-target-stale`, `return-budget-unanchored`, or `return-headroom-short` as Bucket-N gaps that never mint a CFER; returns are `FORGE_VIEW` inputs only and never cross into `CRUCIBLE_VIEW`. `MAESTRO.md` rule 5, the README, bite rows A22, F47, F67, and E42, and the backpressure test carry the same numbers, and `DEFERRED.md` section 6 registers that nothing yet counts a batch's ordinary and anchor slots or reads a return, so both rules hold by prose alone until a reader lands. Contract edit: minor bump at minimum.
- 2026-09-18: ENGRAM Phase S scribe block recording the harness-effects literature behind the measurement-fidelity obligations, kept here because the Trinity repository carries no `.memory/` research log. Five papers join `research/` as side-by-side `<arxiv-id>-<slug>.pdf` and `.md` pairs under E10, each markdown derived from the stored PDF bytes by `pdftotext` version 26.09.0 from poppler and never hand-authored. arXiv 2107.03374, Evaluating Large Language Models Trained on Code, gives the unbiased combinatorial pass-at-k estimator `tools/stats.py` implements in exact rationals, so a release bound is computed rather than approximated. arXiv 2602.07150, On Randomness in Agentic Evals, measures single-run pass-at-1 estimates moving 2.2 to 6.0 percentage points across repeated runs, with standard deviations above 1.5 points at temperature zero, which bounds how much apparent difficulty is noise and is why one rollout is never a measurement. arXiv 2605.23950, Stop Comparing LLM Agents Without Disclosing the Harness, reports an aggregate harness-variance to model-variance ratio of 7.8 times on its controlled grid, and it is the most load-bearing citation of the five because it says the quantity being measured is dominated by the quantity nobody discloses. arXiv 2608.11242, Lost in Compaction, finds the compactors it tested retain only 17 percent of injected side constraints on average, which quantifies the incident mechanism exactly: a compaction boundary drops the constraints a task declared, and the solver then fails for the harness's reason rather than its own. arXiv 2609.09218, The Double Measurement Confound in Agent Benchmarks, names the double measurement confound this taxonomy inherits, execution-critical decisions taken by a fixed scaffold instead of the model and scored by criteria that need not reflect task correctness, so the score measures the pipeline and reports it as capability. The engineering sources carry the same standing, which is grounding and never measurement. claude-code emits a `compact_boundary` system event at an automatic compaction and marks the resumed turn `isCompactSummary`, so both markers are readable in a native trace and neither reaches a normalized result file. `DISABLE_AUTO_COMPACT` is reported unreliable in anthropics/claude-code issues #42817 and #57490, and release v2.1.159 changed the default silently under issue #64520, which is why a supported-version set is mandatory rather than advisory and why an unsupported version can never resolve as clean. OpenHands emits a `Condensation` event carrying `forgotten_event_ids`, `summary`, and `summary_offset`, and declares its condenser as `condenser_config.type`, so any type other than the no-op is a declared context transformation that still needs authorization. Harbor ATIF version 1.8 adds `continued_trajectory_ref`, per-step `Metrics` token counts, and `Agent.model_name` beside `Agent.version`, which is what lets a normalized trajectory point back at the native source a detector must read. Harbor terminal-bench-science commit `deef067` excludes timeout trials from scoring, which is prior art for treating a non-conforming trial as its own class rather than a failure, and evidence that the practice already exists upstream. The citation set recorded here is exactly the set `docs/measurement-fidelity.md` cites, and `tests/test_research_fidelity_citations.py` holds the document's set to a subset of it, because a document citing what the repository never stored is a bibliography rather than a corpus. Not scribed: `CITATION.cff`, which carries software metadata and one preferred citation and enumerates no references, so it gains no row; and `paper/data/corpus.json`, whose anchor set is the paper's own selection and is reconciled by `paper/data/corpus.py` rather than by this block. No contract obligation changed.
- 2026-09-18: Front doors are installed, never hand-authored. Every command door under `.opencode/commands/` and every gate skill under `.agents/skills/` now ships as a template under `templates/doors/`, nineteen commands and four skills including the new `next.md`, `harness.md`, and `rig` doors, and `gate.py install`, which every gate preflight runs, writes each one into the vendoring parent byte-for-byte and refreshes any door whose bytes drifted, the same way it refreshes the pre-push hook. `check_front_doors` compares every rostered door against its template and refuses a drifted one as `FRONT_DOOR_DRIFT` beside the existing missing, stale, unknown, oversized, restating, escaping, and forbidden-name refusals, and the roster test proves every template names its contract, stays under the body ceiling, and carries both gate commands. The door paragraphs in `ENGRAM.md` Phase 1 item 9, `FORGE.md` Phase 1 item 12, `CRUCIBLE.md` Phase 1 item 13, and `MAESTRO.md` rule 8 now say a run never writes a door and the Trinity repository carries only the templates, so the hand-placed `.agents/skills/` copies inside this repository are removed. Contract edit: minor bump at minimum.
- 2026-09-18: `harness/`, the OpenHands-style benchmark extension an external runner uses to run inference and evaluation over the resident bundles, has one owner. ENGRAM Phase G step 8 registered it as a submodule shell beside `deliverables` and Phase H step 2c described its target plugin structure as harvested shape facts, but no instrument was told to generate its contents, so the release record's harness image digest and the `build_assets_harness` control bound a surface nobody built. The split that already governs the bundle roots now governs `harness/`: ENGRAM registers the shell and writes nothing inside it, and new `FORGE.md` Phase 1 item 1b generates and reconciles every byte under it to the step 2c structure, with an inference entry point and a separate evaluation entry point, the three shared-interface duties over resident bundles, orchestration inherited and never reimplemented, the agent under test behind a pinned vendored interface, scoring delegated to each bundle's own `tests/` under the pinned `harbor_release` so the extension carries no grading logic, every dependency pinned, no private byte read, the image digest bound at Phase 4.5, and a `harness/README.md` regenerated from the code. Phase 4 item 3 names that pinned image as what the external solver registry drives, and FORGE never runs it against a solver itself: an author-side run is a `.seed/probe.yaml` record, Bucket N and never difficulty evidence. Two FORGE doors join the roster, `.opencode/commands/harness.md` running item 1b alone and `.agents/skills/rig/SKILL.md` as the operator door for its entry points; `check_front_doors` now requires both and maps them to `FORGE.md`. The README repo map and door tables, bite rows F65 and F66, and a `DEFERRED.md` row registering that no check reads the extension's shape carry the same rule. Contract edit: minor bump at minimum.
- 2026-09-18: Harness creation moves to the head of each scaffold phase. `FORGE.md` Phase 1 scaffolded or reconciled `.seed/` at item 12, after items 2 through 11 had already written `.seed/proof.yaml`, `.seed/budget.yaml`, `.seed/feasibility.yaml`, and the leak controls and had already run the normalizer, verifier, and provenance modules that item built; the scaffold is now item 1a, the first write after the digest check, and names the parent project root beside `trinity/` as its location. `ENGRAM.md` Phase G scaffolded `.memory/` at step 9, after step 4 wrote `.memory/genesis.yaml`, step 7 stored `.memory/implementations/`, and step 7a stood up `.memory/hardness.yaml`, and its wording said to scaffold the harness into every submodule, contradicting step 1 and the README placement rule that the dotted harness roots are parent-root siblings; the scaffold is now step 6a, the first write after approval, placed at the parent root and never inside `trinity/` or any other submodule, steps 10 through 12 renumber to 9 through 11, and step 7's corpus-before-scaffold clause no longer names `.memory/`. `CRUCIBLE.md` Phase 1 already stood `.audit/` up at item 2 and is unchanged. Contract edit: minor bump at minimum.
- 2026-09-17: The workflow-tamper check now tiers by evidence instead of blocking on every co-edit. A commit that edits a gate workflow or signing authority beside a governed subject with no bypass syntax on a gate step and no SHIP token entering a root report is `SAB_WORKFLOW_TAMPER_SUSPECT`, caps at HOLD through a `gate.py` ceiling exception, never reaches the sentinel ledger, and closes when the same actor lands the flagged governance paths and the flagged subject paths again in separate commits inside the scan window. Either signal keeps the finding `SAB_WORKFLOW_TAMPER` at BLOCK with the signed remediation record as its only remedy. Governed subjects narrow to root reports, disposition records and signatures, verdicts, attestations, the bound contract, and bundle bytes, so a workflow tweak beside a lane README, `.gitmodules`, or the `trinity/` gitlink is housekeeping. The pinned historical sabotage paragraph in the three contracts, the SHARED_BLOCKS pins, bite rows A6, A40, A68, A69, and A70, and their tests carry the same rule. Contract edit: minor bump at minimum.
- 2026-09-17: Direct pushes to `main` are lawful and no pull request is required. `gate.py install` no longer asks GitHub for an approving review or a code-owner review, a ruleset without a `pull_request` rule now counts as protection when it carries the required status check and refuses force pushes, and the candidate `sentinel.yaml` reruns `pipeline.py reconcile --check` and `pipeline.py verify --accepted` from the pinned probe checkout on every pushed commit, so the pre-push hook is the admission and continuous integration names a bypassed hook after the fact. The register row on push admission says exactly that: a required status check never blocks a direct push, so the rerun is detection.
- 2026-09-17: `TRACKING.md` is incremental. The tracker rendered one four-line section per instrument from the current state alone, so every invocation overwrote what the previous one showed and a reader who opened the file after three runs saw only the third. The three instrument sections stay exactly four lines, and below them `tools/reports.py` now renders a GENERATED `## History` ledger, one table row per run directory on disk under every harness, oldest first, naming start instant, instrument, run id, principal, disposition, and phase. A later run appends a row and never overwrites an earlier one, the ledger is a pure function of disk so `check_root_reports` still refuses a hand edit, and no run parses or takes it as input, so the tracker stays outside the firewall. The tracker-close paragraph in `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md`, E40, and the README tracker paragraph name the ledger. Contract edit: minor bump at minimum.
- 2026-09-17: Trinity runs under many hands at once and syncs only through git. Every invocation is a run under a `run_id` minted by `tools/runs.py` and passed as `gate.py --run`, and it writes its mutable state only under `<harness>/runs/<run_id>/`, its receipts and its `trinity.qualification/v2` record included. That record binds the run's subject closure computed by new `tools/subject.py`, the run namespace, every bundle the run sealed or judged at its full digest, `requirements/`, `touchstones/`, the projection it reads, and the `trinity/` gitlink, so another runner's commit outside the closure never unbinds it; harness-root records keep the legacy HEAD rule. The sealed queue is a union of per-run streams under `.podium/queue/`, resealing is confined to the sealing run, a uuid sealed at two digests is `PIPELINE_STREAM_CONFLICT`, backpressure counts every stream, and `pipeline.py verify --accepted <ref>` refuses a stream the accepted ref holds that was rewritten rather than extended. Audit claims are one file per run under `.audit/queue.claims/<uuid>/`, the earliest claim at the sealed digest owns the bundle, verdicts land at `.audit/verdicts/<uuid>/<digest>.json` with the writer's run, a verdict by a non-owner is `PIPELINE_VERDICT_UNOWNED`, and a claim is never deleted by a verdict. Lanes are one immutable record per uuid under `.seed/lanes/`, `route` accepts only `starter` and `sample` and counts nothing, `place` renames into `samples/` alone, and new `pipeline.py reconcile` decides both ceilings at the merge: it orders every resident bundle by seal order with starters first, keeps thirty under `samples/`, routes the overflow into `delivery/` up to ten thousand, renders `samples/README.md`, `delivery/README.md`, and every root report from run reports through new `tools/reports.py`, and in `--check` mode reports `PIPELINE_RECONCILE_DRIFT` without writing; the pre-push hook runs it with `verify --accepted` before every push. Sentinel refusals append to per-run streams under `.sentinel/streams/` while repeat detection reads every stream, and every run namespace's feedback chain is walked. New `tools/epochs.py` gives ENGRAM proposals under `.memory/proposals/` and immutable epochs under `.memory/epochs/` with both projections bound to the manifest digest, refusing `proposal-conflict` and `proposal-stale` and reporting `EPOCH_DRIFT` at the gate; MAESTRO becomes a dependency scheduler. `gate.py promote`, `sign-partial`, `cosign-assemble`, `signing-status`, and `release.py` take `--release <id>` to sign against a frozen candidate under `<harness>/releases/<id>/`. Two-clone adversarial scenarios live in `tests/test_parallel.py`, twelve bite rows A56 through A67 register them, and five register rows name what the merge still cannot enforce: branch protection, run principal authentication, the human information barrier, claim-instant arbitration, and epoch publication races.
- 2026-09-17: Task lane routing is machine-decided. New `tools/lanes.py` owns the residency rule and `trinity/tools/pipeline.py route` binds every staged bundle's `task_lane` before its seal: the first thirty bundles, starters counted within them, route to `samples/` and the next ten thousand route to `delivery/` under the new structural `delivery_ceiling`, and the verb writes the `.seed/lanes.yaml` row itself while refusing a hand projection that disagrees as `lane-mismatch`, a second lane for a registered uuid as `lane-immutable`, and a slot that fits under neither root as `lane-quota-exceeded`. `pipeline.py place` refuses a rename past either root's ceiling with `ceiling`. `samples/<uuid>/` and `delivery/<uuid>/` are plain directories in the parent repository, never submodules: ENGRAM genesis no longer scaffolds `samples` or `delivery` submodules, `gate.py install` no longer treats them as gated submodule repositories, `samples/` is the public marketing root and `delivery/` the private corpus root, and publishing either beyond the parent stays a human request. `FORGE.md` Phase 0 item 7, Phase 2 item 7i, Phase 4.5 item 5, and invariant 35 carry the doctrine, bite rows F42 and F46 gain registered tests, rows F63 and F64 are new, and the samples residency-cap row leaves `DEFERRED.md` beside the code that evaluates it.
- 2026-09-17: `gate.py install` reads branch protection back before it writes it, and reads rulesets as well as classic protection. It used to PUT classic protection unconditionally and count only a successful PUT plus classic read-back, so a principal without administrative rights received 404 on every preflight, the receipt said `applied: false`, GATE_PROTECTION_MISSING held every parent at HOLD, and a ruleset an administrator had applied cleared nothing because rulesets never populate the classic endpoint. `read_protection` now reads the classic endpoint first and then the effective branch rules, accepts a ruleset only when it requires the `gate` check, forbids force pushes, requires pull request review, is actively enforced, and names no bypass actors, receipts protection already in force without a write, and PUTs classic protection only when nothing stands. A refusal now names the administrator path beside the reason. Bite row A45 records the refusal with a registered test proving both halves against a fake `gh` that answers the classic, rules, and rulesets endpoints. No contract sentence changed.
- 2026-09-17: The sentinel re-derives every standing SAB_REPEATED_ATTEMPT alert from the attempts ledger instead of restating it. The 2026-09-16 repeat rule related attempts by shared author at a threshold of two, so one gate run emitting several SAB codes by one person raised an alert on its first attempt; the same-day fix in `is_repeat` stopped raising such alerts but left every alert already raised standing until a distinct principal cleared it, and parent runs were reading "attempts 1 to 1" alerts as live incidents. `alert_stands` now re-derives each uncleared alert against the attempts it names under the rule in force, an alert the ledger no longer supports surfaces as an ADVISORY naming it superseded and caps nothing, it stays in the append-only ledger as history, and `raise_alerts` no longer folds a later genuine repeat into it. A genuine repeat still caps at BLOCK until a distinct principal records a clearance, because a repeat is evidence about the operator that fixing the tree does not undo. Bite row A44 records the refusal with a registered test proving both halves. No contract sentence changed; the shared paragraph already required continuous integration to re-derive both the ledger and the refusal.
- 2026-09-17: Added `staging/`, a plain directory or a submodule at the operator's choice, as the pre-placement bundle root and the `.seed/lanes.yaml` lane registry written at seal. `pipeline.py verdict` records claim-and-digest-bound immutable outcomes; `pipeline.py place` performs an idempotent digest-bound rename into the registered lane root, invoked only by the FORGE lane at Phase 4.5. FORGE and CRUCIBLE alternate movements with boundary commits, and unaudited bundles no longer count toward residency. `STAGING_RESIDUE` rejects a uuid under staging and a lane root; `PIPELINE_PLACED_UNCLEAN` flags placed bundles without a clean verdict. Every existing parent holds on `missing shared directory "staging/"` until a human creates the directory, with `mkdir staging` or `git submodule add <url> staging`; `gate.py install` protects it only when `.gitmodules` declares it. Contract edit: minor bump at minimum.
- 2026-09-17: Gate-bypass doctrine and the pre-push hook defect behind it. `templates/pre-push` invoked `trinity/tools/gate.py` with the root `.` while the gate accepts only canonical `./` project paths, so every installed hook exited at usage, every push was refused with "could not run", and a parent run answered by recommending `git push --no-verify`, while the hook's own header comment named that flag as available. The hook now passes `./`, creates `.sentinel/` before redirecting, keeps the gate's stderr in `.sentinel/pre-push.err`, and names the only recovery, fix the finding or rerun the preflight so `gate.py install` refreshes the hook, in the refusal message; install already rewrites a hook whose bytes differ from the template, so every parent picks the fix up at its next preflight. The shared adversarial operator paragraph in `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md`, pinned in `SHARED_BLOCKS`, gains the sentence that bypassing a scaffolded gate surface, a verification-skipping push flag, unsetting or redirecting `core.hooksPath`, editing the installed hook, or disabling a registered check, is the same class of sabotage attempt as history rewriting and that no contract, instrument, root report, or lane may recommend it. Bite row A41 records the refusal with a registered test proving both halves: the template passes `./` and never names a bypass, and the gate refuses `.` while accepting `./`. Contract edit: minor bump at minimum.
- 2026-09-17: ENGRAM Phase S scribe block for the work `main` on the legacy flat layout, recorded here because the Trinity repository carries no `.memory/` research log. Sources: the checked-in `research/` corpus, `CHANGELOG.md` entries dated 2026-09-03 through 2026-09-17, `POSTMORTEM.md`, `FEEDBACK.md`, `Ethara-Orion-Quality-Assessment.md`, and the sixteen-row `study/interview.yaml` recorded on 2026-09-17. Ledger digest: none, this repository holds no measurement ledger, so scribe stays capped at draft and no measured-difficulty claim appears on any surface. Prior headline numbers: 20 anchors, 7 themes, largest theme 6, 17 of 20 from 2026, agentic case studies 1. Current headline numbers: 21 anchors, 7 themes, largest theme 6, 18 of 21 from 2026, agentic case studies 2, largest-theme margin 2; the change is recorded in the paper's Erratum because reconciling `paper/data/corpus.json` against on-disk `research/` names found the one omitted paper 2606.19149. Emitted: `paper/data/corpus.json` 1.1.0 with a `--render-tables` and `--check-manuscript` mode in `corpus.py`, regenerated `paper/figures/` with `generate_figures.py` now emitting byte-identical output across runs by dropping the embedded creation timestamp, `paper/main.md` and `paper/latex/main.tex` with Section 4 reconciled to the current contracts, harness paths dotted, and four new appendices for reproducibility, per-instance record, notation, and sensitivity, plus `references.bib` and a zero-warning build at 72 bibliography entries; `patent/application.md` subsections 8.32 through 8.44 and proposed claims 35 through 37 with `claims-skeleton.md` cleared of dashes; the three `media/` files reconciled to the five work orders and the dotted harness roots; the new `study/` surface with `interview.yaml`, `case.md`, `teaching-note.md`, and `exhibits/regenerate.py`; and `tools/justfile` publication recipes `paper-data`, `paper-figures`, `paper-latex`, `paper-check`, `paper`, `pitch-deck`, `study-exhibits`, `surfaces-lint`, and `publish`. Not scribed: `pitch/`, which now trails the paper's 21-anchor headline and owes the step 17 erratum pass through `just pitch-deck`. No contract obligation changed.
- 2026-09-17: Historical sabotage doctrine joins the shared adversarial operator paragraph in `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md`, pinned in `SHARED_BLOCKS`. A parent run answered a historical SAB_WORKFLOW_TAMPER finding by recommending a force-push; the contracts now state that rewriting, force-pushing, replacing, or padding published history to move a finding out of the scan window is itself a sabotage attempt that no contract, instrument, root report, or lane may recommend, that the finding is suspicion rather than proof of intent, and that its only remedy is an append-only signed remediation record authorized by a distinct externally enrolled principal group that preserves the flagged commits and their finding as evidence. The same paragraph prescribes the forward-only recovery a run names beside the finding: review the flagged diff, re-establish authority through the rotation ceremony, land governance and subject corrections separately, rerun the gate, and submit the remediation record for a distinct principal group to sign. `check_workflow_tamper` now carries that recovery in every SAB_WORKFLOW_TAMPER message, so the parent block reads the civil path rather than a force-push, and a registered test refuses a message that names one. Bite row A40 records the refusal and `DEFERRED.md` section 10 registers that no check yet reads such a record, so the finding truthfully holds at BLOCK until one lands. Contract edit: minor bump at minimum.
- 2026-09-17: Documentation now matches the implemented collaborative disposition interface: new promotion defaults to group `trinity.release-disposition/v3` under externally governed `gate_producer` and `gate_approver` thresholds, existing valid signed v2 bytes remain accepted, and explicit `gate.py promote --approver NAME` remains the named-v2 compatibility path. Operational guidance now uses repeatable `--envelope`, software private key terminology, canonical `./` paths, transport-free incident-store v2 and authenticated observer collection, and fail-closed migration language for unknown disposition formats. No runtime behavior changed.
- 2026-09-17: The transport-free `tools/incident_store.py` replaces `tools/incident_mail.py` while retaining authenticated source and target identity, closed bounded incidents, equivocation evidence, threshold logic, and durable SQLite repeat-alert records. `status` and `check` provide bounded human inspection and return nonzero for a meaningful repeat alert without any reset path. Versioned migration preserves existing `incidents.sqlite3` records, renames the complete historical queue table to inert `legacy_mail_outbox_v1`, seeds repeat-alert history, and preserves exact v1 config bytes plus unknown fields in a private backup before activating closed `trinity.incident-store-config/v2`. Contract edit: removal of a delivery obligation, major bump required.
- 2026-09-16: Real project-structure migration replaces the earlier prose-only directory-rename promise. `tools/migrate.py` owns a closed v1 journal, exclusive lock, atomic writes, immutable content-addressed backups, rollback and retry, canonical `./` project paths, and actionable HOLD reports. The supported matrix is limited to missing project-manifest bootstrap, `trinity.project/v1` bare standard paths to canonical v2, recognized Harbor schema 1.0 and retired schema 1.4 fields through the existing Harbor migration recipe, and the historical pipeline digest only when it independently seals the current bytes. Task ids and pending queue progress survive; old verdicts and signed v2 or group v3 bytes remain untouched, and affected Harbor receipts, audits, and signatures require current revalidation rather than grandfathering. Authoring preflight applies safe plans, while check, report, signing, promotion, and release paths are migration-read-only. `docs/migrations.md` defines the exact matrix and limits. Contract edit: minor bump at minimum.
- 2026-09-16: Signing is now software-only across ENGRAM, FORGE, CRUCIBLE, the release templates, and hostile-operator guidance. The repository contained no command instructing `ed25519-sk` or FIDO key generation; the prior loophole was wording that called hardware optional or merely unnecessary. `SshKeygenBackend` now parses bounded OpenSSH private-key public headers before signing and parses SSHSIG public-key and signature fields plus `allowed_signers` public blobs before verification, rejects both OpenSSH security-key algorithms and their certificate forms with `key_algorithm_forbidden` before any subprocess or physical-device discovery, and retains software Ed25519, RSA, and NIST P-256, P-384, and P-521 ECDSA support. The documented key-generation command uses a prompted passphrase and a project-relative `./.secrets/levi_attestor` path. Contract edit: minor bump at minimum.
- 2026-09-16: Historical entry, superseded by the 2026-09-17 group-v3 default. Hostile-operator release hardening integrated release authority into a separate governance publisher repository and first documented separate co-signed FORGE and CRUCIBLE `trinity.release-disposition/v2` domains. Current verification also supports default group v3 role-threshold domains while preserving valid signed v2 bytes and the explicit named-v2 compatibility interface. The external quality-controls runner remains absent. Contract edit: minor bump at minimum.
- 2026-09-16: `tools/sabotage.py` classifies `.memory/roots.yaml`, `.memory/allowed_signers`, `.memory/trusted-root-version`, and `.memory/root-rotation/` as governance paths for the existing same-commit tamper rule. Regression commits combining each governance edit with a root report, disposition, or contract subject now produce SAB_WORKFLOW_TAMPER, while a governance-only rotation commit remains clean. This is a narrow commit-shape detector and is not presented as external trust.
- 2026-09-16: Historical entry, superseded by the 2026-09-17 transport removal. Alert doctrine made candidate `sentinel.yaml` a secretless static diagnostic and moved authority to a separately governed observer that accepted dispatch through an allowlisted GitHub App bridge, API-authenticated sender, observer source, target repository, run, actor, ref, and commit, and reran `incident_probe.py` read-only. Its separate delivery job and transport requirements no longer describe the active design; durable incident and repeat-alert state remains.
- 2026-09-16: Historical entry, superseded by the current ownership split and later observer rename. `trinity/tools/gate.py install`, run by every gate preflight, originally copied both workflow templates into the parent. It now installs only candidate qualification surfaces: `templates/sentinel.yaml`, `templates/pre-push`, CODEOWNERS, candidate branch protection, and receipts. Governance installs `templates/release-gate.yaml` only in a separate publisher repository and installs the current `templates/sentinel-observer.yaml` only in a separate observer repository. `check_gates_installed` still compares the legacy local scaffold with candidate source templates and cannot prove either external installation. Contract edit: minor bump.

### Fixed

- 2026-09-18: Sentinel finding paths and embedded project roots leaked absolute host paths into `.sentinel/attempts.jsonl` and its hashed bytes; gate recording now reuses public finding redaction, and sentinel chain and repeat findings use canonical `./` paths without changing the ledger schema or hash computation, so existing ledgers remain readable. Feedback and ledger checkpoint signature payloads now use the canonical `./` audience and verify after a parent moves; existing absolute-audience signatures require the operator to re-sign the checkpoint over the `./` audience, as signature refusal messages now explain. Execution and approval policies now use the audited project root's canonical `./` path relative to the authority root for both `expectedAudience` and the hashed policy filename, and external producers sign that same audience, so relocating the whole authority tree preserves verification without rewriting policies or signed statements. An absolute audience is refused with a remedy naming `expectedAudience` and requiring fresh signatures over the canonical relative identity.
- 2026-09-18: The pilot evaluator now reconciles each primary solver's realized groups against the fixed policy population, refuses repeated group identities across attempts, calls `minimum_groups` for feasibility, and applies the Bonferroni-corrected exact group upper bound rather than a descriptive rollout rate. Its worst-case rule counts a group as a success whenever any member succeeds or is nonconforming or indeterminate, never drops an invalid group from N, and refuses an affected-group fraction above the governance maximum using exact rationals. Declared pilot evidence without a policy and an execution operator in the repository's own signer set are refused. The statistical evaluator row leaves `DEFERRED.md` in the same commit as this evaluator and its regression tests; no external execution authority is created or claimed.
- 2026-09-18: SAB_INERT_INSTRUMENT no longer feeds the repeat counter. The sentinel related attempts by code alone, and an inert instrument is a state of the tree that every gate run re-observes until someone builds it, so five preflights in a month over the same unbuilt audit stubs raised SAB_REPEATED_ATTEMPT, a BLOCK that finishing the instruments could not lift and that a sole operator could not clear because clearance requires a distinct principal; `check_instrument_liveness` was already refusing the gate directly, so the escalation added no protection. New `STATE_CODES` in `tools/sentinel.py` names the code as a tree condition rather than an operator act, `is_repeat` never counts a member, every occurrence still reaches the append-only ledger and still refuses the gate on its own, and an alert already raised over it re-derives through `alert_stands` as a superseded ADVISORY that needs no clearance. Event-class codes such as self-approval, forged disposition, and ledger rollback keep the distinct-principal clearance unchanged. No contract sentence changed.
- 2026-09-17: SAB_DIRTY_TREE fired on every gate run for two structural reasons. The preflight's own `gate.py install` rewrote `.trinity-install/protection/<repo>.json` with a fresh `applied_at` on every invocation and then graded the tree it had just dirtied, so `_write_protection_receipt` now leaves an unchanged receipt untouched and `.trinity-install/` joins the one sealing set in `tools/release_evidence.py`. The report moment ran over a movement's uncommitted phase work while the record it mints binds the commit at HEAD, so `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md` now state in the gate binding paragraph that every byte the phase work wrote is committed before the report moment and only reports, receipts, disposition records, install receipts, and the sentinel ledger land after that commit; `MAESTRO.md` boundary commits carry only what a movement left. The refusal message now names that remedy.
- 2026-09-17: At-most-once alert semantics. Parent projects reported the same sentinel alert raised again after every clearance, six times in one day against one root cause, because one authoring pass tripped the repeat threshold against itself. Two defects fed the loop and both are closed. First, `tools/gate.py` minted a fresh `local-<uuid>` run identity for every invocation outside CI, so the preflight, the report, and the pre-push hook of one pass counted as two or three distinct attempts under a threshold of two; `local_run_id` now derives the identity from the commit the gate runs against, one attempt per HEAD, matching `GITHUB_RUN_ID` sharing across the moments of one workflow run, and falls back to a uuid only without a resolvable HEAD. Second, `tools/sentinel.py` mapped `LEDGER_CHECKPOINT_CHAIN_INVALID` onto `SAB_LEDGER_ROLLBACK`, so the missing or empty `.memory/checkpoints.yaml` every project carries until checkpoint signing lands was recorded as a rollback attempt on every run; the mapping is removed and only `CHECKPOINT_HEAD_ROLLED_BACK`, a head that moved backwards against a recorded one, still counts. `tools/incident_store.py` gains the same guarantee on the observer side: a repeat cluster that already carries a `repeat_alerts` row records later events without inserting a second alert, and `ingest` returns true only on the one ingest that raised it, while a cluster whose alerted run has aged out of the window still raises afresh. Registered tests cover all three. Harness fix, no obligation changed: patch.
- 2026-09-17: Repeat threshold raised from two to five distinct runs inside the thirty-day window, in `tools/sentinel.py`, `tools/incident_store.py`, and the `templates/SENTINEL.md` example configuration. Two distinct runs carrying one code was one authoring pass plus one retry on a small team; five is a pattern. Alerts already raised under the old threshold stand until cleared, and an alert line still records the threshold it was raised under. Harness fix: patch.
- 2026-09-17: One sealing rule, defined once. External signing and the dirty-tree check were at loggerheads because four binders each carried their own allowlist of paths a commit may touch after the commit a receipt or disposition binds: `gate.binds_head` accepted exactly the parent commit touching reports, receipts, records, and `.sentinel/` but not the envelope; `sabotage._binds_head` and `gate._candidate_commit_is_sealed` accepted any ancestor touching only `disposition.json` and its `.dsse`; `release._binds_candidate_snapshot` carried a fourth copy; and `check_dirty_tree_at_gate` tolerated only `.sentinel/`. No ordering of commits satisfied all four, so a report run dirtied the tree with its own receipt, committing the receipt forged the disposition, committing the envelope unbound the receipt, rerunning the report rewrote the record and invalidated every collected signature, and each turn of that loop landed a `SAB_DIRTY_TREE_CLAIMED_CLEAN`, `SAB_DISPOSITION_FORGED`, or `SAB_STALE_DIGEST_REUSE` attempt in the sentinel ledger. `tools/release_evidence.py` now owns `SEALING_PATHS`, `SEALING_PREFIXES`, `is_sealing_path`, and `seals_only`: the three root reports, `TRACKING.md`, `SCORE.md`, every harness `gate-receipts/` and `disposition.json` with its `.dsse`, `.sentinel/`, and `.trinity-runtime/` where partial envelopes are staged. Every binder now applies one rule, the bound commit is an ancestor of HEAD and `git diff --name-only <bound>..HEAD` names sealing artifacts alone, so a report commit, a promotion commit, one commit per external signer, and an assembly commit chain lawfully. The dirty-tree check tolerates uncommitted sealing artifacts and nothing else, because they are the gate's and the signers' own outputs, verified by content rather than by residency. `mint_record` is idempotent: it leaves an unsigned record alone while it still binds HEAD with the same ceiling, trinity gitlink, and bundle digest, and leaves a promoted final record alone while it is unexpired, still binds HEAD, and the ceiling is still `SHIP_ELIGIBLE`, so a rerun cannot silently kill collected signatures; `promote` reads such a record back through `load_qualified_record`. Registered tests chain receipt, promotion, partial, and envelope commits through every binder and prove a smuggled subject byte still breaks the chain. Harness fix, no obligation changed: patch.

### Removed

- 2026-09-17: Removed `tools/incident_mail.py`, `tests/test_incident_mail.py`, and `templates/sentinel-notifier.yaml`. Their replacements are `tools/incident_store.py`, `tests/test_incident_store.py`, and `templates/sentinel-observer.yaml`. Runtime mail imports, recipients, sender and address settings, credential lookup, transport configuration, queue dispatch, retry, delivery scheduling, and notifier runner workflow are gone; no external messaging substitute was added.
- 2026-09-16: Historical entry, superseded by the 2026-09-17 transport removal. The candidate sentinel mail path was removed the same day it landed: `notify`, SMTP transport, the `sentinel` environment, and every candidate `SENTINEL_SMTP_*` secret left `tools/sentinel.py` and `templates/sentinel.yaml`. A later observer-plus-notifier design was also removed; the active observer performs authenticated collection into durable incident state and has no SMTP credentials, recipient, or send path. Bite rows A24 and A39. Contract edit: minor bump.

- 2026-09-16: Historical entry, superseded by the 2026-09-17 group-v3 default for new promotion. `tools/gate.py` remains the candidate qualification entry point and mints BLOCK, HOLD, or SHIP_ELIGIBLE, never final SHIP. This entry introduced named-v2 full-snapshot and candidate binding; valid signed v2 bytes remain accepted, while new promotion now defaults to group v3 and explicit `--approver NAME` selects compatibility. The external publisher, not candidate CI, decides release. Contract edits: minor bump.
- 2026-09-16: Release note for whoever cuts the next tag: SHIP, SHIP:INFERRED, and CURRENT are now signed machine records rather than report prose, which changes what a disposition means for every downstream project and therefore meets the major-bump rule stated above, beyond the minor bump each contract edit below already owes.
- 2026-09-16: Historical entry, superseded for signing and notification. The adversarial operator doctrine originally described one gate-approver envelope and mail from candidate continuous integration. At the stage this entry recorded, final release required producer and approver co-signatures over separate FORGE and CRUCIBLE `trinity.release-disposition/v2` payloads accepted by the external publisher, candidate CI sent no mail, and a separate notifier held the historical fixed destination. The 2026-09-17 removal supersedes that delivery design entirely.
- 2026-09-16: `tools/sabotage.py`, seven parent checks registered in `PARENT_CHECKS`: SAB_DISPOSITION_UNBOUND and SAB_DISPOSITION_FORGED over the signed disposition record, SAB_INERT_INSTRUMENT by AST scan of `.audit/` and `.seed/`, SAB_AUDIT_UNCOMMITTED, SAB_SUBMODULE_REMOTE and SAB_SUBMODULE_OFF_MAIN against the canonical EtharaOrion remote, SAB_TYPED_SIGNOFF, SAB_WORKFLOW_TAMPER, and SAB_DIRTY_TREE, with both-halves tests.
- 2026-09-16: Historical entry, superseded by current release schemas and workflow ownership. The first `tools/release.py` gate verified `trinity.oracle-run/v1` and described `templates/release-gate.yaml` for parents and samples repositories. Current verification uses closed `trinity.release-policy/v2`, `trinity.oracle-run/v3`, default group `trinity.release-disposition/v3`, and compatible named-v2 dispositions, including complete export snapshot and candidate-commit binding plus mandatory control outcomes. Governance installs `release-gate.yaml` only in its separate publisher repository; candidate installation is forbidden.
- 2026-09-16: `tools/harbor.py`, the Harbor format-currency gate: `resolve` writes `harbor.lock` from the package index, `validate` runs Harbor's own manifest validator under the pinned release in an isolated interpreter and writes digest-bound receipts, `migrate` upgrades a pre-0.3.0 manifest, and two parent checks refuse HARBOR_PIN_MISSING, HARBOR_PIN_MALFORMED, HARBOR_PIN_STALE, HARBOR_PIN_UNTESTED, HARBOR_LOCK_MISSING, HARBOR_LOCK_MALFORMED, HARBOR_LOCK_STALE, HARBOR_SCHEMA_DRIFT, HARBOR_SCHEMA_INVALID, HARBOR_RECEIPT_TAMPERED, and HARBOR_RECEIPT_MISSING. FORGE item 2b now pins the latest stable release at batch open, exact but never stale, item 3a runs the validator, and ENGRAM Phase H step 2e tracks releases like papers. The Harbor manifest conformance row leaves `DEFERRED.md` in this changeset.
- 2026-09-16: `tools/pipeline.py`, the sealed-bundle producer and consumer queue at `.podium/queue.jsonl` so FORGE and CRUCIBLE run as one pipeline movement: `seal`, `pending`, `claim`, `release`, `verify`, and a parent check refusing PIPELINE_CHAIN_BROKEN, PIPELINE_SEALED_MUTATED, PIPELINE_UNSEALED_AUDIT, PIPELINE_BACKPRESSURE, PIPELINE_STALE_CLAIM, and PIPELINE_CONFIG_INVALID. MAESTRO cycle rule 5 admits the movement, FORGE Phase 2 item 7i seals per slot, CRUCIBLE rule 5 consumes per sealed digest, and ENGRAM folds each verdict as it lands.
- 2026-09-16: Historical entry, superseded by the 2026-09-17 transport removal. `tools/sentinel.py` retains local diagnostic ledger and repeat-refusal behavior, but candidate `templates/sentinel.yaml` is now a secretless static probe with no mail or dispatch authority. The active separately governed observer API-authenticates dispatch and target run identity, reruns the probe read-only, and stores admitted incidents in durable SQLite state without a notifier, SMTP credentials, recipient, or send path.
- 2026-09-16: CRUCIBLE step 8t states the ADVERSARY-PROBE family `G-ADV` with its null-floor rule, FORGE Phase 2 item 7j states the alternate-solution, mutant-ladder, and graded-literal obligations, CRUCIBLE rule 2 makes the Disposition line machine-readable, and `DEFERRED.md` section 10 registers what those sentences state without yet deciding. `tests/bite/shared.md` gains the thirty-two A rows, twenty-six of them registered against executable tests, and the closed-vocabulary check exempts refusal codes by prefix.
- 2026-09-14: The measured-headroom gate is enforced in the harness. `tools/pilot.py` accepts an optional `measuredPassCeiling` field in the closed `trinity.pilot-policy/v1` schema, validated as a float inside `(0, 0.40]` under the new `PILOT_MEASURED_CEILING_INVALID` refusal, and `evaluate_attempt_registry` refuses a complete attempt registry whose recomputed familywise Clopper-Pearson bound sits at or above the declared ceiling under the new `PILOT_MEASURED_HEADROOM_BREACH` refusal, while a provisional registry below its attempt budget stays provisional and a policy declaring no ceiling is unchanged. The check ships with a planted breach fixture it refuses and a clean fixture it accepts, plus parse-boundary tests, and the `DEFERRED.md` measured-headroom row leaves the register in the same changeset that lands the code evaluating its predicate. Harness enforcement of an obligation FORGE hardness rule 2f already states; patch bump.
- 2026-09-14: `TRACKING.md` is compact, three sections, and always renders its percentages. The tracker now carries exactly the ENGRAM, FORGE, and CRUCIBLE sections: the tracker close in `MAESTRO.md` and `GAUNTLET.md` becomes a tracker abstention, neither writes a section or edits the tracker, MAESTRO's harness-ownership rule drops its tracker write exception, and each instrument close deletes a stray section any work order outside the three once wrote. Each instrument section is exactly four lines, the disposition headline, the current phase or stop reason, a progress line rendering each ratio with both counts and its integer percentage in the form `phases N of M (P%), gates N of M (P%)`, and a one-line pending count of open gates by name and named gaps, with target paths, findings, and per-gap detail left to the root report; a section exceeding four lines or rendering a ratio without its percentage is non-conformant and the next close rewrites it. The shared programme line and the genesis day-one scaffold render their integer percentages, ENGRAM invariant E40 binds the three-section roster, the four-line shape, and the counts-beside-percentage rule, and `README.md`, `DEFERRED.md`, and the `tools/integrity.py` comment restate the narrowed ownership. Contract change, minor bump at minimum.
- 2026-09-14: Agent-supplied grant values route through proposal surfaces, never into `requirements/`. The three transcription clauses that instructed a run to write an invocation-supplied value into `requirements/` are removed, because `requirements/` is human-write-only and an agent transcription is an agent write however human its source. `ENGRAM.md` 6a and Phase N step 2 now stage a supplied budget or saturation floor as a dated grant-proposal record under `brief/` under E9a and halt for the human to author the matching `requirements/` row, and E9a's closed `brief/` content list admits those records while stating that a staged proposal is never a grant, binds nothing, and is recorded superseded once the human authors or declines the row. `FORGE.md` item 2e now keeps the supplied budget, rate, margin, or authoring-effort value on its own harness surfaces, captured verbatim in `.seed/feedback.yaml` and surfaced as a `.seed/TODO.md` row naming the `requirements/` row to author, then halts and binds only after human authorship and re-approval, because `brief/` has ENGRAM as its sole writer and a FORGE-written brief would open a FORGE-to-CRUCIBLE channel. Contract change, minor bump at minimum.
- 2026-09-14: GitHub identity attribution in feedback capture, because Trinity runs under many hands. Every Feedback capture section in `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md` gains step 1a, a fixed resolution ladder run once per invocation before the first record is appended: first the login of the authenticated GitHub CLI user, then the `GITHUB_ACTOR` value a CI runtime sets, with the answering rung recorded in `invocation_context`, an unresolvable identity recorded as `unattributed` with a named coverage gap, never guessed from a git config name, an email address, or a filesystem owner, and capture never suppressed by an unresolved identity. The closed ledger schema gains `github_id`, the GitHub login of the human whose turn the record captures, and the closed chain-line field set gains `github_id` carrying the same login into the tamper-evident chain, enforced by `CHAIN_FIELDS` and `chain_line_shape` in `tools/integrity.py`, which now refuse a chain line missing the field or carrying an empty value. `README.md` names the attribution in its ground rules, `DEFERRED.md` registers the unenforced authenticity predicate, since no check verifies a login against an authenticated session or a signature, and the bite corpus gains shared rows P7, CI-covered by a registered planted-positive test for a missing or empty `github_id` chain field, and P8 for an identity guessed outside the resolution ladder. Existing chains written under the nine-field set fail the closed-set walk and need re-capture or a human-signed reconciliation entry, which the standing tampering protocol already provides. Contract change, minor bump at minimum.
- 2026-09-14: `TRACKING.md`, the GENERATED sectioned executive tracker at the parent project root, refreshed by every work order at the close of every invocation. Each of the five work orders gains a tracker close, in the invocation model of `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md`, in the gauntlet run of `GAUNTLET.md`, and in the cycle of `MAESTRO.md`: read the file, rewrite exactly the run's own section from its own root report and progress cache, naming the disposition headline, current phase or stop reason, a done list, a pending list with every open gate and named gap, and a completion percentage that is a count over a count with both counts named beside it, and preserve every peer section byte-for-byte as opaque bytes never parsed, never taken as input, and never allowed to steer a decision, so the tracker is fresh after every invocation without becoming a channel between instruments. The shared programme line, resident `samples/` bundles over the structural ceiling of thirty, is refreshed only by ENGRAM, FORGE, and MAESTRO, whose contracts lawfully carry the quota, and CRUCIBLE and GAUNTLET refresh only the dated header and their own sections. ENGRAM Phase G step 8c scaffolds the day-one tracker at genesis, ENGRAM Phase 2 step 9a runs the close right after `DIRECTIVE.md` is emitted, MAESTRO rule 8 admits the tracker section to its write surface, and new invariant E40 fixes the tracker as derived, non-authoritative, sectioned, human-only, and free of difficulty, hardness, or quality claims, with the root reports winning on every conflict. `tools/integrity.py` adds `TRACKING.md` to ENGRAM's `EXTRA_REPORTS`, so `check_uppercase_md` admits the name and `check_parent_tree` requires the file in a parent project. `README.md` names the tracker in the parent repo map and the run walkthrough, and `DEFERRED.md` registers the unenforced freshness, content, and section-ownership predicate, with presence enforced today. Contract change, minor bump at minimum.
- 2026-09-14: Measured headroom and frontier reconnaissance. `FORGE.md` gains Hardness rule 2f binding `measured_pass_ceiling` at a conservative default of `0.40` on the pass-at-8 rate scale, so a slot earns `SHIP` only when the corrected Clopper-Pearson upper bound on every primary solver's pass-at-8 rate stays strictly below the effective frontier-defeat floor, which may never exceed the ceiling; the item 9l `probe_ceiling` default tightens from `0.50` to `0.40`, Phase 4 item 4 voids a pilot evaluated against a floor above the ceiling, invariant 23 names the new rule, and a new invocation-model item 8 fires a hardness swarm of capability-reconnaissance, composition-brainstorming, and adversarial-preview lanes before Phase 0 binds any composition. `ENGRAM.md` raises the Phase H step 6 target headroom from 50 to 60 percent, so every frontier-defeat target sits at or above 1.6 times its anchored evidence, mirrors the floor in the step 11 `HARDNESS.md` rendering and the Phase N successor bundle, projects the measured pass ceiling through the `FORGE_VIEW` hardness contract as a lower-only knob, and gains invocation-model item 7, a frontier reconnaissance swarm that sweeps evaluator surfaces, system cards, aggregator indexes, and the arXiv saturation literature on every invocation under a thirty-day staleness ceiling and two-source corroboration, with output held to Bucket-N demotion-only standing under E7. `README.md` restates the split headroom doctrine, and `DEFERRED.md` registers the unenforced measured-headroom gate. Contract change, minor bump at minimum.
- 2026-09-14: Absolute path containment as shared doctrine. The opening paragraphs of `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md` now state that the parent project root is exactly the directory whose direct child is the `trinity/` submodule, never that directory's own parent and never any higher ancestor, and that a run touching any path above or outside that root stops that work and records a named blocking coverage gap; the block is pinned in `SHARED_BLOCKS` in `tools/integrity.py`. `MAESTRO.md` confirms `trinity/` is a direct child of the working directory before the first lane launches and names that verified root in every lane prompt, `GAUNTLET.md` binds every path it touches, `.trial/` included, inside the same root, ENGRAM Phase G step 1 names the genesis target as the directory that holds or will hold `trinity/` as its direct child instead of the ambiguous parent knowledge-repository phrasing, and `README.md` restates the rule beside the vendored parent tree. Bite row R15 records the refusal and a deferred-register row records that no check enforces it yet.
- 2026-09-07: MAESTRO is extracted from ENGRAM Phase C back into the standalone `MAESTRO.md` work order outside the three instruments, with its own `.podium/` harness, append-only `.podium/cycles.yaml` ledger, derived `.podium/progress.yaml` cache, and `SCORE.md` root report. One MAESTRO invocation now conducts fixed-order isolated cycles toward FORGE's structural `sample_ceiling` of thirty resident bundles under `samples/`, with `starter` task-lane bundles counted within the same constant, while preserving the information barrier and stopping lawfully at quota, steady state, pending human gates, a terminal disposition, or a block without ever self-discharging a gate. The maestro front door now defers to `trinity/MAESTRO.md`, and the FORGE and CRUCIBLE conducted-launch rules name that standalone conducting authority while retaining their refusal of any lane prompt carrying extra context. Contract change, minor bump at minimum.
- 2026-09-05: Parent-gate enforcement for trust-root rotation, ACTIVE CFER evidence, committed trajectory bytes, the scaffolded front-door roster, declared judge reliability, and declared pilot-attempt correction. `check_root_rotation` calls `verify_rotation` whenever the active trust-root version advances and requires continuity through both old-root and new-root authorization; `tools/attest/intoto.py` registers the closed `engram.cfer/v1` predicate and `check_active_cfer_attestations` walks current `ACTIVE` rows in `.memory/ledger.yaml`, requires digest-named `.memory/proofs/<digest>.dsse` envelopes, verifies `execution_operator` authorization at the gate instant, checks digest and validity-window binding, and reconciles CFER identity and disposition; `tools/attest/merkle.py` implements the RFC 6962 `trinity.trajectory-merkle.v1` profile specified in `docs/trajectory-merkle.md`, and execution-attestation verification now recomputes the signed root from the committed manifest and rollout bytes; `check_front_doors` enumerates the seventeen command doors and the `seal`, `temper`, and `assay` skills against the contract rosters and refuses missing, stale, unknown, oversized, restating, escaping, or `AGENT.md`-named doors, with all twenty thin doors scaffolded at the repository root; `tools/judge_reliability.py` and `check_judge_reliability` require each score declared in `.audit/judgments.jsonl` to be supported by a hash-chained trial registry with at least eleven position-randomized trials, retest agreement, Fraction-exact Cohen kappa, an exact McNemar position-bias test, and a singleton split-conformal prediction set, while undeclared scores remain outside that gate; `tools/pilot.py`, `bonferroni_confidence` in `tools/stats.py`, and `check_pilot_attempt_registry` recompute Bonferroni-corrected cumulative Clopper-Pearson bounds over every declared hash-chained pilot-attempt registry and refuse an uncorrected best-of-many bound, while the Phase 4 evaluator remains separate; and `tools/probe.py` adds the advisory `prepare` and `score` localization-contamination smoke test without registering a gate. ENGRAM Phase 1 step 4 now pins the envelope name, `proofDigest` meaning, and ledger-row grammar, its citations are regenerated, and `PARENT_CHECKS` orders the new structural, rotation, CFER, pilot, and judge decisions before their dependent or terminal scans. `DEFERRED.md` removes the trajectory-root and front-door-roster rows, narrows the pilot, judge, conformal, and hardness-ablation rows to their genuinely unenforced remainder, and updates the verified test counts; `README.md` names the enforced roster and the new tool modules. Contract and guide change, minor bump at minimum.
- Phase N, the successor proposal branch in `ENGRAM.md`. Given a benchmark whose published pass rates the frontier now clears, the standalone phase designs its intellectual successor the way Terminal-Bench 2 succeeded Terminal-Bench, and proposes it rather than building it: the lane resolves the parent from an existing `brief/<stem>/brief.md`, a `requirements/` row, or a bare arXiv identifier chained through the Phase H step 2d ingest lane, determines saturation from published evidence alone against a `requirements/`-declared pass-rate floor defaulting to 0.80, researches the succession neighborhood under the Phase H step 2 protocol, derives the residual composition axes and unmeasured durable failure modes, and writes one successor bundle at `brief/<stem>-next/brief.md` under the new closed `engram.next/v1` schema carrying saturation evidence, candidate successor levers, intended cross-axis composition, frontier-defeat targets with at least 50 percent headroom over the strongest saturation evidence, pinned parent corpus and harness identities, and named gaps for every grant. The hardness catalog reconciles differentially under Phase H with saturation evidence superseding parent-anchored rows through the standing step 10 path, and the lane halts at the Phase 0.5 digest gate because a brief write moves bound bytes. New invariant E39 keeps the successor proposed, never authored, and saturation published evidence, never measurement; E9a grows the successor proposal fields; the resume preflight names Phase N an independent branch; the Phase 1 door roster gains `.opencode/commands/next.md`; `README.md` names the branch in its run walkthrough and front-door table; and `DEFERRED.md` registers the unevaluated schema and floor. Contract change, minor bump at minimum.
- Stratified committed-sample certification and grant-governed baseline bands across `FORGE.md`, `ENGRAM.md`, `README.md`, `tools/stats.py`, `tools/integrity.py`, `tools/attest/report.py`, the FORGE report schema, `tests/bite/forge.md`, `tests/bite/engram.md`, `tests/bite/README.md`, `tests/bite/citations.lock`, and `DEFERRED.md`. FORGE Phase 4 items 4f through 4h predeclared per-stratum sampling rules in `.trinity/seed/pilot.yaml`, required the external operator to commit and select each sample after batch freeze, and bound signed exact hypergeometric stratum records; Phase 4.5 item 2i added the `SHIP:INFERRED` release path with certificate class `inferred`; invariant 36 kept inference derived, labeled, and expiring; ENGRAM Phase H step 6d and E38 added `.trinity/memory/strata.yaml` ingestion and a recomputable inferred-hardness projection; `tools/stats.py` added `lot_defective_upper_bound` and `lot_defective_rate_upper_bound`; and the closed disposition vocabulary grew by exactly one token through corresponding vocabulary growth in `tools/integrity.py`, `tools/attest/report.py`, and the FORGE report schema, which the standing policy classifies as at least a minor bump. FORGE Phase 3 item 5c and item 9l.1 moved band cut points, `band_targets`, `mix_tolerance`, and admissibility under `requirements/` grants with current values retained as defaults, anchored `baseline_member` to the latest Anthropic Opus snapshot in the projected cohort registry, demoted `TRIVIAL` refusal to default policy unless a grant admits it, required each granted policy change to re-trigger Phase 0.5 sign-off, and added invariant 37 so bands are graded by contract and admitted by grant. The bite registers added refusal scenarios and updated corpus counts, the citation lock was regenerated by the citation tool, and the deferred register recorded the absent operator and unevaluated signature, bound, grant, and projection checks. Contract change, minor bump at minimum.
- Per-surface and per-lane ENGRAM front doors, and the default-branch scribe flow. ENGRAM Phase 1 step 9 grows the door roster with the surface commands `.opencode/commands/paper.md`, `.opencode/commands/pitch.md`, `.opencode/commands/media.md`, `.opencode/commands/patent.md`, and `.opencode/commands/study.md`, each running Phase S scoped to its one surface, plus the lane commands `.opencode/commands/ingest.md` for the Phase H step 2d paper ingest lane, `.opencode/commands/works.md` for the explicit human work registry instruction, and `.opencode/commands/interview.md` for the Phase S step 20a elicitation rounds. Phase S gains the surface dispatch rule, a derived surface door re-verifies the step 9 paper gates before deriving and stops at a named `DIRECTIVE.md` coverage gap when they fail, and Phase S now also runs as the closing movement of the default branch once Phase 2 passes, so one ENGRAM paste completes every publication surface in a single flow. The resume preflight names the closing movement so a Phase S stop never invalidates completed phases, and `README.md` names every command in a front-door table. All three door items also gain the self-checkout refusal: a checkout where the contracts sit at the root is the Trinity repository itself, never a parent, so a run there scaffolds no door, keeping every door in the vendoring parent alone. Contract change, minor bump at minimum.
- The case-study publication surface `study/` in `ENGRAM.md`, beside `paper/`, `pitch/`, `media/`, and `patent/`. Each registered work that carries a study holds `study/<work>/case.md`, a decision-forcing case narrative to the Harvard Business Review case-method standard, `study/<work>/teaching-note.md`, the paired teaching note carrying the synopsis, learning objectives, assignment questions, analysis, and withheld resolution, and `study/<work>/exhibits/`, reusing regenerated paper figures or deriving from the data artifacts by regenerable code. The case opens on a named protagonist facing a live decision and closes before the resolution, every named fact, number, quotation, and exhibit traces to `paper/<work>/data/` or dated ledger evidence, an invented quotation or composite fact is a defect, and journal submission stays human-gated. Phase S gains step 20 for the upgrade pass, Phase G scaffolds the surface at genesis, `README.md` names it in the genesis bullet and both repo maps, and `tools/integrity.py` adds `study` to `SHARED_DIRS` and `PARENT_SHARED_DIRS` so the unknown-path lint and the parent-tree check cover it. Step 20a grounds the case by directed elicitation: closed-ended questions drawn from a fixed coverage checklist, each naming the single fact it seeks and offering the candidates the ledger already suggests, with every dated verbatim answer recorded in the closed-schema append-only `study/<work>/interview.yaml`, answers classed as attested rather than measured, declined items surfacing as named teaching-note limitations, and a case fact grounded in neither measured evidence nor a dated interview answer remaining a defect. Contract change, minor bump at minimum.
- Phase C conduct in `ENGRAM.md`, where ENGRAM runs as MAESTRO, the conductor branch that drives the whole trinity from one paste. ENGRAM is the sole bridge and therefore the only lawful conductor, so the branch lives inside its contract rather than in a fourth instrument: movements run in fixed order, ENGRAM's own default branch in place and then FORGE and CRUCIBLE as isolated delegated lanes whose prompts carry nothing beyond the contract file and the parent project root. A halted gate halts its own movement and never the cycle, every pending gate lands on one consolidated sheet under `Escalations` in `DIRECTIVE.md`, and the loop re-opens only while a root report moved and an instrument can still proceed without a human, with a two-cycle identical-fingerprint guard closing at `steady-state`. The branch owns the append-only conduct ledger `.trinity/memory/conduct.yaml`, which carries digests, headlines, pending gates, and named conditions and never findings, hardness rows, or evidence bytes. ENGRAM's Phase 1 door roster gains `.opencode/commands/maestro.md`, `FORGE.md` and `CRUCIBLE.md` gain the conducted-launch invocation rule that refuses a lane prompt carrying any context beyond the contract file, and `README.md` names the branch in its front-door table and run walkthrough. Contract change, minor bump at minimum.
- The pantheon visual identity: `banner.jpg` regenerated as the five-figure dark-baroque plate with composited typography, and five mascot bust portraits under `images/`, one per work order, embedded in `README.md` as the gallery beneath the banner. Art assets beside a `README.md` edit; no contract obligation changed.
- Task lanes across `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md`. Every batch slot binds `task_lane` at Phase 0.5 from the closed set `starter`, `sample`, and `delivery`, and that field routes the slot to its task lane root: `starter` and `sample` write under `samples/`, `delivery` writes under `delivery/`. `sample_ceiling` is a structural contract constant fixed at thirty with starters counted within it, in the same class as the twenty-slot batch floor and the one-H100 envelope, and no `requirements/` grant relaxes it. A starter is the designated anchor task, the designation tightens from at least three to at least three and at most five, and no new anchor machinery is added. `task_lane` is per-slot, so the twenty-slot batch floor and the samples quota never collide: a batch is task lane mixed by construction and every surplus slot routes to the delivery task lane instead of refusing the batch. A task lane is never a swarm lane, a validation lane, an optimization lane, or an authoring lane. `delivery_target` is carried by name from a `requirements/` grant, never authored and never defaulted. FORGE invariant 35 and ENGRAM `E37` fix the routing and the partition, `lane-quota-exceeded` caps at `HOLD:PILOT_REQUIRED`, and `lane-overlap` is `BLOCK:INVALID_TASK`. The partition is safe where the retired `dataset/` and `samples/` twin split recorded below was not: that split kept one bundle's bytes in two places and two copies drift, while a partition keeps every uuid in exactly one root, so there is no second copy to drift from. CRUCIBLE's new `G-DEL-DISJOINT` instrument and the new `check_lane_disjoint` parent check convert that claim into a check, re-deriving task lane residency from committed bytes across both roots. Both projections are unchanged, the closed disposition vocabulary does not grow, and `DEFERRED.md` registers the unenforced residency cap and the unenforced `delivery_target` progress. Contract change, minor bump at minimum.
- The FORGE Opus baseline band lane and batch mix gate. Phase 3 item 5c draws exactly `baseline_n` single-attempt rollouts of `baseline_member`, the latest Anthropic Opus snapshot pinned in the projected cohort registry, and grades every slot on the closed band scale `TRIVIAL`, `EASY`, `MEDIUM`, `HARD`, `EXPERT` cut at eighths of the pass-at-8 rate: 87.5 to 100, 50 to under 87.5, 37.5 to under 50, 25 to under 37.5, and under 25 percent. A `TRIVIAL` slot cannot fill a batch slot and returns to the mutation ratchet at `HOLD:FRONTIER_NOT_DEFEATED`, and a batch whose realized mix misses `band_targets` of about 10 percent `EASY`, 30 `MEDIUM`, 40 `HARD`, and 20 `EXPERT` by more than `mix_tolerance` caps at `HOLD:PILOT_REQUIRED` with reason `mix-unmet`. Item 9l.1 predeclares the parameters in the `frontier_probe` block, bands land in `.trinity/seed/batch.yaml` and the `EDICT.md` slot table, and a band stays a Bucket N probe grade that never carries a difficulty claim. `tools/integrity.py` gains the closed `BAND_STATES` vocabulary, and `DEFERRED.md` registers the unenforced evaluator. Contract change, minor bump at minimum.
- Task economics through `.trinity/seed/economics.yaml`, `.trinity/memory/economics.yaml`, `task_cost`, `task_price`, `rate_card`, and `pricing_policy`, opt-in from human grants with no closed-disposition growth or new coarsened escalation flag. FORGE Phase 1 item 2e binds monetary rates without defaults and fixes eight hours as the structural workday unless `requirements/` overrides it. Phase 2 item 7a.3 meters authoring lanes, and Phase 4 item 4e resolves authoring and complete stages through a distinct signed pilot consumption receipt. Hardness rule 2d and invariant 34 keep economics outside difficulty evidence, while `economics-unbound` fails enabled work closed. The signed `cost_outcome` group names its resolution stage. ENGRAM Phase H step 6c folds once per reconciliation pass into the projected `average_task_cost` and `average_task_price` pair, Phase 2 step 7l checks it, and invariant E36 acknowledges arithmetic recoverability while storing no per-task row. `CRUCIBLE.md` has zero diff, and E31 stands unchanged because no token count crosses either view. Contract change, minor bump at minimum.
- Budget anchoring through `memory/anchors.yaml`, the projected `anchor_ceiling`, and the `budget-unanchored` coverage-gap reason. ENGRAM Phase H step 6b, Phase 2 step 7k, and invariant E35 fit and constrain the signed-evidence mapping. FORGE Phase 1 item 2e, Phase 2 item 7a.2, Phase 4 item 4d, Hardness rule 2d, and invariant 33 designate anchors, consult the ceiling, and preserve Phase 4 reconciliation as sole budget authority. Anchor rows reuse `CANDIDATE`, `ANCHORED`, and `SUPERSEDED` with zero growth of the closed disposition vocabulary. `CRUCIBLE.md` has zero diff, and orthogonality narrows to the evidence plane without changing its existing clauses. Contract change, minor bump at minimum.
- `tools/attest/`, a stdlib-only DSSE and in-toto signing suite, and the gate wiring that calls it. Fourteen modules across the package and its `policies/` subpackage: `canonical.py` fixes the `trinity.jcs-lf.v1` byte profile as RFC 8785 canonical JSON plus one terminating newline, refusing lone surrogates, non-finite numbers, duplicate object keys, cyclic containers, integers outside exact binary64, and nesting past sixty-four levels; `dsse.py` builds DSSE v1 pre-authentication encoding and verifies against the exact transmitted payload bytes, never a reserialization, because reserializing reintroduces the parser differentials DSSE exists to close; `backend.py` and `backend_ssh.py` put an `ssh-keygen -Y` backend behind a `SignatureBackend` protocol, pin the executable to an absolute path, run it with `shell=False` and an environment scrubbed to one variable, and pass an explicit `-O verify-time` to both `find-principals` and `verify` so no verification reads a clock the caller did not choose; `intoto.py` carries in-toto Statement v1 with the two registered predicate types `trinity.approval/v1` and `trinity.execution/v1`, each with a closed field set and a signed replay block of nonce, audience, sequence, and run identity, and it maps each predicate type to exactly one role; `trustroot.py` counts distinct verified principals against a per-role threshold, refuses a revoked or expired principal, refuses a root whose monotonic version has rolled back, and refuses to answer at all without an explicit evaluation instant; `replay.py` owns a persistent gate-owned store that takes an exclusive lock, spawns a contending process to prove that lock excludes before it writes, chains every entry under a keyed digest anchored outside the store file, and reports a distinct fatal outcome when a commit result is uncertain rather than guessing which way it fell; `verify.py` orchestrates the seventeen-step check and derives the required role from the statement's own `predicateType`, so a caller may veto a role and may never select one, and takes replay metadata only from the signed predicate; `report.py` binds the three root reports to per-instrument predicate types and roles over the closed six-section skeleton; `policies/execution.py` resolves an execution attestation against the `execution_operator` role and refuses an envelope whose gate instant sits outside the signed `executionInterval`; and `errors.py`, `__init__.py`, and `__main__.py` carry the closed refusal vocabulary, the export surface, and the `python -m tools.attest verify` entry point. Two hundred fifty-three tests cover it. Zero runtime dependencies were preserved throughout, and the `runtime-dependency-free` recipe now proves both that `project.dependencies` is empty and that every `[project.optional-dependencies]` group is empty, because an extra is a dependency wearing an opt-in. The reasoning lives in `docs/adr/0001-signing-backend.md`, `docs/adr/0002-information-barrier.md`, and `docs/adr/0003-canonicalization.md`, with the normative detail in `docs/canonical-bytes.md` and `docs/trust-root.md`.
- Six new decisions in `tools/integrity.py` that move stated obligations out of `DEFERRED.md` and into code. `evaluate_trinity_freshness`, behind `check_parent_submodule`, runs `git fetch --no-tags --quiet origin refs/heads/main` inside the vendored checkout and compares the checked-out commit to the fetched tip, naming five distinct stale outcomes, `TRINITY_FRESHNESS_BEHIND`, `TRINITY_FRESHNESS_DIVERGED`, `TRINITY_FRESHNESS_LOCALLY_MODIFIED`, `TRINITY_FRESHNESS_UNVERIFIABLE`, and `TRINITY_FRESHNESS_GIT_ABSENT`, and treating a failed fetch as unverifiable so offline is a hard block rather than a waiver; the `.gitmodules` wiring check it replaces still runs beside it. `check_approval_envelopes` verifies a `trinity.approval/v1` envelope at each of the three SHA-256 gates and refuses a forged, mis-predicated, untrusted, expired, revoked, under-threshold, replayed, or self-approved approval, alongside a missing or mismatched artifact digest and a missing or unreconciled failed-break-attempt transcript. `check_feedback_chain` walks each harness chain and then verifies the detached checkpoint signature over the chain head against the `feedback_checkpointer` role. `check_audit_forbidden_paths` refuses an `audit/` evidence line citing `memory/hardness.yaml` or `HARDNESS.md`, making one leg of the information barrier a check rather than a promise. `check_human_only_history` reads git history and refuses an agent-attributed or bot-attributed commit touching `requirements/` or `touchstones/`, refusing a shallow or unattributable history outright rather than passing it. `check_split_brain_writers` statically resolves write paths across `open`, the pathlib write and move methods, the `os` write, move, and remove functions, and the `shutil` copy, move, and tree-removal functions, following parent-segment traversal and function-local variable binding, and refuses a harness writing into a directory a sibling harness owns. The gate-owned policy those checks read lives at `~/.config/trinity/parents/<sha256-of-resolved-project-root>.json`, outside the audited project's writable area; it supplies the evaluation instant, the expected audience, the replay-store path, and the trusted root version, and an absent, malformed, or wrong-rooted policy fails closed.
- `tools/bite.py`, which makes bite coverage machine truth. A `tests/bite/` row claiming `CI: yes` with no registered test is `BITE_CLAIM_UNVERIFIED`, a registered test with no row is `BITE_TEST_ORPHANED`, and a coverage count restated in `DEFERRED.md` or `tests/bite/README.md` that drifts from the corpus or is reworded past the point the check can read it is `BITE_COUNT_DRIFT` or `BITE_COUNT_MISSING`. Current output: two hundred ten rows, twenty-seven claiming CI coverage, twenty-seven with registered tests. New `just bite` recipe, wired into `just ci`.
- `tools/_findings.py`, a structured `Finding` type carrying code, severity, path, line, and message, replacing the flat strings every linter used to pass around. It also closed a real defect: a finding message containing a colon was silently corrupted in JSON output, so the machine-readable surface disagreed with the human one on exactly the findings whose text was most specific.
- Two silent passes closed. An HTML comment used to satisfy the entire closed-vocabulary and shared-block layer, because those checks read raw bytes and a commented copy counted as a stated rule; `visible_markdown` now strips comments before any of them read a line. A fence tagged `text` hid hard-wrapped prose from every style check; `tools/prose.py` now exempts a fence only when its info string names a recognized non-prose language, so an untagged or `text`-tagged fence is read as prose. Both were inert checks rather than wrong answers, which is the failure mode Trinity's own doctrine calls a BLOCK.
- Corrected Harbor delivery-format attribution in `FORGE.md`, a pinned `samples/README.md` skeleton, pinned optimization plot filenames, and three new parent-mode checks in `tools/integrity.py` that hold a bundle to the layout the contracts describe. FORGE previously read as though Harbor required the whole Trinity bundle. Harbor requires `task.toml`, `instruction.md`, and `tests/` and nothing further, `solution/` is a stricter Trinity extension, and the six private filenames `TRUTH.md`, `grounding.yaml`, `recompute.py`, `rubrics.json`, `provenance.yaml`, and `policy.yaml` are Trinity extensions sitting inside the Harbor-defined `solution/` and `tests/` trees rather than Harbor requirements. The manifest key is `schema_version` and never `harbor_schema_version`, Harbor fixes the reward path to `/logs/verifier/reward.json` and then `/logs/verifier/reward.txt`, and the zero-to-one reward range is Trinity-local policy Harbor does not enforce. FORGE clause 2g pins the `samples/README.md` skeleton to the three mandated sections `## Promoted set`, `## Standing`, and `## Screening roots` in that reading order, and the two optimization plots are pinned to `dataset/<uuid>/trajectories/reward_by_iteration.svg` and `dataset/<uuid>/trajectories/reward_by_tokens.svg`. `check_bundle_layout` walks every child directory of `dataset/` and `samples/` and refuses a directory name that is not a canonical lowercase uuid5, a missing required file or directory, a missing `tests/test.sh` or `solution/solve.sh` entry point, a missing generated solution artifact, and a `solution/TRUTH.md` that is unreadable or stripped of its `GENERATED SECTION. DO NOT HAND-EDIT.` banner. `check_samples_readme` refuses a missing, unreadable, or unbannered `samples/README.md`, a missing mandated section, sections that depart from the canonical reading order, and a promoted bundle uuid with no README row. `check_samples_twin` implements CRUCIBLE item 8 and refuses a promoted bundle with no `dataset/` twin, a differing twin file set, an unreadable comparison file, and diverging twin bytes, skipping `trajectories/` on both sides because rollout evidence sits outside bundle identity. `check_parent_tree` now requires `dataset/` and `samples/` on a scaffolded parent, closing a gap against ENGRAM clause 8. All three checks run inside `integrity.py parent` and therefore inside `just ci`, and they cover bite rows F16 through F26. `DEFERRED.md` records what they still miss: nothing validates the interior of `task.toml` against the Harbor `TaskConfig` model, nothing resolves an `@sha256:` image digest pin, nothing proves `solution/recompute.py` regenerates `TRUTH.md` bit-identically, and nothing verifies that the two pinned plots exist.
- Total prompt capture and a tamper-evident feedback chain in `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md`. Feedback capture previously fired only on a prompt an agent judged to carry a correction, and a bare contract re-paste captured nothing, so a project could run for weeks and hold an empty ledger that looked healthy. Every human turn a run receives is now captured without exception and without filter, a first paste, a bare re-paste, a correction, a course change, a complaint, a question, a bare acknowledgement, and an out-of-band approver argument record alike. Classification never gates capture, an unclassifiable turn is captured as `other`, and a turn that produced no record is a named coverage gap rather than a quiet human. The closed ledger schema gains `kind`, drawn from the closed set `contract_paste`, `correction`, `instruction`, `question`, `approval_record`, and `other`, and `chain_seq`, which names the chain line attesting the record. Beside each ledger sits a new machine-written and human-read-only hash chain at `memory/feedback.chain.jsonl`, `seed/feedback.chain.jsonl`, and `audit/feedback.chain.jsonl`, one canonical JSON object per line under the closed field set `seq`, `captured_at`, `feedback_id`, `kind`, `verbatim_sha256`, `record_sha256`, `writer`, `prev_hash`, and `entry_hash`, each line sealed over its own fields and linked to its predecessor. FORGE and CRUCIBLE gain the signed checkpoint logs `seed/feedback.checkpoints.yaml` and `audit/feedback.checkpoints.yaml`, ENGRAM extends `memory/checkpoints.yaml` to cover the chain as well as the ledger, and every run publishes the chain head in its root report so the head is attested outside the file that holds it. Each contract's resume preflight now walks its chain end to end before any phase work, and a chain that fails its walk is tampering and a hard block: ENGRAM emits `DIRECTIVE.md` flagged `BROKEN`, FORGE emits `EDICT.md` at `BLOCK`, CRUCIBLE emits `VERDICT.md` at `BLOCK`, and the run resumes only after a human-authored signed reconciliation entry explains the break. A missing chain beside a non-empty ledger, an unreadable chain, and a head no checkpoint covers all fail closed. `GAUNTLET.md` now delivers every argument under the `approval_record` kind with a chain line as proof it arrived unedited. `tools/integrity.py` gains `check_feedback_chain`, registered in the parent scope, which recomputes every `entry_hash`, walks every `prev_hash` link, requires `seq` dense and monotonic from one, and enforces the closed field set and the closed `kind` vocabulary. The same check then resolves the published head against the harness checkpoint log, `memory/checkpoints.yaml` for ENGRAM and the dedicated `feedback.checkpoints.yaml` for FORGE and CRUCIBLE, and refuses a head that is absent from it, so a chain truncated and re-linked from a new genesis no longer walks clean. Twelve fixture-paired tests prove both halves. `tests/bite/` gains eleven scenarios and `DEFERRED.md` records what remains unenforced, namely that nothing verifies the checkpoint signature or resolves the signer against a trust root in `memory/roots.yaml`, so a self-signed checkpoint covering a rewritten head still passes. The property claimed is tamper evidence and not tamper proofing: on a filesystem the operator owns, a rewrite can be made detectable, never impossible. Minor bump at minimum under the policy above.
- `tools/citations.py`, a stdlib-only checker that holds the `tests/bite/` scenario tables to the contract lines they cite. Every `CONTRACT.md:NN` reference is bound in the generated lockfile `tests/bite/citations.lock` by the SHA-256 of the line it points at, keyed by source file, table row, and citation index so the identity survives renumbering. `check` reports a citation that points past the end of a file, lands on a blank line, carries no recorded anchor, or has drifted, naming the line the cited rule now sits at; `update` relocates every drifted citation by its anchor and rewrites both the tables and the lockfile. All one hundred forty-three citations across `tests/bite/` were renumbered against the current contracts before the lockfile was sealed, correcting references that had rotted through many contract edits and, in twenty cases, had never pointed at the rule the row names. New `just citations` and `just citations-update` recipes, covered by `tests/test_citations.py`.
- Ruff and mypy now run inside `just ci` as the `python-lint` and `typecheck` recipes. Both had been available and both had rotted: the harness was failing its own lint while enforcing prose rules on the contracts. A harness that lints the contracts while rotting itself has no standing to fail anyone else.
- The Trinity freshness gate in `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md`, stated as shared doctrine ahead of each contract's resume preflight. Every invocation of every instrument now resolves the vendored `trinity/` submodule against the latest commit on the `main` branch of its upstream remote before any phase work, and only a clean checkout whose commit equals the fetched tip passes. A checkout that is behind, diverged, locally modified, or unverifiable because the submodule, its remote, or the network is unavailable is stale, and a stale checkout is a hard block: ENGRAM emits `DIRECTIVE.md` flagged `BROKEN`, FORGE emits `EDICT.md` at `BLOCK` for the whole batch and authors nothing, CRUCIBLE emits `VERDICT.md` at `BLOCK` and audits nothing, each recording the checked-out commit, the fetched tip, and a dated staleness entry as a named coverage gap. The gate fails closed and no cached record, prior sign-off, or offline convenience waives it. `tools/integrity.py` binds the doctrine into `SHARED_BLOCKS` so a contract that stops stating it is drift, and `check_parent_submodule` now requires the parent's `.gitmodules` to declare the trinity submodule with `branch = main` and `update = merge` rather than merely registering it. `README.md`, `SECURITY.md`, and the vendoring policy above are updated to the tracked-tip surface, and `DEFERRED.md` records that no code fetches a remote or compares two commits. Minor bump at minimum under the policy above.
- Time-budget closure across the three contracts, opt-in per project and orthogonal to hardness. FORGE Phase 1 item 2e and invariant 32 bind each task's `budget_hours`, `budget_envelope`, and `budget_margin` in `task.toml` from the human-approved `requirements/` when a project declares a budget, while ENGRAM Phase H step 6a and invariant E34 hold that the hardness catalog carries no budget value, a tier never derives a budget, and duration evidence is stamped with the budget it ran under so measurements at different budgets never mix. The reference tractability proof records `reference_hours` and treats a reference that exceeds the bound budget as infeasible. The reasons `budget-unverified`, `budget-uncovered`, and `budget-inverted` travel under existing dispositions with zero growth of the closed vocabulary. An optional signed CFER `duration_outcome` group carries per-rollout durations and wall-clock instants, remains firewalled out of both projections, and is enforced by the new `check_timing_closure` lint. CRUCIBLE critical class 16 and Phase 1 step 8s add the three BUDGET-CONFORMANCE auditor instruments `G-BUD-BINDING`, `G-BUD-ENVELOPE`, and `G-BUD-RECONCILE`. Each project gains the budget ledger views `memory/budget.yaml`, `seed/budget.yaml`, and `audit/budget.yaml`, and the any-time intake rule requires a budget value supplied in conversation to be transcribed into `requirements/` and re-approved before it can bind. This lands as part of the next bump under the standing release note.
- The ENGRAM Phase H harvest lane, carried inline in `ENGRAM.md`: dataset references extracted from admitted papers are pinned at immutable revision digests, screened through the six contamination atoms against the effective ENGRAM roots, and staged as candidate touchstone bundles under `memory/staging/` in the closed `engram.harvest/v1` schema for human adjudication. The contract gains the `memory/staging/` output surface, the `.opencode/commands/harvest.md` door, the touchstone adjudication gate, and invariant E30, which holds that the lane proposes and never admits.
- `pyproject.toml` declaring the `uv` managed development toolchain, a Python 3.11 floor, and ruff, mypy, and pytest as a dev dependency group. The harness itself stays stdlib-only with zero runtime dependencies, and the project is deliberately unpublished with no console entry point because `tools/` is invoked by path from `tools/justfile` and from vendoring parents.
- `.python-version` and `uv.lock` so the interpreter and the development toolchain are pinned rather than inferred.
- A pytest suite under `tests/` covering every rule enforced by `tools/prose.py` and `tools/integrity.py`. Each rule carries a planted-positive fixture and a clean fixture, so the suite proves both halves of every decision. Trinity's own doctrine holds that an inert required check is a BLOCK, and this suite is what keeps the harness from becoming one.
- A recorded defect: `prose.run_json` reaches only `all_findings`, which excludes advisory checks, so passive voice surfaces in `report` but can never surface in JSON output. Captured as a strict xfail rather than silenced.
- Continuous integration running the full gate and the test suite on every push and pull request, with workflow actions pinned by commit SHA.
- Governance spine: `CODE_OF_CONDUCT.md`, `CONTRIBUTING.md`, `SECURITY.md`, and `.github/CODEOWNERS`.
- `CITATION.cff` mirroring the `trinity2026` BibTeX entry published in `README.md`, so the project is machine citable.
- This changelog.

### Changed

- 2026-09-20: Every YAML file this repository ships carries the `.yaml` suffix. `ci`, `sentinel`, `release-gate`, and `sentinel-observer` are renamed off the old `.yml` spelling, and every reference in the contracts, the harness, the templates, the docs, and the bite corpus names the new one, so `gate.py install` copies `sentinel.yaml` into a parent and `check_gates_installed` looks for that name. A parent installed before this change still carries the old file and holds at `GATE_WORKFLOW_MISSING` until the next preflight reinstalls; deleting the stale workflow is a human act, the same as every other git write. Two spellings survive on purpose. `AUDIT_EVIDENCE_SUFFIXES` still recognizes the old suffix, because audit evidence a parent wrote under it must stay scannable for a hardness citation rather than silently escape the scan. `POSTMORTEM.md` sections 1 to 11 keep it too, because those bytes are frozen history a test holds identical to `HEAD`. Contract edit: patch bump; three contract sentences change one file name each and no obligation moves.
- 2026-09-18: Release evidence now requires `execution_fidelity` as the sixth mandatory core control. `trinity.release-policy/v2` becomes `trinity.release-policy/v3`, and `trinity.oracle-run/v3` becomes `trinity.oracle-run/v4`. There is no legacy acceptance route for oracle-run/v3 or release-policy/v2: every parent project must re-issue its release policy and obtain fresh receipts. Missing fidelity outcomes fail the existing exactly-one-outcome-per-required-control rule; fidelity may be a positive control and can never be non-applicable. Only the judge control retains the trusted non-applicable allowance. Unsupported schemas are refused with named errors identifying the observed and required versions before current-version field validation. This commit enforces the control carrier only; the cross-artifact verifier binding its evidence digest to the G-FID ledger, pilot registry, and execution attestation remains an obligation in `DEFERRED.md`. The external execution service remains an undeployed obligation; this change verifies signed observations and does not produce them. The active external-release contract clauses and bite row A12 now name the same schemas and six controls; named-v2 disposition compatibility is unchanged. Contract edit: minor bump at minimum.

- 2026-09-17: The sentinel's static predicates narrow to what evidences intent, after teams hit blocks from housekeeping. Instrument liveness has one definition, `tools/sabotage.py::inert_functions`, which `tools/sentinel.py` imports and `tools/incident_probe.py` mirrors under a parity test: an instrument is inert only when every own-scope return is a constant, an empty container, or a gap call and the body never raises, asserts, or calls anything but a gap helper, so a `check_` predicate that branches on real input or raises on mismatch is live. Workflow bypass syntax, `|| true`, `set +e`, and `continue-on-error: true`, counts only inside the step or job that invokes a gate tool, so a cache purge or an unrelated lint job never records `SAB_GATE_WORKFLOW_EDITED_WITH_BYPASS`. The compound code in `classify` fires only when the same push edits CODEOWNERS or a workflow that invokes a gate tool, no longer any path under `tools/` or any workflow. Repeat detection in `is_repeat`, `repeats`, `raise_alerts`, and `incident_store.py` relates attempts by SAB code alone; sharing an author no longer relates two different refusals, because one person tripping two unrelated checks inside thirty days was a block only a second principal could clear. `_false_exemption_declarations` in `tools/integrity.py` also drops a catastrophically backtracking regex that wedged `run_qualification` on an all-indented YAML section, bounds the field's block by its own indent, and scans every occurrence of a bundle uuid rather than only the first. Contract edit: none, `templates/SENTINEL.md` wording updated.
- 2026-09-14: MAESTRO gate handling is inverted from consolidate-and-continue to hazard-scoped block-and-ask. A lane that halts at a named human gate now pauses inside its movement: MAESTRO prints the gate sheet at that moment, presents the verbatim approval instruction, waits for the human to discharge the gate, and resumes the same lane to its next lawful stop once the bound bytes carry a valid approval. An undischarged gate holds exactly the movements it hazards under a hazard relation fixed by the contracts, never computed per run: ENGRAM's projections feed both peers, so an open ENGRAM gate holds FORGE and CRUCIBLE alike, while FORGE and CRUCIBLE share no channel, so an open FORGE gate never holds CRUCIBLE's launch and a movement outside the hazard runs to its own stop with the gate still open on the sheet. The cycle boundary is a synchronization barrier: a new cycle opens only when every gate of the closing cycle is discharged and every lane finished, because the next ENGRAM movement re-projects the views a halted lane is pinned to, and a gate still open when the loop would otherwise continue stops the invocation as `gates-pending`, with hazarded movements never launched written as `not-launched` in `SCORE.md` and a re-paste resuming the cycle at the halted movement. The prior rule that a halted gate halts its own lane and never the cycle is replaced by the hazard rule. `README.md` names the blocking behavior. Contract change, minor bump at minimum.
- ENGRAM step 8b and Phase S step 18 gain a hero-art complexity floor for every scaffolded showcase `README.md` and every `media/kit/` mascot asset: the human-elected mascot is rendered as a mythic embodiment of the project's domain, a goddess or daemoness amid a layered scene of domain artifacts with painterly depth, every work order the project runs appears in the scene as an attendant figure or artifact, and a flat typographic card, a bare geometric diagram, or clip-art minimalism fails the floor and is regenerated. Contract change, minor bump at minimum.
- The machine-owned harness directories moved under one hidden machine root at the parent project root: `memory/`, `seed/`, and `audit/` became `.trinity/memory/`, `.trinity/seed/`, and `.trinity/audit/`, and GAUNTLET's `trial/` became `.trinity/trial/`. Every path reference moved with them across `ENGRAM.md`, `FORGE.md`, `CRUCIBLE.md`, `GAUNTLET.md`, `README.md`, `DEFERRED.md`, `CONTRIBUTING.md`, `docs/`, and the `tests/bite/` scenario tables. `tools/integrity.py` gains the `MACHINE_DIR` and `MACHINE_ROOT_DIRS` constants and a `harness_owner` resolver, so the split-brain writer check, the feedback-chain walk, the approval gates, the trust-root paths, the execution-attestation door, and the barrier scan all resolve harness bytes beneath the machine root, and the unknown-path lint now validates the segment beneath `.trinity/` instead of skipping every dotted root. Bare `memory/`, `seed/`, `audit/`, and `trial/` first segments are no longer known roots, so a reference written against the old layout fails the lint. The barrier regex still matches the bare `memory/hardness.yaml` suffix on purpose, so evidence citing either spelling trips it. Historical entries below this one keep the old spellings they shipped with. Contract change, minor bump at minimum.
- `DEFERRED.md`, `README.md`, `SECURITY.md`, and one `ENGRAM.md` output bullet now describe what the code enforces rather than what it enforced before this work. One register row left `DEFERRED.md` under its own exit rule, the Trinity freshness gate, because `evaluate_trinity_freshness` and `check_parent_submodule` now evaluate its predicate and `tests/test_freshness.py` proves both halves. Four rows are recorded as partly closed rather than removed, naming which half landed: the `execution_operator` authorization has a library, a command-line caller, and fifteen tests but no gate caller; the execution attestation has a pinned format and no operator to produce one; the ledger checkpoint chain now carries real signature verification over the feedback chain and still covers no CFER ledger; and the optimization plot filenames now sit inside a `trajectories/` subtree whose presence is checked and whose contents are not. Every other row stands, and the register says so in the same words it used before. `DEFERRED.md` also corrects its own enforcement summary: `just ci` runs eight recipes rather than five, the suite is five hundred sixty-four tests beside one recorded strict xfail rather than five hundred forty-two, one hundred forty of them evaluate contract obligations inside `integrity.py parent` rather than one hundred seventeen, and two hundred fifty-three exercise `tools/attest`. `SECURITY.md` gains the two threat-model entries the signing stack creates, a forged or replayed attestation and a compromised signing-backend invocation, and drops a stale line count. Understating what the gate enforces is a defect in this repository, and overstating it is a worse one; both directions were repaired.
- Retired the top-level `dataset/` split and made `samples/<uuid>/` the single Harbor-format home for every task bundle. Added the mirrored uuid-keyed `deliverables/<uuid>/` Harbor tree for rollout evidence, including `trajectories/inspector.html`, `trajectories/reward_by_iteration.svg`, and `trajectories/reward_by_tokens.svg`. Promotion now records a bundle's state transition and standing in `samples/README.md` without copying bytes, while changed bytes still require a new uuid and bundle. The source-versus-subset twin reconciliation and its five tests are removed because a single bundle home cannot diverge from a separate source.
- Cohort currency in `ENGRAM.md` and `FORGE.md`. Both contracts previously defined "current" relative to a declared model cohort, so a project could hold `ACTIVE` levers measured against a cohort later models had surpassed while every deterministic check still passed. ENGRAM now pins an append-only externally signed cohort registry under `memory/cohorts/` with the closed predicate `engram.cohort-registry/v1`, signed by an identity authorized for `cohort_registry_issuer`, and joins each CFER's existing `model_cohort` and `solver_registry_digest` against the registry's `required_cohort`. A valid newer snapshot the evidence leaves uncovered expires the lever; a missing, expired, or overdue registry narrows to `WATCH` and holds the ledger at `STALE`; unsigned, unauthorized, rolled-back, forked, or unamended-shrunk registry evidence caps at `BROKEN`. A bound `maximum_cohort_refresh_interval_days` yields an absolute refresh deadline, and lapsing it opens the named `cohort-refresh-overdue` coverage gap and raises the coarsened family flag `cohort:required`. FORGE consumes that flag through `FORGE_VIEW` alone, binds the registry into `seed/contract.yaml`, `seed/pilot.yaml`, and the signed release closure, and refuses `SHIP` while the flag stands or while the pilot solver registry leaves a required model uncovered. The `engram.cfer/v1` envelope is unchanged, so historical CFERs stay compatible. No new lever state, ledger disposition, FORGE disposition, invariant, or Bucket-D instrument was added; the existing signature verifier, freshener, and provenance gate carry the work. `CRUCIBLE.md` is unchanged, and the registry is named forbidden in `CRUCIBLE_VIEW` in full because the frontier an author must defeat is standing design intent.
- `tools/integrity.py` enforces the new closed vocabulary in `ENGRAM.md` and `FORGE.md` and rejects every cohort-currency term from `CRUCIBLE.md` outside a prohibition, with fixture pairs proving both halves.
- Bundle identity in `FORGE.md`, `CRUCIBLE.md`, and `ENGRAM.md` is now computed under canary normalization. Phase 1 item 2 previously hashed all runtime-relevant bytes while item 11a derived the canary tokens from that hash and planted them inside private artifacts, so the hash depended on the tokens and the tokens depended on the hash. The definition was recursive and no bundle could satisfy both clauses as written. `canonical_content_hash` is now computed after replacing the value of each designated 11a token slot with a fixed public placeholder, and nothing else changes: slot count, ordering, position, surrounding bytes, and every byte outside a designated slot stay inside the hash domain. Planted token bytes sit outside the hash preimage but are fixed exactly by the derivation relation, so no slot is a free region and no byte is both mutable and valid. `seed/contract.yaml` names the normalization domain version and Phase 0.5 binds it, so hashes computed under different domains are never comparable. Item 11a fixes the generation order as hash, then plant, then bind, and item 10i binds the normalized hash. FORGE Phase 2 items 7 and 9 prove the hash reproduces, the uuid derives, every slot holds its exact derived token, and final planted bytes agree across two host identities. CRUCIBLE step 8b recomputes the same normalization with the audit harness own normalizer and treats a non-deriving slot as an integrity defect of critical class 7 even when nothing surfaced, and `G-CON-RECORD` binds the same domain. ENGRAM defines the CFER `task_hash` under that domain and carries the domain version. FORGE Phase 1 item 2 and Phase 2 item 7 state and prove the property. This is breaking for any frozen bundle whose uuid was derived by another route: such a bundle keeps its historical CFERs as history, cannot silently satisfy a current binding, and needs a fresh hash, uuid, screening result, and signed pilot to remain current.
- Rubric criterion prose in `FORGE.md` may now be drafted by a model at authoring time and is then frozen as a literal in `dataset/<uuid>/solution/grounding.yaml`. `dataset/<uuid>/solution/recompute.py` reads that literal and still invokes no model, network, clock, locale, or random source, so the two-host byte equality item 7e proves is untouched and `rubrics.json` and `tests/test_output.py` keep full byte determinism. Drafting reads the structured obligation rather than raw `TRUTH.md` bytes, freezes only a candidate clearing a drafting acceptance predicate bound at Phase 0.5, and records model identity and prompt and output digests in `grounding.yaml` as non-evidentiary metadata no closed schema receives. Reroll history is unobservable from frozen bytes, so the predicate bounds what an accepted string may say and nothing claims a draft was the first. Drafting never changes an item `mode`. A model invoked inside `recompute.py`, or criterion prose that is not a frozen literal, is `BLOCK:INVALID_TASK`.
- Optional `seed/results/rubric.md` in `FORGE.md`, a stochastic natural-language display of the frozen rubric. It lives in the gitignored volatile output area, never inside `dataset/<uuid>/` or `samples/`, so it changes no canonical hash, no uuid, no Harbor path, and no release evidence. New item 10f.1 governs generation and privacy, new item 7e.1 admits a materialization at the point of use by matching the declared `canonical_content_hash` and `rubrics.json` byte digest, reconciling item identifiers in both directions, matching every section digest, validating the planted canary block, and running the 11b containment comparison; a failing candidate is deleted and treated as absent. New invariant 24a fixes it as presentation only: no checker, grading judge, pilot, release record, provenance carrier, ENGRAM memory, or projection consumes it, and its presence, absence, wording, generation failure, or rejection never moves disposition. Agent-visible leakage stays governed by item 11 and caps at `BLOCK:INVALID_TASK`. CRUCIBLE never reads it because it is neither projected nor committed bundle content, and grounds no rubric conclusion on its presence, wording, or absence. ENGRAM excludes the volatile `seed/results/` area from the seed digest, names the file forbidden in both projections, and never ingests, remembers, or evaluates it.
- `FORGE.md` moved from one task bundle per invocation to a mandatory batch cadence: every invocation opens one batch of at least twenty task slots worked as parallel lanes, each slot designed against the Frontier-defeat tier by default, with per-slot instruments under `seed/tasks/<uuid>/`, a batch manifest at `seed/batch.yaml`, archetype spread across the batch, and a per-slot disposition table in `EDICT.md`. `README.md` updated to match. Minor bump at minimum under the policy above.
- Invocation model sections in `ENGRAM.md`, `FORGE.md`, and `CRUCIBLE.md`, inserted after each contract's resume preflight. One pasted prompt now runs the whole contract flow end to end without pausing between phases, fanning independent units out to parallel subagent lanes (FORGE batch slots, ENGRAM ingest units and hardness rows and doc-spine files, CRUCIBLE instrument families), stopping only at the named human gates, a terminal disposition, or a blocking gap. Each contract enumerates its gates, fixes a halt protocol that writes the awaiting artifact, prints the exact approval instruction, and records the waiting state in the harness-private progress cache, and states that re-invocation resumes via the resume preflight rather than restarting. `README.md` "How a run works" updated to tell the same story. Minor bump at minimum under the policy above.
- Doc-spine charters in `ENGRAM.md` Phase G no longer bake Trinity internals into the generated parent-project files. The spine now documents the parent project exclusively: no spine file may mention the `trinity/` submodule, the contract files, the instrument names, or instrument-internal surfaces such as ledgers, views, projections, or CFERs, and tooling surfaces to the parent only through the root reports `DIRECTIVE.md`, `HARDNESS.md`, `EDICT.md`, and `VERDICT.md`. `ARCHITECTURE.md` is rechartered around the parent repository topology and its dataset, harness, and delivery surfaces, `ASSURANCE.md` around isolation boundaries and signed evidence attestation, `GROUNDING.md` around measured results, and `OPERATIONS.md` around changing governed files safely. A Trinity reference found in a spine file is a named scaffold defect repaired on reconcile. Minor bump at minimum under the policy above.
- The shared sibling directory `data-spec/` is renamed `schema/` in `ENGRAM.md` Phase G, `README.md`, and the `tools/integrity.py` shared-directory vocabulary, bringing the last hyphenated compound in the sibling list under the single-noun house rule. Genesis reconcile now migrates a pre-rename parent project by moving every `data-spec/` entry into `schema/` and deleting the emptied directory, recording any conflicting entry as a named `DIRECTIVE.md` coverage gap. Minor bump at minimum under the policy above.
- `HARDNESS.md` is rechartered from a root report into the one GENERATED member of the parent doc spine, which grows from ten files to eleven. Hardness now lives in two parts: `memory/hardness.yaml` holds the machine-configured knobs FORGE picks up through the `FORGE_VIEW` projection, and `HARDNESS.md` is the human-readable knob sheet rendering each configured knob beside its arXiv proof, regenerated byte-identically from the yaml and never hand-edited. A 50 percent headroom floor now binds both sides: ENGRAM Phase H derives every frontier-defeat target at or above 1.5 times the strongest anchored frontier evidence it cites, and FORGE Hardness rule 2e over-provisions every governing tier floor by the same multiple, so the Frontier-defeat floors of 12 levers from 9 categories compose to at least 18 levers from 14 categories, as a design obligation that never raises a disposition. The hand-authored `research/HARDNESS.md` lever catalog is deleted because `research/` admits only paper pairs and the catalog's single source of truth is `memory/hardness.yaml`. `ENGRAM.md`, `FORGE.md`, and `README.md` updated to match. Minor bump at minimum under the policy above.
- Release note for whoever cuts the next tag: the projection narrowing in `ENGRAM.md` and the breaking bundle-identity change together meet the major-bump rule stated above. This is not a minor release.
- The shared hidden `.trinity/` machine root is retired and the four machine-owned harness directories become sibling hidden roots at the parent project root instead: `.trinity/memory/` became `.memory/`, `.trinity/seed/` became `.seed/`, `.trinity/audit/` became `.audit/`, and GAUNTLET's `.trinity/trial/` became `.trial/`, with every path reference moving across `ENGRAM.md`, `FORGE.md`, `CRUCIBLE.md`, `GAUNTLET.md`, `README.md`, `DEFERRED.md`, `CONTRIBUTING.md`, `docs/`, the `tests/bite/` scenario tables, and the test fixtures. `tools/integrity.py` drops the single `MACHINE_DIR` constant for a per-harness `HARNESS_ROOTS` map and rejects any `.trinity/`-prefixed spelling outright as a retired legacy root, and the information-barrier detector now trips on `.memory/hardness.yaml`, the retired `.trinity/memory/hardness.yaml` spelling, and the bare `memory/hardness.yaml` suffix alike. A runtime migration clause, stated verbatim across all four contracts, renames a detected legacy layout to the new roots in fixed order before any phase work, treats a byte-identical destination as already migrated, blocks on a destination whose bytes differ, and removes the emptied `.trinity/` root once every harness has moved. This retires the shared machine-root obligation the prior layout imposed, and the standing rule above that a change removing an obligation is a major bump makes this a major bump, not a minor one.

### Removed

- The `specifications/` folder and the contract-attachment mechanism. An attachment was a second document a contract had to win against, so the surviving content moved into the contracts themselves: the reward-claim boundary model, the threat vocabulary, and the capability channels from `reward-hacking-v1.md` now live verbatim in `CRUCIBLE.md` step 8q, and the touchstone harvest design lives in `ENGRAM.md` Phase H. `tools/integrity.py` drops its `spec` mode, `tools/justfile` drops the `spec` and `spec-json` recipes and their aggregate wiring, and the spec test suite is deleted. The attachments were unimplemented design surfaces whose atoms were coverage gaps by their own terms, so no obligation in force is removed, and the release note above already marks the next tag as a major bump.

## [0.3.0]

### Added

- `ENGRAM.md`, the memory instrument. Remembers measured failure evidence across tasks, expires stale or frontier-caught levers, reports the current front line, and scaffolds new knowledge-repository projects. Owns `memory/`, the append-only ledger, the two computed projections that form the only bridge between the other two instruments, the hardness contract, and the root reports `DIRECTIVE.md` and `HARDNESS.md`.
- `FORGE.md`, the authoring instrument. Scopes, scaffolds, locally verifies, hardens, and pilots one task per invocation. Owns `seed/`, writes one runnable bundle to `dataset/<uuid>/` in Harbor delivery format, and the root report `EDICT.md`. Local verification caps at `HOLD:PILOT_REQUIRED`, because difficulty is measured, never claimed, and only an external signed pilot over frozen bytes reaches SHIP.
- `CRUCIBLE.md`, the audit instrument. Audits the complete project against instrumented evidence. Owns `audit/` and the root report `VERDICT.md`. It never sees the hardness contract, because an auditor holding the author's playbook grades intent instead of bytes.
- `specifications/reward-hacking-v1.md`, a subordinate attachment elaborating the reward-hacking detection surface CRUCIBLE must carry. It is an unimplemented design specification, and every atom in it is a coverage gap until code and fixtures exist.
- `tools/prose.py`, `tools/integrity.py`, and `tools/justfile`: the shared sanity harness that lints the contract documents for prose discipline and self-referential integrity, and lints a scaffolded parent project for spine, shared directories, root reports, submodule registration, and front doors.
- `research/`, the shared corpus of load-bearing papers that anchor the hardness contract.
- `README.md`, the orientation guide. It is a guide, not the authority. When the guide and a contract disagree, the contract wins.
