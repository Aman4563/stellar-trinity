# Deferred register

Trinity's founding claim is that a property nobody measured is a property nobody has. This register applies that claim to Trinity itself.

Every row below is a rule the contracts state as an obligation and that nothing currently enforces. The rules are not wrong and they are not abandoned. They are written down here so that a reader can tell, without reading fifty thousand words, which of Trinity's refusals are real today and which are intentions wearing the grammar of a requirement.

The pattern is borrowed from CRUCIBLE itself. The contamination history-absence check already discloses in line that it resolves only the merged container filesystem and not the unpacked layers, caps on that limit, and proceeds. That is the correct habit. This file generalizes it.

## How to read a row

Each row names the obligation, where it is stated, what it would refuse if it were live, and what would have to exist for it to bite. A rule leaves this register in exactly one direction: something is built that evaluates its predicate, and the row is deleted in the same commit that lands the check.

A rule may not leave this register by being reworded.

Each contract reads this register during Phase 0, at ENGRAM step 7c, FORGE step 8a, and CRUCIBLE step 7g. A run whose required instruments depend on a deferred obligation records that dependency in its scope artifact before sign-off, so the operator learns which of the run's refusals cannot fire while the scope is still open rather than at disposition time. A deferred obligation caps exactly as the matching coverage gap would. The difference the scope artifact records is not severity but cause: a coverage gap says this project has not built the machinery, and a deferral says nobody has.

Project-structure migration is not deferred. `tools/migrate.py` implements the closed transformations documented in `docs/migrations.md`, including journal, lock, backup, rollback, idempotence, path confinement, historical queue-digest proof, and preflight integration. Its limits remain explicit rather than entering this table: it does not infer unknown schemas, migrate arbitrary layouts, run an external Harbor service, execute an audit, or mint replacement signatures. Those cases hold for their ordinary validator, auditor, or governance authority and are never grandfathered.

## 1. Cryptographic verification

Signature verification exists and runs. `check_root_rotation`, `check_approval_envelopes`, `check_execution_attestations`, `check_feedback_chain`, `check_ledger_checkpoint_chain`, `check_active_cfer_attestations`, `check_bundle_manifest_signature`, and `check_bundle_provenance_signature` are registered in `PARENT_CHECKS`. Together they verify trust-root rotation continuity and the detached signatures or DSSE envelopes they locate at an explicitly supplied evaluation instant. All except the manifest check resolve the verified signer against the applicable trust-root role or, where the contract defines no purpose-specific role, at least one current role in `.memory/roots.yaml`. `tools/attest/verify.py` derives the required role from the statement's `predicateType` rather than from its caller, so a caller may veto a role and may never select one, and `tools/attest/trustroot.py` counts distinct verified principals against a per-role threshold, refuses a revoked or expired principal, refuses a root whose monotonic version has rolled back, and refuses to read a root at all without an explicit evaluation instant. `check_root_rotation` calls `verify_rotation` whenever the active trust-root version exceeds the gate-owned trusted version and refuses a transition without authorization from both the old and new roots. What remains below is one carrier no check opens and one signing pipeline no component implements.

| Obligation | Stated in | Would refuse | Needs |
| --- | --- | --- | --- |
| Cohort registry snapshot reaches CURRENT only by detached external signature | ENGRAM E4 | A self-attested cohort snapshot promoted to CURRENT | An authorized issuer identity, and a check that reads a cohort snapshot at all. Nothing in `integrity.py` references one |
| CFER envelope signed by an external signer | ENGRAM E1, E8 | An authored difficulty claim entering memory as measurement | The signing pipeline that no component implements |

The producer and approver identity comparison in E33 is the one part of this family that is decidable today, because comparing two recorded identity strings needs no cryptography. It is covered by bite test G5 and is not deferred. `tools/attest/intoto.py` also refuses an approval predicate whose `producerIdentity` equals its `approverIdentity` and one whose `producerModelLineage` equals its `approverModelLineage`, and `tools/attest/trustroot.py` excludes the producer principal from the eligible set before counting the threshold, so self-approval is refused three ways rather than compared once.

## 2. Merkle and checkpoint chains

| Obligation | Stated in | Would refuse | Needs |
| --- | --- | --- | --- |
| Ledger checkpoint root recomputed from committed CFER leaves | ENGRAM memory model | A memory ledger rewritten together with invented checkpoint roots | A specified Merkle construction over `.memory/ledger.yaml` and a comparison against each recorded `new_root`. `check_ledger_checkpoint_chain` now validates the closed checkpoint schema, prior-root continuity, growing leaf counts, detached signatures, and `feedback_checkpointer` authorization, but the contracts define no leaf construction and the check therefore cannot determine whether a root commits to the ledger bytes |

The trajectory row left by the required route. `tools/attest/merkle.py` fixes the RFC 6962 `trinity.trajectory-merkle.v1` construction, and execution-attestation verification recomputes its root from the committed manifest and rollout bytes before accepting the operator-signed descriptor. The ledger-checkpoint construction remains unspecified and unenforced.

## 3. External attestation

Every attestation-dependent rule assumes an actor outside this repository produces a signed document in a pinned format. No such document has ever been produced.

| Obligation | Stated in | Would refuse | Needs |
| --- | --- | --- | --- |
| Execution attestation present, unexpired, and field-consistent | CRUCIBLE step 8p | A pilot result with no execution record | An execution operator. The attestation format half is closed: `tools/attest/intoto.py` registers `trinity.execution/v1` and `trinity.execution/v2` with closed field sets, with v2 the release predicate and v1 retained only for diagnosis. It requires the pre-run commitment to predate the execution interval, hashes every bound resource descriptor against supplied collateral, and requires a signed replay block carrying nonce, audience, sequence, and run identity. No operator has produced one |
| Platform attestation matches declaration | CRUCIBLE step 8k | A bundle claiming a platform it did not run on | The same |
| Repository-bound freeze exemption requires an independent generator-origin attestation | CRUCIBLE Phase 0 step 7d | A contamination exemption taken on trust | An independent attestor, which the contract concedes it cannot verify |
| Near-duplicate ancestry or event attestation available | CRUCIBLE Phase 0 step 7e | An unverified lineage claim | The same |
| Observation capture precedes operator editing | CRUCIBLE step 8p | A trace the operator could have edited before it was recorded, offered as proof that the execution was faithful | The custody duty of an execution authority controlled by neither author nor auditor. Step 8p states that presence establishes nothing and that a capture the operator could have edited proves custody of whatever survived rather than faithful execution. Nothing on disk separates a captured trace from a rewritten one, because both arrive as operator-submitted bytes under an operator signature |
| Effective request and response capture | CRUCIBLE step 8u, the `G-FID-TRACE` instrument | A fidelity claim resting on the agent's own log rather than on the wire | The observation duty of the same authority. `G-FID-TRACE` reads committed rollout bytes and resolves a rollout it cannot cover as `D-COVERAGE-GAP`, which is the honest answer and not the missing one. No component records the effective request as submitted or the response as returned, so every adapter reads a log the executing party wrote |
| Provider-internal routing declared and verified | `docs/execution-authority.md`, pending | A silent model substitution or fallback route presented as a measurement of the declared model | Provider-side attestation that no provider offers. A model identity holds only as far as the provider's own response asserts it, so routing inside the provider stays a declared trust assumption rather than a checked fact |

The generator-origin row is worth reading twice. The contract names its own verification gap at the point of use and then proceeds anyway. That is honest and it is also a hole.

## 4. Statistical machinery

| Obligation | Stated in | Would refuse | Needs |
| --- | --- | --- | --- |
| Judge reliability for judged scores not declared in `.audit/judgments.jsonl` | CRUCIBLE verifier principle 5a | An undeclared single-shot judgment treated as a grade | A declaration-completeness check that identifies every judged score before it can bypass the registry. `check_judge_reliability` now requires each declared judgment to carry at least eleven position-randomized trials in a hash-chained registry and recomputes retest agreement, Fraction-exact Cohen kappa, and the exact McNemar position-bias test |
| Conformal prediction sets for judged scores not declared in `.audit/judgments.jsonl` | CRUCIBLE verifier principle 5a | An undeclared, unqualified judged score | The same declaration-completeness check. `check_judge_reliability` now recomputes split-conformal prediction sets for every declared judgment and refuses a set that is not the singleton aggregate verdict |
| Preregistered exact statistics over the reward population | CRUCIBLE step 8o | A statistic chosen after seeing the population, a bound set that is not exactly the six named figures, or a committed preregistration digest disagreeing with the attested descriptor | An execution authority that produces the pre-run commitment. The statistic half is closed: step 8o names six exact figures, `tools/reward_distribution.py` recomputes each as an exact rational and refuses any seventh or missing name, and the preregistration digest rides as a bound resource descriptor inside the `trinity.execution/v2` pre-run commitment whose duty to predate the execution interval is the ordering proof. No operator has ever produced that commitment, so the ordering cannot be proved here and the row stands |
| Author-side frontier probe gate over `probe_n` pass-at-k groups per required cohort member | FORGE Phase 3 item 5 | A task nothing probed advancing as though its difficulty were tested | A probe evaluator in the parent `.seed/` harness and a `.seed/probe.yaml` the evaluator reads |
| Opus baseline band lane and batch mix gate over `baseline_n` single-attempt rollouts | FORGE Phase 3 item 5c | A batch shipped with an untested or lopsided difficulty spread | A baseline evaluator in the parent `.seed/` harness reading the `frontier_probe` baseline parameters and writing bands into `.seed/batch.yaml` |
| Statement-only leak probe against the no-op control threshold | FORGE Phase 3 item 5a | A task whose graded answer sits inside its own statement | A `statement_closure` assembler and the same evaluator |
| Probe parameters predeclared before the first rollout | FORGE Phase 0 item 9l | A ceiling relaxed after the outcomes came back | An ordering record binding the `frontier_probe` digest ahead of the first recorded rollout |
| Mutation ratchet bounded by `mutation_cap`, fresh seed blocks, and `mutation_margin` | FORGE Phase 3 item 7 | A lucky child selected from a sibling pool, or a parent and child compared on different seeds | A lineage-wide proposal registry and a spent-seed-block ledger |
| Lever ablation costed at `ablation_n` and `ablation_margin` | FORGE hardness rule 5 | A decorative lever counted toward a tier it does not defend | An ablation runner that disables one lever and re-probes frozen bytes |
| Discovery asymmetry proved over frozen bytes | FORGE Phase 2 item 7a.1 | An environment that decorates a task whose answer the statement already carries | A closure assembler, a paraphrase containment check, and a graded-weight accounting of the discovery set |
| Signed stratum bound verifies and recomputes from recorded outcomes | FORGE Phase 4 items 4f through 4h and Phase 4.5 item 2i | An unsigned, malformed, or incorrectly computed stratum bound supporting `SHIP:INFERRED` | A closed stratum-bound predicate, signature verification against the external operator role, and a caller that reads the recorded sample outcomes and invokes `lot_defective_upper_bound` or `lot_defective_rate_upper_bound`. `tools/stats.py` implements and tests the exact hypergeometric math, but no caller consumes a stratum record |
| Precommitted population roster reconciled against realized groups | FORGE rule 2f | A group set chosen after outcomes were visible, or a fixed group count lowered to fit the rollouts that arrived | The admission duty of an external execution authority. Rule 2f fixes the group count, the permitted looks, and the comparison family before any outcome is observed, and `tools/stats.py` computes the worst-case group bound over that fixed count, but no roster is committed by a principal outside this repository, so the declared groups and the realized groups still have one author |
| Effective band policy conforms to the `requirements/` grant | FORGE Phase 0 item 9l.1, Phase 3 item 5c, invariant 37 | FORGE-authored cut points or targets, an ungranted admissible band, a batch mix outside the effective tolerance, or a granted change applied without Phase 0.5 sign-off | A check that reads a `requirements/` band grant, resolves defaults only for silent fields, and compares the effective policy and `.seed/batch.yaml` against the approved contract digest |

The eleven-trial floor and conformal singleton requirement now bite every score declared in `.audit/judgments.jsonl`. An undeclared judged score still bypasses the registry, so both rows remain narrowed to declaration completeness rather than leaving the register.

The reward row is now closed on both halves this repository owns, and reconciled against the step it contradicted. Step 8o names the six exact statistics, `tools/reward_distribution.py` computes them, and the ordering proof reuses the `trinity.execution/v2` pre-run commitment rather than inventing a second scheme. What remains is the external execution authority section 3 already registers: nobody produces that commitment, so the ordering cannot be proved here and the row stays. The disposition split matters more than the arithmetic. Step 7g governs every instrument whose obligation this register defers and fixes that it caps exactly as the matching coverage gap would, which for step 8o is `HOLD`, while step 8o read flatly that an absent preregistration is `BLOCK`. Both readings were textually supported, so the contract gave two answers to one question. A moved preregistration now stays `BLOCK`, because that is misconduct an operator chose. An absent one caps at `HOLD` while this row stands, because a refusal aimed at an operator for machinery no contract ever specified measures the auditor rather than the audited, and a finding that fires identically on every corpus carrying rollouts discriminates between none of them.

## 5. Named without a method

One instrument names an operation and never says how it is performed.

| Instrument | Stated in | The gap |
| --- | --- | --- |
| `G-REF-OBTAIN` | CRUCIBLE step 8d | Obtains the real upstream artifact at the pinned version, with no method, protocol, or fallback specified. The instrument that acquires ground truth does not say how it acquires anything |

`rewarddistribution` left this section. Step 8o now names its six exact figures, `tools/reward_distribution.py` computes each as an exact rational, and `tests/test_reward_distribution.py` proves both halves of every branch, so the instrument states how it is performed. Its remaining dependency is an execution authority rather than a missing method, and that dependency is registered in section 4 rather than here.

## 6. Known incomplete coverage

Checks that exist in specification, would run, and are already known to miss part of what they claim.

| Check | Stated in | Known miss |
| --- | --- | --- |
| History-absence under `G-CON-STRIP` | CRUCIBLE step 8i | Resolves the merged container filesystem only, never the unpacked layers. Disclosed by the contract in line |
| Optimization plot filenames | FORGE optimization lane | The two plots are pinned to `<task lane root>/<uuid>/trajectories/reward_by_iteration.svg` and `<task lane root>/<uuid>/trajectories/reward_by_tokens.svg`. `check_bundle_layout` now requires every placed lane bundle, at `samples/<uuid>/` or `delivery/<uuid>/`, to carry a `trajectories/` directory, so the subtree can no longer be absent. Nothing enumerates its contents, so an optimization slot that ships an empty `trajectories/`, or ships its plots under other names, passes |
| Author-bound robustness parameters | FORGE Phase 0 | FORGE binds `load_profile` and `window_ceiling` and requires proof of both. CRUCIBLE has no instrument that re-verifies either. The auditor does not check what the author is required to bind |
| Authored feasibility hours | FORGE Phase 2 | `reference_hours` is authored and no budget instrument re-derives it. The anchoring process is the intended closure and is itself deferred until anchors are piloted, so the miss narrows rather than closes |
| Anchoring design-fit check | FORGE Phase 2 item 7a.2 | Nothing evaluates it, because no anchor task has been piloted and no `.memory/anchors.yaml` exists |
| Fitted time-anchor mapping | ENGRAM Phase H step 6b | No instrument re-derives it from the anchor rows |
| Judge discipline asymmetry | FORGE, CRUCIBLE | CRUCIBLE mandates judge reliability discipline. FORGE imposes nothing on its three judge usages |
| Per-lane consumption metering | FORGE Phase 2 item 7a.3 | `tools/meter.py` re-derives per-lane token and wall-clock totals from an `opencode export` session tree, but it is an external harness no gate runs, no instrument meters its own consumption, and the recorded totals in a report stay authored records that nothing compares against the harness output |
| Signed cost predicate | ENGRAM CFER `cost_outcome` group | `tools/attest/intoto.py` registers no cost field, so the group is prose an operator could emit and no code validates |
| Pilot consumption receipt | FORGE Phase 4 item 4e | No predicate for the operator-signed consumption receipt exists in `tools/attest`; `intoto.py` registers `trinity.approval/v1`, `trinity.execution/v1`, and `trinity.execution/v2`, with v2 the release predicate and v1 diagnosis-only. None is a consumption predicate, so the receipt is prose an operator could emit in any shape and no code validates |
| Authoring-effort attestation | FORGE Phase 1 item 2e | Nothing verifies an attested `authoring_workdays` count against any record, and the count is accepted as declared |
| Aggregate economics fold | ENGRAM Phase H step 6c and invariant E36 | No instrument re-derives `average_task_cost` or `average_task_price` from the folded envelope digests |
| Feedback `github_id` attribution authenticity | Feedback capture steps 1a, 2, and 4, all three contracts | `chain_line_shape` requires a non-empty `github_id` string and nothing more. No check verifies the login against an authenticated GitHub session, walks the resolution ladder, or binds the value under any signature, so a fabricated or borrowed login passes every check the harness runs |
| `TRACKING.md` executive tracker freshness, content, and section ownership | The tracker close in the three instrument contracts, the tracker abstention in `MAESTRO.md` and `GAUNTLET.md`, and ENGRAM invariant E40 | `check_root_reports` now renders the whole dashboard through `tools/dashboard.py` and refuses any drift by exact comparison, `check_tracker_cards` refuses a malformed card as `TRACKER_CARD_MALFORMED`, and the installed hook roster refuses a commit whose tracker is unstaged or stale. What stays unenforced is the projection-leak check: no checker proves that a run never reads `TRACKING.md` or a peer's tracker card, so the firewall across the tracker holds by contract prose alone. Nothing validates the `.podium/cycles.yaml` and `.trial/gates.yaml` ledgers the renderer projects against any schema, so a malformed ledger is projected as whatever it parses to |
| Successor bundle schema and saturation floor | ENGRAM Phase N and invariant E39 | No checker parses `brief/<stem>-next/brief.md` against the closed `engram.next/v1` schema, resolves the effective saturation floor from `requirements/` or the 0.80 default, or compares the recorded saturation evidence against it, so a malformed bundle, a proposed grant, or a saturation claim below the floor passes every check the harness runs |
| Delivery task lane progress against `delivery_target` | FORGE Phase 0 item 9 | `delivery_target` is carried by name from a `requirements/` grant, never authored by FORGE and never defaulted. `delivery_ceiling` is a structural constant fixed at ten thousand that `pipeline.py reconcile` enforces as `PIPELINE_LANE_FULL`, but the delivery task lane has no floor beyond the batch cadence. No instrument compares delivery task lane residency against the declared target, so a project that declares a corpus size and delivers a fraction of it passes every check the harness runs |
| Push admission is the local hook, and the rerun on `main` is detection | The gate binding paragraph in all three contracts | A push whose tree differs from its reconciliation, or that rewrites a queue stream, landing on `main` | Direct pushes to `main` are lawful by design, so `reconcile --check` and `verify --accepted` refuse in the pre-push hook and continuous integration reruns them on the pushed commit. A hook can be bypassed, and a required status check never blocks a direct push, so the rerun names a bypass after the fact and caps the disposition; nothing here makes an unreconciled tree unable to reach `main`. `tools/transition.py` now decides whether one candidate is a lawful successor to a pinned accepted base, which is the check an admission boundary would run, but no process holds exclusive write on an accepted ref, so nothing invokes it as admission |
| Run ownership is authenticated only by `run.json` bytes | The gate binding paragraph in all three contracts, `tools/runs.py` | A second principal adopting another run's namespace, or a forged principal in `run.json` | `open_run` refuses a different principal, but the principal is whatever `GITHUB_ACTOR` or the git identity says. No check authenticates the run's principal against enrolment, so ownership is a convention a hostile operator can rewrite. `tools/operations.py` now carries each authoritative write as a DSSE envelope binding project, run, verb, the SHA-256 of the record it authorizes, its place in the run's hash chain, and its principal, verified against `allowed_signers` and the versioned trust root, so a signature never speaks for another identity and an operation cannot be removed, reordered, or inserted without breaking the following link. `validate_transition` refuses an operator outside the enrolment set, refuses a write into a namespace the accepted marker binds to another principal, and, when a signing policy is supplied, refuses a claim or verdict whose bytes no verified operation names. What stays open is enrolment itself and the binding between a key and a human: the trust root names principals, nothing here proves a principal is a distinct person, and a parent with no enrolled `run_operator` role is judged on structure alone |
| A full clone does not enforce the information barrier between humans | Firewall rule 3 in `FORGE.md` and `CRUCIBLE.md` | A FORGE runner reading `.audit/runs/<run>/` or a CRUCIBLE runner reading `.memory/hardness.yaml` on their own clone | The repository-boundary half is closed. `.memory/` is a roster root registered as a submodule against its own remote, `check_parent_layout` requires that gitlink and refuses a `HARDNESS.md` or `.memory` path resident in any roster checkout, and `docs/adr/0004-parent-submodule-layout.md` records the enforcement ADR 0002 adopted and never landed, so a parent-only clone no longer carries the hardness contract in its history at all. Three holes remain. A runner who initialises the `.memory/` submodule anyway holds both roles again, and nothing refuses that. `.seed/` and `.audit/` are still fully present in every clone, because neither is a roster root and neither moved behind a boundary. The runtime channel stays open: one agent session that ran both instruments carries the findings in context, which is ADR 0002 consequence 5. Run namespaces preserve ownership, never confidentiality, and the parent gate scans `.audit/` evidence for a `hardness.yaml` citation, so cross-role confidentiality against a human holding the other role stays procedural until agents run in exported allowlisted views |
| Verdict arbitration by claim instant | CRUCIBLE rule 5 | A later auditor back-dating `claimed_at` to steal ownership of a contested bundle | `claim_owner` takes the earliest `claimed_at` and nothing authenticates that instant; a hostile auditor writes any instant it likes. `validate_transition` decides ownership by the accepted sequence instead, refusing a claim on a bundle the accepted state already assigns and refusing two claims offered at once rather than settling them by instant, and it refuses a verdict with no accepted claim, no consumer run, or a consumer other than the accepted owner. The same rule refuses a seal instant earlier than the accepted watermark, so `Resident.order` cannot be reordered by a backdated `sealed_at`. None of it binds until an admission boundary serializes transitions |
| Epoch publication races between two clones | ENGRAM rule 10 and the projection paragraph | Two clones each publishing epoch `n+1` from epoch `n` and both pushing | `publish` refuses a stale proposal and an existing epoch directory on one clone, but two clones can each create `.memory/epochs/2/` with different bytes and the merge conflicts on the directory rather than refusing. `validate_transition` holds every published epoch immutable, refuses a publication that is not one past the accepted current, and refuses `.memory/current.json` moving backwards, so a fork is decidable; serializing the two publications still needs admission enforcement on the memory remote, and the epoch manifest still binds its parent by number rather than by the parent manifest digest |
| Starter anchor cardinality and identity | FORGE Phase 1 item 2e.1 and invariant 33 | The designation bound of at least three and at most five anchor tasks, and the identity that every designated anchor binds `task_lane` `starter` and every starter is an anchor, are stated and unevaluated. No instrument counts the designated set in `requirements/` or reconciles a starter-lane slot against the anchor designation, so a sixth anchor or an undesignated starter passes every check the harness runs |
| Batch cardinality and the one-anchor rule | FORGE invocation model item 10 and Phase 0 item 7 | A batch sealed with fewer than thirty ordinary slots, two anchor slots in one run, or an anchor drawn out of designation order | A reader over `.seed/batch.yaml` and `.seed/lanes/` that counts ordinary and `starter` rows per `run_id` against `batch_size` and the `requirements/` anchor designation; today only the seal bound of thirty-one is evaluated, by `pipeline.py` |
| Design returns and the return lane | FORGE Phase 2 item 7i.1 and ENGRAM Phase H step 14 | A sealed uuid with no `.seed/returns/<uuid>.yaml`, a return whose `bundle_digest` disagrees with its seal, or a return the lane never folded, so hardness, budget, and headroom standing drift from what was built | A schema reader over `.seed/returns/` that refuses an unknown field, binds `bundle_digest` to the queue record, and emits `RETURN_MISSING` per unreturned seal, plus a lane record in the dated hardness block a check can compare against the sealed set |
| Inferred-hardness projection recomputation | ENGRAM Phase H step 6d and invariant E38 | An unsigned or stale stratum row feeding a current inferred certificate, or a projection whose recorded function identity and digest do not reproduce | A checker that reads `.memory/strata.yaml`, verifies every external evidence root, evaluates cohort currency at the declared instant, recomputes the pure projection from pinned inputs, and compares its identity and digest |
| MAESTRO thirty-sample single-invocation programme | MAESTRO non-negotiable rule 9 and cycle convergence rule 6 | A MAESTRO invocation stopping without a lawful stop reason while fewer than FORGE's structural `sample_ceiling` of thirty bundles reside under `samples/`, or claiming `quota-filled` from a harness record rather than disk residency | A conductor execution record and a checker that counts resident bundle directories under `samples/`, binds the count to FORGE's structural `sample_ceiling` with starters included, walks `.podium/cycles.yaml`, and proves the invocation continued until quota or exactly one lawful stop reason |
| Rendering derivation is declared, never reproduced | ENGRAM invariant E10 and Phase H step 2d | `tools/corpus_identity.py` holds every `research/` pair to its stem, its retained-byte digest, its rendering digest, and a recorded deriving tool with a pinned version, and it never re-derives a rendering, because the harness declares no runtime dependency and calls no converter. Five of the twenty-six pairs record `pdftotext` at version 26.09.0, reproduced from the retained bytes at bootstrap; the other twenty-one record the token `unestablished`, because no repository byte names the tool that produced them and at least three carry LaTeXML alt-text artifacts a PDF text extractor never emits. A pair whose rendering was produced by a path other than derivation from its retained bytes therefore passes this check |

The last three are separation-of-duties gaps rather than tooling gaps. They were found by reading, which is what an auditor is for, and they went unnoticed because the corpus was too long to read.

## 7. External execution and independent acceptance

FORGE cannot reach SHIP without external execution and independently governed acceptance controlled by neither the candidate operator, the author, nor the auditor. The repository now verifies closed `trinity.release-policy/v3`, `trinity.oracle-run/v4`, default group `trinity.release-disposition/v3`, and compatible named-v2 disposition evidence against trust supplied outside the candidate tree, including the complete export snapshot digest, immutable candidate commit, six mandatory core controls, common manifest closure, and schema-specific producer plus approver authorization. Group v3 counts distinct authenticated principals against externally governed `gate_producer` and `gate_approver` thresholds with producer principals excluded from approval; valid signed v2 disposition bytes remain accepted, and explicit `gate.py promote --approver NAME` selects that compatibility form. The repository does not operate the service that executes a benchmark, observes every control, signs receipts, or publishes accepted snapshots. No deployment or pilot is established by these repository bytes.

| Obligation | Stated in | Would refuse | Needs |
| --- | --- | --- | --- |
| External operator commits and selects each stratified sample after batch freeze | FORGE Phase 4 item 4g and invariant 36 | A sample FORGE selected, influenced, predicted, or committed after a covered rollout supporting `SHIP:INFERRED` | An external stratified-sample operator controlled by neither author nor auditor that commits the frozen stratum and sample uuid sets before every covered rollout. No such operator exists |
| External execution service produces accepted oracle receipts | FORGE Phase 4.5 item 4, all three external release authority clauses | A locally typed reward or control outcome presented as acceptance evidence | A deployed service authorized for `execution_operator` that runs the immutable bundle under the policy-pinned Harbor, image, and harness identities, observes every policy-required control, and emits signed `trinity.oracle-run/v4` receipts with one outcome per control and one manifest closure digest. `tools/release_evidence.py` verifies such receipts but does not produce them or execute a benchmark |
| Oracle execution-fidelity outcome binds the G-FID ledger and execution attestation | CRUCIBLE step 8u, `docs/measurement-fidelity.md`, `docs/execution-authority.md` | A receipt whose `execution_fidelity` outcome's `evidence_digest` does not equal `sha256:` plus the digest of the canonical ledger bytes from `tools/forensics/ledger.py` function `ledger_bytes`, does not agree with the pilot's `fidelityLedgerDigest`, or covers a ledger holding HOLD or BLOCK conditions | A cross-artifact verifier linking the receipt, G-FID ledger, pilot registry, and `trinity.execution/v2` evidence. The parent gate now enforces the pilot-to-ledger digest and auditor-derived trial-status bindings before computing any bound. Receipt verification with supplied ledger bytes enforces the canonical receipt-to-ledger digest binding and refuses a passing outcome for HOLD or BLOCK ledger conditions. Only the execution-attestation and authenticated roster cross-binding remains deferred; the optional receipt ledger input is not a claim that the publisher resolves that resource |
| Externally governed publisher evaluates immutable snapshots | All three external release authority clauses | A candidate-controlled verifier, mutable checkout, operator-selected trust directory, or candidate-installed release workflow authorizing its own release | A separately administered publisher repository that exclusively owns `release-gate.yaml`, pins verifier, candidate, export, and trust commits and repository identities, mounts each data input as an immutable snapshot, prevents candidate influence over verifier and trust selection, and exposes an independently reviewable release result. Candidate `gate.py install` cannot establish or prove this deployment |
| Independent judge replay covers every judged score | CRUCIBLE verifier principle 5a, FORGE Phase 1 item 10f | A judged score that cannot be reproduced independently, or that contradicts deterministic tests | A shipped judge packet and independently governed replay runner that pins model, prompt, reducer, temperature, seed, complete transcript, and trial rows and reconciles the replay with deterministic test outcomes |
| Independent execution authority controls admission, execution, observation, and custody | FORGE Phase 4 item 2b and `docs/execution-authority.md`, both pending | A pilot whose population, configuration, and capture are all operator-asserted | A deployed authority controlled by neither author nor auditor that admits the population, runs the execution, captures the observation, and holds custody of the recorded bytes. FORGE Phase 4 item 2a already requires an authorized `execution_operator` and a pre-run commitment that predates the interval it covers, and no such authority has ever been deployed, so all four duties stay assertions by the party whose work they grade |

The consequence is operational: until those services are deployed and produce accepted evidence, FORGE remains capped at `HOLD:PILOT_REQUIRED` and neither `SHIP` nor `SHIP:INFERRED` is reachable. The receipt verifier closes a validation predicate, not the execution or governance service that supplies its input.

## 8. Ground rules with no dedicated check

Two of the ground rules that once sat here left by the only lawful route. `check_human_only_history` reads git history and refuses a commit touching `requirements/` or `touchstones/` under an agent or bot identity, and it resolves the symlink and worktree escapes around those roots rather than trusting the path text. `check_split_brain_writers` walks every Python write path in the tree, following `shutil` copy and move calls, parent-segment traversal, and function-local variable binding, so a harness writing into a peer's surface is a finding rather than a convention. Both are registered in `PARENT_CHECKS` and both carry planted-positive and clean fixture pairs.

Four rules still have an unenforced remainder. They are recorded here with the verdict each one earns, because the four do not fail in the same way and a register that made them look alike would be hiding the part that matters. Three retain tooling gaps, where a remaining predicate reads files that exist and nobody has written the reader. One is a structural gap, where no arrangement of the files on disk distinguishes the thing the rule forbids from the thing it requires.

| Obligation | Stated in | Would refuse | Needs |
| --- | --- | --- | --- |
| Difficulty claims in `.seed/hardness.md` are backed by costed lever ablations | FORGE non-negotiable rule 6, README ground rules | A lever counting toward a tier without resolved intact and ablated rollout evidence | DECIDABLE BUT NOT YET IMPLEMENTED. Every lever row in the generated `.seed/hardness.md` that counts toward a tier must carry ablation status `costed` with intact and ablated pass rates naming `.seed/probe.yaml` rollout identifiers that resolve. No check opens either file. The ACTIVE-ledger half has left this row: `tools/attest/intoto.py` registers `engram.cfer/v1` with a closed field set, and `check_active_cfer_attestations` walks current `ACTIVE` entries in `.memory/ledger.yaml`, requires digest-named `.memory/proofs/<digest>.dsse` bytes, verifies each signature against `execution_operator` at the gate instant, checks the signed validity interval, and binds the CFER identity and disposition back to the ledger row. The external signing-pipeline gap remains separately in section 1 |
| Work is differential beyond current-byte digest binding | ENGRAM non-negotiable rule 6, FORGE non-negotiable rule 5, CRUCIBLE non-negotiable rule 5 | A run rebuilt from scratch behind correct current digests, or an iteration log whose claimed moved-root set is false | `check_work_differential_digests` now deterministically hashes the current bytes under `requirements/`, `brief/`, `touchstones/`, `samples/`, and `delivery/`, refuses a present root omitted from each present harness digest carrier, and refuses a recorded digest that differs from current bytes. The contracts still define neither iteration-log filenames and schema nor a canonical tree digest, so no check compares dated deltas with the exact moved-root set; disk also cannot establish whether the labour was differential rather than a rebuild |
| Every path an instrument resolves stays inside the parent project root whose direct child is `trinity/` | Opening doctrine of all three contracts and `MAESTRO.md`, GAUNTLET rule 9 | A run that roots itself one directory above the project that vendors `trinity/` and scaffolds a harness there | The escape half visible from inside the audited tree is closed. `PARENT_LAYOUT_UNKNOWN_SUBMODULE` refuses a parent gitlink outside the closed roster, so an unregistered surface registered beside the roster is a finding at BLOCK, and `PARENT_LAYOUT_SYMLINK` refuses a symlinked roster root and every symlink met inside a roster checkout, which the boundary scan never follows and never descends, so a roster root cannot point outside the parent. What remains is the runtime guard comparing every resolved path against the invoking root. `integrity.py parent` audits whichever directory it is handed and cannot observe the directory an agent chose, so a harness scaffolded above the true root is invisible to every registered check |
| Reference fidelity and verifier robustness are proven, never assumed | FORGE invariant 18, FORGE invariant 20 | An emulation task shipped on a fidelity score nobody outside the author produced | NOT DECIDABLE FROM DISK. A REAL-UPSTREAM-SCORE and a robustness-obligation result are each written by the instrument whose own work they grade, no signature covers either, and no predicate type models either, so a replayed score and an invented score are the same bytes in the same field. `G-REF-OBTAIN` names no acquisition method, recorded in section 5, so no second reader can obtain the pinned upstream and re-derive the number. It becomes decidable when the score is carried by a signed attestation from a party that is neither the author nor the auditor, and when `G-REF-OBTAIN` binds an acquisition protocol that pins the upstream by digest. `check_bundle_false_exemptions` now enforces one strictly weaker carrier predicate: for a bundle declaring `reference_emulation.present: false` or `verifier_robustness.present: false`, it reports checker files with an unconditional successful exit, a long literal output comparison with no visible computation, an unparsable Python test, or a Python test with no assertions, and it reports a declaration with no checker files. Those hard-coded heuristics can falsify some claimed exemptions and never establish either proof, so this row remains for the external evidence obligation |

The fourth row is the one to read twice, because it is the only permanent entry in this file that is not waiting on labour. The first three rows shrink when someone writes each remaining reader. The fourth shrinks only when an actor outside this repository signs a number, which places it beside section 7 rather than beside its own neighbours, and until then the strongest honest statement about reference fidelity is that the contract requires a proof and the repository stores an assertion.

Each of the four was assessed against the tree rather than against the contracts alone. The registered checks now open `.memory/ledger.yaml`, the harness digest carriers, and `.seed/contract.yaml`, but they do not open `.seed/hardness.md`, `.seed/probe.yaml`, or a contract-defined iteration log. The ACTIVE CFER check verifies each current signed envelope without recomputing the ledger-checkpoint Merkle root, the digest check binds current bytes without proving differential labour or iteration deltas, and the false-exemption check catches only its documented hard-coded checker heuristics without proving reference fidelity or verifier robustness.

## 9. Front doors and the scribe flow

`gate.py install` writes every front-door command and gate skill into a vendoring parent byte-for-byte from `templates/doors/` at each gate preflight, and the contracts route the ENGRAM default branch through Phase S. `check_front_doors` now opens every rostered door, refuses a missing, stale, unknown, drifted, oversized, restating, escaping, or `AGENT.md`-named door, and is registered in `PARENT_CHECKS`. The two remaining rows concern execution flow that the thin door files do not record.

| Obligation | Stated in | Would refuse | Needs |
| --- | --- | --- | --- |
| A surface door runs Phase S scoped to its one surface, and a derived surface door stops when the step 9 paper gates fail | ENGRAM Phase S surface dispatch rule | A pitch, media, patent, or study pass built from a paper whose gates fail | A recorded per-surface scribe block a check can read, and a reader that compares the derivation order against the paper gate outcome |
| The default branch closes through Phase S so one paste completes every publication surface | ENGRAM resume preflight rule 3, ENGRAM Phase S | A default run reported complete with a publication surface its flow never touched | An iteration record binding the default run to its closing scribe block, and a reader that compares the two |
| `harness/` holds a FORGE-generated benchmark extension in the Phase H step 2c plugin structure, with inference and evaluation entry points, scoring delegated to each bundle's own `tests/`, every dependency pinned, and no byte written by another instrument | FORGE Phase 1 item 1b, ENGRAM Phase G step 8 | An empty or hand-written `harness/`, an extension carrying its own grading logic, or an ENGRAM or CRUCIBLE byte under it | The registration half is closed. `harness` is a roster root, so `check_parent_layout` requires its gitlink with `branch = main` and `update = merge`, refuses a remote it shares with any other roster root, and refuses a `.memory`, `.seed`, `.audit`, `.trial`, `.podium`, `trinity`, `samples`, `delivery`, `deliverables`, `requirements`, `touchstones`, `HARDNESS.md`, or `.secrets` path resident inside the checkout. What remains is the shape reader over the submodule checkout that finds the two entry points, the three-duty implementation, the pinned agent interface, and the pin set, plus the git-author reader that attributes each `harness/` commit to its run identity |

## 10. Delivery integrity and the adversarial operator

The 2026-09-07 sample delivery left ten parents while their dispositions stood at BLOCK, HOLD, BROKEN, or STALE, and the client found the defects Trinity's contracts already named. The repository now decides more of that failure from disk. `tools/sabotage.py` refuses unbound or forged dispositions, inert instruments, a fork in place of the canonical `trinity/` remote, typed sign-off, governance edits committed beside their subject, uncommitted audit citations, and a dirty tree. `tools/release.py` refuses export mismatch, release waivers, candidate-local trust, missing instrument-separated co-signed default-v3 or compatible named-v2 records, malformed or unauthorized `trinity.oracle-run/v4` receipts, incomplete or invalid mandatory control coverage, snapshot or candidate-commit mismatch, unsafe bundle trees, and empty release populations under `trinity.release-policy/v3`. `tools/incident_store.py` supplies bounded authenticated event ingestion and durable external SQLite incidents, equivocations, and repeat-alert records, while the observer template API-authenticates sender, source, target, run, actor, ref, and commit before rerunning the reviewed probe. It has no message transport, SMTP configuration, recipient, or send path. `tools/harbor.py` refuses stale or untested Harbor pins and manifests Harbor's validator rejects, while `tools/pipeline.py` refuses a sealed bundle whose bytes moved. Candidate sentinel state remains diagnostic. The rows below are the obligations the repository still does not decide or deploy.

| Obligation | Stated in | Would refuse | Needs |
| --- | --- | --- | --- |
| Adversary probes run against every shipped verifier | CRUCIBLE step 8t | A verifier that pays a null, negated, keyword, self-reported, hook-planted, partial, format-only, or fabricated submission above the declared floor | A probe runner in the parent `.audit/` harness that submits each probe through the stock Harbor path in the pinned image and binds the measured rows into `VERDICT.md` |
| Three independent correct solutions each reach full reward | FORGE Phase 2 item 7j | A verifier that grades the gold patch's incidental names, strings, layout, or mocking strategy | Authored alternates under `solution/` and a runner that replays each through the oracle path |
| One-fault mutants score monotone, never negative, and trip no cheat check | FORGE Phase 2 item 7j | A single-token cliff, a negative score for honest partial work, or colinear rubric items | A deterministic mutant generator over the gold patch and a ladder runner bounded per family |
| Every graded literal is in the statement, the base commit, or an accepted alternate | FORGE Phase 2 item 7j | A hidden helper name, error string, line number, key order, or private field literal in the tests | A per-language extractor over the tests tree reconciled against the statement closure and the base commit |
| Judge runner, prompt, reducer, model id, temperature, seed, and trial rows ship inside the bundle and replay to the recorded score | CRUCIBLE verifier principle 5a, FORGE Phase 1 item 10f | A judged score nobody can recompute, a judge that saw a truncated transcript, or a rubric pass that contradicts a failing test | A `tests/judge/` layout the layout check requires and a replay check registered beside `check_judge_reliability` |
| Oracle receipts are produced by a deployed external execution service | FORGE Phase 4.5 item 4 | A receipt whose signature, core-control manifest, and shape verify but whose claimed executions never occurred | `tools/release_evidence.py` verifies an authorized `execution_operator` signature, closed v4 receipt fields, policy v3 identities and bounds, one outcome for every required control, the common manifest closure digest, and judge-only trusted non-applicability, but the signer remains trusted to have observed the executions. A separately governed quality-controls runner must execute the immutable bundle, run every control, and sign the resulting observation; that runner remains absent |
| External release trust and verifier pins are administered outside the operator's repository | The adversarial operator doctrine and all three external release authority clauses | A self-minted `.memory/roots.yaml`, candidate verifier, or operator-selected external directory authorizing its own release | `tools/release_evidence.py` rejects candidate-local trust and `templates/release-gate.yaml` pins verifier and trust repositories by full commit SHA, but organization ownership, immutable snapshot presentation, variable protection, role enrollment, rotation ceremony, and publisher deployment remain external governance work. The same-commit classification in `tools/sabotage.py` is a detection rule, not the trust boundary |
| Configuration adequacy judged by a governance authority | FORGE Phase 4 item 3a | An approved-but-inadequate configuration, such as a context window too small for the declared turn horizon | A human `requirements/` value or a recorded `touchstones/` disposition naming the approving authority and the headroom argument behind it. The schema half is closed: `tools/harness_config.py` parses closed `trinity.harness-config/v1` bytes and refuses a record carrying no `configurationAdequacy` block. Whether a declared headroom is adequate for the task it runs is not decidable from disk, so the block's contents are accepted as declared |
| The observer and incident store are deployed with independent sender identity, durable state, and operational monitoring | The alert authority clauses and `templates/SENTINEL.md` | A candidate dispatching its own accusation, a target identity accepted from payload text, or repeated findings disappearing with candidate-local state or unavailable durable storage | Provision the separately governed observer repository, allowlisted GitHub App webhook bridge, App token, sender and source and target repository-id allowlists, dedicated observer runner, owner-only config, external durable SQLite volume, repository rulesets, and real operational monitoring. The template API-resolves the target, while `tools/incident_store.py` implements authorization, immutable incident and equivocation retention, repeat-alert status, and safe legacy migration; this session configured none of that infrastructure |
| The gate preflight ran before phase work, not after | Gate preflight and report binding doctrine in the three contracts | An invocation that did its phase work first and ran `trinity/tools/gate.py` only to mint a record afterwards | `check_gate_receipts` proves a report-moment receipt binds `HEAD` and that the report token never exceeds the record, but a receipt's `generated_at` is self-reported and no instrument orders it against the phase work; a local run can still skip the preflight, and the pre-push hook is advisory. The decision that counts is the same command run by continuous integration on the pushed commit, which is itself deferred to org configuration in the row above |
| A historical SAB_WORKFLOW_TAMPER finding is resolved only by an append-only signed remediation record | Historical sabotage doctrine in the three contracts, bite row A40 | A published-history rewrite, force-push, replacement, or padding commit offered as the recovery, or an unsigned or self-authorized remediation note presented as clearance | `check_workflow_tamper` scans the latest fifty commits and holds the finding, and no registered check reads a remediation record, so today the finding stays at BLOCK until that reader lands: a closed record binding flagged commit identities, corrective commits, the re-verified trust snapshot, and rerun evidence, verified against externally enrolled principals distinct from every key the flagged commits introduced, with the original commits preserved and the finding retained as resolved rather than erased |
| A clearance for an alert raised against an unverified actor excludes nobody | The adversarial operator doctrine in the three contracts, bite row A93 | Nothing today | clearance_status excludes the alerted actor's verified principals from the sentinel_clearer threshold, so an operator whose commits carry a verifiable signature cannot clear their own alert; an operator whose HEAD commits carry no signature this repository can verify is attributed as claimed, that identity names no principal to exclude, and a sentinel_clearer who is that operator can still clear it. Closing it needs commit-signing enrolment for every principal that runs a gate, which is external key governance rather than a check |
| An admin insider is out of scope | This section | Nothing | An org administrator can rewrite branch protection, secrets, and the transparency log together, so the residual defense is a client-checkable external log rather than any check in this repository |

## What is enforced today

The honest counterpart of the rows above.

`just ci` proves that `pyproject.toml` declares no runtime dependencies and no populated optional-dependency group, runs the prose and contract-integrity linters, checks bite citations and executable coverage, guards this register against unsupported deletion or shortening, and runs ruff, mypy, and pytest. The registered checks cover Harbor currency and validation receipts, the sealed-bundle pipeline queue, anti-sabotage refusals, candidate sentinel diagnostics, versioned bundle and complete export snapshot identity, externally rooted signed release evidence, and the release gate. The release verifier rejects release waivers, candidate-local trust, missing or cross-domain default group v3 or compatible named-v2 FORGE and CRUCIBLE evidence, malformed or unauthorized `trinity.oracle-run/v4` receipts, incomplete or invalid mandatory controls, snapshot and candidate-commit mismatch, empty release populations, unsafe bundle trees, and export mismatch under `trinity.release-policy/v3`. The observer implementation covers API-resolved run identity, allowlisted source and target identity, read-only probe rerun, closed incidents, external SQLite incidents and equivocations, immutable repeat-alert records, bounded fail-closed status, deduplication, and legacy state migration under tests, with no SMTP, recipient, secret, or send surface in its active v2 config or workflow. These facts do not establish publisher, GitHub App bridge, observer, quality-controls runner, or immutable snapshot deployment, and they do not establish external execution, complete adversary coverage, alternate correct solutions, mutant ladders, graded-literal reconciliation, or independent judge replay.

`check_parent_submodule` resolves the freshness gate rather than describing it. It runs `git fetch --no-tags --quiet origin refs/heads/main` inside the vendored checkout and compares the checked-out commit to the fetched tip, and it separately holds `.gitmodules` to `branch = main` and `update = merge`. It names five distinct stale outcomes: `TRINITY_FRESHNESS_BEHIND`, `TRINITY_FRESHNESS_DIVERGED`, `TRINITY_FRESHNESS_LOCALLY_MODIFIED`, `TRINITY_FRESHNESS_UNVERIFIABLE`, and `TRINITY_FRESHNESS_GIT_ABSENT`. A failed fetch resolves to unverifiable, so offline is a hard block rather than a waiver, and `tests/test_freshness.py` proves both halves.

Three hundred thirteen tests exercise `tools/attest`, the stdlib-only signing suite the checks above call. It fixes canonical bytes, constructs DSSE v1 pre-authentication encoding, and verifies against the exact transmitted payload bytes without reserializing them. It invokes `ssh-keygen -Y` at an absolute path with `shell=False`, a scrubbed environment, and an explicit `-O verify-time` passed to both `find-principals` and `verify`. It resolves roles and thresholds against a versioned trust root and enforces old-root and new-root continuity across trust-root rotation. It recomputes signed trajectory roots from committed rollout bytes. Its replay store admits each replay-protected envelope once, takes an exclusive lock, proves that lock excludes a contending process before writing, chains every entry under a keyed digest, and reports a distinct fatal outcome when a commit result is uncertain rather than guessing which way it fell.

The hand-runnable regression suite under `tests/bite/` states three hundred eighty-one scenarios the contracts must refuse. One hundred fifty-two are fully covered by `just ci` and one is partly covered. The remaining two hundred twenty-eight have never been verified by anything, including by hand. That split is now machine truth rather than a maintained claim: `tools/bite.py` refuses a row claiming CI coverage with no registered test, refuses a registered test with no row, and refuses a restated count in this file or in `tests/bite/README.md` that drifts from the corpus or is reworded past the point the check can read it.

Of the two hundred fifty refusal conditions harvested from CRUCIBLE, two hundred twenty-two are decidable from files on disk today. Decidable is not the same as decided. A bounded few of them are decided today, by the parent checks registered in `PARENT_CHECKS`, and the rest are not, because no runtime other than a coding agent reading the contract exists. The exact overlap between that check set and this harvest has never been computed, so the count above is a ceiling on what is deferred rather than a measurement of it. Computing the overlap is itself deferred.

That is the accurate summary of Trinity's present enforcement: the contracts are readable, internally consistent, and lint clean; a bounded set of their refusals is evaluated by `integrity.py parent` and by the attestation verifier it calls; and every refusal outside that set depends on an agent choosing to honor it.

Two bypasses that made parts of that bounded set inert are closed. An HTML comment used to satisfy the closed-vocabulary and shared-block layer, because the checks read raw file bytes and a commented copy counted as a stated rule; `visible_markdown` in `tools/integrity.py` strips comments before any of those checks read a line. A fence tagged `text` used to hide hard-wrapped prose from every style check; `tools/prose.py` now exempts a fence only when its info string names a recognized non-prose language, so an untagged or `text`-tagged fence is read as prose. Both were silent passes rather than wrong answers, which is the failure mode this register exists to surface.

## Row count is the progress metric

Every other number this repository can report grows whether or not anything works. Word counts grow. Instrument counts grow. Contract length grows. Rule counts grow. None of them distinguishes a project that built a verifier from a project that wrote another paragraph about verification.

This register is the only artifact here that gets shorter as Trinity gets stronger, because a row leaves only when code lands that evaluates its predicate. Its row count is therefore the one honest progress metric available, and the direction of travel is down.

That property is also the register's own attack surface. Making the count matter creates an incentive to shorten it, and the cheapest way to shorten it is to reword a row rather than build a check. So the exit rule above is a refusal condition, not a convention: a row deleted in a commit that lands no evaluating code is a conformance failure, and a row narrowed so that its remaining text no longer describes the obligation the contract still states is the same failure wearing a smaller word count.

Two syntactic slices of that rule are enforced today. `tools/bite.py` binds this file's three restated coverage counts to the `tests/bite/` corpus, so a count edited downward without the corpus moving is `BITE_COUNT_DRIFT` and a count reworded until the check can no longer read it is `BITE_COUNT_MISSING`. Its register mode also refuses a deleted obligation-name cell or a shortened replacement when no Python file under `tools/` or `tests/` changed in the same changeset. It does not determine whether accompanying code evaluates the removed predicate, and any accompanying Python change bypasses both register findings.

| Obligation | Stated in | Would refuse | Needs |
| --- | --- | --- | --- |
| A register row leaves only alongside code that evaluates its predicate, not merely any Python change | This file, How to read a row | A row deleted to shrink the count beside unrelated code | Semantic comparison between the removed predicate and the accompanying registered check. `tools/bite.py register` now refuses deletion with no accompanying Python file under `tools/` or `tests/`, but it treats any such file as sufficient |
| A register row is never narrowed below the obligation its contract still states, even beside Python changes | This file, Row count is the progress metric | A row reworded until it describes less than the rule it discloses | Semantic comparison between the old and new row and the cited contract. `tools/bite.py register` detects a shortened obligation-name cell only when no Python file under `tools/` or `tests/` changed, and it does not compare the other cells or the contract obligation |
